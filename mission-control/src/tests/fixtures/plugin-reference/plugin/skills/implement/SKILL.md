---
description: Starts and runs the build of a Pandacorp project. Launches a dynamic workflow that builds feature by feature. Use inside the project after /pandacorp:architecture.
---

# /pandacorp:implement

Starts (and resumes) the build. Launches a dynamic workflow inside the project.

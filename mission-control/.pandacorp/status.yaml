# Machine-readable status for Pandacorp (the factory dashboard) — read by the factory and the dashboard itself.
# Human-readable Spanish narrative lives in .pandacorp/comms/summary.md (gitignored).
project: "Pandacorp"
phase: implementation
version: v1
overlay_version: "8.33.0"
created_with: "6.0.0"
running: true
run_started_at: "2026-06-21T14:38:17Z"
repo: ""
updated_at: "2026-06-21T15:42:30Z"
last_event_at: "2026-06-21T15:42:30Z"
work_orders_total: 78
work_orders_verified: 77
work_orders_in_review: 0
work_orders_planned: 1
work_orders_in_progress: 0
work_orders_blocked: 0
work_orders_done: 77
pending_decisions: 1
pending_bugs: 0
rethink_pending: false
advance_pending: false
last_green_sha: "a372c99"
safe_to_test: true  # FRD-07 gate REJECT #4 (2026-06-21, opus reviewer, DEEP mode, DR-072): WO-07-005 reopened IN_REVIEW->PLANNED, reopen_count 1->2 (cap 3, not hit — defect class is NEW). The reject #3 DEAD-CODE blocker is RESOLVED (grep DETAIL_TABS_STYLE/detailTabStyle in src/ returns NOTHING; KNIP clean) and the surface is OTHERWISE CORRECT & honest, verified INDEPENDENTLY against the REAL factory tree (23 skills, 14 agents): four sections in order; copy-to-clipboard CopyButton on the skill-detail command; interno chip on the two real internal skills (scaffold, work-orders) and absent on the rest; Produce section (21/23 skills carry a derived produces line); run-location grouping with counts via shared SectionHead; forward skill->agent flow chips; reverse agent->skill "Usado por" chips; model-assignment explanation (opus=judgment / sonnet=mechanical); SectionTabs + StandardsSection/DetailPanel delegate to the shared SubTabs (DR-057). /configuration renders clean LIVE (Preview Smoke 2 viewports desktop 1280 + mobile 390, HTTP 200, 0 console/pageerror/failedReq; VLM mock-judge: recognizably configView() — PageTitle "Configuración" = nav label DR-062, 4 SubTabs Habilidades active, "En la fábrica" 2 / "En el proyecto" 11 SectionHead groups, RPG wand cards, interno chips visible — NO gross structural mismatch). Mutation-confirmed reverse cross-nav PRECISION (DR-016): the agent->skill inversion is EXACTLY skills.filter(s.agents.includes(agentId)) — no over/under-listing (busiest agent architect=13 skills; orphan-agent empty case). THE SINGLE BLOCKING CORRECTION DEFECT: the skill-detail "Volver a habilidades" Back button is DEAD after a reverse cross-nav (agent->skill). When the owner jumps agent->skill, ConfigurationShell.handleSkillCrossNav sets crossNavSkillSlug; SkillsSection makes controlledSkill (derived from selectedSkillSlug) take precedence over localSelectedSkill; the Back handler is only onBack={() => setLocalSelectedSkill(null)} (SkillsSection.tsx:87) — it never clears the PARENT crossNavSkillSlug, so controlledSkill stays truthy and the detail stays open. The owner is permanently STUCK on the skill detail (proven: 3 consecutive Volver clicks -> still on detail); the only escape is switching tab and back (handleSectionChange clears crossNavSkillSlug). A delivered, visible control that does nothing in a reachable state is a correctness defect (EARS "click -> detail" implies a navigable way back), NOT a visual nit -> BLOCKS. Why prior gate tests missed it (DR-015): the existing reverse test only asserts the chip OPENS the skill detail (forward landing), never presses Back. FIX (small): the Back action must reset BOTH selections — pass an onBackToList from ConfigurationShell into SkillsSection->SkillDetail.onBack that does setLocalSelectedSkill(null) AND setCrossNavSkillSlug(null) (or an onClearCrossNav the Back handler also calls). Self-check: open an agent -> click a "Usado por" chip -> press Volver -> the skill LIST must reappear and skill-detail must be gone. Anchor reviewer test RECORDED (not committed RED — DR-070 keeps it out of siblings' global suite; rebuild WITH the fix): src/app/configuration/_tests/frd07.deep-gate-opus.reviewer.test.tsx (was 4 passing / 1 RED on this defect) drives the WHOLE surface through real ConfigurationPage + real factory tree: (1) reverse cross-nav PRECISION exact-set-equality; (2) the RED one — agent->skill cross-nav then Volver shows skill-list + hides skill-detail; (3) internal-chip honesty (scaffold/work-orders yes, a non-internal no); (4) produces honesty (non-empty Produce section). DR-070 revert performed: the 6 cycle-changed existing files (AgentDetail.tsx, ConfigurationShell.tsx, SectionTabs.tsx, SkillsSection.tsx, StandardsSection/parts.tsx, StandardsSection/styles.ts) + the additive src/components/core/Tabs/Tabs.tsx were git checkout 5e2ca47 --'d; the WO-newly-created _tests (dr057-reuse.test.tsx, frd07.cross-nav-reverse.gate-opus.reviewer.test.tsx) git rm'd; my RED anchor removed from the tree (recorded here only). git diff 5e2ca47 -- src/app/configuration src/components/core/Tabs/Tabs.tsx is EMPTY (byte-identical to last green); 277 existing config tests GREEN against the reverted code. last_green_sha ADVANCES to a372c99 + safe_to_test STAYS true: the committed tree is the green point — the FULL global gate (bash .pandacorp/verify.sh, no --since) EXITs 0 (biome+tsc+knip+madge clean, vitest 327 files / 7060 passed + 2 expected-fail, smoke 14/14 + visual Layer A 14/14, blessed routes unchanged — no regression). The ONLY non-VERIFIED WO is FRD-07 WO-07-005 (PLANNED, reopen_count 2 — its RED anchor reverted out per DR-070, so the global suite is green; the engine retries it next run; per DR-072 C2 the NEXT gate must set it BLOCKED:needs-owner if still not green rather than reopen again at cap). frd-07 frd.md+blueprint.md rollup stay PLANNED (one WO planned). Advisory nits (NOT blocking, on .pandacorp/comms/visual-punch-list.md): light-mode skin; px-not-token inline values; AgentDetail "Usado por" chip uses a bespoke inline button (must be clickable, borderline reuse note not a duplicate); /configuration registered blessed:false in e2e/routes.ts until VERIFIED. Counts: 77 verified / 1 planned / 0 blocked / 0 in_review. | --- prior: FRD-03 gate PASS (2026-06-21, opus, DR-072): WO-03-002 VERIFIED (reopen_count reset 0) -> both FRD-03 WOs VERIFIED -> frd-03 rollup VERIFIED. The historical DR-057 "two rails" blocker is RESOLVED per owner Option A & mutation-locked (page.tsx imports the ONE shared ProjectRail + passes selectedSlug; SelectableProjectRail is a 9-line pass-through wrapper, not a fork). /portfolio BLESSED (empty-state baseline, no active projects on this machine). Global gate EXIT 0 at 5e2ca47. Counts then: 77 verified / 1 planned (WO-07-005) / 0 blocked / 0 in_review. | --- prior: FRD-07 gate REJECT #3 (2026-06-21, opus): dead-code KNIP fail (DETAIL_TABS_STYLE+detailTabStyle orphaned after the SubTabs refactor) — now FIXED. Reverse agent->skill cross-nav was resolved & mutation-locked. | --- prior: FRD-08/FRD-05/FRD-17/FRD-10 gate PASSes (2026-06-21, DR-072): each resolved its DR-057/DR-062 reuse-fork blocker, mutation-locked, blessed where applicable. | --- prior: FRD-07 REJECT #2 (reverse agent->skill cross-nav half-built) and REJECT #1 (5 missing EARS + 2 reuse forks) — both resolved in later passes. | --- prior: FRD-04 gate PASS (nested <button> #418 fixed); FRD-12/FRD-11/FRD-06/FRD-18/FRD-14 gate PASSes. Full history in git log + each WO's gate-verdict notes.
blocked: []
supervisor_heartbeat: "2026-06-21T15:42:10Z"

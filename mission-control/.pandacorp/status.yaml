# Machine-readable status for Pandacorp (the factory dashboard) — read by the factory and the dashboard itself.
# Human-readable Spanish narrative lives in .pandacorp/comms/summary.md (gitignored).
project: "Pandacorp"
phase: release
last_harvest: "2026-07-06T02:00:00Z"
version: v1
overlay_version: "8.69.0"
created_with: "6.0.0"
target_platforms: desktop
deploy_target: internal  # internal tool (runs on 127.0.0.1) — no external host (DR-085)
deploy_url: "http://192.168.18.227:1987"  # MC's internal release on the LAN
running: false
run_started_at: "2026-07-07T04:32:22Z"
run_completed_at: "2026-07-07T05:30:00Z"
repo: ""
updated_at: "2026-07-07T05:30:00.000Z"
last_event_at: "2026-07-07T05:30:00.000Z"
work_orders_total: 104
work_orders_verified: 104
work_orders_in_review: 0
work_orders_planned: 0
work_orders_in_progress: 0
work_orders_blocked: 0
work_orders_done: 104
pending_decisions: 0
pending_bugs: 0
rethink_pending: false
advance_pending: false
last_green_sha: "aaf72d1a"
safe_to_test: true  # RELEASE (DR-085) 2026-07-07: whole-project cross-feature INTEGRATION review (DR-060) PASSED + full verify.sh GREEN at aaf72d1a. Gate: biome (797 files, 0 fixes, 1 schema-version INFO only), tsc/knip/madge clean (no circular deps, no unused), 7576 unit tests (425 files) GREEN, 68 e2e GREEN (smoke+shell+visual+responsive, desktop+mobile — every route renders, all baselines match). Gate quarantine set EMPTY (0 WOs with frontmatter implementation_status:BLOCKED+blocked_reason:needs-owner — the WO-07-005 "BLOCKED" match was PROSE not frontmatter) → full gate ranged over EVERY route, no PANDACORP_GATE_SKIP_ROUTES exported. Cross-feature SEAMS all AGREE (independently re-verified, not trusted from the prior note): ONE canonical WorkOrderState in lib/work-orders (board/DAG/wo-detail all `import type` it; DagCard/wo-detail carry Record<WorkOrderState,…> exhaustive maps tsc-enforces); ONE GuildOutcomes/computeGuildLevel (getGuildState) feeding hero+header+dashboard; ONE useLiveSnapshot/LiveFrame (Party+WoDag); ONE AgentRole/AGENT_ROLES in _design/tokens (no divergent color map); FRD-23 read-model fully fail-loud (PortadaResult/AggregateResult/FactoryResult discriminated unions, re-throw on non-ENOENT, honest fallback to live derive* cores, SSOT split so a cross-project phase change re-derives factory-wide facts live — never fabricates a zero, DR-078); deriveKpis/KpiHeader fully removed (SSOT); Manual reuses FRD-07 catalog card primitives VERBATIM (no fork). Hardening artifacts present & dated this run (BL-0012): docs/reviews/security-2026-07-07.md (No Critical/High, release NOT blocked) + events.md "## Verification 2026-07-07". Did NOT kill the live launchd MC instance (PID 25324, cwd local-deployments, serves LAN :1987) — it is the deployed dashboard, not an orphaned lock; playwright ran on its own fixture port 3900 (reuse disabled) and cleaned up. Advisory-only (visual punch-list, non-blocking, NOT reopened): _observability/FreshnessBadge is a divergent 2nd FreshnessBadge with ZERO production consumers (superseded by DR-066 modules/FreshnessBadge; tested-but-unmounted so knip won't flag — cleanup nit, not a broken seam); components.md carries 4 duplicate INVENTORY rows (FreshnessBadge divergent-file / LiveRegion,StateBadge,ThemeToggle same-file dupes — doc hygiene); header-scan + @390px responsive advisories are internal-tool/off-target (deploy_target internal, target desktop). All 104 WOs VERIFIED. Prior implementation-phase evidence below.
_release_prior_note: true  # RELEASE (DR-085): whole-project cross-feature INTEGRATION review (DR-060) PASSED + full verify.sh GREEN at ebe1c58f (biome/tsc/knip/madge clean, 7538 unit tests, 68 e2e — smoke+shell+visual+responsive, desktop+mobile). Cross-feature seams all AGREE: one shared WorkOrderState (board/DAG/detail import it, exhaustive Record maps tsc-enforced), one GuildOutcomes/computeGuildLevel, one LiveFrame/useLiveSnapshot (FRD-18/12/06/05), the FRD-23 stats read-model fail-loud (AggregateResult/PortadaResult discriminated unions + honest fallback, never fabricates a zero, DR-078), Manual reuses Config's catalog cards (no fork), deriveKpis/KpiHeader fully removed (SSOT). Hardening artifacts present & dated this run: docs/reviews/security-2026-07-06.md (no Critical/High) + events.md "## Verification 2026-07-06". Gate quarantine set EMPTY (0 BLOCKED needs-owner) → full gate ranged over every route. Killed one orphaned same-project next-dev server (PID 29197, BL-0049/LESSON-0040) that blocked the first Playwright run, then re-ran clean. Advisory-only (visual punch-list, non-blocking): _observability/FreshnessBadge (CMP-12-freshness) is a divergent 2nd FreshnessBadge with zero production consumers — superseded by the DR-066 modules/FreshnessBadge; tested-but-unmounted so knip doesn't flag it (cleanup nit, not a broken seam). All 102 WOs VERIFIED.
# --- FRD-23 SSOT-split CLOSE (WO-23-006) 2026-07-07 ---
# WO-23-006 (composed Informe reader) VERIFIED at the split-gate CLOSE → FRD-23 fully VERIFIED (all 6 WOs).
# The reader now composes per-project facts (portada, per-project seal) with factory-wide facts (stats-factory.json,
# factory seal), each with an INDEPENDENT fail-loud fallback to the live derive* cores (REQ-23-007). Cross-project
# staleness bug fixed (AC-23-007.3): a phase change in project B mismatches the factory seal → project A re-derives
# the factory-wide facts live instead of serving B's stale embedded copy; A's fresh portada can no longer carry a
# stale copy because factory-wide facts left it (SSOT split, WO-23-005). Wired at the page seam (app/achievements/page.tsx
# reads readStatsFactory(factoryRoot) and passes the FactoryResult). Reviewer-authored adversarial integration suite
# added (composedReader.reviewer.test.ts): full aggregate→factory-store→resolve chain proven to never fabricate a zero,
# fail loud on a corrupt stats-factory.json, and treat a null-seal store as stale. Focused gate GREEN at 412b2af2 (biome
# + tsc global clean, 168 affected unit tests + 3 new reviewer tests, 42 e2e smoke+shell desktop+mobile incl. /achievements).
# FRD-23 is ui:false. Advisory (punch-list, non-blocking): no browser gate exercises the materialized fresh-store render
# path — every e2e /achievements run hits only the live-git fallback branch (no stats.json/stats-factory.json e2e fixture).
# --- prior note (FRD-23) ---
# FRD-23 (materialized stats read-model) VERIFIED at the split-gate CLOSE. WO-23-004 (aggregate index + Stop/post-commit regeneration + one-shot backfill) — reverted last run for AC-23-003.1 being unwired — was REBUILT and is now wired end-to-end: page.tsx reads the O(1) aggregate (readStatsAggregate + resolvePortadaFromAggregate), the sync-portfolio skill (step 5c) invokes `pnpm stats:sync-aggregate`, and the stats:regen/stats:sync-aggregate/stats:backfill scripts + scripts/read-model/*.mjs exist. The preserved reviewer adversarial spec (aggregateWiring.reviewer.test.ts, the RED baseline that forced the revert) now passes GREEN and was restored into the tree as a permanent gate + a new reviewer integration spec (aggregateChain.reviewer.test.ts) proves the honest-fallback chain never fabricates a zero (DR-078). Full suite 7538 tests GREEN, tsc + biome clean. The Playwright layer hit a transient orphaned-dev-server port/lock (LESSON-0040 / BL-0049, another session's PID) — /achievements verified live at HTTP 200; FRD-23 is ui:false. All 102 WOs VERIFIED. NOTE (owner, not a WO defect): the approved per-project portada embeds factory-wide facts (phaseTransitions/scalars.projects/scalars.decisions/lessons) that a per-project seal can't validate — a latent SSOT concern in the FRD/ADR design itself, logged to inbox/decisions.md. 2026-07-06
blocked: []  # WO-23-005 gate-test-defective RESOLVED: the blessed aggregateChain.reviewer.test.ts:111-137 was re-blessed to the SSOT-split contract (fresh portada → factory-wide from live/factory-store) at the WO-23-006 gate; all 104 WOs VERIFIED.
pending_changes: 0
supervisor_heartbeat: "2026-07-07T04:32:22Z"

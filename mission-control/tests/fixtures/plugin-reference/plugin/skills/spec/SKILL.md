---
description: Creates the project and documents an idea's MVP. Handoff skill that runs from the factory. Runs in the factory context with the idea name as argument.
---

# /pandacorp:spec

Takes the idea indicated in `$ARGUMENTS`, creates its project and documents the MVP.

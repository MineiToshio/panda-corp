- (agent-inferred) gate worktree anidado: `next dev` del webServer de Playwright fallo UNA vez con "next/font/google queries have exactly one entry" (import trace resuelto relativo al arbol principal) y paso al reintentar sin cambios — transitorio de entorno, no del codigo; reintentar antes de atribuir un RED de playwright al build. Ademas: `verify.sh --since` solo corre smoke+shell, un spec e2e nuevo del revisor solo lo recoge el run completo de close-out.
- 2026-09-26 (agent-inferred) Date.parse NO es chequeo de validez para fechas: el fallback legacy de V8 acepta "pendiente 1"→2001-01-01, "1", "TBD 2" y desborda "2026-02-30"→2 mar. Para una fecha de solo día (YYYY-MM-DD) usar regex estricta + round-trip Date.UTC, y tratarla como día de calendario comparando contra los componentes LOCALES de now (no UTC, no instante). Visto en FRD-04 WO-04-008 formatChangeDate (hermano del fix formatLastSync de FRD-03).

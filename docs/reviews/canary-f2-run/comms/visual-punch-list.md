- [ ] frd-04-project-workspace · /projects/<slug>?tab=changes · the raw ISO date on the change card is exposed only through a `title` tooltip on a non-focusable span (not reachable by keyboard/touch, unreliable for screen readers); consider a visually-hidden raw date or a focusable tooltip · src/app/projects/[slug]/_components/tab-changes/ChangeCard.tsx:149
- [ ] frd-05-work-orders · /projects/<slug>?tab=work-orders · two sibling buttons share the accessible name "Todos" (FRD row + state row); a screen-reader button list cannot tell them apart. Consider aria-label "Todos los FRD" / "Todos los estados" · src/app/projects/[slug]/_components/wo-state-filter/wo-state-filter.tsx:~104 + wo-frd-filter/wo-frd-filter.tsx:~96
- [ ] frd-05-work-orders · /projects/<slug>?tab=work-orders · the state row's first pill reads "To do" (English) among Spanish pills (inherited from the board's COLUMNS copy; the WO text suggested "Por hacer") · src/app/projects/[slug]/_components/wo-state-filter/wo-state-filter.tsx:34 + wo-board/wo-board.tsx:55
- [ ] frd-05-work-orders · /projects/<slug>?tab=work-orders · asymmetric row labels for the two sibling filter rows ("FILTRAR:" for FRD vs "ESTADO:" for state); consider "FRD:" / "Estado:" · src/app/projects/[slug]/_components/wo-frd-filter/wo-frd-filter.tsx:~90
- [ ] frd-05-work-orders · /projects/<slug>?tab=work-orders · state->label map is declared twice (wo-state-filter STATE_OPTIONS and wo-board COLUMNS); export one map from wo-board and consume it (DR-115 follow-up; drift is guarded meanwhile by wo-frd-filtered-board.review.test.tsx "carry EXACTLY the board's column labels") · src/app/projects/[slug]/_components/wo-state-filter/wo-state-filter.tsx:34
- [x] frd-05-work-orders · /projects/<slug>?tab=work-orders · a long FRD slug chip (e.g. `frd-23-materialized-stats-read-model`) had its tail visually swallowed by the next wrapped chip: the toggle `<button>`'s `maxWidth: 200px` shrank the flex-item BOX below the nowrap text's natural width while `overflow: visible` let the text render past it, so the following chip's opaque pill painted over the overflow. FIXED (visual-qa sweep): dropped the `maxWidth` cap, chip now sizes to content and wraps as a whole unit · src/app/projects/[slug]/_components/wo-frd-filter/wo-frd-filter.tsx:50
- [ ] frd-04-project-workspace · /portfolio?project=<name> (Resumen tab) · REQ-04-003 specifies a THIN header progress bar (visible on all tabs) PLUS a separate DETAILED "Mission Objectives" bar (done/total + %, prototype's "🎯 Objetivos de la misión" card) living inside the Summary tab body; the build renders only the header's `ObjectivesBar` (`ProjectWorkspace.tsx:310`) and has no second bar inside the Resumen panel content. Structural gap between the current frd.md wording and blueprint.md's older single-bar description (blueprint.md:42) — needs owner/spec reconciliation, not a bounded visual-qa fix · src/app/projects/[slug]/ProjectWorkspace.tsx:270-320
- [ ] frd-02-ideas-board · /board (card detail → Campaña tab) · the room label chip ("1 Investigación") over a bright/busy zone background (bookshelves) reads noticeably less legible than the approved `la-campana.html` mock at the same spot, even though the scrim is the documented verbatim token `rgba(12,17,19,.82)` (design-tokens.json `party.partyStructural.scrim.label_0_82`) applied correctly and at full coverage (verified via pixel sampling — no clipping, no missing background). Likely a room-size/asset-scaling difference vs the mock rather than a wrong token; touches the shared `Room` primitive also used by FRD-06 (Party/Fragua), so left for a designer call rather than touched here · src/components/modules/party/Room/Room.tsx:171-186

- (agent-inferred) 2026-09-26 gate FRD-03: un WO puede cumplir sus AC sobre un componente huerfano (sin importador de produccion) y dejar el REQ invisible en la ruta real; el reviewer debe montar la ruta real (page -> datos -> componente montado) antes de dar por cumplido un REQ de UI. Date.parse de V8 acepta "N/A 3" y "2026-02-30" -> validar forma ISO + dia real antes de parsear.
- (agent-inferred) 2026-09-26 FRD-02 gate: gate worktrees live under .pandacorp/run/, so the block-dangerous hook refuses ANY `rm` (even a temp src file, or `rmSync(..., {recursive})` text inside a heredoc) run from there; write probe files with the Write tool and remove temp copies with `mv` to scratch. vitest excludes `.pandacorp/run/**`, so a drift probe must be exercised from a temporary copy under src/.
- 2026-09-26 (agent-inferred) V8 Date.parse es laxo: "3" y "N/A 3" parsean a 2001-03-01 y "2026-02-30" rueda a 2026-03-02 — un lector de fechas que solo chequea NaN fabrica edades; exigir forma ISO + día de calendario real (round-trip Date.UTC) ANTES de Date.parse (WO-03-006 patch, DR-078).
- 2026-09-26 (agent-inferred) WO-04-008 patch: un validador de fecha basado solo en Number.isFinite(Date.parse(s)) no falla ruidoso: V8 acepta '3' y 'N/A 3' como 2001-03-01 y hace rollover de '2026-02-30' a marzo; y un 'YYYY-MM-DD' parseado se lee como medianoche UTC (desplaza el dia en UTC-5 de 19:00 a 24:00). Fix: regex ISO estricta + round-trip y/m/d via Date.UTC, fecha sola tomada verbatim como dia local, fecha-hora por getFullYear/getMonth/getDate locales, diff en dias Date.UTC (DST-safe). Tests con NOW construido de componentes locales (new Date(y,m,d,h)) para no depender del TZ del runner. Nota aparte: changes.ts coerceString trunca un timestamp YAML sin comillas a su dia UTC (toISOString().slice(0,10)).

# Decision Log — Factory

Decisions about operating the factory: constitution, standards, flow, and conventions. Most recent on top. See index and format in [DECISION-LOG.md](../DECISION-LOG.md).

## 2026-07-03 — BL-0016 cerrado: cableado el disparador del harness canónico para stacks provisionales B/C/D (DR-105, plugin v9.53.0)

**Qué:** se cerró el item de backlog BL-0016 cableando el **disparador** (no construyendo los tres harnesses especulativamente — eso es fuera-de-alcance por DR-105, cada uno se construye EN PRIMER USO). El paso 6 de `plugin/skills/architecture/SKILL.md` ahora lleva una **CANONICAL-HARNESS TRIGGER GATE** que corre ANTES de la copia VERBATIM (DR-059): resuelve el stack elegido y comprueba si existe su `verify.sh` canónico. Stack A lo tiene → sigue el camino VERBATIM sin cambios. Los stacks provisionales B (Hono) / C (FastAPI) no lo tienen (STACK.md solo-prosa con banner PROVISIONAL) → el gate DISPARA: la skill debe construir el harness canónico de ESE stack primero, replicando la forma de stack A (`verify.sh`+`canary.sh` fail-closed, config de lint/tipos, fixtures rotas DR-079, opt-out de gates de navegador DECLARADO como vacuous-pass para stack headless), probarlo con `verify.sh --canary`, o si es demasiado grande para hacerlo inline, DETENERSE y escalar al owner — **nunca** hand-rollear un `verify.sh` en silencio (el modo de fallo pre-auditoría que cerró DR-059). Stack D se maneja como stub-puntero que resuelve a stack C. Se marcó `BL-0016` como `done` y se actualizó la `nota` de DR-105 registrando que el disparador ya está cableado. **Por qué:** era la deuda diferida de la decisión del owner (audit-20, DR-105). Sin esto, el `/pandacorp:architecture` del primer proyecto no-Next intentaría copiar una plantilla inexistente y degradaría en silencio a un gate hand-rolled. Probado con un check scripteado (verify.sh solo en stack A; condición de disparo se cumple en B/C; D reconocido como redirect a C). Solo-skill → MINOR, sin bump de OVERLAY. Impacto (docs canónicos): `plugin/skills/architecture/SKILL.md`, `factory/decisions/registry.yaml` (nota DR-105), `factory/backlog/BL-0016-*.md`, `plugin/.claude-plugin/plugin.json` (v9.53.0), `plugin/docs/decision-log.md`.

## 2026-07-02 — Nueva regla propagada: cálculo de tier de modelo al delegar subagentes (CONV-12, DR-111, plugin v9.52.0)

**Qué:** a pedido del owner, se codificó una regla nueva (CONV-12) en `factory/standards/conventions.md` — junto
a la de idioma (CONV-1) y la de estilo de interacción (CONV-11) — que gobierna cómo el agente principal (en la
fábrica, en Mission Control y en cualquier proyecto construido con Pandacorp) elige el modelo de un subagente
cuando lo delega SIN especificarlo. Hoy, si no se especifica, el subagente hereda el modelo de la conversación
padre (Sonnet/Opus/Fable) — el problema concreto que reportó el owner: sesiones en Opus/Fable que lanzan fan-outs
de investigación tipo deep-research a ese mismo tier caro, agotando el presupuesto de tokens. La regla, en cambio,
exige **calcular el tier por la complejidad de la SUBTAREA**: haiku para trabajo mecánico/sin juicio (un commit,
un rename, un ajuste de texto de una línea), sonnet como piso por defecto (implementación, investigación en
fan-out, la mayoría de la ejecución de subagentes), opus para juicio genuino/alta complejidad (arquitectura,
revisión adversarial, red-team de una propuesta). **Fable queda explícitamente excluido del cálculo automático**
— nunca se elige solo; solo entra si el owner lo pide explícitamente, o si el agente ve un beneficio real y pide
confirmación antes de lanzarlo (corrección del owner durante la planificación). Escala hacia arriba, nunca hacia
abajo, si un tier menor resulta insuficiente (mismo espíritu empírico que el `reopen_count` de DR-073).
**Alcance, acotado dos veces igual que CONV-11:** NO reabre los agentes propios de Pandacorp (`plugin/agents/*.md`,
que ya fijan su `model:` estático en frontmatter) ni el escalado del motor de build (`pickWorkerModel`, DR-073/
DR-108, ya calibrado para las work orders) — cubre exclusivamente la delegación ad-hoc (tipos genéricos del
`Agent` tool, o un `Workflow` escrito fuera del motor de build). Severidad **SHOULD**, enforcement **manual**
(autocheck del agente antes de cada delegación sin modelo + spot-check del owner) — consistente con el catálogo
de cero MUSTs aspiracionales (es un juicio cualitativo, no lintable). Se propagó por el mismo canal DR-051 que
CONV-11: `conventions.md` → resumen en `AGENTS.md.tpl` + `.pandacorp/guide.md.tpl` (regla 11) → `CLAUDE.md` raíz
de la fábrica (regla 11, efecto inmediato en esta sesión). Registrada como `DR-111` en
`factory/decisions/registry.yaml`. **Por qué:** el owner agotaba presupuesto de tokens cuando un deep-research
(u otra delegación ad-hoc) corría todos sus subagentes al mismo tier caro que la conversación padre, sin ningún
cálculo de por medio. MINOR + OVERLAY 8.57.0 → 8.58.0. Ver `plugin/docs/decision-log.md`.

## 2026-07-02 — Nueva regla propagada: estilo de interacción del agente con el owner (CONV-11, DR-110, plugin v9.51.0)

**Qué:** a pedido del owner, se codificó una regla nueva (CONV-11) en `factory/standards/conventions.md` — junto a la regla de idioma (CONV-1) — que gobierna cómo el agente principal le responde al owner en el chat: marcar dónde empieza la respuesta real tras la narración de progreso (`---` + etiqueta en negrita), default conciso/escaneable/alto nivel (técnico solo si se pide o el tema lo exige — seguridad, dinero, pérdida de datos, acciones irreversibles), storytelling sobre jerga, visualizar flujos/algoritmos complejos con un widget/diagrama en vez de prosa, y red-team a propuestas que requieren juicio (nunca *rubber-stamp*) salvo saludos/preguntas simples. Severidad **SHOULD**, enforcement **manual** (no aspiracional — consistente con el catálogo de 0 MUSTs sin mecanismo). Se propagó por el mismo canal que ya usa la regla de idioma (DR-051): `conventions.md` → resumen en `AGENTS.md.tpl` + `.pandacorp/guide.md.tpl` (regla 10) → `CLAUDE.md` raíz de la fábrica (regla 10, efecto inmediato en esta sesión). Registrada como `DR-110` en `factory/decisions/registry.yaml`. **Alcance explícito, acotado dos veces a pedido del owner durante la planificación:** NO toca copy/microcopy de ninguna app (sigue siendo del agente `copywriter` + i18n), y NO toca la comunicación interna orquestador↔subagentes (`Agent`/`Task`), que se queda como esté. **Por qué:** el owner se desenganchaba de las conversaciones porque no distinguía la narración de progreso de la respuesta real, y las respuestas eran muy verbosas/técnicas/condescendientes por defecto con sus propuestas. MINOR + OVERLAY 8.56.0 → 8.57.0. Ver `plugin/docs/decision-log.md`.

## 2026-07-02 — Upgrade completo del catálogo de estándares (proposal 24, plugin v9.50.0)

**Qué:** el owner aprobó el plan completo de `docs/proposals/24-standards-catalog-audit-2026-07-02.md`
(auditoría de 3 agentes: contenido + inyección E2E verificada + research del estado del arte) y las 4
fases quedaron implementadas el mismo día, en orden 1→2→4→3. **(F1) Plantilla + registro:** el
"estándar ejecutable" es ahora formal (preámmbulo `Dominio · Severidad · Enforcement` + Regla + "cómo
se verifica" con el check NOMBRADO — una regla sin mecanismo nombrado no se admite + Por qué), los
17 archivos existentes migrados; nace `factory/standards/rule-registry.md` (~116 reglas con estado
`wired`/`manual`/`aspirational`) y `check-standards.sh` que valida la plantilla, el contrato
standards↔rules (DR-051) y avisa si reaparece un MUST aspiracional. **(F2) Cero MUSTs
aspiracionales:** los 6 detectados (API-1 contrato RFC 9457, STRUCT-2 aislamiento de capa de datos,
SEC-2 CSP, SEC-7 safeFetch, SEC-8 rate limiting, PRIV-3 PII en logs) quedaron cableados como gates
fail-closed con canario (los 3 primeros — incluido el Header-Scan Gate que `web-security.md`
prometía y NUNCA existió en el template) o con checks manuales NOMBRADOS + helpers canónicos (los
demás). Hooks nuevos evaluados y descartados: el Stop-gate + verify.sh ya cubren los invariantes.
**(F4) Inyección afinada:** capa always-loaded podada ~236→169 líneas (presupuesto de contexto —
lo que Biome/tsc ya enforcean sale de la prosa), canario post-upgrade BLOQUEANTE, vigilancia de
drift de overlay en `sync-portfolio`, auditor standards↔rules en el checker. **(F3) Catálogo
ampliado:** 8 estándares nuevos (error-handling, data-modeling, auth, resilience, background-jobs,
dependency-lifecycle, feature-flags, **ai-implementation** — disciplina de código escrito por IA:
comentarios que no narran, sin árboles de docs paralelos, verificar contra la versión instalada,
citar LESSON-NNNN), `patterns.md` profundizado como respaldo canónico React/Next (caching/tags,
forms, Suspense, client-state), 6 reglas inyectables nuevas, y `stack.md` con golden paths B (Hono)
y C (FastAPI, absorbe el viejo D) reales + puntos de partida (CLI, WXT, Astro, agentes IA).
**Por qué:** la auditoría encontró el meta-patrón promesa-sin-mecanismo en la capa de estándares
(~18 MUSTs sin gate), 3 estándares raquíticos, ~18 dominios ausentes y la capa siempre-cargada
sobre el presupuesto que degrada el cumplimiento de los agentes. **Impacto:** `factory/standards/*`
(25 archivos), `plugin/` v9.50.0 / OVERLAY 8.56.0 (detalle en `plugin/docs/decision-log.md`),
Manual de MC actualizado (páginas `estandares-y-reglas` y `stacks-golden-paths`, TSX + md, DR-046).

## 2026-07-02 — Loop de autoaprendizaje v2 IMPLEMENTADO completo (proposal 23, plugin v9.49.0)

**Qué:** el owner aprobó el plan completo de `docs/proposals/23-self-learning-loop-v2.md` y las 4
fases quedaron implementadas el mismo día. (F1) El motor: cosecha OBLIGATORIA al close-out de
`implement` con stamp `last_harvest` en status.yaml; la rutina programada `pandacorp-memory-review`
pasó de semanal a **diaria con umbrales** (trabaja solo con ≥20 notas / ≥7 días / proyecto en release
sin cosechar; silenciosa si no hay nada); hook de Stop `capture-lessons-reminder.sh` (una vez por
sesión: "¿te corrigieron? ¿algo falló y se arregló?" — la corrección es el evento de captura). (F2)
Recuperación medida: `factory/memory/INDEX.md` (1 línea por lección activa con su `trigger:` "úsala
cuando…", ediciones delta), campo `trigger:` requerido por el validador y backfilled en las 22
lecciones; los 7 agentes builder + `guide.md.tpl` leen INDEX primero y CITAN `LESSON-NNNN` en el
artefacto durable; `count-lesson-citations.sh` cuenta las citas y actualiza
`times_applied`/`applied_in` determinísticamente (probado: idempotente). (F3) Validez:
default-reject en el refinado, `candidate → active` exige corroboración de OTRO build (o provenance
owner/CI), lecciones de fracaso por contraste, y la poda por "nunca usada" CONGELADA hasta medir ≥3
proyectos. (F4) La escalera: ≥3 proyectos citando → `promotion: proposed` automático; gate de
`learn` explícitamente tierado (SHOULD+verificador auto con aviso; MUST/DR/skill siempre owner);
pase de reflexión; el cierre de FRD-17 (Propuestas + memory-health) quedó como change en la cola de
Mission Control (`mc-frd17-propuestas-memory-health-loop-v2`).

**Por qué:** dos auditorías consecutivas (2026-06-22 y 2026-07-01) encontraron el loop "prescrito
pero sin cablear" (22 lecciones con `times_applied: 0`, cero jobs, cosecha en prosa); el dolor del
owner era exactamente el motor ausente. Base externa: Letta sleep-time, Mem0, Devin, EDV,
ReasoningBank, ACE, Voyager. **Impacto:** DR-047 reescrito en `factory/decisions/registry.yaml`;
`factory/memory/{README,INDEX,_lesson-template,LESSON-*}`; plugin v9.49.0 (OVERLAY 8.55.3) — detalle
en `plugin/docs/decision-log.md`; Manual `concepts/autoaprendizaje.md` actualizado (DR-046);
proposal 23 → IMPLEMENTED.

## 2026-07-02 — Auditoría del loop de autoaprendizaje (DR-047) + plan v2 propuesto

**Qué:** auditoría exhaustiva del flujo de mejora continua (memory/learn/librarian) pedida por el
owner ("tengo que correr /memory y /learn a mano; ¿cómo lo mejoramos y validamos?"), cruzada con
research externo (Letta sleep-time, Mem0 A.U.D.N., Devin suggested-knowledge, EDV, ReasoningBank,
ACE, Voyager). Diagnóstico medido: la captura funciona (22 lecciones, ruteo DR-103 ok) pero TODO lo
posterior es prosa sin mecanismo — recuperación jamás demostrada (`times_applied: 0` en las 22),
cero jobs programados pese al "SWEEP ACTIVATED" de DR-047, cosecha de close-out documentada pero no
invocada, cola de promoción sin superficie (FRD-17 parcial), poda que condenaría todo el store, y
`validate-backlog.sh` inexistente con colisiones vivas (BL-0010/0011 duplicados). Plan de 4 fases en
`docs/proposals/23-self-learning-loop-v2.md`: (1) motor — cosecha ejecutable al close-out + barrido
programado + captura por corrección via hook; (2) recuperación — INDEX.md inyectado al overlay +
`trigger:` + medición determinista por citas; (3) validez — default-reject, corroboración cruzada,
lecciones de fracaso por contraste; (4) escalera — promoción por uso + cerrar FRD-17 + tier medio
real. Sustrato se mantiene (markdown+git validado por la literatura); el hueco es cableado.

**Por qué:** el dolor del owner es exactamente la mitad ausente del loop (el motor); la literatura
(EDV: 10% de memorias erróneas degrada el rendimiento medible) exige además validación antes de que
la recuperación se cablee en serio. **Impacto:** `docs/proposals/23-self-learning-loop-v2.md`
(PROPOSED, pendiente de ok del owner; nada implementado). Confirma y absorbe proposal 17 P0.2 y
proposal 20 P1-6/P1-7.

## 2026-07-02 — DR-096 recalibrado: el merge NO borra el worktree de la sesión viva (BL-0023)

**Qué:** el invariante de visibilidad de DR-096 cambia: ya no es "worktree sobreviviente = trabajo
sin mergear" sino "worktree con rama SIN mergear o árbol sucio = pendiente; sobreviviente mergeado y
limpio = cascarón de sesión removible". Causa: `merge-queue.sh` borraba el directorio DONDE ESTÁ
PARADA la sesión que lo invocó — el cwd de la sesión quedaba colgando, el siguiente mensaje moría
con "Path … does not exist" y todo proceso en segundo plano se cancelaba con el reinicio forzado
(recurrente, 3× solo el 2026-07-02). El borrado pasa a `ExitWorktree` (el harness, que además
restaura el cwd); fallback: `git worktree remove` desde fuera. `pending-work.sh` lista los
cascarones aparte ("♻ removible") sin contarlos como pendientes.

**Por qué:** el borrado vivía en la capa equivocada — un script jamás puede mover el cwd de la
sesión. **Impacto:** `plugin/templates/shared/.pandacorp/{merge-queue.sh,pending-work.sh,guide.md.tpl}`
(v9.48.0), overlay de Mission Control sincronizado, `factory/standards/build-orchestration.md`,
`factory/decisions/registry.yaml` (DR-096), `factory/backlog/BL-0023` (done).

## 2026-07-02 — Observability standard gains the build telemetry producer map

**Qué:** `factory/standards/observability.md` suma la sección **"The build telemetry pipeline —
producer map"**: la tabla única de los 4 canales de señal del build — `dashboard-events.ndjson`
(liveness/narrativa; emisores EMIT/GATE_EVENT/ACHIEVEMENT/WO_COMMIT + SupervisorTick + hooks),
`track.jsonl` (duraciones reales, DR-086), `status.yaml` (`running` + `supervisor_heartbeat`,
cruce DR-066) y el frontmatter de WOs (LA estructura, DR-050/DR-087/DR-092) — con el contrato de
consumidor en una línea ("estructura del frontmatter, vida de eventos+heartbeat, duraciones del
track, honestidad del cruce"). Contraparte navegable: página nueva del Manual de Mission Control
(`la-fragua.md`) + blueprint §7 del FRD-06 (DR-046, mismo change).

**Por qué:** pedido del owner (2026-07-02): documentar el Party/La Fragua de punta a punta para
que cualquier agente entienda quién escribe cada señal y qué puede leer de dónde, sin
redescubrir el flujo. El conocimiento estaba disperso (motor, FRDs, contexto de sesión); el
estándar es la casa del lado productor. **Impacto:** `factory/standards/observability.md`;
Mission Control `docs/frds/frd-06-party/blueprint.md` + `content/manual/concepts/la-fragua.md` +
su `docs/decision-log.md`.

## 2026-07-01 — Granularity calibrated with real-build numbers: DR-100 gains a floor + concrete ceiling; DR-073 gains the fix-forward mandate

**What:** The owner-approved close of the granularity audit (evidence mined from the
personal-page-v2 and Mission Control builds). `build-orchestration.md` §2 now pins the WO size band
with MEASURED numbers — target ~25–50 min / ~1.5–4k LOC; ceiling ~4k LOC (rejects escalate
superlinearly above it: 3.0 vs 0.8 average rejects); floor ~20 min (the per-FRD gate costs a fixed
~9 min → a tiny feature pays >100% overhead; the PM merges it or it goes to the change queue).
DR-100 amended accordingly + a new readiness-gate assertion: declared `artifacts` must be COMPLETE,
not merely disjoint (under global waves an undeclared output can collide across FRDs). DR-073 gains
the FIX-FORWARD MANDATE: a bounded ≤~30-line fault must exit as findings→patch, never a bare
failure (80% of real first-gate fails had ≤6-min fixes; the revert path cost ~1.5h of a run's 2.2h
rework). The blueprint template's Build Plan now REQUIRES the DAG table; the engine records
`review_end` on every gate exit and `wo_reopen` per revert so the durable timeline stops
under-reporting rework.

**Why:** replace adjectives ("coarse", "small enough") with the empirical sweet spot, and make the
convergence path prefer the cheap fix over the expensive revert — without weakening any gate.

**Impact:** `factory/standards/build-orchestration.md` §2, `factory/decisions/registry.yaml`
(DR-100, DR-073), plugin v9.46.0 (agents, blueprint template, engine — see
`plugin/docs/decision-log.md`), Manual `el-pipeline.md`. Baseline for validation: the ppv2 run's
metrics (first-pass 3/8, rework ~30% of wall-clock).

## 2026-07-01 — Build orchestration: GLOBAL WAVES replace the strictly-sequential FRD-by-FRD loop (BL-0021, DR-050 amended)

**What:** The build standard now schedules each wave from the READY work orders of ALL FRDs
(`dependsOn` satisfied + artifacts disjoint across the wave, DR-060) instead of finishing one feature
before starting the next; the per-FRD review gates queue and run SERIALIZED at wave boundaries so
their whole-project checks always see a quiet tree. The trust boundary (one gate per FRD, quiet-tree
whole-project verification, Option B single commit writer) is unchanged — only the scheduling unit
moved from "feature" to "ready set".

**Why:** Owner-confirmed with evidence (2026-07-01): personal-page-v2's `track.jsonl` shows six
independent 1-WO FRDs building single-file for ~4.5h after the foundation verified, in `powerful`
mode (wave 8) — DR-100 right-sizing produces small FRDs, which had made the intra-FRD wave a no-op.

**Impact:** `factory/standards/build-orchestration.md` (§2 "Global waves"), DR-050 in
`factory/decisions/registry.yaml`, engine rework in the plugin (v9.45.0 — see
`plugin/docs/decision-log.md` for the verification evidence), `factory/backlog/BL-0021` → done,
Manual narrative `construccion-desatendida.md` (Paralelismo — oleadas globales).

## 2026-07-01 — Design-skill audit, phases A+B+D: canvas relay automation + on-disk tracker + canvas journal + closing sweep + engine routing (DR-109)

**What:** The owner audited the design phase after `personal-page-v2` (`docs/proposals/22-design-skill-audit-2026-07-01.md`)
and approved phases A+B+D. New DR-109: the EXPLORE+Claude-Design path is driven by a numbered stateful
algorithm (`plugin/skills/design/references/canvas-procedure.md`) — outbound prompts by the best transport
(claude-in-chrome browser / `pbcopy` clipboard: one Cmd+V per round / manual) with every prompt persisted;
inbound POLLED via `DesignSync list_files` (the agent never asks "¿ya generó?"); an on-disk
`docs/design/canvas/tracker.md` (every `ui:true` screen enumerated up front) checked by the design advance
gate; per-round journaling into `canvas/log.md` + `iteration.md` (DR-032 now explicitly covers external-tool
rounds); a closing bidirectional component-reconciliation sweep; and an engine routing table (Claude Design
default on greenfield EXPLORE / HTML fallback / extract on ADOPT-VISUAL / in-repo on brownfield+post-freeze).
**Why:** on personal-page-v2 the owner was the message bus both directions, screens were skipped until asked
page-by-page, page-level components never entered the system gallery, and `iteration.md` recorded "1 clean
round" over a many-round canvas reality; the DR-058/DR-101 content fixes were already in place but the
MECHANICS (relay, completeness state, journaling, routing) were not — and the old mega-paragraph procedure
was Opus-shaped (Sonnet would drop items; the tracker externalizes the state). **Impact:** registry
(+DR-109), `factory/standards/design.md` §1c/§11/§12/§13, `plugin/skills/design/` (SKILL.md + new
`references/canvas-procedure.md`), MC Manual `el-pipeline`. Plugin v9.43.0; no OVERLAY change. **Phase C spike
VERIFIED same day:** via claude-in-chrome the agent opened the PersonalPage v2 project on claude.ai/design
in the owner's logged-in browser, submitted a test prompt into the canvas chat and read the reply — the
browser transport (relay rung 1) works end-to-end (recorded in DR-109's nota + `standards/design.md` §1c).
Pending: phase E (Opus+Sonnet validation on the next UI project).

## 2026-07-01 — Implement-speed audit, phases 3+4: builder context packs + cheap mechanical tier (DR-108) + Party producer events (closes BL-0002/0019/0020)

**What:** Phase 3 — DR-108: the build planner extracts per-WO context packs (WO file path + verbatim EARS
AC lines) that the engine injects into every builder prompt; the solo builder folds its self-test +
Status Note into one spawn (the FRD gate remains the trust boundary); mechanical steps (per-WO commit,
wave dispatch stamp, safe-point, rollup sync, archive, notify) run on the cheap `MECH` tier (haiku).
Phase 4 — the engine emits the `achievement`/`gate` Party events FRD-06 always expected (BL-0020), stamps
`IN_PROGRESS` at wave dispatch (BL-0002, LESSON-0003 promoted), and the MC-side trophy fallback is queued
in Mission Control's own change queue. **Why:** two same-model spawns per WO + one-line builder scope were
the token sink and the first-attempt-failure cause; the Party "no conecta" was a pure producer gap (0
achievement/gate events in 13MB). **Impact:** registry (+DR-108), `build-orchestration.md` §5+§6,
`quality`/skills/agents untouched beyond phase 1+2, Manual `el-pipeline` synced, backlog BL-0002/0019/0020
closed, LESSON-0003 promoted. Plugin v9.41.0, OVERLAY 8.55.0.

## 2026-07-01 — Implement-speed audit, phases 1+2: scoped e2e gate (DR-106) + reject-cycle convergence budget (DR-107, closes BL-0001/0017/0018)

**What:** The owner audited why the personal-page-v2 build took ~14h45m and ~20% of a Max plan, and approved
a 4-phase improvement plan; phases 1+2 shipped now. **Phase 1 — wall-clock (DR-106):** `verify.sh --since`
(the per-FRD gate) now also scopes the BROWSER layer — only `smoke` + `shell` run (the e2e face of the
DR-072 blocking lenses); `visual` + `responsive` moved to the FULL unscoped run (close-out), and the
intermediate re-verifiers (Visual QA after its fixes, the hardening security stage) run `--since` instead of
a second/third full suite. Evidence: 11 FRD gates each paid the whole `workers:1`-serialized Playwright
suite. **Phase 2 — convergence (DR-107 + BL-0001):** the DR-073 patch gains a SELF-REPAIR budget (a red its
own edits introduced is fixed in place, ≤2 internal cycles — the incident: a 1-line i18n fix was discarded,
and its 1,397-line WO rebuilt, over a trivial TS2345 in the patch's own new test); a failed patch returns a
DISCRIMINATED cause — `gate-test-defective` routes to a new `repairGateTest` (an independent reviewer fixes
the defective TEST, never discarding a correct build — LESSON-0002 `promotion: approved`); the revert
PRESERVES reviewer-authored / Status-Note-referenced test files (`.pandacorp/run/preserved-tests/`); and
reopened WOs get ONE IN-RUN RETRY from the clean base before deferring to the next pass (a deferral re-paid
baseline + full re-plan + sync + safe-points). The satisfiable-adversarial-test convention (viewport rules)
landed in `reviewer.md` + `quality.md`.
**Why:** Both reverts of WO-01-004 were procedurally correct under the old contracts and still wasted
~2-2.5h + a large token share — the contracts, not the agents, were the defect. `reopen_count` stays the
single budget; DR-072's split gate is untouched.
**Impact:** `factory/standards/build-orchestration.md` §5+§6, `factory/standards/quality.md` (DR-015),
`factory/decisions/registry.yaml` (DR-106, DR-107, DR-073 refined-by), `factory/backlog/` (BL-0001 done,
BL-0017/BL-0018 filed+done), `factory/memory/LESSON-0002` (promotion approved), plugin engine + verify.sh
template + `implement` SKILL.md + `reviewer.md`, Manual `concepts/el-pipeline.md` (DR-046 sync). Plugin
v9.39.0→v9.40.0 (MINOR), OVERLAY 8.53.0→8.54.0. Phases 3 (context packs + cheap-model mechanical steps) and
4 (Party events) follow.

## 2026-07-01 — Audit-20 EXECUTED in full: the owner approved 100% and the whole plan shipped (DR-104/DR-105 + 10 DR amendments)

**What:** The owner approved the complete audit-20 plan ("quiero solucionar todo, el 100%") and it was
executed in five groups, all landed on `main`: **G0** backlog id collision fixed (BL-0010b→BL-0014,
BL-0011b→BL-0015) + NEW `validate-backlog.sh` (id-uniqueness, next-free-id) + `/bug` drainable + `/upgrade`
active-build guard + dead card enums; **G1** release gated on hardening EVIDENCE in both engine paths
(closes BL-0012) + the DR-069 drain/decide-unblock/archive triangle cabled IN-ENGINE at every FRD boundary
+ templates born post-DR-069 (closes BL-0007; BL-0004/0005 closed as already-fixed-in-code); **G2** honest
gates — DR-100 by a fresh agent, gate stamps (`readiness_gate`/`grounding_gate`) + a REAL `/implement`
preflight re-check, the DRAFT→ACTIVE flip owned (spec/architecture), mechanical DR-004 prod-deploy gate +
hard-reset SHA allowlist (NEW **DR-104**), independent post-patch verification (DR-073 amend), `difficulty`
into the WO template; **G3** loops closed — PRD/events/research templates gain the DR-035/041/042/043/074
anchors, DR-047 RETRIEVE + `times_applied` wired into all 7 builder agents, weekly scheduled tasks created
(review-launch Mondays, memory review Thursdays), backlog drain ritual; **G4** sweeps + doctrine —
blueprint→architecture / DR-085 / DR-052 / DR-090 / DR-069-vocab sweeps (constitution §23 included),
build-orchestration §8/§9 duplicates renumbered (→§10/§11, cross-refs updated), registry hygiene (DR-011
propose-only discard, DR-020 supersession, DR-082..084 tombstones, DR-092/057 pointers, fencing-token claim
removed, NEW `factory/decisions/README.md` index), rules↔standards re-mirrored + NEW
`factory/standards/accessibility.md` (24px gated floor / 44px design target), coverage gaps (backup/restore,
incident response, cost monitoring, data migrations), stacks B/C/D declared PROVISIONAL (NEW **DR-105**, NEW
BL-0016 for the harness on first use), funnel P2 tail (`score_rationale`, selection verb `/spec`, adopt
ports+telemetry, anonymized example card), 12 orphan worktree manifests removed.
**Why:** The audit's headline — promise-without-mechanism — meant each fix either CABLES the promise
(engine/hook/validator/scheduled task) or corrects the claim. Six owner decisions resolved in the batch:
(1) hard-reset → SHA allowlist; (2) patch → independent verify; (3) pnpm; (4) DR-011 → propose-only;
(5) stacks B/C/D → declared provisional + BL-0016; (6) 24px gated floor / 44px design target.
**Impact:** ~90 files across plugin/factory (commits `1ba9f5c`..`470a861` + the version bump). Plugin
v9.38.2→**v9.39.0** (MINOR), OVERLAY 8.52.1→**8.53.0** (engine + shared templates — projects upgrade on
their next `/pandacorp:upgrade`). Backlog: 5 done / 11 open (validator green, next free BL-0017). Registry:
105 ids (100 real + 3 tombstones + DR-104/105). Detail: `plugin/docs/decision-log.md` (2026-07-01).
**Follow-ups (open, not lost):** the MC Manual hand-authored narrative pages (DR-046) need a sync pass for
the changed flows (hardening-gated release, in-engine drain, gate stamps, /change-only routing) — MC is a
product project, routed via its own change queue; the remaining open BL items (BL-0001/0002/0003/0006/
0008/0009/0010/0011/0013§2/0014/0015/0016) drain via the new ritual.

## 2026-07-01 — Full end-to-end factory process audit (proposal 20)

**What:** Owner-requested audit of the ENTIRE factory process (Mission Control excluded) — funnel
(discover→recommend), spec→design→architecture, the build engine + change flows, release + post-launch +
self-learning, the doctrine layer (constitution/17 standards/100-DR registry/rules/stacks), and the
end-to-end state machine. Six parallel read-only auditors, deduped against proposal 19 + BL-0001..0013 +
the 2026-06-14/22 audits; top claims re-verified directly. Written up as
`docs/proposals/20-factory-process-audit-2026-07-01.md` (plane 2, DR-103).
**Why:** The owner asked for the complete map of weak spots before planning the next improvement round.
Headline: the recurring disease is **promise-without-mechanism** — canonical docs assert enforcement that
no code implements (implement's claimed DR-100/102 preflight re-check doesn't exist; DR-069's drain is
zero-implemented in the engine and `/bug` files an undrainable `status: pending`; a second hardening-free
path to `phase: release` beyond BL-0012; `/upgrade` can rewrite the engine mid-build; the prod-deploy gate
is prose-only vs infra.md's deny-rule claim; review-launch has never run; memory RETRIEVE is wired to 1 of
8 agents). Plus live store breakage: duplicate backlog ids (2×BL-0010, 2×BL-0011) with MC's fail-loud
reader consuming them. Four P0s, ~20 P1s, a long P2 tail, 6 contradictions needing owner decisions, and a
4-phase improvement plan with ~18 candidate BL items — all in the proposal.
**Impact:** NEW `docs/proposals/20-factory-process-audit-2026-07-01.md`. No behavior changed (read-only
audit; one micro-fix: stray tool-call tags removed from the tail of the gitignored `factory/profile.md`).
Actionable findings route to `factory/backlog/` BL items when the owner approves the plan (DR-103).

## 2026-07-01 — validate-memory.sh now enforces LESSON id uniqueness (BL-0013 fixed)

**What:** Added an id-uniqueness gate + a filename-number↔`id` consistency check to
`plugin/scripts/validate-memory.sh`. It now fails (exit 1, listing the colliding files) if two lessons
share an `id`, or if a file's `LESSON-NNNN` filename number disagrees with its frontmatter `id`.
**Why:** The validator checked each file's frontmatter in isolation but not cross-file id uniqueness, so a
mis-numbered `LESSON-0005/0006` pair (created without scanning that the store had grown to 0020 via a
parallel harvest) collided with existing ids and still passed "OK" — the owner caught it, not the tooling.
**Context:** Proven on a deliberate dup-id fixture (INVALID + `duplicate id …`) and a clean pass on the
real store. Tooling script only — no plugin/overlay version bump. Residual (low priority, not blocking,
now that the gate hard-blocks it): BL-0013 §2 — have the `/pandacorp:memory` harvest + `/pandacorp:learn`
flows scan for the next free id at creation time (prevention on top of the gate).
**Impact:** `plugin/scripts/validate-memory.sh`, `factory/backlog/BL-0013-*.md` (closed).

## 2026-06-30 — Three planes separated by durability + a factory backlog; memory is not an action queue (DR-103)
**What:** Drew an explicit line the factory was blurring: **durable knowledge** (retrieved forever) vs **actionable work** (opened → done → closed) vs **narrative proposals** (decided once → archived). New policy **DR-103** and a new store **`factory/backlog/`** — the factory's actionable, closeable work queue for its OWN tooling (plugin, build engine, templates, standards machinery, hooks), the plane-3 home the factory lacked (a project already has one: `.pandacorp/inbox/changes/` via `/change`; the factory never mirrored it for itself). A **routing rule** (would a future agent RETRIEVE this? → `factory/memory/`; must someone DO-and-CLOSE it? → `factory/backlog/`; a research doc decided once? → `docs/proposals/`; BOTH? → split, defect to backlog + generalizable lesson to memory, linked) is now enforced at TWO points: the `librarian`/`/pandacorp:memory` at harvest, AND **`/pandacorp:learn` as the front-door router** (new step 0) — it was the only factory front door but had no route for a plain fix/defect, so an actionable factory-tooling change now files a `BL-*` item instead of being forced into a standard. `CLAUDE.md` gains a "Changing the factory itself — route by plane" table (the scheme, mirror of a project's routing). **Migrated the store:** 3 of 4 existing lessons (L-0002/03/04 — all anti-pattern/`promotion: proposed`) were actually build-engine/upgrade DEFECTS mis-filed as lessons; each was **split** — the actionable fix → `factory/backlog/BL-0001/0002/0003`, the durable anti-pattern rewritten to pure "why" (file-by-file plan removed) and `promotion` reset to `none` (they leave Mission Control's promotions queue — the surface the owner found confusing). L-0001 (a research-derived principle, `promotion: none`) stays pure knowledge. **Bridged proposals:** the audit `docs/proposals/19`'s open FACTORY-TOOLING findings were extracted to `factory/backlog/BL-0004..0009` (the narrative stays as history, with a routing note); its Mission-Control-specific drift findings route to MC's own change queue, not the factory backlog.
**Why:** The owner saw LESSON-0002 in the promotions queue and felt — correctly — that a bug to fix does not belong in a "lessons learned" store; memory should be only durable things-we-learned to apply to a similar future situation. Deep-research (deep-research skill: 23 sources, 25 claims 2/3-adversarially verified, 2 refuted) UNANIMOUSLY confirmed it: durable memory and an actionable queue are two stores with OPPOSITE lifecycles (A-MEM, MemGPT/Letta, ExpeL, the 2026 Memory-for-Agents survey; Google SRE lesson-vs-action-item "each lesson maps to ≥1 AI"; ITIL Known-Error-DB vs Problem Record), and piling one-off/resolved items into a learning store degrades retrieval ("temporal memory contamination"). Follow-up web research (RFC/ADR vs backlog) settled proposals as plane-2 RFCs that FEED the backlog, not merged into it. Store named "backlog" not "inbox" per the owner (the factory has no inbox; `.pandacorp/inbox/` is the project→factory channel).
**Impact:** **NEW** `factory/backlog/` (`README.md` = plane model + routing rule + item schema, `_item-template.md`, `BL-0001..BL-0009`); `factory/decisions/registry.yaml` (DR-103); `factory/memory/README.md` ("What this is NOT" + routing rule); `factory/memory/LESSON-0002/0003/0004-*.md` (split + `promotion: none` + `BL-*` links); `docs/proposals/19-*.md` (routing note); `CLAUDE.md` (backlog in "What's here" + the "Changing the factory itself" routing table); `plugin/agents/librarian.md` + `plugin/skills/memory/SKILL.md` (harvest routing) + `plugin/skills/learn/SKILL.md` (step 0 front-door router) → plugin v9.37.0→**v9.38.0** (MINOR), no OVERLAY change (factory-internal). Validated: `validate-memory.sh` green (4 lessons active). Plugin-side history: `plugin/docs/decision-log.md`. **Follow-ups (flagged, not done here):** the MC Manual "three planes / factory backlog" concept page (DR-046 narrative sync); a Mission Control surface for the factory backlog (needs `/iterate` + gate); and the Mission-Control drift findings from audit-19 routed to MC's `.pandacorp/inbox/changes/`.

## 2026-06-30 — Back-port: stack-a-nextjs biome.json → biome 2.5.1 form (OVERLAY 8.52.0)
**What:** Fixed `plugin/templates/stack-a-nextjs/biome.json` to `"$schema": .../2.5.1/...` + `"preset": "recommended"` (was the stale 2.5.0 schema + deprecated `"recommended": true`). Bumped `OVERLAY_VERSION` 8.51.0→8.52.0 and plugin 9.36.0→9.36.1.
**Why:** During personal-page-v2's overlay upgrade, the DR-059 conformance overwrite reverted the project's correct biome-2.5.1 config to the stale template form; biome 2.5.1 ERRORS on it under `--error-on-warnings`, red-locking the project's whole-project baseline gate on resume (the build blocked with `baseline:error`). DR-076 back-port so `/upgrade` stops shipping the broken form to every Next project. Captured as `factory/memory/LESSON-0004` (+ proposed upgrade-skill hardening: run the gate after conformance; detect project-config-newer-than-template).
**Impact:** `plugin/templates/stack-a-nextjs/biome.json`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`, `factory/memory/LESSON-0004-*.md`. Project unblocked in-tree (personal-page-v2 commit 34934fc, last_green updated). Activation: `claude plugin update pandacorp@panda-corp` + projects' next `/upgrade`.

## 2026-06-30 — Gate canary now covers EVERY fail-closed gate (DR-079 extended)
**What:** On the owner's go-ahead ("cover everything"), extended the DR-079 canary from three checks to all eight fail-closed gates — biome, structure-guard, DR-100, tsc, madge, vitest, knip, and Playwright spec discovery. A full Playwright browser run and the advisory `doc-lint.sh` are intentionally out of scope (rationale in the registry/standard). Coverage is now complete for what's sensible to canary. Implementation + versions in `plugin/docs/decision-log.md` (v9.34.0, OVERLAY 8.51.0); doctrine updated here: `factory/decisions/registry.yaml` (DR-079) and `factory/standards/quality.md` (advisory list + the canary is no longer a backlog item).
**Why:** Conformance proves a gate's config matches the template; only the canary proves the gate still FAILS on bad input. Extending it to every gate closes that meta-gap across the board (constitution principle 24).
**Impact:** `factory/decisions/registry.yaml`, `factory/standards/quality.md`. Validated against Mission Control (8/8 gates RED on broken input) + a negative test (a clean fixture correctly flagged the gate as rotted → not a tautology). The only remaining gate-hardening backlog item is the greenfield-only fail-closed doc-lint.

## 2026-06-30 — Gate canary built (DR-079 amended) — the anti-cheat assertion is no longer vacuous
**What:** Acted on the owner's go-ahead to build the DR-079 gate canary (the P1-6 backlog item). A real harness now ships and is wired into the flow; the standard + registry are updated to match. Details + versions in `plugin/docs/decision-log.md` (v9.33.0, OVERLAY 8.50.0). Canonical doctrine updated here: `factory/decisions/registry.yaml` (DR-079 amended — BUILT) and `factory/standards/quality.md` (the advisory list now says the canary ships as a real harness covering biome / structure-guard / DR-100, with tsc/vitest/knip/Playwright as the documented next additions).
**Why:** Conformance (DR-059/076) proves a gate's config FILE matches the template; it never proved the gate still FAILS on bad input (a rule can be present but disabled, an exit code swallowed). The canary closes that meta-gap (constitution principle 24: "every gate is periodically proven to still go RED on a deliberately broken input").
**Impact:** `factory/decisions/registry.yaml` (DR-079), `factory/standards/quality.md` (advisory gates list). Implementation (the harness + skill wiring + plugin/overlay bumps) is logged in `plugin/docs/decision-log.md`. Validated by running it against Mission Control (3/3 gates rejected broken input). The greenfield-only fail-closed doc-lint remains the one open P1-6 backlog item.

## 2026-06-30 — Owner directives on audit follow-ups: doc-lint proactive, responsive two-tier, write-gate stays a nudge
**What:** Three owner decisions on the factory-flow audit's open items:
- **doc-lint: loud + proactive, still non-blocking (DR-077 amended).** Keep `doc-lint.sh` advisory (never blocks — no red-lock), but surface findings PROMINENTLY and FIX them proactively in the same pass when touching docs, rather than merely reporting. Recorded in `quality.md` (advisory list) + registry DR-077.
- **Responsive Gate: off-target is no longer a full skip (DR-074 amended).** A desktop-only app must still not BREAK on mobile (and a mobile-only app on desktop): the FUNCTIONAL "nothing hidden / everything reachable" subset (clipped-off-canvas + occluded `<main>`) is checked at the off-target width as an ADVISORY report (loud, never blocks); cosmetic imperfection there is accepted as low-priority. Owner principle: "nada se debe romper, debes poder acceder a todas las funcionalidades, aunque se vea feo en la plataforma que no es la objetivo." Recorded in `quality.md` + `design.md` (§ responsive) + registry DR-074.
- **Write-gate stays a NUDGE, not a hard block (point 4 / DR-044/DR-553 confirmed).** The owner confirmed: keep the freedom to make changes through a skill OR directly for now; do NOT hard-block ad-hoc edits. Revisit later — as the workflow matures we may restrict to skills-with-factory, but for now it stays free/circumstantial. No change made (current behavior already matches); recorded so the decision is durable.
**Why:** Owner clarification (2026-06-30) refining audit findings P1-6 (advisory-vs-hard gates), P2-15 (desktop-only responsive) and P1-7 (write-gate enforcement).
**Impact:** `factory/standards/quality.md` (Responsive Gate paragraph + advisory list), `factory/standards/design.md` (responsive §), `factory/decisions/registry.yaml` (DR-074 + DR-077 amendments). Implementation + plugin/overlay bumps logged in `plugin/docs/decision-log.md` (v9.31.0, OVERLAY 8.49.0). The canary fixtures (P1-6 (2)) and a hard write-gate remain explicitly NOT done (the latter by owner decision). MC Manual narrative (estandares-y-reglas / stacks-golden-paths) sync remains the tracked queue item `mc-manual-narrative-sync.md`.

## 2026-06-30 — Factory-flow audit: git-policy reconciliation, explicit hard-vs-advisory gates, `/change` front door
**What:** Resolved the factory-doctrine findings of the factory-flow audit (`docs/proposals/19-factory-flow-audit-2026-06-30.md`):
- **Git policy contradiction resolved (P2-13).** `factory/standards/quality.md` §CI still described PR-based **protected branches** as the model, contradicting constitution §11 + DR-040 (the solo operator commits/pushes to `main` directly; the quality gate is the `reviewer` + `verify.sh`, not human PR review). Rewrote §CI so the **local gate is primary**, direct-main is the default, and **GitHub Actions / PRs / protected branches are an OPTIONAL external-governance layer** (for a remote/team or belt-and-suspenders CI), never a replacement for the local gate.
- **Hard vs advisory gates made explicit (P1-6).** Added a clear list to `quality.md`: HARD/fail-closed = tests, tsc, biome (`--error-on-warnings`), knip, madge, smoke/visual/responsive/shell browser gates, the structure guard, and the DR-100 readiness gate; ADVISORY/report-only = `doc-lint.sh` (DR-077), bless-provenance (DR-080), the reviewer's visual-fidelity NITS punch-list (DR-072); the canary (DR-079) is vacuous-until-fixtures-installed. Documented that doc-lint/canary are advisory **by design** (a hard doc gate would block adopted/partial-spine projects; a canary run before its fixtures exist would red-lock builds) and flagged two backlog hardening items: greenfield-only fail-closed doc-lint, and a real template canary harness + broken fixtures.
- **`/pandacorp:change` surfaced as the front door (P1-11).** `CLAUDE.md`'s operation list now names `/pandacorp:change` as the single owner front door for any change (feature/adjustment/bug, DR-069), with `iterate`/`bug`/`decide` as its engines — it was reachable but under-surfaced.
**Why:** The audit found two incompatible Git operating models in the docs and a scattered/implicit gate-strength story; agents reading the standard need one unambiguous answer. Surfacing `/change` matches the DR-069 simplification.
**Impact:** `factory/standards/quality.md` (§CI rewritten + new HARD-vs-ADVISORY subsection), `CLAUDE.md` (operation list). No code/behavior change. The standards Reference catalog in Mission Control auto-derives; the hand-authored Manual narrative sync (gates/git) is the already-tracked queue item `mc-manual-narrative-sync.md`. P1-7 (a hard write-gate block) deliberately NOT changed — already settled in the registry (a hook can't distinguish a significant change from a micro-edit; the persistent nudge is the chosen enforcement).

## 2026-06-29 — Skill renamed `/pandacorp:blueprint` → `/pandacorp:architecture` (the command reflects the whole architecture phase)
**What:** Renamed the architecture-phase SKILL command from `/pandacorp:blueprint` to `/pandacorp:architecture` — the phase produces the *whole* platform architecture (stack + data model + ADRs + per-FRD blueprints + work orders + the digest), not just "a blueprint", and the command now matches the existing `phase: architecture` and the design→architecture step. **Surgically the COMMAND only** — the per-FRD `blueprint.md` artifact, `blueprint-template.md`, the "Build Plan" section and the `BP-NN` ids are PRESERVED (they are part of the `FRD > FDD > design-tokens > blueprint > work order` source-of-truth hierarchy; renaming them would break every project's docs and the readiness gate). The `pandacorp:architect` AGENT (distinct namespace) is unchanged. `git mv plugin/skills/blueprint → architecture`; updated the H1 and every LIVE command reference across skills (scaffold/spec/design/implement/work-orders), templates (STACK guides, rules/work-orders READMEs), `factory/decisions/registry.yaml` (the live canonical pointer + the DR-032/100/102 command shorthands), the factory `README.md`/`CLAUDE.md`/`standards/infra.md`, the Manual narrative (concepts/el-pipeline, el-plugin, tutorial) and Mission Control's UI + next-step map + the FRD-02 next-step doc. Historical decision-log entries (which name the old command/path as they were) were left intact.
**Why:** Owner ask (2026-06-29): "blueprint" undersells the phase, which is the full architecture. Aligning the command with the phase name removes the mismatch.
**Impact:** `plugin/skills/architecture/` (renamed from `blueprint/`, H1 + new digest-emit step 9c), `factory/decisions/registry.yaml` (live pointer + shorthands), `README.md`, `CLAUDE.md`, `factory/standards/infra.md`, plugin templates (stack-*/STACK.md, docs/work-orders-readme, rules/README), `plugin/scripts/verify-before-stop.sh`; Mission Control (next-step.ts, card.ts, phases.ts, PipelineDiagram, wo-empty, manual narrative + the matching tests). Plugin v9.28.2→**v9.29.0** (MINOR — renamed command). **No OVERLAY change** (no `templates/shared` file referenced the command — only `blueprint.md` the doc, which stays). MC Manual skill catalog auto-derives from the renamed dir. Plugin-side log: `plugin/docs/decision-log.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-29 — Architecture digest artifact: the architecture skill emits `arquitectura-resumen.md` (powers MC's Arquitectura tab)
**What:** The renamed architecture skill now EMITS `.pandacorp/comms/arquitectura-resumen.md` at the end of the phase (gitignored Spanish, owner-facing — same class as `spec-resumen.md` from `/spec`), following a new `plugin/templates/docs/architecture-digest-template.md`. The digest is a high-level, scannable Spanish narrative: a stack table (capa · elección · por qué), a CONDITIONAL data-model section (the "Sin BD — contenido como código" branch for content-as-code projects, or the DB entities), communication/services, and one card per FRD (its blueprint one-liner + its work orders). The implementation-plan DAG, the ADRs and the env vars are NOT duplicated in the digest — Mission Control reads those LIVE from the real artifacts so they never drift.
**Why:** Mirrors the spec digest so the owner sees the architecture natively on the board (DR-046 — the factory's operable surface surfaces in MC). Single-source: narrative in the digest, machine-truth (WOs/ADRs/env) live.
**Impact:** `plugin/skills/architecture/SKILL.md` (step 9c), **NEW** `plugin/templates/docs/architecture-digest-template.md`. Same plugin bump as the rename (v9.29.0). MC consumes it via the new Arquitectura tab (see `mission-control/docs/decision-log.md`). Plugin-side log: `plugin/docs/decision-log.md`.

## 2026-06-30 — Blocked merge must reach the OWNER, in the conversation (DR-099 refinement)
**What:** Strengthened DR-099's "loud merge hand-back" so a `merge-queue.sh` hand-back (rebase conflict 10 / red gate 11 / busy main 12) is loud on **two** channels, not one: (a) the script fires a desktop notification (it already did, in the template), AND (b) **the agent MUST surface the blocked merge IN THE CONVERSATION immediately** — what's blocked, why, that the work is preserved on its branch, and what's needed — and must NOT go off doing silent workarounds. Made explicit that this is the session's OWN deliverable and is therefore EXEMPT from the conversation-isolation rule (3) that silences *other* sessions' reds. Also fixed a concrete drift: Mission Control's `.pandacorp/merge-queue.sh` was STALE — its `fail()` lacked the desktop-notification the template carries — so even channel (a) was dead; synced it to the template.
**Why:** A Party-liveness change (REQ-06-018) was built green in a worktree but its merge-queue handed back repeatedly (the parallel FRD-10 build left `main` red on its OWN dead exports; then a worktree-e2e env gap couldn't find gitignored `factory/profile.md`). The owner NEVER HEARD — the agent stayed silent (over-applying DR-099's "don't narrate other sessions") and went off doing `:1987`/`:3100` workarounds; the owner only found out by asking and warned the finished work could be **lost** when the conversation ended. Two holes: the stale script (no notification) and a rule that only mandated a desktop notification (easily missed), never an in-chat tell. The owner asked to fix the rule so a blocked merge can never be silent.
**Impact:** `factory/decisions/registry.yaml` (DR-099 default (2) + nota), `factory/standards/build-orchestration.md` §9 (Loud merge hand-back bullet), `plugin/templates/shared/.pandacorp/guide.md.tpl` + `mission-control/.pandacorp/guide.md` (DR-099 paragraph), `plugin/templates/shared/.pandacorp/merge-queue.sh` (comment) + `mission-control/.pandacorp/merge-queue.sh` (synced `fail()` notification), `plugin/.claude-plugin/plugin.json` (v9.28.2). DR catalog in the Manual auto-derives; no narrative-flow change. Activation: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-29 — Back-port: canonical knip.json recognizes Next 16 `src/proxy.ts` (DR-076/DR-059)
**What:** Added `src/proxy.ts` to the `entry` list of the canonical `plugin/templates/stack-a-nextjs/knip.json` (kept `src/middleware.ts` for Next ≤15 back-compat). Next 16 renamed the middleware file convention to `proxy.ts`; without it as a knip entry, a project's real `src/proxy.ts` isn't traced (a non-fatal "no matches" hint on the stale `middleware.ts` entry + risk of its imports being flagged). Propagated in the SAME change to the live project `personal-page-v2` (its conformance-checked copy now matches the template). Bumped `OVERLAY_VERSION` 8.46.0→8.47.0 so every other Next project picks it up on the next `/pandacorp:upgrade`.
**Why:** DR-076 — a conformance-checked gate file is fixed at the SOURCE (the template), never in-place, else `/upgrade` reverts it and the template stays stale for all projects. Found while grounding `personal-page-v2` to the installed Next 16 stack (the same DR-102 currency class of bug).
**Impact:** `plugin/templates/stack-a-nextjs/knip.json` (+`src/proxy.ts`), `plugin/templates/OVERLAY_VERSION` (8.47.0), `plugin/.claude-plugin/plugin.json` (v9.28.1); project: `personal-page-v2/knip.json` + `.pandacorp/status.yaml` (overlay 8.47.0). No DR change (application of DR-076/DR-059). Activation: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-29 — /blueprint gains a repo-grounding validation gate + grounded artifacts (versions/.env.example/AGENTS.md/explicit NFRs) (DR-102)
**What:** Extended `/blueprint` (the architecture phase) with the external-fidelity half DR-100 was missing. Five changes: **(1) a REPO-GROUNDING VALIDATION GATE** (new step 9b, before any doc goes ACTIVE; re-checked by `/implement` preflight) — a fresh reviewer validates every concrete claim in `architecture.md` + blueprints + work orders against the ACTUALLY-INSTALLED scaffold (file paths, framework APIs/config current for the installed major via the AGENTS.md version-matched docs, dep versions installed-or-pinned-in-a-WO, hard guarantees verified against a real `pnpm build`). **(2) Pin dependency versions AT THE INSTALL POINT** (install at the chosen version or write the exact `pnpm add pkg@version` into the foundation WO), never prose-only. **(3) Generate `.env.example`** as a standard blueprint artifact. **(4) Generate/refresh `AGENTS.md`** pointing agents at the framework's version-matched bundled docs (the create-next-app pattern). **(5) NFRs explicit per-WO** in the work-order completeness-20% (security + performance + a11y + reduced-motion).
**Why:** On `personal-page-v2`, immediately after `/blueprint` produced a strong, DR-100-clean plan AND installed the stack green, an adversarial audit still found 4 BLOCKERS — all "context blindness" against the real installed stack (docs said `middleware.ts`, Next 16 renamed it `proxy.ts`; missing `setRequestLocale` made `[locale]` DYNAMIC, breaking the $0/static guarantee; theme-attribute mismatch; dep versions pinned only in prose). Verified deep-research (22 sources, 25 adversarially-verified claims) confirms validating artifacts against the real repo before codegen is the #1 AI-build error-reducer (arXiv:2604.05278), AGENTS.md→version-matched docs measurably cuts outdated-API errors (Next.js /evals), and NFRs are systematically dropped (~14%, arXiv:2511.12884). Honored caveat (SSRN 6515898): spec VOLUME/quality alone does NOT reduce defects — so this GROUNDS the plan, it does not inflate it.
**Impact:** `factory/decisions/registry.yaml` (new DR-102, extends DR-100); `plugin/skills/blueprint/SKILL.md` (step 6: version pins at install + `.env.example` + version-matched `AGENTS.md`; new step 9b repo-grounding gate); `plugin/templates/docs/work-order-template.md` (completeness-20% names explicit NFRs). Plugin v9.27.0→v9.28.0 (MINOR). Factory-internal skill/template guidance (consumed by `/blueprint` + architect/reviewer) — **no rule-library/OVERLAY change** (no per-project rule injected). MC Manual DR/skill catalogs auto-derive; the visible pipeline phases + owner gates are UNCHANGED (9b is an internal architect check within the existing architecture phase, like DR-100's readiness gate), so no narrative-page edit. The 4 blockers were fixed in `personal-page-v2` the same session. Plugin-side log: `plugin/docs/decision-log.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-28 — The design phase's Claude Design path, batch 2: full screen set, weight=importance, real content, per-surface playbooks, per-screen rubric (DR-101)
**What:** Second batch of Claude Design EXPLORE-path learnings from `personal-page-v2`, codified ON TOP of DR-058 §1c (do NOT redo: real connection, exhaustive anti-omission brief, image placeholders, mandatory motion, breakpoints by `target_platforms`, pull-and-review, token authorship). Seven rules. **(1) Two-stage cadence, never stop at the landing:** Stage 1 generates the design *system* (owner approves it), then Stage 2 generates **ALL** the UI-FRD screens — **enumerated by the skill from the FRDs** (`ui:true` → its screen(s)) — one screen at a time with pull-and-review; the owner must never have to ask "where are the other pages?". **(2) Weight = importance (hierarchy/proportion):** the primary action/conversion gets prominent treatment (the shared `cta-band`), never an undersized one-line link (the contact CTA shipped as a tiny link); no context-free hero opening — lead with title+context+at-a-glance (recruiters skim <60s). **(3) ONE shared closing `cta-band`** reused on every page (reinforces DR-062) — a per-page ad-hoc "let's talk" link is both a hierarchy and a cohesion defect. **(4) Real content, never invented** — lists/cards/stack/archive from the owner's REAL sources, balanced lengths; placeholders only where data is genuinely pending and visibly marked. **(5) Explicit prompts** — name the element + the action + what stays ("Delete the ENTIRE element including its text and link; replace with nothing"). **(6) Per-surface playbooks** (case study / Now / blog index / blog post / contact / projects-archive / stack) captured once. **(7) A per-screen review rubric** the agent runs (pulling each real file, not from memory) before the owner gate.
**Why:** Owner ask via `/pandacorp:learn` (batch 2). The skill's canvas guidance was still too loose: only the landing generated until the owner asked page-by-page; the conversion CTA came out as a one-line link; prompts were ambiguous (a vague "remove the note" left stray text); per-surface quality had to be re-researched live. Source lesson: `personal-page-v2` memory `claude-design-skill-improvements` (now `promotion: approved`).
**Impact:** `factory/decisions/registry.yaml` (new DR-101; DR-058 + DR-062 notes extended with a pointer); `factory/standards/design.md` (new §9 hierarchy, §10 real content, §11 two-stage cadence + explicit prompts, §12 per-screen rubric; §7 extended with the shared closing cta-band; §1c checklist extended; wired table renumbered §9→§13 + 2 rows); `plugin/skills/design/SKILL.md` (EXPLORE procedure → two-stage cadence + prompt discipline + hierarchy + real content; DR-058 rule + a new DR-101 rule); **NEW** `plugin/skills/design/references/surface-playbooks.md`. Plugin v9.26.0→v9.27.0 (MINOR). Factory-internal design-phase guidance (consumed by the design skill + designer/reviewer) — like DR-058/DR-062, **no rule-library/OVERLAY change** (it does not inject a per-project rule). MC Manual standards/DR catalogs auto-derive — no hand-copy; the design phase's visible flow + owner visual gate are UNCHANGED (only the internal generation discipline got sharper), so no narrative-page edit. Plugin-side log: `plugin/docs/decision-log.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-28 — Precise blueprints + work orders for an ideal AI build: a readiness gate, a blocking marker, right-sizing, anti-omission (DR-100)
**What:** Hardened the build-side document generation (`/blueprint` + `/work-orders`, plus `/spec` where it fed them) so the docs come out precise — neither more nor less than needed — and the 100%-AI build is ideal. Seven pieces: **(1) Readiness gate** (a BMad `bmad-check-implementation-readiness` analogue): before a blueprint goes `ACTIVE` — and re-checked by `/implement`'s preflight — the architect asserts fail-closed that every `REQ`→component, **every `AC` is covered by exactly one work order**, the data model has no `TBD`, `dependsOn` is acyclic+complete, wave artifacts are disjoint by design, the foundation is complete, each backend WO's `docs/api/<wo>.md` contract is materialized before its consumer, and **no `[NEEDS CLARIFICATION]` survives**. "A blueprint with holes produces ambiguous work orders" becomes a CHECKED gate. **(2) `[NEEDS CLARIFICATION: q]` blocking marker** (Spec Kit): an unresolved, build-changing question blocks (distinct from a resolved `[ASSUMPTION]`); cabled fail-closed in `verify.sh` (a marker in an `ACTIVE` doc is RED). **(3) Right-sizing with a named ceiling:** the coarse-WO policy (DR-050) is the floor; the ceiling is now concrete (too big to review in one sitting / past the model's reliable context zone / mixes >1 concern → split). **(4) Anti-over-specification:** a blueprint/WO that "reads like code" is over-specified — specify at the altitude (contracts/shapes); a canonical example beats enumerating every case. **(5) Completeness 20%** (anti-omission): AI builders ship the ~80% happy path and drop the production 20% (error path per failure mode / validation beyond types / empty-partial states / observability / security) — the FRD names the applicable items, the WO Status-Note enumerates met/unmet AC (surfacing, NOT self-certification — rule 4 intact), the reviewer's correctness lens verifies it (a missing item BLOCKS). **(6) Structured contracts** (input/success/errors/invariants). **(7) EARS calibration** (>~3 preconditions → a table). The factory's design was already validated by the state of practice; this round wires the precision that lived only as prose.
**Why:** Owner ask (voice): make `/blueprint` + `/work-orders` produce consistently precise docs so the AI build is ideal, with freedom to touch `/spec`. Deep-research (28 sources, 125 claims, 24 confirmed via 2/3 adversarial verification, 1 refuted) cross-walked with the internal skill map: the FRD→blueprint→work-orders shape is validated by Kiro/BMad/Spec Kit/EARS — bones correct, the gap was ENFORCEMENT. This is the **build-side counterpart of DR-095** (which fixed the spec/FRD side). One claimed statistic was REFUTED in verification and is not used; the **single-concern-per-task** principle it touched stands on its own (curse-of-instructions + mixing-work-types-is-an-anti-pattern) and is what the work-order ceiling encodes.
**Impact:** `factory/decisions/registry.yaml` (DR-100); `factory/standards/build-orchestration.md` (§2 right-size ceiling + new §3b the readiness gate), `factory/standards/quality.md` (Completeness 20% section). Plugin side: `plugin/skills/{blueprint,work-orders,spec}/SKILL.md`, `plugin/agents/{architect,product-manager,reviewer}.md`, `plugin/templates/docs/{frd,work-order,blueprint}-template.md`, `plugin/templates/stack-a-nextjs/verify.sh`, plugin v9.25.0→v9.26.0 (MINOR). MC Manual skills/agents/DR/standards catalogs auto-derive — no hand-copy; narrative `concepts/el-pipeline.md` updated (the architecture→build flow now names the readiness gate). Plugin-side log: `plugin/docs/decision-log.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-27 — Commit-time silence on foreign WIP (DR-099 mechanism 3, hardened)
**What:** The conversation-isolation rule (DR-099 mechanism 3) now says explicitly what to do at COMMIT time when `git status` shows files this session did not touch (another session's WIP): `git add` ONLY this session's own files (never `git add -A`) and commit — do NOT ask the owner whether to commit, and do NOT mention the other session's files. Committing only your own files is correct, permission-free, and leaves the foreign WIP untouched (DR-093 no-sweep); pausing to narrate or ask is the cross-session noise the rule removes.
**Why:** Owner ask — a PersonalPage V2 session, mid plugin-fix, ran `git status`, saw another session's `TimelineView.tsx` WIP in the shared factory checkout, and STOPPED to narrate it + ask "commit only my 5 files or leave it?". That's the SAME cross-session noise the owner finds stressful, but a DIFFERENT source than the Stop-gate dump fixed earlier today (that was the hook; this is the agent's own commit-time narration). DR-099 said "don't narrate other sessions" but didn't spell out the commit moment, so the agent defaulted to asking. This closes that gap. Note: still a SOFT rule (agent behavior, not hook-enforced) — the durable fix remains worktree isolation, where `git status` only shows the session's own work; and any rule only reaches a session after `claude plugin update` + `/upgrade` + restart.
**Impact:** `factory/decisions/registry.yaml` (DR-099 mechanism 3 extended); `factory/standards/build-orchestration.md` (§9 Conversation isolation bullet); `plugin/templates/shared/.pandacorp/guide.md.tpl` + live `mission-control/.pandacorp/guide.md`; MC Manual `concepts/estandares-y-reglas.md`. Plugin v9.24.0→v9.25.0 (MINOR). Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-27 — The design phase's Claude Design path: real connection + an exhaustive, anti-omission brief (DR-058)
**What:** Hardened the EXPLORE-path Claude Design procedure from real-session experience (`personal-page-v2`). (1) **Connection facts corrected:** the generative engine is the owner-driven web canvas (`claude.ai/design`) — the agent CANNOT pilot it; the agent only has the `DesignSync` TOOL for FILE SYNC. Auth is a one-time `/design-login` (interactive terminal); `/design-sync` is PUSH-ONLY React code (not a generator, not a pull); `/design` may be absent; **never assert command availability from the filesystem** (built-in skills are bundled at runtime — defer to the owner's autocomplete). (2) **The generation brief MUST be exhaustive,** with an **anti-omission completeness checklist** the agent injects and then verifies by **pull-and-review** before the gate: every component (nav/menu, footer, avatar, media frames, case-study card + detail header, post card, social links, podcast block, form fields) + **every state** (success/error/empty/loading/404/…) + **image placeholders** (so layouts render and you see where photos go) + a **mandatory motion layer** (no flat "only link-colour" systems — motion is part of the proof for developer/portfolio products) + **breakpoints by `target_platforms`** (desktop / mobile / responsive→mobile+tablet+desktop). (3) **Token authorship:** on EXPLORE+Claude Design the canvas proposes the palette from the brief's qualitative direction; the agent verifies AA + freezes.
**Why:** Owner ask during `personal-page-v2`'s design phase. The skill's prior DR-058 text assumed a `/design`-style command and agent-driven generation that don't exist; and a thin brief let Claude Design ship only tokens+brand (deferring the whole gallery + screens, with no menu/avatar/cards/social/forms/states) and a visually FLAT first system. Both failures are now structural rules, not lore.
**Impact:** `factory/standards/design.md` (new §1c — connection, procedure, token authorship, the anti-omission checklist); `factory/decisions/registry.yaml` (DR-058 `default` + `nota` corrected/extended); `plugin/skills/design/SKILL.md` (EXPLORE bullet + DR-058 rule rewritten). Plugin v9.23.0→v9.24.0 (MINOR). MC Manual standards/DR/skills catalogs auto-derive — no hand-copy; the design phase's visible flow + owner visual gate are UNCHANGED (only the internal Claude Design mechanism got precise), so no narrative-page edit. Plugin-side log (`plugin/docs/decision-log.md`) pending. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-27 — The Stop gate stays SILENT on a foreign red (DR-099 mechanism 4)
**What:** The Stop gate (`verify-before-stop.sh`) now ATTRIBUTES a red before surfacing it. `warn-adhoc-write.sh` records every edit of a session to `.pandacorp/run/sessions/<session_id>.touched` (gitignored runtime); on a red the gate compares the dirty + blamed files (by basename) against that record. If NONE of the session's edits are implicated, it ALLOWS THE STOP SILENTLY (logs to `run/foreign-red.log` for PULL, emits no stderr) instead of dumping another session's failure into the conversation. FAIL-CLOSED: a red touching any file the session edited (dirty or blamed), or one it cannot attribute (no per-session record), still BLOCKS.
**Why:** Owner ask — in a shared checkout the whole-program gate fails on a PARALLEL session's in-flight WIP, and the Stop hook kept DUMPING that foreign failure into the conversation ("the gate is red because of the other session's FRD-21…"). The owner found that cross-session noise stressful and useless: "if it's not this session's, don't even mention it — only interrupt me if it genuinely needs my decision." DR-099 already SAID handle foreign reds silently, but the hook itself contradicted it. This gives the rule teeth at the gate. The needs-a-decision case (an unresolvable conflict) is still surfaced — by the loud merge hand-back (mechanism 2), not the gate.
**Impact:** `plugin/scripts/verify-before-stop.sh` (attribution + silent allow) + `plugin/scripts/warn-adhoc-write.sh` (per-session touched-files recorder); `factory/decisions/registry.yaml` (DR-099 default extended with mechanism 4 + refinement nota); `factory/standards/build-orchestration.md` (§9 new bullet); `plugin/templates/shared/.pandacorp/guide.md.tpl` + live `mission-control/.pandacorp/guide.md`; MC Manual `concepts/estandares-y-reglas.md`. Tested: 8-case attribution unit harness + end-to-end hook test (foreign→silent, own→block). Plugin v9.22.2→v9.23.0 (MINOR). Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-27 — Each project earns its own visual identity — never a house style (DR-098)
**What:** Design standard reinforced into an explicit rule: there is **no factory-wide default aesthetic**. Every project's look is chosen FOR THAT PROJECT, fit to its **audience / use case / purpose** (the EXPLORE path researches the domain/audience and that constraint drives the directions). **Mission Control's gamified pixel-art / RPG skin is MC's OWN identity, not a template** — never carried into another project by default; reusing one project's look for another is a defect (same class as ignoring an approved visual). Does not override the ADOPT-VISUAL path (an approved visual for the project still wins).
**Why:** Owner ask while re-speccing the personal-portfolio project (`personal-page-v2`): the gamified/RPG/pixel-art aesthetic must stay scoped to Mission Control; this project needs a **professional, trust-inspiring** look for a recruiter audience. The principle was already a parenthetical in `design.md §1`; the owner asked to elevate it to a rule for all projects.
**Impact:** `factory/standards/design.md` (new §1b); `factory/decisions/registry.yaml` (DR-098). MC Manual standards/DR catalogs auto-derive — no hand-copy; no flow/gate changed, so no narrative-page edit.
## 2026-06-27 — The visual gate honors target_platforms, like the responsive gate (DR-074)
**What:** `visual.spec.ts` no longer diffs a mobile-width snapshot for a desktop-only project — the per-surface visual test early-returns (vacuous pass) for the `mobile` playwright project when `target_platforms` doesn't include mobile, gated on the SAME `TARGETS_MOBILE` signal `responsive.spec.ts` already uses. Deleted MC's 6 now-obsolete `*-mobile-*` baselines; re-blessed the one desktop baseline (`portfolio`) that legitimately grew in height with data.
**Why:** MC is desktop-only, yet the visual gate kept diffing 390px-wide snapshots — just the desktop layout squished, pure noise that drifted with content/time and repeatedly RED'd the gate (it even blocked an unrelated merge this session). The responsive gate already skips for desktop-only; now `target_platforms` chooses the asserted widths for BOTH gates, killing this false-alarm class at the root instead of re-blessing forever. Early-return (not `test.skip`) to satisfy Biome `noSkippedTests`.
**Impact:** `plugin/templates/stack-a-nextjs/e2e/visual.spec.ts` (template) + `mission-control/e2e/visual.spec.ts` (materialized, byte-identical per DR-076); deleted `mission-control/e2e/visual.spec.ts-snapshots/*-mobile-mobile-darwin.png`; re-blessed `portfolio-desktop`. Plugin v9.21.0→v9.22.0 (MINOR). Builds on DR-074. Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-27 — Enforce the parallel-isolation rule; don't leave it soft (DR-099)
**What:** Three mechanisms turn DR-096's self-isolation from a documented rule into something an agent meets in the loop: (1) the PreToolUse write hook (`warn-adhoc-write.sh`) nudges to isolate when product CODE is edited directly in the SHARED main checkout (git-dir without `/worktrees/`) outside an active build — proactive, attributable, non-blocking; (2) `merge-queue.sh` fires a desktop notification on any merge that can't LAND (conflict/red-gate/busy-main) — a blocked merge is never silent; (3) the conversation-isolation rule — handle a foreign red silently, never narrate other sessions to the owner (cross-session status is PULL via Mission Control), never report "done" until in main.
**Why:** The agent built FRD-21 directly in the shared main checkout ("the tree is quiet") instead of a worktree, leaving uncommitted WIP that RED'd a parallel session's gate — exactly what DR-096 prevents, but DR-096 was a SOFT rule the agent rationalized past (and wasn't even live in that already-running session). The owner asked to make it enforced. A hard PreToolUse BLOCK was rejected: a hook can't reliably tell a significant change from a micro-edit, so it would trap legit work or mis-fire — hence a persistent in-the-loop nudge, not a lock.
**Impact:** `factory/decisions/registry.yaml` (DR-099); `factory/standards/build-orchestration.md` (§9 Enforcement); `plugin/scripts/warn-adhoc-write.sh` (isolation nudge) + `plugin/templates/shared/.pandacorp/merge-queue.sh` (loud hand-back); `plugin/templates/shared/.pandacorp/guide.md.tpl` + live `mission-control/.pandacorp/guide.md` (conversation-isolation clause); MC Manual `concepts/estandares-y-reglas.md`. Plugin v9.20.0→v9.21.0 (MINOR); `OVERLAY_VERSION` 8.45.0→8.46.0. Plugin side: `plugin/docs/decision-log.md`. Builds on DR-096/097.

## 2026-06-27 — Direct changes must advance the work-order state (DR-097)
**What:** Propagable overlay rule: whoever implements a change DIRECTLY (by hand, in OR out of `/implement`) owns moving its work order's `implementation_status` to match reality — `IN_PROGRESS` while building, `VERIFIED` the moment the green gate (`.pandacorp/verify.sh`, the objective scripts/CI) passes clean. Never strand finished, gate-green work at `IN_REVIEW`; the board derives the Kanban column from that field.
**Why:** The owner finished FRD-21 manually and the work orders stayed at `IN_REVIEW` (shown as not-Done) because the agent over-applied constitution rule 4 — treating the independent `reviewer` AGENT as mandatory for VERIFIED. Rule 4 forbids SELF-ASSESSMENT; the `verify.sh` gate IS the objective check, and it was green. So manual work must reach VERIFIED on a green gate, not wait for a reviewer nobody runs. Owner asked to codify it. Pairs with DR-096 (the manual parallel-session flow).
**Impact:** `factory/decisions/registry.yaml` (DR-097); `plugin/templates/shared/.pandacorp/guide.md.tpl` + the live `mission-control/.pandacorp/guide.md` (rule now active); MC Manual `concepts/estandares-y-reglas.md`. Plugin v9.19.0→v9.20.0 (MINOR); `OVERLAY_VERSION` 8.44.0→8.45.0. Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-26 — Parallel manual sessions: worktree isolation + serialized merge queue (DR-096)
**What:** A propagable standard for the owner's **manual parallel workflow** (N independent conversations launched by hand, *outside* `/implement`). A session about to make a **non-trivial code change** (same frontier as the write-gate; micro-edits stay in-tree) **self-isolates in its own git worktree** — the agent calls `EnterWorktree` itself, sanctioned by a standing overlay rule (NO orchestrator skill) — bootstraps with `.pandacorp/worktree-bootstrap.sh` (pnpm hardlink install, autoPort `launch.json`, secrets referenced not copied, per-worktree DB or `PANDACORP_FACTORY_ROOT`), and lands its green branch through a **serialized merge queue** `.pandacorp/merge-queue.sh` (atomic `merge.lock` in the shared git dir → rebase onto current main → integration gate → ff-merge → auto-remove worktree; a non-auto-resolvable conflict or red gate is HANDED BACK). Plus the **"not-mine → report, don't fix"** rule wired into `verify-before-stop.sh` (a foreign red is reported, never masked) and a gitignored **active-worktree manifest** for visibility. Every project declares 3 things: fixed-vs-autoPort ports, a reproducible bootstrap, and its per-worktree state-isolation strategy.
**Why:** Owner pain — fanning out agents in parallel kept flapping the gate: session A finishes clean but its whole-program `verify.sh` (tsc/knip/visual read the *entire* tree) REDs on session B's in-flight WIP, and A, blind to B, tries to "fix" code it never wrote. **Verified live this session:** the Stop hook RED'd on a `documentacion`-mobile visual diff caused entirely by a parallel session's uncommitted `globals.css`/`Banner` — zero edits from this session. You can't scope a whole-program gate (tsc is whole-program), so only isolation makes "my gate sees only my work" true. Distinct from `/implement`, which is collision-safe by construction (DR-060/086) and deliberately does NOT use worktrees; this is the uncoordinated case (operationalizes DR-093). Research-grounded (merge-queue semantics, atomic locks, `CREATE DATABASE … TEMPLATE`, pnpm hardlink store, the semantic-conflict ceiling).
**Impact:** `factory/standards/build-orchestration.md` ("Parallel manual sessions" section); `factory/decisions/registry.yaml` (DR-096); `plugin/templates/shared/.pandacorp/merge-queue.sh` + `worktree-bootstrap.sh` (new); `plugin/scripts/verify-before-stop.sh` (red attribution); `plugin/templates/shared/.pandacorp/guide.md.tpl` (self-isolation overlay rule); `mission-control/.claude/launch.json` (autoPort). **Visibility layer (DR-096 §7, owner-driven follow-up):** the invariant "a surviving worktree = unmerged work" + `plugin/templates/shared/.pandacorp/pending-work.sh` (new — unions surviving worktrees with `git branch --no-merged`, age/status, `--json`/`--notify`) + the MC surfacing spec (GLOBAL ambient "N pendientes" indicator in the persistent shell primary, per-project summary-tab breakdown secondary). Plugin v9.17.0→**v9.19.0** (MINOR ×2: isolation+merge-queue, then the visibility check); `OVERLAY_VERSION` →**8.44.0** (new `.pandacorp/` scripts + overlay rule, DR-051). Proposal: `docs/proposals/18-parallel-isolation-and-merge-queue.md`. Follow-ups: MC pending-merge UI (via `/iterate`), an optional thin `/pandacorp:merge` escape hatch. Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-26 — Spec quality: clarification gate (ask-before-generate, origin-calibrated) + complete-not-verbose FRDs (DR-095)
**What:** The product phase (`/spec` + `product-manager`) now (1) runs a **clarification gate** before generating — infer all you can as visible `[ASSUMPTION]`s, then ask only the material gaps, with a mode flag (`--ask`/`--auto`/`--infer`) defaulting by the idea's `origin` (new-idea → ask, discover → auto); and (2) requires the **FRD to carry the agent-critical fields** (data model, edge cases & error states, non-goals, a worked example, a contract sketch) written **complete-not-verbose** (over-specification overloads the agent — the engine already passes one slice per agent, DR-056). PRD stays thin.
**Why:** Owner wants the PRD/FRDs to be the IDEAL input for build agents — "documentation determines whether agents build well or badly." Deep-research (27 sources, 24/25 confirmed) backed the shape (spec-driven dev, EARS, Kiro/Spec Kit) and the two correctives: ambiguity → wrong build (so ask to resolve intent), and over-specification/context-overload is a real failure mode (so complete-not-verbose, NOT "as detailed as possible"). The strong "66% / d>1.2" clarification stat was refuted → framed as an alignment lever. Pattern modelled on PandaTrack's `create-frd-package` (3 discovery roles, "don't write until critical ambiguity is resolved"). A discovered idea's intent is inferred → more elicitation than a user-proposed one.
**Impact:** `factory/decisions/registry.yaml` (DR-095); `plugin/skills/spec/SKILL.md`, `plugin/agents/product-manager.md`, `plugin/templates/docs/{frd,prd}-template.md`. Plugin v9.16.0→v9.17.0 (MINOR); no `OVERLAY_VERSION` change. Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-26 — Visual coherence binds at the VALUE level, not just structural patterns (DR-062 reforzado)
**What:** `factory/standards/design.md` §7 (one-cohesive-app) now states explicitly that cohesion is enforced at the **token/value level** — colors, type sizes, spacing, radii, card/block styles — not only the structural pattern. A new page/section/tab MUST read its siblings and reuse their values; the factory prefers a **shared primitive/token map over a copied `*.styles.ts` constant** so surfaces can't drift. The injectable rule `plugin/templates/rules/styling-and-ui.md` gains a matching "Visual coherence" section.
**Why:** Live incident — Mission Control's new "Spec" tab and the existing "Propuesta" tab diverged (title size, description colour, block tints, card title colour: two style files re-deriving the same look); the owner saw a visible jump on tab switch and asked to codify coherence as a binding rule.
**Impact:** `factory/standards/design.md` §7; `plugin/templates/rules/styling-and-ui.md`. Plugin v9.14.0→v9.15.0 (MINOR). `OVERLAY_VERSION` bump owed on next overlay release (injectable rule text). Plugin side: `plugin/docs/decision-log.md`.

## 2026-06-26 — Enforcement batch: single-source state, concurrency, back-port detector (DR-092/093/094)
**What:** Three reinforcements (proposal 17 P2.6/P2.7/P2.8). **DR-092 (single source for derived state):** a value derived from the data layer that several surfaces show lives in ONE cached resolver; a second independent derivation is REJECTED at review (the data-layer analogue of DR-057). **DR-093 (concurrent sessions):** isolate a second session in its own worktree/branch by default; never sweep another session's WIP into your commit nor edit it to force the gate green; keep `decision-log.md`/`registry.yaml`/`plugin.json` append-only (`requiere_humano` — the isolation mechanism is an owner workflow choice). **DR-094 (back-port detector):** `/upgrade` must LOUDLY report any in-place gate-config divergence with the DR-076 back-port instruction BEFORE the (still unconditional) overwrite, so an in-place fix can't be silently reverted.
**Why:** All three are failures observed live this session: NV3-vs-NV1 (re-derived state), the parallel session breaking the shared gate 4× + plugin.json/decision-log collisions, and MC's biome `!**/*.py` nearly reverted by the upgrade overwrite (caught manually).
**Impact:** `factory/decisions/registry.yaml` (DR-092/093/094); `factory/standards/clean-code.md` (single-source rule, injectable) + `plugin/agents/reviewer.md` (quality lens) for DR-092; `factory/standards/build-orchestration.md` (concurrency section) for DR-093; `plugin/skills/upgrade/SKILL.md` (loud back-port detector) for DR-094. Plugin v9.10.0→v9.11.0 (MINOR); `OVERLAY_VERSION` 8.42.2→8.42.3 (clean-code rule changed, DR-051). Proposal: `docs/proposals/17-enforcement-and-learning-loop.md`.

## 2026-06-26 — Resilient visual-fidelity oracle + UI-FRD oracle enforcement (DR-091)
**What:** The reviewer's per-route fidelity check no longer no-ops when a project has no per-FRD `mocks/`. New **fallback chain (DR-091)**: per-FRD `mocks/<route>` → ELSE the FRD's `visual_source` (global prototype view/shard, rendered live + A/B) → ELSE a `ui: true` FRD with neither is a surfaced finding. Stays **advisory** (DR-072 — visual judges are reward-hackable; not a hard block). And `doc-lint.sh` now flags a `ui: true` FRD with no `fdd.md`, or empty `mocks/` AND no `visual_source` (the un-sharded surface that lets the check no-op).
**Why:** Forensic review (proposal 17) found the verified root cause of the long MC hand-fixing session: MC was never sharded (17/19 FRD `mocks/` empty, `visual_source` → global `index.html`), so the reviewer's `mocks/`-gated per-route check silently skipped 17 surfaces. Owner's own insight shaped the fix ("look in the FRD's mocks first, else the global html in design"). First P0 item of proposal 17.
**Impact:** `plugin/agents/reviewer.md` (fallback chain), `plugin/templates/shared/.pandacorp/doc-lint.sh` (UI-oracle completeness check), `factory/standards/build-orchestration.md` (visual-fidelity gate), `factory/decisions/registry.yaml` (DR-091). Plugin v9.9.0→v9.10.0 (MINOR); `OVERLAY_VERSION` 8.42.1→8.42.2 (doc-lint changed, DR-051). Proposal: `docs/proposals/17-enforcement-and-learning-loop.md`.

## 2026-06-25 — Review worktrees live under one canonical root, named per project (DR-090)
**What:** Sibling rule to DR-089, for the OTHER derived worktree: the manually-created **review/test worktree** (the "test the latest green build without stopping the agent" copy that `/implement` keeps pinned at `last_green_sha`, and that Mission Control's snapshot panel emits a `git worktree add` command for) MUST live under **`/Users/Shared/review-worktrees/<project>/`** — a `Shared`-level sibling of `Proyectos/`, named exactly after the project (no `-review` suffix; the root already says it's for reviews), **never** inside `Proyectos/` and **never** inside `.claude/` (tool-internal — risks Claude Code's worktree sweep deleting a deliberately-persistent review worktree). `Proyectos/` now holds only source checkouts; the two derived-worktree roots are `local-deployments/` (always-on prod deploy, DR-089) and `review-worktrees/` (ephemeral green-snapshot testing, DR-090).
**Why:** Same cleanup the owner asked for with the deployment: `<project>-review` cluttering `Proyectos/` looked like a stray project and confused them. They asked whether review worktrees should go in `.claude/worktree` or a dedicated root like the deployments → dedicated root chosen (`.claude/` rejected: the factory's review worktrees are persistent/manual and must stay out of Claude Code's auto-sweep). Owner-stated (2026-06-25).
**Impact:** Standard: `factory/standards/infra.md` (Worktrees → review-worktree root). Policy: `factory/decisions/registry.yaml` — **new DR-090**. Flow: `plugin/skills/implement/SKILL.md` (the `git worktree add` path) — **plugin v9.8.1→v9.8.2 (PATCH)**. MC: the snapshot command it shows — `mission-control/src/lib/snapshot/snapshot.ts` + tests + FRD-14 (AC-14-001.1/.2) + the Manual "probar" guide (`manualPages.tsx`). The stale `panda-corp-review` (FRD-17 era, 2026-06-18, 732 MB) was removed; the next `/implement` recreates it under the new root. **Manual (DR-046):** DR + standards catalogs auto-derive; the hand-authored "probar" guide updated in the same change → done.

## 2026-06-25 — Local deployments live under one canonical root, named per project (DR-089)
**What:** New convention for a project's **always-on local deployment** (the built, served snapshot of an `internal` tool — e.g. Mission Control on `127.0.0.1:1987`). It MUST live under **`/Users/Shared/local-deployments/<project>/`** — a sibling of `Proyectos/`, at the `Shared` level — in a subfolder **named exactly after the project/repo** (no `-live`/`-deploy` suffix), **never** co-located inside `Proyectos/` with the source checkouts. It is a **production build** (`next build` + `next start`) in an **isolated git worktree** (so its `.next`/`node_modules` never collide with the dev checkout), served always-on via launchd (KeepAlive) on the project's reserved port; live gitignored factory data is read from the main checkout via `PANDACORP_FACTORY_ROOT`. Redeploy = sync the worktree to green `HEAD` + rebuild + restart.
**Why:** Mission Control's local deploy was a worktree confusingly named **`panda-corp-live` sitting inside `Proyectos/`**, mixed in with the real source projects — so the owner couldn't tell what it was or whether to delete it, and it poisoned the visual gate (`reuseExistingServer` reused the stale snapshot server, masking dev changes). One predictable home for all local deployments removes the ambiguity and the cross-talk with dev tooling. Owner-stated (2026-06-25).
**Impact:** Canonical standard: `factory/standards/infra.md` (new "Local deployments" section). Policy: `factory/decisions/registry.yaml` — **new DR-089**. **Migration done** (MC): `git worktree move Proyectos/panda-corp-live → /Users/Shared/local-deployments/panda-corp`; updated `mission-control/.pandacorp/run/serve.sh` (cd) + `deploy-local.sh` (LIVE path); reloaded the launchd job (verified `:1987` serves from the new path). Release skill gains a pointer to DR-089 (**plugin PATCH**). **Manual (DR-046):** the DR + standards catalogs auto-derive; this is an infra location convention (no operator flow/gate changed) → no narrative edit.

## 2026-06-25 — Visual baselines mask live, variable-length data with `data-volatile` (DR-088)
**What:** Generalized the visual-fidelity gate's per-region exclusions (dashboard-banners / activity-log / summary-text — DR-076, commit b50fa7a) into one durable convention: any region whose CONTENT is **live, variable-length data** whose row count changes the page height (an idea-card grid, a proposals/feed list, an activity log) is marked **`data-volatile`** in the component and **masked** from the `fullPage` baseline (`visual.spec.ts` now hides `[data-volatile]` alongside the existing data-testid exclusions). The baseline asserts the page CHROME (layout, nav, headers, empty-state structure) deterministically; the masked region's correctness stays covered by its own unit/component/integration tests, and the shell+smoke gates still assert the route renders without console errors / error boundaries. Recommended marker shape: `style={{display:'contents'}}` so the live render is byte-identical and the marker only takes effect under the test stylesheet.
**Why:** Mission Control's gate kept failing FALSELY on `/board` and `/proposals` — both baselines were coupled to live factory data (idea cards; the four self-learning streams + promotions queue) plus a cold/warm render reflow, so the page height drifted (e.g. `/proposals` 9769→9986→10203px in one session; `/board` on every card add/discard/edit) and the screenshot mismatched even though the route rendered correctly. That forced a manual `--update-snapshots` re-bless on almost every idea-base change — whack-a-mole. A height-drifting data list is not a fidelity regression, so it must not turn the gate RED. (Aligns with Constitution §24: the gate should prove the chrome FAITHFUL, not re-bless drift.)
**Impact:** Canonical: `plugin/templates/stack-a-nextjs/e2e/visual.spec.ts` (the `[data-volatile]` exclusion + comment). Policy: `factory/decisions/registry.yaml` — **new DR-088**. **Plugin v9.7.0→v9.7.1** (PATCH — template-only conformance file, no skill/agent behavior change; conformance-checked by `/pandacorp:upgrade`, DR-059). MC-side application (markers on `IdeaBoardView` + the proposals feed wrapper, MC's copy of `visual.spec.ts`, and the re-blessed tablero+propuestas × desktop/mobile baselines) lands under `mission-control/` — see `mission-control/docs/decision-log.md` (2026-06-25). **Manual (DR-046):** the DR catalog auto-derives; no flow/gate/concept changed → no narrative edit.

## 2026-06-25 — Dependency-capture rubric: declare architectural couplings, not just build order (DR-087a)
**What:** Sharpened the `dependsOn` guidance with a **"what counts as a dependency"** rubric in `factory/standards/build-orchestration.md` (Declared dependencies) + a pointer in the `work-orders` skill. `A depends on B` whenever A reads/consumes/imports / renders-based-on / is-built-on / extends / consumes-the-contract-of B — broader than "what builds before what". A UI-surface WO almost always depends on (1) its own libs, (2) the design-system foundation WOs it uses, (3) the data-reader WOs it renders, (4) any other feature whose data/engine it renders. Under-capturing yields a sparse, misleading dependency graph; only evidence-based couplings (what the WO actually imports/reads), never speculative.
**Why:** Mapping Mission Control's own dependency graph for the DAG revealed the real couplings were **under-captured** — the work-orders READMEs listed only partial build order, so the frontmatter (and the DAG) looked disconnected until a full audit (3 agents reading real source imports) surfaced the architectural couplings (party←build-modes, achievements←gamification+events, every UI surface←FRD-13 foundations, dashboard←half the system). The fix for future projects is guidance: tell authors what a dependency *is*, so deps are complete from the start, not retrofitted.
**Impact:** `factory/standards/build-orchestration.md` (Declared dependencies → new rubric paragraph) + `plugin/skills/work-orders/SKILL.md`. Refines DR-087 (no policy change → no new registry default). **Plugin v9.6.0→v9.6.1** (PATCH — guidance clarification, no behavior change). MC-side proof: `mission-control/docs/decision-log.md` (2026-06-24, the audited 244-dep map written to frontmatter).

## 2026-06-24 — Work-order dependencies are machine-readable `dependsOn` frontmatter (DR-087) — real DAG, no fabrication
**What:** Work orders now declare their dependencies in a **`dependsOn: [WO-NN-MMM]`** frontmatter field — the canonical machine-readable source, mirroring the blueprint Build Plan DAG + the work-orders README "Depends on" column. It lists the **real upstream WOs** (a WO can depend on several, across FRDs); `[]` for a root. Honest data, never a fabricated chain. The work-orders skill writes it; Mission Control's dependency DAG reads it verbatim (independent node for `[]`, real cross-FRD edges otherwise).
**Why:** The owner spotted that MC's DAG showed every WO depending on exactly one other — a pretty linear chain that never happens in reality (a WO blocks several / needs several). Root cause: the real cross-deps existed only in human prose (each FRD's `work-orders/README.md` "Depends on" column, 15/19 FRDs, with genuine fan-in/fan-out + cross-FRD), `lib/work-orders` read no deps, and the DAG **fabricated** a sequential per-FRD chain as a fallback. Same anti-pattern as the timeline's fake bars: don't invent — read real data or show honest emptiness. The owner chose deps-as-frontmatter (durable, machine-readable, like `artifacts`/`source_requirements`).
**Impact:** `factory/standards/build-orchestration.md` (new "Declared dependencies — `dependsOn`" section), `factory/decisions/registry.yaml` (DR-087), `plugin/templates/docs/work-order-template.md` (+`dependsOn: []`), `plugin/templates/docs/work-orders-readme-template.md` ("Depends on" column ↔ frontmatter), `plugin/skills/work-orders/SKILL.md` (populate `dependsOn`). **Plugin v9.5.0→v9.6.0** (MINOR — new compatible field), **`OVERLAY_VERSION` 8.41.0→8.42.0** (templates + skill changed → projects get it on `/upgrade`). MC side (reader + DAG de-fabrication + backfill of its 33 WOs) lands under `mission-control/` (FRD-12). Manual: `concepts/estado-y-archivos` (WO frontmatter). Pending: commit + `claude plugin update`.

## 2026-06-23 — Build engine writes a durable timeline log `.pandacorp/track.jsonl` (timeline data for FRD-12, builds on DR-086)
**What:** Building on DR-086 (commit each WO as it greens), the engine now appends a per-project timing log to `.pandacorp/track.jsonl` at the points it already touches: `wo_start` (a work order begins building), `wo_end` (at its commit, with state), and `review_start`/`review_end`/`frd_end` (the FRD gate). It is **committed machine-state** (like `status.yaml` — staged by the WO commit + the gate), so it is a **durable** record that survives the build, unlike the global `~/.claude/dashboard-events.ndjson` (which rotates + feeds the live Party view). This is the source Mission Control's **Observabilidad → Línea de tiempo** reads: FRD ▸ work order ▸ review, real wall-clock durations, "keep the last attempt" per WO.
**Why:** The timeline needs precise, DURABLE per-WO/FRD timing for every future build; git gives only the finish (commit time — distorted for parallel waves) and the event stream is ephemeral. Recording start/end at the engine's existing transition points (DR-086 made the commit per-WO — the natural `wo_end`) gives accurate durable durations with a small in-place addition. Caveat: only future builds get a track; already-built projects (MC) have none → their timeline falls back to git structure (no fine durations).
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (new `TRACK` helper; `wo_start` in both build paths, `wo_end` in `commitWOGreen`, `review_*`/`frd_end` in the gate; track.jsonl staged by the WO commit + the gate). `factory/standards/build-orchestration.md` §2. `plugin/templates/shared/.gitignore` (track.jsonl committed). **Plugin v9.4.0→v9.5.0** (MINOR — new capability), **`OVERLAY_VERSION` 8.40.0→8.41.0** (templates/shared changed → projects get it on `/upgrade`). The MC reader + `TimelineView v2` (FRD-12) land separately under `mission-control/`. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — Build engine commits each work order the instant it greens, not batched per wave (DR-086)
**What:** The build engine (`pandacorp-build`) now commits **each work order the moment its self-test passes** — through a single SERIALIZED git writer (a promise-chain, one commit at a time) staging only that WO's own disjoint `artifacts` + its work-order .md — instead of **batching the commit after the whole build wave finished**. The resume path is unchanged and now strictly finer: a committed WO is `IN_REVIEW` → skipped on relaunch (never rebuilt); only the **still-building (uncommitted)** WOs are redone. So an interruption (cancel / internet drop / crash) discards only the WOs actively building at that instant, not the whole wave (up to `wave` WOs — 8 in powerful mode). The single-writer serialization preserves the no-`index.lock`-race / no-merge invariant (DR-060) even while sibling WOs of the wave are still writing (selective `git add` of disjoint artifacts can't capture a sibling's in-flight files). `last_green_sha` is still advanced ONLY by the FRD gate (the review-verified anchor); the per-WO commits are finer RESUME points via the frontmatter, not new VERIFIED points.
**Why:** Owner: *"descartar trabajo al 99% por un corte de internet es un desperdicio — commiteemos más pronto."* Validated with a 5-agent investigation + red-team (workflow `build-resume-policy`): the **RESTART-from-last-commit** policy is correct and STAYS (resuming partial multi-agent file state is unsafe — no merge path; the durable-execution rule: resume only when steps are idempotent + the checkpoint is durable; the la-campana stash-pop wedge, DR-067, is the concrete failure). The right lever is **finer save points**, not resuming partial disk state. Reading the engine showed it already committed per-wave + resumed committed WOs; the only gap was the **batched** wave-end commit. This fix also makes the engine match what Mission Control's Manual (`concepts/construccion-desatendida`) **already** described ("cada work order que termina verde se commitea inmediatamente") — engine↔Manual now consistent (no Manual edit needed).
**Impact:** Canonical: `factory/standards/build-orchestration.md` §2 (Single-writer commit — finer save points). Engine: `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (new `commitWOGreen` serialized writer in `buildWO`; the batched after-wave commit removed; build/self-test prompts updated). Policy: `factory/decisions/registry.yaml` **new DR-086**. **Plugin v9.3.0→v9.4.0** (MINOR — backward-compatible engine improvement). **`OVERLAY_VERSION` 8.39.0→8.40.0** (templates/shared engine changed → projects get it on `/upgrade`). Restart policy reaffirmed, not changed (refines DR-060/067). **Manual (DR-046):** the DR catalog auto-derives; the narrative (`construccion-desatendida`) already describes this behavior → no narrative edit. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — Pipeline redefined to 6 phases: `operation` folds into `release`, the audit moves into construction, `deploy_target` tracks internal vs external (DR-085)
**What:** Redefined the project lifecycle to **six phases** matching Mission Control's six Campaña rooms: research (pre-project) → `product` → `design` → `architecture` → `implementation` → `release`. Three coupled changes:
- **The audit/security/quality/metrics hardening MOVED from `release` into construction** — it is now the **last step of `/pandacorp:implement`** (once every FRD is `VERIFIED`): security audit (OWASP + ASI Top 10), quality close-out, and telemetry/metrics verification. A build is not "done" until it is secure, quality-gated and instrumented. `/pandacorp:release` no longer audits.
- **`operation` no longer exists as a phase — it is folded into `release`.** `release` no longer means "the act of auditing+deploying"; it means the product is **DEPLOYED/LAUNCHED** (internal or external) and the **terminal** phase, from which the product is iterated (`/pandacorp:iterate`) and its results read (`/pandacorp:review-launch`). Reaching `phase: release` is what triggers review-launch eligibility (was `phase: operation`).
- **Internal vs external is a status field, not a phase:** a new `deploy_target: internal | external` in `.pandacorp/status.yaml`. Internal = an in-house tool used as-is, no external host (e.g. Mission Control on `127.0.0.1` — no production gate, no landing/GTM); external = deployed to an external server (Vercel/AWS/…) behind the human production gate (DR-004) + launch plan. Both are a real release of a software product, distinguished only by `deploy_target` (derived from `return_type`). `/pandacorp:release` performs the deploy/launch (branching on `deploy_target`) + the launch plan and sets `phase: release`. Iterating from `release` may loop back to construction in a future second build pass (conceptually allowed; not over-specified).
**Why:** Owner redefinition — *"release y operación son lo mismo; el concepto es un release de software, interno o externo; la auditoría/calidad/seguridad/métricas es el último paso de la construcción, no una fase posterior."* The old model split "release" (audit+deploy, a gate) from "operation" (live, read metrics) and had no clean way to express an in-house tool that is genuinely launched but never externally deployed. Folding `operation` into `release`, moving the hardening into construction (so the build owns "make it work, secure and measured"), and tracking internal-vs-external as `deploy_target` matches how the owner actually thinks about shipping and aligns the factory's phase vocabulary with Mission Control's six Campaña rooms.
**Impact:** Canonical: `factory/standards/build-orchestration.md` (new §5b "The phase model & `deploy_target`" + §6 "Nothing is left" now runs the hardening before `phase: release`), `factory/standards/observability.md` (telemetry wired in construction, not post-launch), `factory/standards/infra.md` (internal vs external deploy note). Policy: `factory/decisions/registry.yaml` — **new DR-085** + DR-043 reframed (review-launch precondition `phase: release`). Skills: `plugin/skills/release/SKILL.md` (release = deploy/launch internal/external + launch plan; audit removed; `deploy_target`; terminal), `plugin/skills/implement/SKILL.md` (new final hardening step; ends at `phase: release`), `plugin/skills/review-launch/SKILL.md` (under the release phase; trigger `phase: release`), `plugin/skills/adopt/SKILL.md` (phase enumeration drops `operation`, live→`release`+`deploy_target`), `plugin/skills/new-version/SKILL.md`. Root `CLAUDE.md` (operation list + rule-5 board mapping `release→shipped`, no `operation`). **Plugin v8.45.0→v9.0.0** (MAJOR — redefines the lifecycle, a breaking conceptual change; see `plugin/docs/decision-log.md`, same date). **Mission Control** (the 6-room Campaña + its phase→column derivation) is the other agent's scope, owned in `mission-control/`. **Manual (DR-046):** the Reference catalogs (commands + DR) auto-derive the updated skills/DR-085; the hand-authored narrative pages (the pipeline concept page) are part of the Mission Control Manual, deferred to its owner. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-22 — Page chrome lives in a layout component, not per-screen markup (DR-062 extension)
**What:** Strengthened the cohesion standard (`factory/standards/design.md` §7, DR-062) with an explicit structural rule: the persistent header/nav lives in the app shell, and the **page title + body are rendered through ONE shared page-layout component** (e.g. `PageLayout` = the page's single `<main>` + the title block + the body slot) that **every** page wraps its body in — so the title's placement and the title→body spacing are enforced from one place, not re-implemented (and drifting) per screen. A page that hand-rolls its own `<main>`/title wrapper with bespoke padding/background is a cohesion defect. Conditional titles (detail views) and dynamic title tails (live counts) are props to the same layout, not a reason to fork it. Justified exceptions (full-height two-pane "app view", shell-exempt drill-in) are recorded.
**Why:** Mission Control dogfood surfaced it — the page title landed in a different place/spacing per screen because each page rendered its own title inside bespoke wrappers (the Board nested its title in a full-height `<main>` with extra padding, so it sat differently from every sibling; Inicio showed a stray gap from an empty banner slot). Owner: *"no estamos siguiendo el estándar… deberíamos tener un layout… para integridad y coherencia visual en todas las páginas."* Reusing the title *component* (DR-062 already) wasn't enough; the *placement* must be owned by a layout too. Confirmed with the owner that a wrapper each page invokes (vs the root shell rendering the title) is the right shape — it keeps dynamic tails + conditional titles local while enforcing one chrome.
**Impact:** `factory/standards/design.md` §7 (the "Page titles" bullet gains the page-chrome-via-layout sub-rule). Reference catalog of standards in the MC Manual auto-derives (DR-046) — no hand-copy; this strengthens an existing concept (cohesion), not a new flow/gate, so no narrative page change. Applied in Mission Control (see `mission-control/docs/decision-log.md`: new `PageLayout` primitive adopted across the stacked top-level pages + Board). Not a new DR number — extends DR-062.

## 2026-06-22 — Process audit → preventive structural fixes: independent-oracle principle + DR-078/079/080 + self-learning activated (v8.42.0)
**What:** A 31-agent deep-research + red-team audit of the dev PROCESS (seeded with Mission Control's empirical failures: a 100%-done/green build that looked broken, ~half its data readers returning `[]`, the global nav menu missing, the Manual a `<pre>` dump). The red team confirmed the meta-pattern and that ~24 DRs (054–077) had already patched the VISUAL/coherence axis reactively, so the genuinely net-new gaps were narrow. Implemented the survivors worth standing behind (2 P0 + 4 P1 + the canary); deferred 4 low-ROI/overlapping P2s.
- **Constitution §24 — Independent-oracle verification** (the north star): a gate proves a build FAITHFUL, not self-consistent; every gate anchors to a source OUTSIDE the artifact it checks (the prototype for the look, a real+malformed fixture for a reader, a different-family reviewer for behaviour, a human for the value) — never the build's own prior output blessed as its baseline. When a failure escapes the gates, the fix is the **missing independent oracle**, not a 25th self-referential check — this names the reactive-accretion disease. Corollary: every gate is periodically proven to still go RED on a deliberately broken input.
- **DR-078 — fail-loud read boundaries** (the clearest net-new gap; the data plane had NO oracle): parse-don't-validate at every internal-artifact boundary (a reader distinguishes "empty" from "couldn't interpret" and FAILS LOUD on the latter, never a silent `[]`); illegal-empty unrepresentable (`NonEmpty`); every reader unit-tested vs a REAL fixture (incl. the gitignored/foreign-language shape it actually meets) AND a malformed one; an advisory render backstop. Canonical: `quality.md` + the rule templates.
- **DR-079 — gate canary**: `verify.sh --canary` runs deliberately-broken fixtures through the gates on each `/upgrade` and REDs a gate that should reject but stays green (a rotted gate). Complements DR-059/076 (which prove the gate FILES propagate, NOT that they still FUNCTIONALLY reject). No-red-lock (vacuous until fixtures installed).
- **DR-080 — fidelity by independent oracle**: provenance on the bless (the commit records the prototype shard + sign-off + a `prototype_blessed_at` SHA; `verify.sh` advises on a baseline with no recorded oracle), a prototype-freshness pin, and a builder-blind acceptance suite the implementer may not edit. Canonical: `reviewer.md` + `implementer.md` + `verify.sh` + `fdd-template.md`.
- **DR-047 amendment — self-learning ACTIVATED**: the audit verified the loop captured but never refined (1 graduated lesson vs 103 piled-up inbox notes, no trigger wired). Now `/implement` runs a `librarian` harvest at build close-out (lessons freshest there); a periodic review sweep is registered (the in-framework close-out harvest is the durable driver, a machine-local scheduled review the secondary sweep).
**Why:** The owner's intuition held — the process improved reactively, a DR per failure signature, because the gates proved self-consistency not fidelity to an independent oracle (E1–E7). The structural cure is the §24 principle + giving the two un-oracled axes (DATA correctness, GATE liveness) their oracles, and activating the anti-poison loop (DR-047) that was prescribed-but-dormant — so the next build doesn't relearn this one's lessons.
**Deferred (red-team low-ROI / overlapping — flagged, not built):** the advisory `data-presence.spec.ts` render backstop (DR-078 §4, the lowest-value sub-piece + conformance/red-lock complexity); a calibrated VLM fidelity judge (audit P10 — needs a committed gold set, its own change); a traceability-totality gate (P04, overlaps DR-075), a whole-app integration gate (P05, overlaps DR-060/062), an injected-invariants preamble (P14); a permanent LaunchAgent for the review sweep (the registered cron is 7-day session-bound). Red team rejected as already-covered: P06/P07/P11/P12/P13 → DR-075/057/056/074.
**Impact:** `factory/constitution.md` (principle 24), `factory/decisions/registry.yaml` (DR-078/079/080 + DR-047 nota), `factory/standards/quality.md` (Fail-loud read boundaries + Gate canary + the DR-080 bullet). Plugin side (see `plugin/docs/decision-log.md`): `plugin/agents/{reviewer,implementer}.md`, `plugin/skills/{implement,upgrade}/SKILL.md`, `plugin/templates/rules/{quality-and-testing,typescript}.md`, `plugin/templates/stack-a-nextjs/verify.sh`, `plugin/templates/docs/fdd-template.md`, `OVERLAY_VERSION 8.38.0→8.39.0`, `plugin.json v8.42.0` (MINOR). Rationale + the full prioritized list (P0/P1/P2 + dropped): the audit memory `factory-process-audit-2026-06-22`. **Deferred (DR-046):** the MC Manual narrative for §24 + DR-078/079/080 (the Reference catalogs auto-derive). Pending: commit + `claude plugin update`.

## 2026-06-21 — Change-queue completion: verify-then-ARCHIVE, never hard-delete (DR-069 2nd amendment) — plugin v8.41.0
**What:** When a queued change lands, the build now **archives** it instead of leaving it in the active queue or hard-deleting it. On completion it (1) **independently** verifies the durable record exists (generator ≠ verifier — the landing commit touched the canonical doc *and* a `docs/decision-log.md` entry references the change), (2) stamps `status: done` + `shipped_sha` + `shipped_at`, (3) **moves** the file to `changes/done/` (a move, never an `rm`). The active `.pandacorp/inbox/changes/` then holds **pending-only** (the owner's clean queue) while the Spanish owner-language audit trail survives in `done/` (Mission Control reads it for a "recently shipped" view). If the durable record is **missing**, the change is **not** archived — it stays in place and the missing decision-log/FRD update is flagged (archival is the enforcement point for the documentation discipline). **Never auto-delete** `done/`; any prune is a separate, owner-gated, reversible action.
**Why:** The owner asked whether a done change should be deleted (his instinct: keep the folder clean) — a red team of 3 adversarial agents (pro-delete / pro-keep / web-researcher) converged that the GOAL is right but hard-delete is the wrong METHOD *here*: (i) `changes/` is **gitignored** → a delete is **irreversible** (no git history to recover the owner's original Spanish phrasing/intent/classification/`draft→ready` provenance); (ii) the decision-log is **not a guaranteed superset** of the change file (curated-English-technical vs raw-Spanish-intent), so the researcher's own hedge ("delete is only clean when the log is a true superset") flips to archive; (iii) "done" is build-emitted, so wiring destruction to an unverified self-report violates generator ≠ verifier. Industry evidence is lopsided: Kanban/Jira/Linear/GitHub archive-or-close (never auto-delete, "irreversible loss of audit history"); event-sourcing is append-only; even SQS/Sidekiq delete-on-done *only because* a separate system-of-record exists — and the universal reconciler is **commit-before-retire**. Archive captures **100%** of the owner's clean-folder goal at **0%** irreversible risk; hard-delete buys only kilobytes for a one-way door.
**Impact:** `plugin/skills/change/SKILL.md` (archive-not-delete rule), `plugin/skills/implement/SKILL.md` (step-3 drain: verify durable record → stamp `done`+`shipped_sha`+`shipped_at` → move to `changes/done/`), `factory/standards/build-orchestration.md` §8 (the completion paragraph), `factory/decisions/registry.yaml` (DR-069 amended — clause 7 + the nota), `plugin/.claude-plugin/plugin.json` v8.41.0 (MINOR — new lifecycle capability). **No `OVERLAY_VERSION` change** (skills + a standard only). **Deferred:** the MC Manual narrative (DR-046) — folded into the queued "Manual narrative sync" change item (its scope now lists this amendment). Pending: commit + `claude plugin update`.

## 2026-06-21 — Gate-template fidelity + a back-port discipline (DR-076) — plugin v8.40.0, OVERLAY 8.38.0
**What:** The MC re-anchor's `/pandacorp:upgrade` revealed the canonical `stack-a-nextjs` gate templates had drifted BEHIND real projects + carried a DR-074 bug, and that conformance "overwrite from template" would have REGRESSED a project that was ahead. Four fixes (red-team-hardened, 3 opus agents, 2 ran empirical biome/knip tests on MC): **(a)** back-ported the UNIVERSAL `biome.json`/`knip.json` deltas — biome `css.parser.tailwindDirectives:true` (or biome parse-errors on `@theme` in every Tailwind-v4 `globals.css`), `!.pandacorp/**`, `style.useExportType`, **KEEP `noImportantStyles:off`** (the WCAG `prefers-reduced-motion` reset, NOT MC-specific — the v1 misclassification A caught); knip `@6` schema, drop the v6-rejected `_pandacorp` key, `ignoreDependencies:["tailwindcss","@axe-core/playwright"]`. **(b)** `@axe-core/playwright` → `ignoreDependencies` (not tracing `e2e/`, which A proved adds a new false-positive). **(c)** `verify.sh` runs the e2e gates in ONE `playwright test e2e/` (was four; one webServer boot), gating on the spec FILE — a cleanup, NOT the fix for the `ERR_CONNECTION_REFUSED` (C: that is `.next`/process contention, mid-invocation, fixed by the deferred worktree work). **(d)** softened the DR-075 shell wording — a deliberately-authored app-shell FRD (PRD-rooted) is correct, with `foundation:true` WOs; only the greenfield AUTO-emitted orphan is forbidden. **Mechanism UNCHANGED (B):** conformance keeps unconditional overwrite-on-drift (DR-059's binary guarantee intact); the root cause (templates go stale because someone edits the COPY) is fixed by a discipline rule (edit canonical gate files at the SOURCE, never in-place) + a new `PreToolUse` hook `remind-backport-gate-config.sh`.
**Why:** Caught by hand during the MC overlay upgrade — the conformance overwrite would have regressed MC's already-fixed biome/knip. Git-forensics (B): the template `knip.json` was touched once at DR-059 install and never again; MC's v6 fix landed in-place and was never back-ported — a discipline violation, so the fix is enforced discipline (hook + rule), not weakening the gate. Three decisive red-team corrections to v1: keep `noImportantStyles:off` (A), reject the "diff-and-flag" mechanism change (B), the `ERR_CONNECTION_REFUSED` is contention not invocation-count (C).
**Impact:** `plugin/templates/stack-a-nextjs/{biome.json,knip.json,verify.sh,STACK.md}`, `plugin/scripts/remind-backport-gate-config.sh` (new) + `plugin/hooks/hooks.json`, `plugin/skills/upgrade/SKILL.md` (Rules), `factory/standards/{quality.md,build-orchestration.md §3,design.md §5b}`, `factory/decisions/registry.yaml` (DR-076), `plugin/.claude-plugin/plugin.json` v8.40.0 (MINOR — new hook), `plugin/templates/OVERLAY_VERSION` 8.37.0→8.38.0 → projects re-sync the corrected gate config on `/upgrade` (MC already carries it, no regression). Proposal: `docs/proposals/15-gate-template-fidelity.md`. Refines DR-059/074/075. **Deferred (flagged):** factory-side template-lead CI check; `biome.project.json` opt-out (YAGNI); the `.next`-contention worktree fix; biome `recommended`→`preset`; the MC Manual narrative (DR-046). Pending: commit + `claude plugin update`.

## 2026-06-21 — Template library: second-pass audit — agent-created docs + top-level README (DR-077 follow-up) — plugin v8.39.0
**What:** Audited every recurring file: all 12 doc templates are referenced by their creating skill, and the remaining gaps are closed. Added 4 templates (API contract `docs/api/<wo-id>.md`, component inventory `docs/design/components.md`, the changes-queue index, the decisions inbox) and wired each to its creator. Made explicit a distinction the first pass missed: some docs are created by **build agents** (backend-dev, designer) mid-`implement`, not phase skills — so their template is referenced in the **agent prompt**, not a skill. Added a top-level `plugin/templates/README.md` that indexes all template kinds and documents, per creator class (skill / agent / hand-authored-or-tool-generated), HOW the template is honored — plus the list of docs that are **intentionally freeform** (no template by design, anti-over-templating).
**Why:** Owner's second-pass request (no constantly-created file without a template; all creators reference it; and a mechanism for the docs created outside a skill). The non-skill docs (API contracts, component inventory, design tokens) are exactly the ones the owner worried about — their enforcement is the agent prompt + the advisory doc-lint, now documented.
**Impact:** `plugin/templates/docs/{api-contract,components-inventory,changes-readme,decisions-inbox}-template.md` + `plugin/templates/docs/README.md` + `plugin/templates/README.md` (new), `plugin/agents/{backend-dev,designer}.md`, `plugin/skills/{design,change,bug,decide}/SKILL.md`, `plugin/.claude-plugin/plugin.json` v8.39.0 (MINOR). No `OVERLAY_VERSION` change. Extends DR-077. **Deferred:** the MC Manual narrative (DR-046).

## 2026-06-21 — Complete the document-template library + a doc-lint; reject per-doc version-stamp + migration engine (DR-077) — plugin v8.38.0, OVERLAY 8.37.0
**What:** Every recurring document a project generates now has a THIN base template in `plugin/templates/docs/` — added FDD, platform `architecture.md`, ADR, events plan, research, design-system (root `DESIGN.md`), per-FRD work-orders `README`, and the owner-channel change-request card (joining PRD/FRD/blueprint/work-order), indexed by a `README.md` that also states the policy. Each doc-generating skill POINTS at its template (the template is the single structural contract; skills don't re-derive sections). A shipped, conservative **doc-lint** (`.pandacorp/doc-lint.sh`, run from `verify.sh`) checks required frontmatter + that the `REQ→AC→WO` stable-ID spine resolves (no `docs/` ⇒ vacuous pass). The owner's (b) per-document `template_version` stamp and (c) structural migration engine were **deliberately NOT built**: provenance already lives at project grain (`status.yaml` `created_with`/`overlay_version`), a third axis re-creates the state-fracture the pipeline audit flagged, and retrofitting an LLM-generated doc is done by REGENERATION (the agent that wrote it re-emits it from the still-true upstream), not a structural migrator whose only safe surface is low-value boilerplate.
**Why:** Owner-asked; decided by a 10-agent research + red-team workflow (verdict do-reduced, high confidence, red team unanimous reduce). Prior art validates (a) strongly and warns hard on (b)/(c) for this context: copier/cruft are fleet-scale tools with deterministic regeneration; the AI-spec tools (spec-kit/Kiro/Tessl) chose regenerate-or-flag, never auto-migrate. In-repo ground-truth killed (b)/(c): 1 project in the portfolio, the templates' entire git history additive (0 breaking bumps), 0 migration events. The doc-lint — the highest-leverage move — was the one piece the owner's proposal omitted, and `verify.sh` confirmed no doc checks existed.
**Impact:** `plugin/templates/docs/*` (8 new templates + README), `plugin/templates/shared/.pandacorp/doc-lint.sh` (new), `plugin/templates/stack-a-nextjs/verify.sh` (lint call), `plugin/skills/{spec,blueprint,design,work-orders,change,bug}/SKILL.md`, `factory/standards/structure.md` (Templates note), `factory/decisions/registry.yaml` (DR-077), `plugin/.claude-plugin/plugin.json` v8.38.0 (MINOR), `plugin/templates/OVERLAY_VERSION` 8.36.1→8.37.0 (shared changed). Refines DR-049 (structure) + DR-048 (upgrade) + DR-059 (verbatim propagation). **Note:** DR-076 was concurrently claimed by the in-flight proposal 15 (gate-template fidelity) → this took **DR-077**. **Deferred:** the MC Manual narrative (DR-046) — MC is mid-re-anchor. Pending: commit + `claude plugin update`.

## 2026-06-21 — Change-queue readiness gate: `status: draft | ready` (DR-069 amendment) — plugin v8.37.0
**What:** The change queue (DR-069) gains a **readiness gate** so the owner can park half-formed ideas in `.pandacorp/inbox/changes/` for visibility **without the build acting on them**. The `status` field becomes `draft | ready | done`: **`ready`** = complete enough to action → the build drains+builds it; **`draft`** = captured but the owner is still working out the details / it depends on something undone / it needs a design pass / it is infra-deployment the build doesn't implement → **the build SKIPS it untouched**; **`done`** = built, set by the build with the landing commit. **The supervisor's drain takes ONLY `ready` and skips `draft`.** The owner promotes a draft with a single frontmatter edit (`draft → ready`). `/pandacorp:change` defaults a clear, actionable request to `ready`; "just note it" → `draft`.
**Why:** Owner-driven, verbatim: *"que el implement solamente tome las ideas que ya están listas, las que están draft no, porque todavía las estamos trabajando."* The owner had four pending MC items — two cooked (Prompt-2 observability consumer, Manual narrative sync), two still being shaped (a changes/decisions view in MC, the Party rework) — and needed to queue all four but build only the cooked ones. The pre-existing queue had a single `pending` status with no hold switch, so any queued item was build-eligible. This separates "I'm still thinking" from "go build it."
**Impact:** `plugin/skills/change/SKILL.md` (the `status: draft|ready` field + the ready-vs-draft guidance + the only-`ready`-is-built rule), `plugin/skills/implement/SKILL.md` §step-3 drain (take only `ready`, skip `draft`), `factory/standards/build-orchestration.md` §8 (the readiness-gate paragraph + the consumer takes only `ready`), `factory/decisions/registry.yaml` (DR-069 amended — clause 6 + the enum + the nota), `plugin/.claude-plugin/plugin.json` v8.37.0 (MINOR — new queue capability). **No `OVERLAY_VERSION` change** (skills + a standard only — no overlay/template file changed). **Deferred:** the MC Manual narrative (DR-046) — folded into the queued "Manual narrative sync" change item. Pending: commit + `claude plugin update`.

## 2026-06-21 — App shell as a captured foundation + a Shell-Presence Gate that verifies fidelity-to-prototype (DR-075) — plugin v8.36.0, OVERLAY 8.36.0
**What:** The two highest-leverage gaps left after DR-074, both factory-level, both behind "Mission Control shipped with NO global navigation menu and passed 112/112 VERIFIED GREEN." **(B — structural)** A whole-app prototype's **persistent shell** (topbar/primary nav/footer/the layout frame every screen mounts into) belongs to no single FRD, so the per-FRD shard (DR-056, "only that FRD's screens, never the whole app") silently drops it. Fix: the shell is captured as a **foundation work order** — NOT a new FRD (a design-emitted `frd-NN-app-shell` would be an orphan in the `REQ-NN-MMM→WO` spine and the engine has no "build this FRD between foundation and surfaces" primitive; the existing foundation-completeness gate already enforces "no surface until the shell is green" for free, with zero engine change). Captured in BOTH the greenfield path (`design` new **Step 8a** + `designer`) AND the brownfield path (`adopt` step 6 — *where MC actually lost it*, since MC was adopted): register `AppShell`/`Nav` in `components.md` (`artifacts` include `app/layout.tsx`) + record the prototype's nav contract; `blueprint` schedules it in the existing foundation wave (DR-057). **(A — enforcement)** The visual gate (`toHaveScreenshot`) proves *consistency* (matches its own committed baseline), not *fidelity* to the prototype — a menu-less build blessed menu-less baselines and passed green. Fix: a shipped, deterministic, fail-closed **Shell-Presence Gate** — a VERBATIM `e2e/shell.spec.ts` (joins the DR-059/074 byte-diffed conformance set → six e2e files; `verify.sh` gains a fail-closed `test:shell` shim) that asserts the app against the **prototype-anchored** nav contract `e2e/shell.ts` (per-project seed, never byte-diffed; author-declared at design time from the prototype, NOT derived from the app's routes — which would just move the consistency-not-fidelity trap up a level). It iterates `SURFACES` minus author-declared `SHELL_EXEMPT` (**not `BLESSED`** — the MC failure is a route that shipped *blessed* without the shell), settle-first like `_responsive-helper.ts` (abort live transport → `domcontentloaded` → `fonts.ready` → `<main>` visible → web-first auto-retry, DR-071), and per route asserts the shell landmark is VISIBLE, every top-level destination is a VISIBLE in-shell link to its correct path, each destination 2xx-resolves, and on mobile (via `_target.ts`) the nav is reachable (visible or via the author-declared `data-nav-toggle`). Empty contract (no shell — single-screen tool/landing/API) ⇒ vacuous pass. The `reviewer` gets ONE advisory whole-app sentence (the block is the deterministic gate; shell *resemblance* — placement/order/active-state — is a punch-list nit, DR-072 preserved). **Conservative retrofit:** `upgrade` adds `shell.spec.ts` to the byte-diffed set + seeds `shell.ts` **EMPTY** (never a filled contract — a filled nav on an app with no shell red-locks all commits; the real contract is filled in the same change that builds the shell, e.g. MC's re-anchor).
**Why:** Owner-found (continuation of `mc-design-fidelity-failure`): MC's prototype is a SPA with a persistent topbar (Inicio·Tablero·Portfolio·Documentación) the build never constructed — `layout.tsx` mounts only GuildBar+ProposalsBadge+children. The owner sharpened the diagnosis: MC was **adopted** (brownfield, `/pandacorp:adopt`), and adopt's shard had no shell-capture step — so the chrome was dropped at adopt time, not just design time. Red team (3 adversarial opus agents) found v1 over-engineered the same way DR-074-v1 was: (1) shell-as-FRD was the wrong unit → a foundation WO (cheaper AND better-enforced, kills the spine collision + needs no engine change); (2) the A2 semantic whole-app blocking gate was redundant with A1 and re-opened the DR-072 churn → demoted to one advisory reviewer sentence; (3) the nav contract must be PROTOTYPE-anchored, proven against MC's own data (8 SURFACES but 6 nav tabs — `workspace`/`configuration` are routes, not top-level nav; deriving from routes false-reds AND re-creates the consistency trap); (4) iterate `SURFACES` not `BLESSED` (gating on blessed inherits the exact MC blind spot — "the whole ballgame"); (5) A1 must assert visible/in-shell/href-correct/2xx + settle-first or it false-negatives a dead nav and false-reds a hydrating one; (6) Piece C (responsive-shell + prototype-covers-target) was scope creep DR-074 deliberately cut → dropped (the shell rides the shipped responsive gate; only the mobile-nav-reachability check, which the responsive gate does not do, lives inside A1).
**Impact:** `plugin/templates/stack-a-nextjs/e2e/{shell.spec.ts,shell.ts}` (new) + `e2e/playwright.config.ts`, `plugin/templates/stack-a-nextjs/{verify.sh,STACK.md}`, `plugin/skills/{design,blueprint,upgrade,adopt}/SKILL.md`, `plugin/agents/{designer,reviewer}.md`, `factory/standards/design.md` §5b + §9 (wiring) + `quality.md` (gate) + `build-orchestration.md` §3 (foundation names the shell) + §5 (the gate), `factory/decisions/registry.yaml` (DR-075), `plugin/.claude-plugin/plugin.json` v8.36.0 (MINOR — new capability), `plugin/templates/OVERLAY_VERSION` 8.35.0→8.36.0 (templates changed) → projects get the gate on `/upgrade` (back-filled EMPTY `shell.ts`; MC's `/implement`/re-anchor preflight does it). Refines DR-054/055/056/057/072/074. **Deferred:** the MC Manual narrative (DR-046, concepts/el-pipeline + estandares-y-reglas). **Next (separate, not this DR):** re-anchor MC under the new gate — build the missing shell (responsive) + reconcile. Proposal: `docs/proposals/14-fidelity-gate-and-app-shell.md`. Pending: commit + `claude plugin update`.

## 2026-06-21 — Platform target as a tracked field + a shipped, smart responsive verification gate (DR-074) — plugin v8.35.0, OVERLAY 8.35.0
**What:** The owner-stated principle — "the target platform (desktop / mobile / both) must be decided when the app is DEFINED and flow into the verification gates" — implemented as the MINIMAL, robust v2: a tracked `target_platforms` field + a SHIPPED, smart, fail-closed Responsive Gate, NOT per-stage prose ceremony. **(1) The gate ships as VERBATIM stack templates (the real fix).** `plugin/templates/stack-a-nextjs/e2e/` now carries `_responsive-helper.ts` (the `assertResponsive` checks), `responsive.spec.ts`, generalized `smoke.spec.ts`/`visual.spec.ts`, and `playwright.config.ts` — installed by `blueprint`, conformance-checked (byte-diff/overwrite-on-drift) by `upgrade`, exactly the DR-059 mechanism that already works for `verify.sh`/`biome.json`/`knip.json`. This kills the per-project-lenient-authoring root cause (the specs lived in NO template — that is why MC's gate was lenient) AND gives retrofit for free. **(2) The gate is SMART, not naïve.** At the mobile viewport, per scroll-root: RED on `scrollWidth > clientWidth + 1` (sub-pixel only) UNLESS author-marked `data-scroll-x="intentional"` (the escape hatch for legit kanban/DAG/wide-table scroll-x — broken-vs-intentional is DECLARED, never a heuristic); PLUS silent off-canvas clip (`overflow-x:hidden|clip` while wider — what MC actually shipped), tap targets (axe `target-size`, SC 2.5.8 / 24px), and `<main>` not occluded by a fixed bar (WCAG 2.4.11). Settles first (mobile viewport → block the live transport → `domcontentloaded` → `fonts.ready` → `<main>` visible) to kill flake. **(3) Responsive = enforced default, no per-breakpoint mocks** (responsiveness from the fluid design system + the gate; one mock per FRD). **(4) One machine field** `target_platforms ∈ {desktop|mobile|responsive}` in `.pandacorp/status.yaml`, read ONLY by the gate (`e2e/_target.ts`) to choose which widths to assert; `spec`'s PRD records it as a one-line product decision; desktop-only/API/scraper = a vacuous pass. **(5) Conservative retrofit:** `upgrade` back-fills an existing project to `desktop`, `adopt` sets `desktop` for brownfield.
**Why:** MC verified at 390px overflows on every page (the GuildBar, the kanban, the portfolio; the workspace has no mobile baseline at all) yet shipped 112/112 VERIFIED GREEN — because the smoke gate only checks "renders clean" and the visual gate only checks "matches its OWN committed baseline" (even a broken, overflowing baseline). Red team (3 adversarial opus agents) unanimous: the failure is NON-ENFORCEMENT, not a missing field (the rule already existed in `styling-and-ui.md` + `design/SKILL.md` and MC shipped non-responsive anyway), ~80% of the value is in ONE stage (a gate that actually ships), and the proposed naïve `scrollWidth<=width` gate was both unreliable (false-reds MC's legit scroll-x kanbans, which implementers would "fix" with `overflow:hidden` — creating clip bugs) and non-shipping. A rating pass added 3 must-fixes — all applied: (a) `routes.ts`/`_target.ts` EXCLUDED from the byte-diff/conformance set (per-project — byte-diffing the surface manifest clobbers it = data loss); (b) the retrofit back-fills `target_platforms: desktop` NOT `responsive` (an existing non-responsive project would otherwise red-lock all commits on the new mobile gate); (c) the gate READS `target_platforms` to choose the asserted widths.
**Impact:** `plugin/templates/stack-a-nextjs/e2e/{_responsive-helper,responsive.spec,smoke.spec,visual.spec,routes,_target}.ts` + `e2e/playwright.config.ts` (new), `plugin/templates/stack-a-nextjs/{verify.sh,STACK.md}` (the `test:responsive` shim + the Responsive Gate section), `plugin/templates/shared/.pandacorp/status.yaml.tpl` (`target_platforms: responsive` default), `plugin/skills/{spec,blueprint,upgrade,adopt}/SKILL.md`, `factory/standards/design.md` §4b (target platform + responsive gate, + the wiring table), `factory/standards/quality.md` (the gate in the gates list + wiring the previously-advisory axe target-size 2.5.8 into the gate), `factory/standards/build-orchestration.md` §5 (per-FRD + close-out verification), `factory/decisions/registry.yaml` (DR-074), `plugin/.claude-plugin/plugin.json` v8.35.0 (MINOR — new capability), `plugin/templates/OVERLAY_VERSION` 8.34.0→8.35.0 (templates changed) → projects get the gate on `/upgrade` (back-filled `desktop`; MC's `/implement` preflight does it). Refines DR-055/056/059/071. **Deferred:** the MC Manual narrative (DR-046, concepts/el-pipeline + estandares-y-reglas). Pending: commit + `claude plugin update` (the supervisor does both).

## 2026-06-21 — Adaptive repair (patch-in-place before rebuild) + adaptive model escalation (DR-073) — plugin v8.34.0, OVERLAY 8.34.0
**What:** Two engine changes that make a localized gate reject CONVERGE cheaply instead of churning, and put the right model on the right work order. **(1) Patch-in-place before rebuild.** When the FRD gate rejects a SPECIFIC work order for a localized CORRECTION fault, the build is typically ~correct except a bounded fault — so the engine now PATCHES it in place BEFORE reverting, and revert-and-rebuild (DR-070) becomes the *fallback*. The reviewer no longer reverts on a localized reject: it leaves the WO `IN_REVIEW`, keeps the non-progress cap, and returns **`findings: [{ wo, finding (file:line), failingTest (a RED-proven test), files }]`**. The engine runs `attemptPatch` (opus, `xhigh`): fix ONLY the named finding on the EXISTING build, make the RED test pass, then **re-gate WHOLE-PROJECT** — the full FRD adversarial+integration pass AND a whole-project `knip`+`biome`+`tsc` (NOT `--since`: a dead export the patch leaves must not slip to a sibling FRD's global gate — the red-team-A fix) — committing + `VERIFIED` + `reopen_count:0` only on whole-project-clean. The patch runs SYNCHRONOUSLY inside the gate step, so a sibling never sees broken committed code (DR-070's true invariant preserved without a revert). Only if the patch can't green it whole-project does `revertAndReopen` run (set `PLANNED`, INCREMENT `reopen_count`, surgical `git checkout last_green_sha -- <files>` + `git rm`, never a hard reset). **SINGLE counter:** `reopen_count` is the only budget — the in-place patch is a sub-step of one reject cycle, NOT a second `patch_attempts` axis (the CRITICAL refinement over the proposal: it eliminates the nesting/non-termination the red team flagged). **(2) Adaptive model escalation.** `pickWorkerModel(wo)` floors at the mode's `P.worker` and escalates to opus when `difficulty: high` (HYBRID a-priori — owner decision) OR `reopen_count >= 1` (empirical — it already failed once); the patch always runs on opus. **Cost-weighted brake (red-team-B):** `maxAgents` counts agents not tokens, so `agentSpawned` is weighted by model cost (`COST`, opus≈3) — an opus escalation can't silently blow the budget; escalations are logged. **(3) HYBRID difficulty field** — the WO frontmatter carries `difficulty: low|medium|high` (default medium, backward-compatible), populated by the `work-orders` skill + read by the plan agent; a-priori only for `high` (the one case escalating before the first failure pays), with `reopen_count` as the empirical correction.
**Why:** MC Phase-2's WO-07-005 (the complex Configuración surface) needed **4 gate cycles**, each a DIFFERENT progressively-smaller finding (5 EARS → reverse cross-nav → dead-code knip → dead Back button); every reject discarded a ~99%-correct build and the fresh full rebuild re-introduced a new micro-bug. It converged only when a HUMAN recovered the good build from git and patched the one bug (a 4-line fix + a test, ~5 min vs 4 multi-agent passes). Owner: "no tiene sentido descartar todo por un 1% que falla." Revert-and-rebuild is correct only for its original purpose (protecting parallel siblings' global gate); for a localized fault it is waste that actively causes non-convergence. Separately, a complex surface got a sonnet implementer regardless of difficulty or failure history — the engine should escalate to opus when a WO is genuinely hard or has failed, without leaving the mode. Red-team-hardened (3 adversarial opus agents): the core idea is sound, v1 had a fatal whole-project-vs-focused re-gate hole (A) and an unbounded-cost/non-termination hole (B) — both fixed (whole-project re-gate; cost-weighted brake; one unified budget).
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (`attemptPatch`, `revertAndReopen`, `pickWorkerModel`, `COST`, `findings` in `FRD_GATE_SCHEMA`, `difficulty`+`reopen_count` in `PLAN_SCHEMA` + the plan prompt, the patch-first reject handler — syntax-checked via async-wrap), `plugin/agents/reviewer.md` (report findings + a RED test, don't revert on a localized reject), `plugin/skills/work-orders/SKILL.md` (the `difficulty` rubric), `plugin/skills/implement/SKILL.md` (patch-first + escalation note), `factory/standards/build-orchestration.md` §6 (gate-reject patch-first + model selection) + §9 (single budget), `factory/decisions/registry.yaml` (DR-073), `plugin/.claude-plugin/plugin.json` v8.34.0 (MINOR — new capability), `plugin/templates/OVERLAY_VERSION` 8.33.0→8.34.0 (the engine — an overlay file — changed) → MC needs `/upgrade` (its `/implement` preflight does it). Refines DR-070 (revert becomes the fallback) + DR-072 (reopen_count is the single budget). **Deferred:** the MC Manual narrative (DR-046, concepts/el-pipeline). Pending: commit + `claude plugin update`.

## 2026-06-21 — Convergence: the split gate + a non-blocking end-of-build Visual QA pass (DR-072) — plugin v8.33.0
**What:** The build redesign that makes an overnight run FINISH (a working draft + a punch-list) instead of churning. After a deep-research workflow + forensics over the week's 7 failed runs, the root cause of the post-reset grind was clear: every surface got rejected once on **visual fidelity** and deferred to the next overnight pass (~1 net FRD/run). **The gate is now SPLIT:** **(1) CORRECTION** — renders clean, tests+types+lint, acceptance criteria, security, no duplicate primitive, **gross** structural mismatch — stays a HARD block (the no-bugs guarantee, NOT lowered). **(2) Visual-fidelity NITS** (15px-vs-16px, spacing, shade) are **ADVISORY** — appended to `.pandacorp/comms/visual-punch-list.md`, never reopen a WO. **(3) A dedicated end-of-build VISUAL QA pass** (the owner's idea), scoped to the FRDs touched that run, OUTSIDE the convergence loop: render → compare semantically to the mock/fdd/tokens → complete the punch-list → fix the cheap gaps DIRECTLY against the EXISTING design docs (no doc change) → re-verify → leave the residual for the owner. Punch-list + bounded polish, never reject-to-rebuild. **(4) Non-progress stop:** a WO reopened ≥ `MAX_REOPENS` (3) → BLOCK needs-owner. **(5) Selective `effort: xhigh`** on the gate/repair/close-out only. Plus the **3 open risks**: R3 (`repairFoundation` verifies `last_green_sha` is an ancestor before any hard reset — an orphaned SHA would have wiped verified work), R2 (the engine warns loudly if `args` arrived as a non-object — the silent unbounded-run bug), R1 (DR-070's external brake).
**Why:** The owner kept expecting a hands-off 100%-done-no-bugs app overnight — which 2026 SOTA cannot deliver (METR: ~6% success at 4h; SWE-Marathon <30%; Devin ~15-30% unattended). The honest, achievable target: a **functionally-complete gate-green working draft + a short visual punch-list** swept in the morning. Convergence is a gate-engineering problem (noisy LLM/visual judges over-reject; over-broad reviewers over-engineer; verifiable in-loop feedback is what converges) — the fix is splitting the gate, not lowering quality.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (split `frdGate`, the Visual QA phase, `MAX_REOPENS`, `effort: xhigh`, the R3 ancestor-guard, the R2 args warning — syntax-checked via async-wrap), `plugin/agents/reviewer.md` (fidelity advisory, gross-only blocks, reopen-cap, the Visual QA role), `factory/standards/build-orchestration.md` §9, `factory/decisions/registry.yaml` (DR-072), `plugin/.claude-plugin/plugin.json` v8.33.0 (MINOR). **Deferred:** the MC Manual narrative (DR-046, concepts/el-pipeline) + a `quality.md` cross-reference. Pending: commit + `claude plugin update`.

## 2026-06-21 — Gate harness: domcontentloaded (not networkidle) for real-time apps + block the live transport in visual (DR-071) — plugin v8.32.1
**What:** The canonical smoke/visual gate (DR-055/056) used Playwright `waitUntil: "networkidle"`, which **never settles for a page with a long-lived connection** (an open SSE/EventSource, a websocket, a long-poll) → the navigation **times out** → the gate **hangs forever on every real-time route**, so no such surface can ever be blessed. This is **why the MC Phase-2 build churned for hours with 0 gate passes** — every dashboard/observability/party/work-orders route opens a `DashboardLiveWatcher` EventSource, and the gate timed out on all of them. Fix, two parts: **(1) smoke** → `waitUntil: "domcontentloaded"` + the existing `toBeVisible("main, h1")` as the deterministic readiness signal (SSE stays live; smoke tests the real page). **(2) visual** → additionally `page.route("**/api/live**", r => r.abort())` BEFORE navigating (the screenshot must be deterministic — streaming data shifting pixels violates the existing "mocked network" precondition), then `domcontentloaded` + the readiness wait + `document.fonts.ready` before `toHaveScreenshot`. The page still renders its initial server snapshot, which is what the blessed baseline captures.
**Why:** The build's OWN log diagnosed it ("...waitUntil:networkidle never settles with an open SSE → / times out; with domcontentloaded the page renders clean, 0 errors"), but the autonomous engine fix (DR-070) addressed the budget cap + dead-code entanglement, not this root cause — so the build kept churning. This is the missing piece that lets a real-time surface actually pass the gate. Complements DR-070: DR-070 stops the overspend + sibling-poisoning, DR-071 lets the surfaces converge.
**Impact:** `plugin/templates/stack-a-nextjs/STACK.md` (smoke + visual blocks), `factory/decisions/registry.yaml` (DR-071), `plugin/.claude-plugin/plugin.json` v8.32.1 (**PATCH** — gate-config fix, no skill/engine behavior change, `OVERLAY_VERSION` unchanged). The MC instance (`mission-control/e2e/{smoke,visual}.spec.ts`) fixed in the same change (build was stopped). Refines DR-055/056. **Deferred:** MC Manual narrative (DR-046). Pending: commit + `claude plugin update`.

## 2026-06-21 — Build engine: honest fine-grained agent cap + gate-reject reverts its code (DR-070) — plugin v8.32.0, OVERLAY 8.29.0
**What:** Two engine fixes (`pandacorp-build.js`) found by supervising MC Phase-2 pass-1 live. **(1) The `maxAgents` cap was a paper guardrail.** It was checked **only between FRDs** and `agentSpawned` under-counted (baseline/plan/close-out never incremented), so a `maxAgents=40` run reached **68 agents / ~4.95M tokens** for **+1 net FRD**. Fix: count every spawn + check the ceiling via `capHit()` at the per-FRD-loop top **and the top of each build wave**; a mid-FRD hit stops at the wave boundary (built WOs left `IN_REVIEW`, resumable). The **supervisor** also enforces a reliable **external** brake (counts `agent-*.jsonl` files, `TaskStop`s past the ceiling) and treats `maxAgents` as the cumulative walk-away budget (no relaunch once reached). **(2) Rejected code poisoned siblings.** `verify.sh --since` scopes only tests; `biome`/`tsc`/`knip` are whole-project, and the wave-writer commits each surface (Option B), so a reopened/blocked WO's committed dead code reddened **every later FRD's global gate** — `frd-11`/`frd-14` blocked `error` "entangled with a sibling FRD". Fix: on reopen/block, the gate/repair agent **reverts the rejected WO's files to `last_green_sha`** (surgical `git checkout`/`git rm`, never a whole-tree reset) and commits the revert with the status change.
**Why:** Supervising the run surfaced that the owner's budget guardrail silently failed and the build degraded as it progressed (siblings poisoned). Both recur every pass if unfixed; the owner chose "fix the engine first" before relaunching MC Phase-2. Validated by reading + `node --check` on the source (CommonJS context; the workflow runtime allows the top-level `return`/`await` the ESM check rejects).
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/skills/implement/SKILL.md` (supervisor external brake), `factory/standards/build-orchestration.md` §6, `factory/decisions/registry.yaml` (DR-070), `plugin/templates/OVERLAY_VERSION` 8.28.0→8.29.0, `plugin/.claude-plugin/plugin.json` v8.32.0 (MINOR). Applied to MC (overlay 8.28→8.29) and Phase-2 relaunched with the fixed engine + external-brake supervisor. **Deferred:** MC Manual narrative (DR-046) — `mission-control/` is mid-build. Pending: commit + `claude plugin update`.

## 2026-06-20 — One owner door + a durable, classified change queue drained at safe points (DR-069) — plugin v8.31.0
**What:** A way to inject a change/feature/bug into a build **already running** (or queue it for the next), through **one command**. **(1) One door:** new **`/pandacorp:change`** — the owner describes ANY change or bug; it classifies (bug vs feature), gives it a **class of service** (Kanban: `expedite`/`standard`), and files it. `bug` + `iterate` become the **internal engines** (still direct aliases); `decide` stays separate (answering a build question). **(2) The queue:** `.pandacorp/inbox/changes/` (one file per change + README index, **in Spanish** — gitignored owner layer), unifying the old `inbox/bugs/` (folds in as `type:bug`). **(3) The safety property:** `change` is **detection-free and doc-free** — it only writes one file, never edits the build's docs and has NO build-detection logic, so a mis-detection of "is a build running?" **cannot corrupt anything** (the change waits durably until the build pulls it; there is no "if no build, edit docs directly" branch to get wrong). The only detector is `implement`'s concurrent-run guard (lease + heartbeat + TTL + fencing token), safe-when-wrong. **(4) The consumer:** the supervisor **drains + routes at each safe point** (work-conserving): `expedite` first / `standard` FIFO; autonomous-resolvable → integrate via the `iterate`/`bug` engine **at the safe point** → the FRD gate (same quality); needs-owner → `decisions.md` + notify + **defer & continue** with independent FRDs; structural/new-design → pause (`rethink_pending`) + guide the owner.
**Why:** Builds take hours/days, so "wait until it finishes to make a change" is not viable. The owner's two fears, both answered with research (Kanban classes of service; human-in-the-loop checkpoints; production agents' injection-queue-drained-at-iteration-boundaries): **(A)** "guarantee 100% it detects a running build?" → you don't need to — capture is detection-free + doc-free, so a misdetection only affects *timing*, never correctness (durable queue + fencing + DR-067 collision-proofing = safe-when-wrong); **(B)** "too many skills, I get confused" → **one door** (`/change`) routes bug/feature internally, while direct verbs stay direct (`/implement`, `/decide`) — NOT a mega-router for everything (that would hurt agility). Not an overload of `implement`: it stays an orchestrator of specialized subagents over deterministic control flow (DR-013); quality is enforced by the independent FRD gate + `verify.sh`, not trust.
**Impact:** new `plugin/skills/change/SKILL.md`; `plugin/skills/implement/SKILL.md` (drain + route at the safe point), `plugin/skills/bug/SKILL.md` (writes to the unified `changes/` as `type:bug`), `plugin/skills/iterate/SKILL.md` (the integration engine `change`/the build invoke); `factory/standards/build-orchestration.md` §8 ("The change queue"); `factory/decisions/registry.yaml` (DR-069). Plugin **v8.31.0** (MINOR — new skill). **Deferred:** Mission Control surfacing the pending `changes/` + `decisions` (in Spanish) + the Manual narrative (DR-046) — `mission-control/` is mid-build. Pending: commit + `claude plugin update`.

## 2026-06-20 — Build-lifecycle honesty: running:false on every exit + stop-the-loop (DR-068) — plugin v8.30.0
**What:** Closed the gaps the stuck-spinner incident exposed. The engine already clears state on a **normal** end (close-out writes `running:false` + a fail-safe agent "never leave a phantom running build"). Two gaps remained: **(1)** a **KILLED/stopped run never reaches** that close-out, so clearing state is the **supervisor's guaranteed LAST act on EVERY exit** (clean end, budget/health/needs-owner stop, owner stop signal, or interruption) — `running:false` + remove the build lock. A stopped build still reading `running:true` is a **lying flag**. Backstop: a stale `running:true` (heartbeat past the 10-min TTL) is **never trusted as alive** — every reader crosses it with heartbeat recency (DR-066) and reads STOPPED, and the next state-writer auto-corrects it. **(2) Stop-a-run ≠ stop-the-loop** — the supervisor relaunches the next pass by design (the overnight walk-away feature), so a lone TaskStop just spawns another pass ("I stopped it but it came back"). To halt the **whole build** the owner sets `rethink_pending: true` (or `.pandacorp/run/stop`), checked at each safe point **and before every relaunch**; the distinction is now surfaced to the owner, not implicit.
**Why:** The owner saw a stuck loader + `running: true` while the build had been **dead ~1h** (measured: 59m since any write/event), and earlier the build "came back" after he stopped it. Meta-lesson baked into the DR: the agent first claimed "the engine doesn't guarantee `running:false`" from a **grep that misfired**, then **reading** the engine (lines 441/453/457-461) showed it DOES on a normal end — *verify by reading the code, not a tool's output* (the new `verify-before-telling` rule, set the same session after repeated corrections).
**Impact:** `plugin/skills/implement/SKILL.md` ("Stop the LOOP vs stop a run" + "Guaranteed shutdown — running:false on EVERY exit"), `factory/standards/build-orchestration.md` §6 ("Build-lifecycle honesty"), `factory/decisions/registry.yaml` (DR-068). Plugin **v8.30.0** (MINOR). No engine edit (it already had the normal-end close-out + fail-safe). Pairs with DR-066 (consumer recency) + DR-067 (tree health). Pending: commit + `claude plugin update`.

## 2026-06-20 — No-stash recovery + working-tree health is a first-class supervisor signal (DR-067) — plugin v8.29.0
**What:** Hardened the build engine/supervisor so a git-tree breakage can't wedge a build for an hour again. Two corollaries of the resumable, single-writer/no-merge design (DR-050 §2) are now **explicit and enforced**: **(1) Never `git stash` to preserve in-flight work** across a kill/relaunch/stop — the build rebuilds uncommitted between-safe-point work from the frontmatter on relaunch; a stash popped later onto a moved tree **conflicts**. The only safe recovery from a dirty/conflicted tree is to **restore to `last_green_sha`**, never a stash-pop; the baseline self-heal drops stale build stashes + leftover temp `preview-wo*` pages on startup. **(2) The supervisor watches the tree's git health as a first-class signal** — the Monitor runs `git status --porcelain` each tick; any `UU`/unmerged path or `<<<<<<<` marker is a **broken tree**, emits an alarm, and routes to the bounded auto-repair (restore to last green). This closes the gap DR-066 left: DR-066 made *liveness* honest, but **a tree can be broken while perfectly alive** — a monitor blind to a broken tree is the same failure class as one blind to liveness.
**Why:** The MC Phase-2 build went **off-script**: its orchestrator improvised `git stash` when it killed/relaunched runs (the skill never told it to). A later apply of a stale `frd02-lacampana` stash onto an already-`VERIFIED` FRD-02 left `<<<<<<<` markers in `Button.tsx` + `CampaignPipeline` + docs → 130 gate errors → the build wedged **~1h** while the supervisor — blind to `git status` — reported "healthy" and even did a soft reset (which does NOT clear unmerged paths). Also explains the **stash pile-up** (one per kill/stop, never cleaned) and the **half-written files** (stop mid-pass). Owner's ask: *"make sure this never happens again; the supervisor and subagents should detect and resolve problems themselves."*
**Impact:** `plugin/skills/implement/SKILL.md` (Rules: no-stash; baseline self-heal restores a dirty/conflicted tree + drops stale stashes/preview pages; Monitor watches `git status`; React restores a broken tree to last green), `factory/standards/build-orchestration.md` §6 ("Working-tree health & no-stash recovery"), `factory/decisions/registry.yaml` (DR-067). Plugin **v8.29.0** (MINOR). The MC working-tree cleanup itself is deferred — `mission-control/` is owned by the still-running Phase-2 build; the hardened engine applies on the next plugin update + relaunch (the baseline self-heal then cleans the stashes/preview cruft automatically). Pending: commit + `claude plugin update`.

## 2026-06-20 — Observability fidelity: a monitor tells the truth about its own liveness (DR-066) — plugin v8.28.0
**What:** Codified a factory-wide standard so any project with a **dashboard/monitor/status page** never presents a self-reported flag as proof of life. Three rules, both sides of the wire: **(a)** liveness = `running` **AND** fresh (`now − heartbeat < window`), never the flag alone — a frozen `running: true` with a stale timestamp is **not** live; **(b)** the UI **declares its own freshness** in three graded bands off the producer tick `T` — *en vivo* (`< 3·T`), *datos de hace X* (stamped, `3·T..hard_TTL`), *sin señal* (`≥ hard_TTL`, the producer's liveness TTL — for the build, the 10-min concurrent-run TTL); **(c)** the **producer emits a positive, time-driven heartbeat** every tick — append a heartbeat event to the stream AND advance a freshness stamp (`last_event_at` / `supervisor_heartbeat`), scheduled (not only event-driven) so a quiet-but-alive process doesn't read as dead. **Gate:** a "live" transport doesn't ship without proving (1) a real state change reflects in the UI fast — push/watch (fs-watch/SSE/websocket) for near-real-time when cheap, **≤ 30 s** worst-case polling — and (2) the *sin señal* path actually engages when the heartbeat stops.
**Why:** An audit of Mission Control's READ layer found `status.yaml` had `running: true` + a **frozen** `supervisor_heartbeat` while the build was advancing — a naïve monitor would have shown "corriendo" as proof of life. Root cause: the heartbeat was advanced only by the **event-driven** Monitor, so it froze between long quiet agents, and nothing in the consumer crossed `running` with recency. A monitor exists to be trusted exactly when something is wrong; its worst failure is a confident lie. Owner choices (2026-06-20): graded 3-band freshness; gate latency = as real-time as possible without hurting app perf / pegging the machine → push/watch when cheap, ≤30 s worst-case polling.
**Impact:** Standards `factory/standards/observability.md` (new "Liveness & freshness fidelity" section + verification bullet), `quality.md` (new "Observability-fidelity gate"), `build-orchestration.md` §9 (producer heartbeat + consumer rule + gate). Producer side: `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (engine advances `last_event_at` + `updated_at` at each safe point — sync-rollups + FRD gate; EMIT documented as the producer heartbeat), `plugin/skills/implement/SKILL.md` (supervisor appends a time-driven `SupervisorTick` heartbeat event + advances the stamp on the scheduled tick), `plugin/templates/shared/.pandacorp/status.yaml.tpl` (declares `run_started_at`/`supervisor_heartbeat`/`last_event_at` + the "never read `running` alone" rule). `factory/decisions/registry.yaml` (DR-066). Plugin **v8.28.0** (MINOR); `OVERLAY_VERSION` 8.27.0 → 8.28.0 (status.yaml.tpl + engine are injected overlay files → conformance re-syncs them). **Deferred (honest scope boundary):** the Mission Control **consumer** side (the UI applying (a)/(b) when reading `status.yaml`) — `mission-control/` is owned by the parallel Phase-2 build window, so this change ships only the standard + producer; the consumer adoption + the Manual narrative sync (DR-046) land when that window is free. Pending: commit + `claude plugin update`.

## 2026-06-20 — Five process improvements from the Mission Control dogfood (DR-063/064/065 + DR-057 extended) — plugin v8.27.0
**What:** Codified five engine/standard improvements discovered while building Mission Control — all general (they apply to every future project, not MC-specific). **(1) Phase-aware Stop gate (DR-063, +improvement #5):** the strict whole-project `verify.sh` no longer runs as the gate of *every* Stop while an incremental build is active — mid-build the tree is legitimately red between safe points, so it blocked every turn (in the building session **and** any other session parked at that `cwd` in a shared repo). The supervisor now keeps a freshness-stamped lock at `.pandacorp/run/build.lock` (touched at launch + each ~2-min heartbeat, removed at close); `verify-before-stop.sh` no-ops while it's fresh (`find -mmin -10`) and re-engages strictly when the build ends or the lock goes stale (same 10-min TTL as DR-050 §9). Enforcement during a build is owned by the engine: per-FRD (`verify.sh --since`) + the full suite at close-out. `verify.sh` stays always-strict; only the Stop-hook wrapper is phase-aware. **(2) Bidirectional functional reconciliation prototype↔FRD (DR-064, refines DR-056):** blessing an approved prototype now reconciles **behaviour**, not just the look — Group A (prototype has it, FRD lacks it → update the FRD) + Group B (FRD wants it, prototype lacks it → build it into the prototype or flag the gap), itemized and resolved before the prototype is canonical. **(3) Complete foundation before fan-out (extends DR-057):** the foundation = the UNION of every shared primitive any surface's mock references, derived from `components.md`; the engine runs a **foundation-completeness gate** before any surface fans out (refuses to start surfaces until the foundation is complete + green). **(4) Bounded auto-repair (DR-065):** for the high-confidence, recoverable, bounded class "a surface failed because a primitive it needs isn't in the foundation", the engine now **auto-resolves** (reset to last green → add the primitive → rebuild → retry, cap 2) instead of stopping to ask — escalating to the owner only on low confidence / human-judgment / after N failed auto-repairs.
**Why:** The Phase-2 Mission Control build exposed each one by suffering it: the Stop gate blocked every turn of the background build; the design re-anchor reconciled only the visual while behaviour diverged (~7 gaps each direction); the foundation was declared "ready" while incomplete (Party primitives Room/AgentSprite/StoneBridge/FlowStrip missing) so surfaces shipped flat; and the engine **detected** that root cause correctly but **stopped to ask** the owner instead of applying the fix it already knew. The owner's framing: "the implement skill should be smart enough to resolve it like you did" — autonomy without loops or burning the budget.
**Impact:** Engine `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (#3 foundation-completeness gate + #4 bounded auto-repair via `missingFoundation` + `FOUNDATION_REPAIR_CAP=2`); `plugin/scripts/verify-before-stop.sh` + `plugin/skills/implement/SKILL.md` + `plugin/templates/stack-a-nextjs/verify.sh` (#1 lock); `plugin/skills/{design,adopt}/SKILL.md` + `plugin/agents/designer.md` (#2); standards `factory/standards/{quality.md,build-orchestration.md,design.md}`; `factory/decisions/registry.yaml` (DR-063/064/065 new + DR-057 extended). Plugin **v8.27.0** (MINOR), `OVERLAY_VERSION` 8.23.0 → 8.27.0 (the `verify.sh` template comment is an injected-file change → conformance re-syncs it). #1's hook is plugin-scoped, so it needs no `upgrade` change (a deliberate decision). Owner choices (2026-06-20): Stop gate = full no-op during an active build; auto-repair cap = 2. Pending: commit + `claude plugin update`.

## 2026-06-20 — DR-046 prose discipline gets an enforcement hook — plugin v8.26.0
**What:** Added a non-blocking `PreToolUse` (Write|Edit) reminder hook (`plugin/scripts/remind-manual-sync.sh`) that makes DR-046's *prose* half reliable. DR-046 already kept the Manual's **Reference** drift-proof by derivation, but the hand-authored **narrative** (Tutorial/Guides/Concepts) relied on the agent remembering — and most factory changes happen in **free conversation** or in `/memory`/`/learn`, not through a skill. The hook fires whenever a conversation edits the **operable surface** (`plugin/skills/`, `plugin/agents/`, `factory/standards/`, `factory/decisions/registry.yaml`, `factory/constitution.md`) and injects a reminder to update the matching `mission-control/content/manual/` page in the **same change** — clarifying that the Reference catalogs auto-derive (don't hand-copy) and only the narrative needs the manual touch. Scoped to the factory repo (it never fires in a product project); independent of cwd and of any active skill. NEVER blocks (exit 0).
**Why:** The owner asked how to *guarantee* the Manual always reflects the factory — Mission Control being the navigable face of Pandacorp's know-how, so anyone can open it and understand the flow, skills and decisions. The derived Reference was already solved; the gap was the narrative. A hook (the same mechanism as the write-gate and `verify-before-stop`) closes it without any new skill or `iterate`/`implement` ceremony — the docs are plain markdown edited in the same conversation. Owner decision (2026-06-20): **reminder-first** while Mission Control's own build is mid-flight (55/78 WOs — a hard block would fight the build's commits), **harden to a hard-blocking Stop variant** once that build finishes.
**Impact:** `plugin/scripts/remind-manual-sync.sh` (new), `plugin/hooks/hooks.json`, `plugin/.claude-plugin/plugin.json` (v8.26.0), `factory/decisions/registry.yaml` (DR-046 nota). Manual updated in the same change: `mission-control/content/manual/concepts/hooks-gates-seguridad.md`. Enforces DR-046 (no new DR). Pending: commit + `claude plugin update` (the hook activates on session restart); later — the hard-block Stop variant.

## 2026-06-19 — "One cohesive application" is a factory standard (DR-062) — plugin v8.25.0
**What:** Made cross-surface visual cohesion an explicit, enforced standard after the owner reviewed the Mission Control prototype and found surfaces that felt like *different apps*: the Documentation page had a bespoke "Códice del gremio" header in a different style than other sections' titles, and Inicio/Tablero/Portfolio had no page title while other pages did. New `factory/standards/design.md` **§7**: standardize the **framing patterns** across every surface (beyond component reuse, DR-057) — ONE page-title block with the **H1 equal to its nav-menu label** (extra context in a subtitle, never a different title — the *you-are-here* principle), ONE section-header / tab / chip / panel / empty-state language; a surface inventing its own variant of an existing pattern is a **cohesion defect**; deviations (an immersive view) are justified, not silent. These framing patterns are first-class entries in `docs/design/components.md`, and the **`reviewer`'s runtime/visual lens now checks cross-surface consistency** (rejects a screen that looks like a different app from its siblings). Resolved the owner's menu-vs-title question: **align them** (H1 = nav label + subtitle). Applied to the MC prototype (added page titles, folded the Códice header into the standard `pageHead`, unified section-headers/rail-labels via shared helpers, **renamed "Manual" → "Documentación"**).
**Why:** Reusing components (DR-057) isn't enough — if each surface frames itself differently (titles, headers, tabs), the app still feels like a bag of apps. The owner asked to *recalcar* this in the design skill so generated apps feel like one product.
**Impact:** `factory/standards/design.md` §7 (+ wiring row), `plugin/skills/design/SKILL.md`, `plugin/agents/designer.md`, `plugin/agents/reviewer.md`, `factory/decisions/registry.yaml` (DR-062). MC prototype standardized (`docs/design/prototype/index-grupo-b.html`). Plugin **v8.25.0** (MINOR; no `OVERLAY_VERSION` change — design-time guidance). Builds on DR-057.

## 2026-06-19 — Canonical gate-config bugs fixed: bash-3.2 portability + biome machinery/docs exclusion (plugin v8.24.1)
**What:** Installing the DR-059 canonical gate into Mission Control (the first real use) surfaced two template bugs that would break **every** project's gate. (1) `plugin/templates/stack-a-nextjs/verify.sh` used `shopt -s inherit_errexit`, a **bash 4.4+** option — on macOS' default **bash 3.2** the gate aborted at startup ("invalid shell option name"). Made it portable: `shopt -s inherit_errexit 2>/dev/null || true` (no-op on old bash). (2) `plugin/templates/stack-a-nextjs/biome.json` had no path scope, so Biome linted the **machinery** (`.claude/workflows/pandacorp-build.js` — a Workflow script with top-level `return`/`await`, not product code) and the **design mocks/prototype** under `docs/`. Scoped it: `files.includes: ["**", "!.claude/**", "!docs/**", "!public/**"]`. Both are config-only PATCH fixes; no rule semantics changed, so `OVERLAY_VERSION` is unchanged.
**Why:** The canonical gate must be **runnable** on the owner's machine and lint only product code — the MC dry-run proved neither held. Found because MC's Stop gate (`verify.sh`) failed loudly the moment the canonical config was installed.
**Impact:** `plugin/templates/stack-a-nextjs/{verify.sh,biome.json}`, `plugin/.claude-plugin/plugin.json` (v8.24.1). Refines DR-059 (no new DR). MC keeps its lenient gate until Phase 2 (see `mission-control/docs/decision-log.md`).

## 2026-06-19 — Demo-only prototype controls are a marked, factory-wide convention (DR-061) — plugin v8.24.0
**What:** Made the "this is a demo control, not real product behaviour" marking a **factory standard**, after the owner noticed the Mission Control Party prototype's effort picker + Encender/Reiniciar buttons read as if the dashboard could *control* the factory (MC is read-only; the build is launched by `/pandacorp:implement`). New `factory/standards/design.md` **§6**: any affordance a prototype adds purely to **preview states** (mode/effort pickers, play/pause, reset, state togglers) that won't exist in the real (read-only or skill/CI-driven) app MUST be wrapped in a **dashed-border block + a small uppercase `DEMO` tag + a one-line note of how the real thing works**; any value the real app will surface (e.g. the effort a build ran with) goes as **read-only data in a real UI surface**, not only in the demo block; the factory-off/idle state is **derived from real state**, not a button. Canonical CSS/HTML pattern included; the design skill + `designer` agent now reference it. Applied to the Mission Control prototype itself (the SOLO DEMO block + effort in the Misión bar).
**Why:** A consistent visual contract so the owner (and anyone reading a mockup) never mistakes a state-preview affordance for real behaviour — recurring because every navigable prototype tends to add such controls.
**Impact:** `factory/standards/design.md` §6 (+ wiring row), `plugin/skills/design/SKILL.md`, `plugin/agents/designer.md`, `factory/decisions/registry.yaml` (DR-061). Prototype: `mission-control/prototype/party-proposal.html` + its spec. Plugin **v8.24.0** (MINOR; no `OVERLAY_VERSION` change — design-time guidance, not an injected project file).

## 2026-06-19 — Verification pass: made the DR-056/057/059/060 fixes REAL (5 blockers + cross-feature review + Option-B commit) — plugin v8.23.0
**What:** A red-team audit + web research (before trusting the process / MC Phase 2) found the recent DRs were largely **declared but not executable**; fixed all of it. **Engine (DR-057/060):** the planner now emits each WO's `artifacts`+`foundation` (the disjoint filter had no data); a **real regex glob matcher** replaces the crude prefix-match (11/11 unit tests); **foundation-first is engine-enforced** (foundation WOs occupy wave 0 alone); **Option-B single-writer commit** (workers never call git — one serialized writer commits each wave; NOT worktrees — see below); Status Note now carries decisions/assumptions. **Blockers:** the visual gate's **baseline-blessing flow** (a missing baseline on a new route is blessed by the reviewer after the VLM judge confirms the mock, NOT a RED dead-stop); `verify.sh --since <sha>` actually scopes vitest (`--changed`); **`docs/design/components.md` is now produced + existence-gated** in design/adopt (the reuse mechanism was inert); the **conformance check covers the toolchain** (knip/madge/playwright deps + test:smoke/visual scripts + strict tsconfig), so a cold-start project isn't RED on a missing tool. **Architecture (P0):** a **cross-feature integration review** at close-out — traces data flow across feature seams + verifies producer/consumer `docs/api/<wo>.md` contracts agree (the dominant parallel-build failure the per-FRD gate can't see). **Worktree decision (reconciled honestly):** rejected git-worktree-per-agent for our case — Claude Code's own *agent-teams* docs prescribe partition-by-files (not worktrees) for a coordinated engine; with disjoint artifacts the isolation is already achieved, and worktrees cost a per-tree node_modules + cold Next build (a symlinked node_modules breaks HMR) and end in a merge (CoAgent: textual merge ≠ semantic safety). Option-B (single-threaded writes, Cognition) is cheaper + safer.
**Why:** The owner asked to GUARANTEE the process won't fail/rework before building more, and to verify (not trust) — including reconciling a past "worktrees won't work" with the audit's "worktrees are best practice" (both true, different contexts). Verification caught 5 build-blockers that would have dead-stopped a real run.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/templates/stack-a-nextjs/{verify.sh,STACK.md}`, `plugin/agents/{reviewer,implementer,designer}.md`, `plugin/skills/{design,adopt,upgrade}/SKILL.md`, `factory/standards/build-orchestration.md` §2-3. Refines DR-056/057/059/060 (no new DR). Plugin **v8.23.0**, `OVERLAY_VERSION` 8.22.0 → 8.23.0. Next: a **controlled dry-run** (throwaway 2-FRD web project, watched) = the empirical proof before MC Phase 2.

## 2026-06-19 — Process audit fixes: gate-config conformance (DR-059) + engine can't collide parallel WOs (DR-060)
**What:** An exhaustive 3-front process audit (before continuing Mission Control) found two systemic holes; both fixed. **DR-059 (gate-config conformance):** the meta-root-cause — standards were written in prose + a STACK example, but a project's REAL `verify.sh`/`biome.json` never wired them, so the smoke/visual gates (DR-055/056), `knip`, the Biome domains (react/next/test) + a11y group, and complexity=error were all **silently OFF** and nothing checked. Fix (the gate-config analogue of DR-051): the stack ships **canonical `biome.json` + `verify.sh` + `knip.json`** (`plugin/templates/stack-a-nextjs/`) installed **verbatim** by blueprint + upgrade, MAXIMUM fail-closed (`biome check --error-on-warnings`, complexity=error, domains+a11y on, knip + smoke + visual all fail-closed); **`/pandacorp:upgrade` runs a conformance check** that regenerates/fails when a project's gate config drifts. **DR-060 (engine coordination):** the duplicated-banner bug's mechanism, generalized — the `## Status Note` is post-hoc (can't coordinate intra-wave), the disjoint-artifacts rule was unenforced, `docs/api.md` was a shared raced file, and WO state wasn't atomic with its commit. Fix: each WO declares `artifacts: [globs]` and the **engine serializes wave-parallel WOs whose artifacts overlap** (`pickDisjointWave`); per-WO contract `docs/api/<wo-id>.md` replaces the shared file; the implementer/backend reads its deps' Status Notes before coding; the WO commits code+frontmatter+note in **one atomic commit** with index.lock retry.
**Why:** Owner asked for an exhaustive review of the whole process before building more, to avoid building Phase 2 on broken foundations. The audit confirmed: MC's verify.sh was biome+tsc+vitest only (so DR-055/056 + ~14 rules unenforced); the engine could co-schedule colliding WOs (the banner bug, generalized). Owner chose MAXIMUM fail-closed.
**Impact:** New `plugin/templates/stack-a-nextjs/{biome.json,verify.sh,knip.json}`; `STACK.md`, `plugin/skills/{blueprint,upgrade}/SKILL.md` (install verbatim + conformance), `factory/standards/quality.md` (DR-059). Engine `pandacorp-build.js` (artifacts/disjoint selector + per-WO api + dep-note + atomicity), `factory/standards/build-orchestration.md` §2, `plugin/agents/{architect,backend-dev,frontend-dev}.md`, `plugin/skills/{blueprint,work-orders}/SKILL.md`, `plugin/templates/docs/{work-order,blueprint}-template.md` (DR-060). `factory/decisions/registry.yaml` (DR-059, DR-060). Plugin **v8.22.0**, `OVERLAY_VERSION` 8.21.0 → 8.22.0. Follow-up: per-agent git worktree to fully kill the commit race; MC's own gate config + engine copy get wired in Phase 2. Full audit (30+ findings, file:line) in the session.

## 2026-06-19 — Stop parallel agents duplicating components (foundation-first + component inventory) + adopt Claude Design (DR-057, DR-058)
**What:** Two paired changes after the owner spotted two near-identical warn banners in Mission Control (`PluginSyncBanner` + `OrphansBanner`) — built by two agents that never communicated, with duplicated code and slightly different layouts (triangle left vs top) and **no shared `Banner` primitive**. **DR-057 (reuse & coherence):** (1) the Build Plan schedules the shared design-system primitives as a **foundation wave FIRST**, before feature work orders fan out (the owner's "build the common things first"); (2) a living **component inventory `docs/design/components.md`** (name · purpose · path · variants), seeded by design + the foundation WO, that the **engine injects into every UI build prompt** (`reuseRef`) so "reuse → adapt → create-only-if-new" is the default; (3) the `reviewer` quality lens **rejects** a component that re-implements an existing primitive. **DR-058 (Claude Design):** for the EXPLORE path (proposing a UI from scratch) the design phase now **prefers Claude Design** — Anthropic's specialized design tool (claude.ai/design) via the `/design-sync` skill + the `DesignSync` tool — to generate a coherent system + reusable library, whose `_ds_manifest.json` IS the DR-057 inventory; for ADOPT-VISUAL it may push the extracted system up. Hand-coded HTML is the fallback; Claude Design is offered, not forced (needs the owner's claude.ai login).
**Why:** Research (deep-research, 3-0 high confidence): parallel AI agents lacking structured knowledge of the existing system reinvent components (default to shadcn/Tailwind); a machine-readable inventory (Storybook manifests, shadcn registry) measurably raises reuse. Claude Design is REAL — verified by web sources AND by the `DesignSync` tool present in this environment — and converges on the same mechanism (a coherent library + a manifest + self-checking), so it solves coherence-at-design-time and feeds reuse-at-build-time at once.
**Impact:** `factory/standards/build-orchestration.md` §3 (foundation-first + inventory + gate), `factory/standards/design.md` (Claude Design), `factory/decisions/registry.yaml` (DR-057, DR-058). Plugin (same change, **v8.21.0**, `plugin/docs/decision-log.md`): `templates/shared/.claude/workflows/pandacorp-build.js` (`reuseRef` injection), `agents/{implementer,frontend-dev,reviewer,designer}.md`, `skills/{design,blueprint,work-orders}/SKILL.md`, `templates/rules/clean-code.md` (the component-inventory rule → ships to projects). `OVERLAY_VERSION` 8.20.0 → 8.21.0. Builds on DR-049/050 (Build Plan) + DR-054 (does not override an existing approved prototype). Pending: Mission Control's two banners refactored onto a shared `Banner` primitive in Phase 2.

## 2026-06-19 — Guaranteeing 100% visual fidelity when AI agents implement: shard → ground → in-loop → two-layer gate (DR-056)
**What:** Closed the question "how do we *guarantee* an AI agent reproduces the approved HTML prototype 100% visually?" with four required mechanisms, after auditing the engine and researching the field (3 parallel web agents over Design2Code/DCGen/UI2Code^N, v0/Builder.io, Anthropic). The decisive audit finding: **the build engine passes the implementer only a one-line summary** — it never sees the FDD, mocks or tokens; the design reaches it only if it reads fixed paths that were **empty for all ~17 UI FRDs**. DR-056: (1) **Shard** — the design phase splits the approved whole-app prototype into ONE self-contained mock per FRD (`docs/frds/<frd>/mocks/`); the whole-app prototype's defined home is `docs/design/prototype/`; never hand an agent the whole prototype. (2) **Ground/materialize** — the WO's `## Visual reference` is filled with the exact mock + token slice + fdd pointer (the WO body IS the agent's design context), AND the engine **injects** those refs + the in-loop directive into the build prompt. (3) **In-loop** — the builder renders→screenshots→compares-to-mock→self-corrects ≤3 cycles before IN_REVIEW. (4) **Two-layer gate** — Layer A deterministic `toHaveScreenshot` regression (hard block) + Layer B VLM mock-judge (named structural divergences, escalate-on-uncertainty, different model). Plus a **complete-extraction** rule (capture every layer: base + app-wide skin + sub-view specs). Because the reference IS HTML, the agent **translates** it (HTML→React+Tailwind+tokens), the highest-fidelity path.
**Why:** The owner spent significant time polishing the prototype and demanded a guarantee, not a hope. "Use the tokens" never sufficed because the builder never received the design. Research convergence: the dominant failure is element-omission/layout-drift, fixed by scope+ground+loop; and no single verification tool is reliable (pixel diff = "looks the same", VLM = "is the right thing") → layered gate.
**Impact:** New `factory/standards/design.md` §5–6; `factory/standards/build-orchestration.md` §5 (two-layer gate); `factory/decisions/registry.yaml` (**DR-056**). Plugin (same change, **v8.20.0**, `plugin/docs/decision-log.md`): `templates/shared/.claude/workflows/pandacorp-build.js` (`designRef` injection), `agents/{implementer,frontend-dev,reviewer}.md`, `skills/{design,adopt,work-orders}/SKILL.md`, `agents/designer.md`, `templates/docs/work-order-template.md`, `templates/stack-a-nextjs/STACK.md` (Playwright visual config + `verify.sh` Layer-A stage). `OVERLAY_VERSION` 8.19.0 → 8.20.0 (engine template changed → projects get the injection on upgrade). Builds on DR-054 + DR-055. Pending: Mission Control execution (Phase 0 re-skin → Phase 1 shard → Phase 2 re-implement).

## 2026-06-18 — Design anchor refinement: detect-then-confirm, don't ask blind (DR-054)
**What:** The DR-054 anchor flow now **scans the repo first** and **presents what it finds for the owner to confirm**, instead of asking up-front whether a design exists. `design` Step 0 and `adopt` (step 2 inventory + step 3d confirm) scan for a prototype/HTML mockups, `docs/design/`, a token/CSS system, screenshots or the live UI; if found → "encontré esto, ¿es tu referencia? ¿falta alguna?"; if nothing → ask whether it lives elsewhere (Figma); if none → EXPLORE.
**Why:** Owner's call — the factory should do the looking and let the owner validate, not ask blind. A blind question misses a prototype sitting in the repo (how Mission Control's `prototype/` got orphaned in the first place).
**Impact:** `factory/standards/design.md` §1, `factory/decisions/registry.yaml` (DR-054 default). Plugin (same change, **v8.19.1**, `plugin/docs/decision-log.md`): `skills/design/SKILL.md`, `agents/designer.md`, `skills/adopt/SKILL.md`. No overlay file touched (`OVERLAY_VERSION` stays 8.19.0).

## 2026-06-18 — Design fidelity + the Preview Smoke Gate: the approved visual is a frozen contract, and the app must render before VERIFIED (DR-054, DR-055)
**What:** Closed the two systemic holes that made Mission Control ship a UI matching neither its prototype nor a clean render. **DR-054 (design fidelity):** the design phase (`design`) and brownfield adoption (`adopt`) now **ask the owner first** whether an approved visual exists (a prototype, the live UI, an existing token/CSS system, a Figma); if yes, the `designer` takes the **ADOPT-VISUAL path** — extract it *faithfully* into `design-tokens.json` + `DESIGN.md` (+ `design-decisions.md`), stage its assets (incl. embedded iframes), **skip** the 3-direction exploration, and the owner gate becomes a fidelity check. Demoting/relaxing an approved visual is **forbidden**. The frozen contract (DESIGN.md + design-tokens.json + design-decisions.md + per-UI-FRD fdd.md + mocks/) is **mandatory and existence-checked** at the advance gate. Fidelity is enforced downstream (FRD `ui:`/`visual_source`, blueprint Build Plan cites mocks, WO `## Visual reference` + fidelity AC, implementer/frontend-dev must *reproduce* the mock, reviewer verifies the rendered result). **DR-055 (Preview Smoke Gate):** for any FRD with UI routes the gate **renders each route in a real browser** (Playwright) and is **fail-closed** — console error / pageerror / failed request / blank-or-error render fails it, and a *missing* smoke harness is RED, not a skip. It rides inside `verify.sh` and the `reviewer` re-runs it as a 4th lens. New canonical standard `factory/standards/design.md`; build-orchestration §5 layer 3 flipped from opt-in/default-off to mandatory/default-on for UI.
**Why:** The owner's dogfood diagnosis was exact. MC was **adopted** with `prototype/index.html` (+ party iframes) as the agreed 100% visual target, but `adopt` had **no design step**, so the prototype was never migrated; the later `design` brief **demoted it to "functional only" and invented a cold-blue palette** because the `design` skill was hard-wired to invent 3 fresh directions with no anchor-on-approved-visual path; the frozen artifacts were never produced (code referenced a `design-tokens.json` that didn't exist); and **nothing ever opened the app in a browser** — `verify.sh` was biome+tsc+vitest, the reviewer only wrote node tests + read code, the supervisor watched liveness, and the one runtime hook ("run the e2e of critical flows") was a phantom that degraded to vitest (fail-open). So 112/112 WOs went VERIFIED while every route threw React duplicate-key errors and matched no mockup. "Bastaba con entrar una vez a la app."
**Impact:** Plugin (same change, **v8.19.0**, `plugin/docs/decision-log.md`): `skills/{design,adopt,spec,blueprint,work-orders}/SKILL.md`, `agents/{designer,implementer,frontend-dev,reviewer}.md`, `templates/docs/{frd,blueprint,work-order}-template.md`, `templates/rules/quality-and-testing.md` (definition-of-done +smoke), `templates/stack-a-nextjs/STACK.md` (Playwright smoke + fail-closed `verify.sh` stage), `OVERLAY_VERSION` 8.18.0 → 8.19.0. Factory: **new `factory/standards/design.md`** (canonical), `factory/standards/build-orchestration.md` §5, `factory/decisions/registry.yaml` (DR-054, DR-055). Pending: Mission Control remediation (Vía B) re-anchored on the prototype, under the corrected process. Mission Control's Manual surfaces DR-054/055 automatically via the derived decision-rules catalog (DR-046).

## 2026-06-18 — Dev ports allocated from a central ledger, never hashed (DR-022)
**What:** Closed the gap in the port convention: it said "the factory records [a base range] when a project is created" but **nothing recorded it** — there was no allocator or ledger, so the no-collision claim wasn't actually guaranteed. Replaced with a real mechanism: a central ledger `factory/ports.yaml` (personal, **gitignored** — it tracks the owner's local projects, like the portfolio) is the single source of truth. `/pandacorp:scaffold` reserves the next free **10-wide block from base 4000**, records `slug → base` and writes `dev_port_base` into the project's `.pandacorp/status.yaml`; `/pandacorp:blueprint` materializes the app port (`base+0`) into `.claude/launch.json` (the harness preview tool's config) and the app/DB/service ports into `.env`. Offsets within a block keep the agent's build and the owner's review worktree apart (app `+0`/`+1`, Postgres `+2`/`+3`, Redis `+4`/`+5`, `+6..+9` spare). Updated **DR-022**.
**Why:** The owner builds several apps in parallel and asked for a *guarantee*, not a probability, that ports never cross. Rejected the tempting stateless option — hashing the slug into a range — because the birthday problem makes collisions real (~30% at a few dozen projects). Sequential allocation from a ledger gives zero collisions by construction and, because a project keeps its block forever, deterministic re-runs. Same philosophy as the rest of the factory (one source of truth: `registry.yaml`, the portfolio). Block width 10 (owner's choice over 100) → ~600 projects, ample offsets per project.
**Impact:** `factory/standards/infra.md` (Port convention section rewritten — canonical doc), `factory/decisions/registry.yaml` (DR-022 default), `factory/ports.yaml` (new ledger, seeded `panda-corp: 4000`), `.gitignore` (ledger ignored), `.claude/launch.json` (dogfood: panda-corp's app → 4000). Plugin side (same change, **v8.18.0**, `plugin/docs/decision-log.md`): `scaffold` (step 4c reserves), `blueprint` (step 6 materializes), `status.yaml.tpl` (+`dev_port_base`), `OVERLAY_VERSION` 8.17.0 → 8.18.0 (existing projects gain the field on auto-upgrade). Mission Control's Manual surfaces DR-022 automatically via the derived decision-rules catalog (DR-046).

## 2026-06-18 — The owner profile is a living document: bootstrap from history + continuous capture (DR-053)
**What:** Turned `factory/profile.md` from a one-time onboarding form into a **living profile** with two mechanisms. (1) **Bootstrap**: `/pandacorp:onboarding` now, before interviewing, detects the owner's past Claude Code conversations (`~/.claude/projects/*/`, archived included, deleted excluded), **asks yes/no**, and on yes mines them through a **cheap funnel** — layer 0 read distilled auto-memory, layer 1 a *mechanical* `jq` extraction of only the owner's typed turns (0 model tokens), layer 2 parallel agents pulling structured personal signal — then **presents "this is what I already know about you"** and interviews from there, never from zero. (2) **Continuous capture**: a new rule (panda-corp `CLAUDE.md` rule 9 + every product project's `.pandacorp/guide.md` rule 9) appends/updates durable personal facts (interests, hobbies, tastes, goals, assets, working style) into the profile as they surface in any conversation — read-first, deduped. Codified as **DR-053**.
**Why:** The owner wanted the factory to *know him* without re-interviewing from scratch and to keep learning him as they talk — purely for **personalization** (e.g. "I like reading manga" → `/discover` and `/recommend` lean that way). Explicitly **NOT** the engineering self-learning loop (DR-047) nor the decision log: three distinct planes (owner profile vs engineering memory vs decisions), never mixed. Designed cheap on evidence: a measured pandatrack transcript was 18 MB on disk but only ~79 KB (~0.4%) human-typed text, so the bootstrap funnels mechanically before any agent reads — all signal, a fraction of the tokens. `guide.md.tpl` already stamps `{{FACTORY_PATH}}`, so a product project already knows where the factory profile lives; sibling-project layout was already a scaffold rule.
**Impact:** `plugin/skills/onboarding/SKILL.md` (bootstrap step + living-profile note), `plugin/templates/shared/.pandacorp/guide.md.tpl` (rule 9, propagated by upgrade/adopt), `CLAUDE.md` (rule 9 for the factory itself), `factory/profile.example.md` (living-doc note), `factory/decisions/registry.yaml` (DR-053). `OVERLAY_VERSION` 8.16.0 → 8.17.0 (guide.md changed → existing projects gain rule 9 on auto-upgrade); plugin **v8.17.0** (`plugin/docs/decision-log.md`). Mission Control's Manual surfaces DR-053 automatically via the derived decision-rules catalog (DR-046); a build is running there, so it re-syncs via upgrade after.

## 2026-06-18 — Biome is the single lint+format standard (no Prettier, no ESLint) + version policy (DR-052)
**What:** Standardized on **Biome** as the one tool for both formatting and linting — **dropped Prettier and ESLint** from the default stack. Biome's formatter replaces Prettier (~97% compatible); its linter (recommended rules + react/next/test domains + the `a11y` group) replaces ESLint; Tailwind class ordering via `useSortedClasses`. Documented **escape hatch**: a minimal ESLint config running ONLY the rules Biome lacks (Testing-Library-specific query/async lint, full `eslint-plugin-next` parity) — never re-add Prettier. Also added a **version policy**: latest-stable (`@latest`) for new projects/installs; in-flight projects don't churn mid-build; older/brownfield projects install only versions **compatible with their framework major** (a Next.js 13 project doesn't get a lib requiring Next 15). Both codified as **DR-052**.
**Why:** Owner asked to standardize on Biome after the running Mission Control iterate flagged a mismatch (the plan/rules referenced ESLint plugins while the project uses Biome). Confirmed by research (Biome v2.x: 500+ rules, type-aware `noFloatingPromises` without `tsc`, domains, GritQL plugins; gaps are the deep Testing-Library rules, full `eslint-plugin-next`, and the complete `typescript-eslint` type-aware suite → the ESLint escape hatch). `next lint` was removed in Next 16, so a linter is invoked directly regardless. Version policy: owner wants latest-always for new work but compatibility-first for old projects.
**Impact:** `factory/standards/stack.md` (Biome in the stack + Version policy section), `factory/standards/quality.md` (a11y-gate → Biome `a11y` group), `factory/decisions/registry.yaml` (DR-052). Injectable rules updated to Biome rule names / CI-reviewer where Biome lacks them: `code-conventions.md` (versions + deps), `styling-and-ui.md` (`useSortedClasses`), `web-security.md` (`noDangerouslySetInnerHtml`/`noGlobalEval`), `clean-code.md` (`noExcessiveCognitiveComplexity`/`useMaxParams`/`noParameterAssign`/`noImportCycles`/`noBarrelFile`), `quality-and-testing.md` (Testing-Library = escape hatch). Stack template: `plugin/templates/stack-a-nextjs/STACK.md` (Biome setup, hard-enforcement mapped to Biome rules, verify.sh drops the ESLint fallback). `OVERLAY_VERSION` 8.15.0 → 8.16.0; plugin v8.16.0. **Mission Control not touched this turn — a build is running there; it re-syncs via upgrade after.**

## 2026-06-18 — `src/` is now MANDATORY (owner override of the earlier "optional" verdict)
**What:** All application code must live under **`src/`** whenever the stack supports it (`src/app/`, `src/components/`, `src/lib/`, …); the `@/*` alias points at `src/`. Config/tooling and non-code assets (`public/`, `content/`, `docs/`) stay at the repo root. Reversed the prior "pick one, default root-level" rule from earlier today. Updated `project-structure.md` (Source root section → mandatory), `factory/standards/structure.md`, `code-conventions.md` (`@/* → ./src/*`), and the rules README catalog.
**Why:** The owner decided `src/` should not be optional — a clean separation between real code and configuration, an uncluttered root, and a single predictable place every agent looks first. He explicitly overrode the research-based "optional" recommendation (which had been chosen to avoid putting Mission Control in violation); the trade-off he accepts is migrating MC to `src/` (done in the same push). Where a technology genuinely can't use `src/`, apply the spirit (one clear source root separated from config).
**Impact:** `plugin/templates/rules/project-structure.md` + `code-conventions.md` + `README.md`, `factory/standards/structure.md`. `OVERLAY_VERSION` 8.14.0 → 8.15.0; plugin v8.15.0 (`plugin/docs/decision-log.md`). Enforced in Mission Control by an actual `src/` move (same date). Supersedes the `src/`-is-optional note in the "expanded rule set" entry below.

## 2026-06-18 — Rule set expanded from web research: clean-code, TypeScript, accessibility + stack/security/perf deltas
**What:** Researched authoritative best practices (Google eng-practices, Clean Code, Fowler/Metz, official Next.js/React 19/TypeScript/Tailwind v4 docs, OWASP, OpenSSF, WCAG 2.2, web.dev, Testing Library) and added the curated, non-contradicting deltas to the injectable rule library. **3 new rule files:** `clean-code.md` (always — file ≤~300/≤500 lines, function ≤~50, nesting ≤4, cyclomatic ≤~10, SRP/"and"-test, SoC, DRY+rule-of-three, ≤3 params/no boolean-trap, no dead code via knip, purity/immutability, no circular deps, no barrels, JSDoc on the public surface, consistency/explicitness for AI-legibility); `typescript.md` (typescript — `import type`, `satisfies`, discriminated unions + exhaustive switch, no unsafe `as`, `as const` over enums, `readonly`); `accessibility.md` (web-ui — keyboard/focus trap, form label+aria error wiring, prefers-reduced-motion, landmarks/skip-link, WCAG 2.2 drag-alternative/focus-not-obscured/accessible-auth; absorbs the a11y section out of `styling-and-ui.md`). **Extended files:** `react.md` (React 19 `useActionState`/`useOptimistic`/`use`, React Compiler note), `nextjs.md` (caching opt-in + revalidate, granular Suspense streaming, fast Server-only metadata), `styling-and-ui.md` (Tailwind v4 `@theme` tokens, no arbitrary values, prettier CI), `web-performance.md` (long-task breakup, layout-thrash, debounce/throttle, virtualization, content-visibility, passive listeners), `web-security.md` (SSRF safeFetch, no unsanitized HTML/eval, rate-limit, supply-chain: lockfile/`npm ci`/ignore-scripts/cooldown/audit/secret-scan/pinned Actions), `code-conventions.md` (dependency mgmt: packageManager/engines/one-lockfile/justify-deps; git: small commits, no force-push shared). New `typescript` token in the manifest.
**Why:** Owner asked, before continuing the MC migration, to research and fold in additional high-value rules (clean code, scalability, readability, AI-friendliness — file-size limits, SRP/SoC, reuse) and anything else worth enforcing, with the hard constraint that nothing contradict existing rules. The biggest gaps were a clean-code/maintainability file (entirely absent), a TypeScript idiom file, and depth in a11y/security/perf.
**Decision on `src/`:** NOT mandated. Research (official Next.js docs, high confidence) is explicit that `src/` is optional and Next is unopinionated; mandating it would put Mission Control (root-level, working) in violation. Rule is "pick one source-root strategy per project, be consistent, config at repo root; default root-level" (`project-structure.md` + `structure.md`). Flagged to the owner (contrary to his initial lean) — reversible if he wants to mandate `src/`.
**Impact:** `plugin/templates/rules/` (16 files now: +clean-code, +typescript, +accessibility; 7 extended; README manifest + `typescript` token). Canonical: `factory/standards/structure.md` (source-root note). `OVERLAY_VERSION` 8.13.0 → 8.14.0; plugin v8.14.0 (`plugin/docs/decision-log.md`). Propagates to existing projects via `/pandacorp:upgrade` (DR-051). Hard-enforcement lint wiring (knip, max-lines, complexity, import/no-cycle, testing-library, jsx-a11y, no-danger) lands in the stack `verify.sh`/lint config when applied per project.

## 2026-06-18 — Tests live in `_tests/` folders, never loose beside source (file-org aligned to PandaTrack)
**What:** Made test placement **definitive** and enriched the file-organization rules to match PandaTrack's proven convention. **Tests:** unit/component tests live in a **`_tests/` folder inside the component/feature folder** — `Button/_tests/Button.test.tsx` — **never** `*.test.tsx` loose at the same level as implementation files. A component becomes "multi-file" the moment it has any scoped sibling (including a test), so it's promoted to its own folder (`Button/Button.tsx`, never `index.tsx`) with the test under `_tests/`; single-file components stay flat. Shared test infra → `src/test/`; E2E → top-level `e2e/` split by domain. **Also added** to the structure rules: the single-file-vs-multi-file component-folder convention, page-level `_`-sibling folders with scope-by-segment-first, `src/lib/` purpose-grouping with promote-on-second-file, and the smallest-scope promotion rule — all from PandaTrack's `project-structure.mdc`/`testing-strategy.mdc`.
**Why:** The previous wording ("colocated tests next to the code `*.test.ts` **or** in `_tests/`") was ambiguous and permitted loose test files at the same level as source — the exact visual noise the owner flagged in Mission Control (hard to scan the tree, tests clutter file search). PandaTrack already solved this with the `_tests/` folder convention; the owner asked to adopt its file-organization rules verbatim and enforce them.
**Impact:** `factory/standards/structure.md` (test-organization + component-folder + page-folders + lib + promotion sections; tree gains `src/test/`). Injectable rules: `plugin/templates/rules/project-structure.md` (rewritten richer) + `quality-and-testing.md` (test-placement section). Part of the uncommitted plugin v8.13.0 / OVERLAY 8.13.0 change. Retroactive cleanup of Mission Control's existing loose tests is a separate `iterate` migration (deferred).

## 2026-06-18 — Rules are injected as files (docs/rules/), propagated by upgrade, enforced in the gate (DR-051)
**What:** Built the enforcement+propagation system so factory rules actually reach and bind every project — the gap that let Mission Control ignore `core/modules` even though it was written in the embed. **Three enforcement layers + a contract:** (1) **Presence** — a project-injectable rule library lives at `plugin/templates/rules/` (13 files, the portable/operative form of `factory/standards/`, split by purpose/technology, each declaring `applies_when`: `always` | `react` | `nextjs` | `tailwind` | `web-ui` | `public-web` | `prisma` | `next-intl` | `posthog` | `sentry`). Projects carry their rules as FILES in `docs/rules/` (in `docs/`, not `.pandacorp/`, so any agent follows them with or without Pandacorp), loaded by `CLAUDE.md` via `@docs/rules/README.md` (recursive `@import`) and pointed to by `AGENTS.md` — whose old hand-condensed durable-conventions block was replaced by that pointer (kills the two-copies drift). **Selective copy by stack:** `scaffold` injects the `always` files at birth; `blueprint` adds the tech-gated files matching the approved stack (React-only project → `react.md` but not `nextjs.md`); `adopt` injects the set matching a brownfield's inferred stack. (2) **Propagation** — `upgrade` re-copies the plugin's rule files verbatim into existing projects and regenerates `docs/rules/README.md`; bumping `OVERLAY_VERSION` is the trigger (DR-048 auto-upgrade). (3) **Hard gate** — `stack-a/STACK.md` now lists the lint rules to wire into `verify.sh` (`noArrayIndexKey`, `useExhaustiveDependencies`, `noExplicitAny`, barrel-import bans, a11y) so violations FAIL, not just sit in prose. **Contract (DR-051, enforced in `learn`):** a project-facing standard change MUST, in the same change, update the matching `rules/*.md`, wire the hard-enforceable part into the stack lint, and bump `OVERLAY_VERSION`.
**Why:** Root cause of the Mission Control miss: the factory had TWO copies of the rules (canonical `factory/standards/` + a condensed `AGENTS.md.tpl` embed) with nothing syncing them, and `learn` only wrote the canonical copy → new rules never reached projects, and even present rules weren't hard-enforced. Owner's chosen model: rules as portable files in `docs/`, split by technology like PandaTrack's `.cursor/rules`, with a README manifest that doubles as the `@import` loader; copy only what the stack needs. This makes adding a rule in the future automatically integrate into all projects (existing via upgrade, new via scaffold/blueprint).
**Impact:** New `plugin/templates/rules/` (13 files + README manifest). Templates: `shared/CLAUDE.md.tpl` (+rules import), `shared/AGENTS.md.tpl` (durable block → `docs/rules/` pointer). Skills: `scaffold` (copy `always` + generate README — canonical spec), `blueprint` (copy tech-gated by stack), `upgrade` (re-sync `docs/rules/`), `adopt` (inject by inferred stack), `learn` (the propagation contract). Policy: `factory/decisions/registry.yaml` DR-051. Canonical: `factory/standards/README.md` (new section). Hard enforcement: `plugin/templates/stack-a-nextjs/STACK.md`. `OVERLAY_VERSION` 8.7.0 → 8.13.0; plugin v8.13.0 (`plugin/docs/decision-log.md`). Mission Control's own `docs/rules/` cleanup deferred by the owner (separate enforcement task).

## 2026-06-18 — Harvested engineering rules from sibling projects into the standards (PandaTrack + jobresearch-app)
**What:** Audited the in-flight rule sets of two sibling projects — PandaTrack's 37 Cursor `.mdc` rules + the two Vercel skills (`vercel-react-best-practices`, `vercel-composition-patterns`), and jobresearch-app's 6 `.mdc` rules — classified each as portable / tech-gated / project-specific, and adopted the genuinely-new, non-duplicate ones into the factory in three buckets. **Bucket A (general React/programming → `patterns.md` + `conventions.md`):** prop-drilling ≤3 levels → Context; never `index` as list `key`; don't define components inside components; derive-don't-sync (no `useState`+`useEffect` for derived state); composition over boolean-prop explosion (compound components); `useMemo`/`useCallback` only when needed; the "not a magic number" exception for Tailwind values and named-prop literals; `.env.example` kept in sync in the same change; and treat every Server Action as a public endpoint (authn/authz inside it). **Bucket B (performance → `performance.md`):** no request waterfalls (`Promise.all`, defer awaits); `React.cache()` per-request dedup; avoid barrel-file imports in hot paths; dynamic-import heavy non-critical components. (**Bucket C — Prisma/next-intl/PostHog/Sentry/design-system conventions — is tech-gated and went to the stack-a template, logged in `plugin/docs/decision-log.md` v8.12.0.**)
**Why:** Owner noticed the factory wasn't enforcing structure/quality it should (e.g. Mission Control ignoring `core/`+`modules/`, which *already* exists in `structure.md`) and asked to mine the sibling projects' working rules for what's worth standardizing. Most candidates were already covered; this captures only the additive ones. Tech-specific rules go to the stack template (the factory's natural per-stack gate), general ones to the durable standards — so React rules only land in React projects, etc. (Mission Control's actual `core/modules` cleanup was deferred by the owner — an enforcement gap, tracked separately.)
**Impact:** `factory/standards/patterns.md` (Component composition section + Server-Action-as-public-endpoint), `factory/standards/conventions.md` (magic-number exception + Environment variables), `factory/standards/performance.md` (Runtime & data-fetching patterns). Tech-gated half: `plugin/templates/stack-a-nextjs/STACK.md` (plugin v8.12.0).

## 2026-06-17 — Source of truth for build state: the work order; the FRD is a derived rollup
**What:** Codified in `build-orchestration.md` §1 + `registry.yaml` DR-050: the **work order** `implementation_status` is the atomic source of truth; an FRD's/blueprint's `implementation_status` is a **DERIVED rollup** of its work orders (`VERIFIED` iff all; else the worst non-`VERIFIED`: `BLOCKED` > `PLANNED` > `IN_PROGRESS` > `IN_REVIEW`), never an independent state. **On any FRD↔WO discrepancy, the work orders win** — the FRD field is a cache, trustworthy on its own only at `VERIFIED`. "Done" = `VERIFIED`, measured on work orders. The engine keeps the cache honest (re-derives on plan + after each gate); exact readers (Mission Control) derive from the WOs and prefer a breakdown ("7/11 verified") over a single rolled-up word.
**Why:** A stale `frd.md` (FRD-06 said `PLANNED` while its 11 WOs were `IN_REVIEW`) made even the agent misreport whether a feature was done. Needed one unambiguous rule for "what determines done", since docs and WOs can drift (a crash mid-build, an interrupted run, a manual edit).
**Impact:** `factory/standards/build-orchestration.md` (§1), `factory/decisions/registry.yaml` (DR-050). The engine that enforces it: plugin v8.10.0 (`plugin/docs/decision-log.md`).

## 2026-06-17 — The overnight brake is maxAgents (counted in the engine), not maxSpend
**What:** `build-orchestration.md` §6: the real run-stopping budget ceiling is now **`maxAgents`** (a hard cap on the subagents the engine spawns), not `maxSpend` (`budget.spent()`). `budget.spent()` under-counts the heavy subagent work and isn't enforced if the supervisor dies — an MC run hit ~6M tokens against a 2M `maxSpend` after an app restart orphaned it. `maxAgents` is counted inside the engine, so it brakes reliably and survives a dead supervisor; `maxSpend` is demoted to a secondary ceiling.
**Why:** Owner flagged that `maxSpend` could never reliably fire — the orchestrator/verifier writes little, and the engine wasn't capping the real work the subagents do. A spawn-count proxy maps directly to spend AND to the engine's own control flow, so it's both honest and enforceable.
**Impact:** `factory/standards/build-orchestration.md` (§6). Engine + skill + history: plugin v8.9.0 (`plugin/docs/decision-log.md`).

## 2026-06-17 — Build-orchestration §7 documents the supervisor's owner-facing duties (canonical)
**What:** Brought the canonical standard (`build-orchestration.md` §7, "The build supervisor") in line with the supervisor capabilities added in the plugin (v8.7.1/v8.8.0): the **heartbeat is now a VISIBLE periodic message** to the owner (every ~20–30 min via a `ScheduleWakeup` timer, not a silent check); the supervisor **announces each FRD verified** (name, what it does, route to try it, X/Y done); and it **keeps a review worktree** at `last_green` so the owner browses the latest green version (Desktop-session Preview or on-demand dev server) without touching the build. So a future agent reading the *standard* — not only the skill — understands them.
**Why:** Owner asked these be documented for any future agent. The skill is the operable detail; the standard is the canonical statement — both must carry the owner-facing duties (visibility + reviewability), since silence and "discover-it's-all-wrong-at-the-end" were the two real pain points this session.
**Impact:** `factory/standards/build-orchestration.md` (§7). Operable detail + history: `plugin/skills/implement/SKILL.md` + `plugin/docs/decision-log.md` (v8.7.1 heartbeat, v8.8.0 announce + review worktree).

## 2026-06-17 — Build speed & scale: powerful default, focused gates, trimmed cold start (plugin v8.7.0)
**What:** Owner flagged mid-run that the build was slow and the per-FRD gates re-ran the whole test suite (3700+ and growing) — not scalable. Three fixes: `powerful` is the default mode now (more parallelism); the cold start is trimmed (skip baseline `verify.sh` when `HEAD==last_green`; planner reads WO frontmatter only, not full bodies); and the per-FRD gate is **focused** — `verify.sh --since <last_green>` runs biome+tsc globally + only the vitest tests affected since the last green, with the full suite once at close-out. Standards updated: `build-orchestration.md` §5 (focused gate) + `architect.md`/DR-019 (verify.sh gains a `--since` mode).
**Why:** A gate that re-runs every test on every FRD is O(total tests) and collapses past mid-project; focused gates are O(touched). Owner-approved trade-off: an unrelated-feature regression could slip to close-out, mitigated by global tsc/biome each gate + full suite at close-out. Speed is now a first-class concern alongside the rigor we already had.
**Impact:** `factory/standards/build-orchestration.md` (§5). Plugin side (engine + skill + architect + bump 8.7.0, OVERLAY 8.7.0): `plugin/docs/decision-log.md` (same date). MC adopts it next run (engine + verify.sh `--since` copied when the current run ends).

## 2026-06-17 — Overnight test caught a cap-counting bug; engine hardened (plugin v8.5.0)
**What:** The first supervised overnight run of the build engine (on Mission Control) validated the DR-050 core (2 features fully verified, ~400 real tests added, clean consumption) and **caught a real bug the static audit missed**: `maxFrds` counted only features built/blocked, not **reopened** — so when gates reopened features, the cap never tripped and the run crept past its intended limit (the supervising agent stopped it by hand, honoring "better to stop short than burn tokens"). Fixed: the cap now counts features **processed** (built+blocked+reopened); added **`maxSpend`**, an absolute output-token ceiling (via `budget.spent()`) that works without a `+Nk` directive — the real guardrail for unattended runs; and the engine no longer rebuilds `IN_REVIEW` WOs left by an interrupted run. Detail in the plugin log.
**Why:** Validates the "supervise the first real run before walking away" discipline — a static audit (even 4 agents) couldn't catch a bug that only appears with reopens at runtime. The fixes make unattended runs safe without a human as the only token brake.
**Impact:** `factory/standards/build-orchestration.md` (§6: cap counts processed; `maxSpend` is the token guardrail). Plugin side (engine + skill + bump 8.5.0): `plugin/docs/decision-log.md` (same date). MC re-tested next run.

## 2026-06-16 — Worktrees for build parallelism: evaluated, NOT adopted
**What:** Evaluated the owner's idea of running build subagents in isolated git worktrees (`isolation:'worktree'`) for more parallelism / to dodge same-file collisions. Confirmed the harness mechanism: **no automatic merge** — a worktree's changes stay on its own branch and the orchestrator must merge them with raw git; conflicts are manual; `node_modules` is NOT shared (each worktree needs its own `pnpm install`, 30–90s on Next.js); plus open harness bugs (stale-branch reuse on id collision, worktrees forking from `main` not the caller's HEAD). **Decision: do NOT use worktrees in the build engine.** Reasons: (1) it doesn't solve the same-file case — 4 agents writing one file in 4 worktrees = a merge conflict the script can't safely auto-resolve; merging the WOs is strictly better; (2) per-worktree `pnpm install` eats the parallelism gain; (3) script-resolved merges in an unattended overnight build are exactly the silent-breakage risk to avoid; (4) the engine's intra-FRD waves over disjoint files (the new no-same-file-siblings rule) already give real parallelism. **Reserved for:** isolated snapshot-testing (1 read-only worktree on `last_green_sha`, already in the implement skill) and, as a future measured experiment, building fully-independent FRDs in parallel (one worktree per FRD) IF speed ever becomes the measured bottleneck.
**Why:** Owner asked to evaluate worktrees to finish builds faster. Honest verdict: the cost (install per worktree + manual merge + harness bugs) outweighs the benefit for an autonomous JS build, and the problem that motivated it (same-file collisions) is better solved by coarser WOs + the no-same-file rule (v8.4.0). Recorded so a future agent doesn't re-litigate it.
**Impact:** No code change — decision only. Related: `factory/standards/build-orchestration.md` §2 (the real fix), plugin v8.4.0.

## 2026-06-16 — Build orchestration §2: coarser WOs + no same-file wave-siblings (plugin v8.4.0)
**What:** Added a rule to `factory/standards/build-orchestration.md` §2: work orders that build in the same wave must not write the same file/module — parallel implementers collide. Merge (preferred) or serialize with a dependency. Found by an impartial reviewer auditing Mission Control (FRD-10: 4 WOs all creating `lib/achievements.ts`). Also pushed the WO-generating guidance coarser ("group generously — when in doubt, merge") in the `work-orders` skill + `architect`, per owner directive for future projects.
**Why:** Owner wants future projects to generate coarser, better-grouped work orders without the collision problem. Captured as a standard rule + a memory lesson (DR-047), not just an MC patch.
**Impact:** `factory/standards/build-orchestration.md` (§2). Plugin side (skill/architect + bump 8.4.0): `plugin/docs/decision-log.md` (same date).

## 2026-06-16 — Coherence audit: canonical factory docs aligned to DR-050 (plugin v8.3.0)
**What:** An exhaustive pre-overnight audit caught the old per-WO build flow still described in canonical *factory* docs (not just the plugin) and fixed it: `constitution.md` (work order = coarse slice, review per FRD — was "small chunk… reviewer rejects too-large"), `registry.yaml` DR-013 (reviewer at the FRD gate, per-FRD loop — was "reviewer at the close of each work order"), `standards/structure.md` (coarse WOs, order from the Build Plan), `standards/README.md` + `CLAUDE.md` (so a new agent discovers `build-orchestration.md`), and the supervisor is now canonical in `build-orchestration.md` §7. Also fixed the plugin agents (`reviewer`/`implementer`/`architect`) and an engine re-work bug — see the plugin log.
**Why:** The owner asked to be sure everything is documented "so any future agent understands all of this" before an overnight build. The core standard was already coherent; the drift was in the higher-authority docs (constitution/registry) an agent trusts first.
**Impact:** `factory/constitution.md`, `factory/decisions/registry.yaml` (DR-013), `factory/standards/{build-orchestration.md,structure.md,README.md}`, `CLAUDE.md`. Plugin side (agents + engine fixes + bump 8.3.0): `plugin/docs/decision-log.md` (same date). Pending PART 2: MC's v1 engine + Manual prototype still show the old flow (fixed when MC migrates).

## 2026-06-16 — Build freno: health+budget, repair before block, BLOCKED with a reason (DR-050 refined, plugin v8.2.0)
**What:** Refined how a build STOPS, after the owner reviewed the v8.1 guardrail. The build now **runs to completion** by default; it stops only by (a) nothing left, (b) the **budget ceiling** (the real token guardrail), (c) a **health breaker** (3 non-`external` blocks in a row → systemically wrong), or (d) work that **needs the owner**. `maxFrds` is demoted from a default cap of 6 to **opt-in for supervised TEST runs only** — "stop after N features" protects neither tokens nor progress (a feature can cost 10x another). Added **repair-before-block** (a failing work order or FRD gate triggers a repair pass before the engine ever blocks) and a typed **`blocked_reason`** on `BLOCKED` (`needs-owner` | `external` | `error`). Dropped ntfy (the app is poor) — notifications are macOS desktop + a phone `PushNotification`.
**Why:** The owner is about to run unattended/overnight builds and wants the build to *finish the work* rather than halt at an arbitrary count, while staying safe against runaway spend and silent stalls. Stopping by health+budget (not count) + repairing before giving up + a reason on every block expresses exactly "run to completion unless something is genuinely wrong or needs me." A future Pandacorp Mission Control mobile app is the owner's preferred notification channel (noted, not built).
**Impact:** Canonical: `factory/standards/build-orchestration.md` (§6 "How a run stops"), `factory/decisions/registry.yaml` (DR-050 refined). **Plugin v8.2.0 (MINOR)** — see `plugin/docs/decision-log.md` (same date): engine `maxFrds`→Infinity default + `attemptRepair` + health breaker + `blocked_reason` + desktop-only NOTIFY; the `implement` skill aligned; `work-order-template.md` gains `blocked_reason`; `OVERLAY_VERSION` → 8.2.0. Builds on v8.1.0 (the supervisor). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — The build supervisor: unattended runs are watched, not fired-and-forgotten (plugin v8.1.0)
**What:** Made the build **supervisor** a mandatory, canonical part of the orchestration: launching a build is never just firing the workflow — the agent that runs `implement` always mounts a live supervisor. It watches with `Monitor` (a ~2-min bash poll that costs no tokens — only an emitted event wakes it), heartbeats ~every 15 min, reacts to stalls/blocks (unstick, or stop + notify), feeds AVOIDABLE stalls into the self-learning loop (DR-047), and holds a conservative session budget ceiling. Notifications reach the macOS desktop (from the engine) and the phone via `PushNotification` (from the supervisor, when Remote Control is on).
**Why:** A v7.3 run went >1h unwatched with zero signal and the owner was left blind — the monitoring gap that triggered the whole redesign. "Run and walk away" is only safe if something is actually watching the failure signatures, not just success ("silence is not success"). Decided with the owner this session.
**Impact:** Canonical: `factory/standards/build-orchestration.md` (§7 "The build supervisor"). Operable detail in `plugin/skills/implement/SKILL.md` ("Unattended operation"). **Plugin v8.1.0** — see `plugin/docs/decision-log.md` (same date). Followed immediately by v8.2.0 (the freno refinement).

## 2026-06-16 — Document-driven build orchestration ADOPTED (DR-050, plugin v8.0.0)
**What:** Rebuilt how the factory plans/tracks/runs a build, after the Mission Control dogfood showed ~76% of the effort was re-work. New canonical standard `factory/standards/build-orchestration.md` + policy **DR-050**: build STATE lives in the YAML frontmatter of each FRD/blueprint/work-order (`status` + `implementation_status: PLANNED→IN_PROGRESS→IN_REVIEW→VERIFIED`, the engine's source of truth — it never rebuilds `VERIFIED`); WORK ORDERS are coarse (one view/capability, not atomic); the BUILD PLAN (the WO DAG) lives in each per-FRD blueprint and the engine reads it instead of re-inferring; each closed WO writes a `## Status Note` hand-off; the engine builds and reviews/tests PER FRD (cost O(FRDs) not O(WOs)), with an opt-in functional/browser test layer. Added the factory's first document templates (`plugin/templates/docs/`).
**Why:** The MC build ran ~8h / ~944 subagents for ~37 work orders with the owner blind to stalls; forensics traced it to state-not-in-data (the engine rebuilt finished WOs every relaunch), atomic WOs, per-WO review/verify and runtime dependency inference. Recovers what **PandaTrack** already did and DR-049 left behind. Owner-approved across the session (proposal 11): `IN_REVIEW` over `IMPLEMENTED`, iterate reopens WOs, per-FRD testing. The dogfood itself is the value — it exposed every gap in one day.
**Impact:** Canonical: `factory/standards/build-orchestration.md` (new), `factory/decisions/registry.yaml` (DR-050, 50 entries), `docs/proposals/11-build-orchestration-v2.md` (adopted). **Plugin v8.0.0 (MAJOR)** — `plugin/docs/decision-log.md` (same date): doc templates + skills (spec/blueprint/work-orders/iterate/new-version) + `pandacorp-build.js` rewritten to the per-FRD loop; `OVERLAY_VERSION` → 8.0.0. **Pending (part 2):** Mission Control migration (frontmatter + mark the 37 built `VERIFIED` + re-group the ~77 remaining into coarse WOs + Build Plans + relaunch), a pass over the `implement` skill body, and the Manual (DR-046). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — Feature-centric docs structure ADOPTED & implemented (DR-049, plugin v7.0.0)
**What:** The owner approved the feature-centric model (the previous entry's proposal) and it is now LIVE across the factory. Canonical standard rewritten: `factory/standards/structure.md` gains a full **"Documentation structure (`docs/`) — feature-centric"** section (the contract); `factory/standards/documentation.md` owner-doc table repointed + a **source-of-truth hierarchy** subsection added. Policy codified as **DR-049** in `registry.yaml`. The model: each FRD is a self-contained module `docs/frds/frd-NN-<slug>/` (`frd.md` + conditional `fdd.md`/`mocks/` + per-feature `blueprint.md`, large→`blueprints/` folder + `work-orders/`), under `docs/product/{prd.md,architecture.md,research.md}` and a global `docs/design/` (= the PDD); `docs/{adr,analytics/events.md,reviews,decision-log.md}` stay global. Two architecture layers (platform `product/architecture.md` vs per-FRD `blueprint.md`); PRD↔FRD siblings (feature-landscape table); work orders live under their FRD (the build walks `docs/frds/*/work-orders/` + orders by WO `Dependencies`); numeric folder-derived IDs `REQ-NN-MMM → AC-NN-MMM.K → CMP-NN-<slug>/IF-NN-<slug> → WO-NN-MMM`.
**Why:** Owner's directive to implement it fully ("modifica todo, cambia todo") after the evaluation + three rounds of refinement (no "pillars" → a `blueprints/` folder; PDD = the `design/` folder; one structure at two fill levels; extensible base skeleton; numeric IDs). Rationale + benchmark in the previous entry and `docs/proposals/10-feature-centric-doc-structure.md` (now marked ADOPTED).
**Impact:** Canonical: `factory/standards/structure.md`, `factory/standards/documentation.md`, `factory/decisions/registry.yaml` (DR-049, validated 49 entries). **Plugin v7.0.0 (MAJOR, breaking paths)** — `plugin/docs/decision-log.md` (same date): 13 skills + 7 party agents + templates (`guide.md.tpl` doc-map, `AGENTS.md.tpl`, `pandacorp-build.js` per-FRD work-order discovery) rewritten; `OVERLAY_VERSION` → 7.0.0; `upgrade` gained docs-migration step 2b; `claude plugin validate` passes. **Mission Control dogfooded**: its 18 FRDs converted to module folders + `product/prd.md` (feature-landscape table), FRD-01/05/08 content updated, cross-links repaired repo-wide, prototype CONFIG updated (DR-049 + incidental DR-048 drift), MC decision-log entry added (Manual stays in sync, DR-046). Existing projects (cotizapdf, pandatrack) migrate via `/pandacorp:upgrade`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — Feature-centric docs structure evaluated → proposal drafted (pending owner gate)
**What:** Evaluated whether to replace the factory's current *by-type* docs model (`docs/` with flat `frds/`, one global `blueprint.md`, one flat `work-orders/`) with a *by-feature* model where each FRD is a self-contained module folder (`frds/frd-NN/` carrying `frd.md` + conditional `fdd.md` + per-FRD `blueprint.md` + `mocks/` + `work-orders/`), above a thin product layer (`product/prd.md` + a global `product/architecture.md` split from per-FRD blueprints + a global design system). Benchmarked three real systems on disk: ours, **JobLeap** (`0-jobleap-company-context`, 17-FRD/3-repo, the reference the owner liked), and **PandaTrack** (deeper nesting, rejected). Wrote the full proposal in [docs/proposals/10-feature-centric-doc-structure.md](../docs/proposals/10-feature-centric-doc-structure.md). **No standard or skill changed yet** — this is a proposal pending the owner's gate.
**Why:** The owner observed (correctly) that the flat model is comfortable for small/medium MVPs but has a hard ceiling — a single global blueprint becomes a monolith every feature must edit, and growth forces a mid-project flat→nested migration. The by-feature model gives *locality of change* (a new feature = one new folder, nothing else moves), letting a product grow from MVP to corporate platform without restructuring. Key insight from the benchmark: JobLeap separates **two layers of architecture** (global `architecture.md` vs per-FRD `blueprint.md`) which we conflate — that split is why theirs scales. It applies the factory's existing "code by feature" / locality principle (`structure.md`) to `docs/`.
**Context:** Owner chose, via the evaluation, to (a) draft the formal proposal first (not touch skills yet) and (b) adopt the **full package — structure + stable-ID traceability** (`REQ→AC→CMP/IF→WO→tests` with a `FRD > FDD > tokens > blueprint > WO` source-of-truth hierarchy), not folders alone. The proposal resolves his three questions: PRD/FRD as **siblings, not nested** (JobLeap, not PandaTrack); **two design layers** (global design system = our PDD + conditional per-FRD `fdd.md`); **mocks inside the FRD folder**. Governed by **progressive disclosure** (constant folder shape, depth scales with project size, folders on demand) so it doesn't crush small MVPs. On approval: rewrite `factory/standards/structure.md`, add the source-of-truth hierarchy to `documentation.md`, a DR + registry default, a MAJOR plugin bump (6 skills incl. `implement`'s WO-glob → per-FRD walk, + Mission Control + Manual/DR-046), pilot on one project, migrate the rest via `/pandacorp:upgrade`.
**Impact:** `docs/proposals/10-feature-centric-doc-structure.md` (new). Pending owner gate before any change to `factory/standards/`, the plugin, or Mission Control. Owner resolved (2026-06-16): **ID scheme = numeric folder-derived** (`frd-05` → `REQ-05-001`; JobLeap 2-letter code rejected as cryptic); **pilot deferred** (Mission Control is built first regardless, launch-on-new-structure decided later — document fully, don't force a pilot). Clarified for the owner that small vs large are *not two structures* — one shape, two fill levels; growth is additive (a feature = a new `frds/frd-NN/` folder, nothing moves), the only rename ever is `product/prd.md` → `product/prds/`. Further owner refinements (2026-06-16): a heavy per-FRD blueprint splits into a **`blueprints/` folder by coherent concern (NOT "pillars")**, not flat pillar files; the **PDD = the `docs/design/` folder** (there is no separate PDD file — the global design system *is* the PDD); the standard is an **extensible base skeleton** (projects may add their own folders like `runbooks/`, `db/`; only the contract parts — `frds/frd-NN/`, `product/`, `design/`, the ID convention — stay fixed). Still open: analytics global-vs-per-FRD, single PRD vs `prds/` folder.

## 2026-06-15 — Self-learning: durable promotions queue (the `promotion` field)
**What:** Made the rule-promotion approval durable instead of conversation-only. Lessons now carry a **`promotion`** field (`none | proposed | approved | rejected`). `/pandacorp:memory review` sets `promotion: proposed` (with target + rationale) on a rule-worthy lesson; `/pandacorp:memory status` lists the **promotions queue** (all `proposed`); `/pandacorp:learn` sets `approved` on promote or `rejected` on decline. The owner reviews the full list and decides whenever — never forced in the moment. State lives on the lesson (single source); `/memory status` + Mission Control FRD-17 are views.
**Why:** The owner asked how approval works and what happens if he wants to decide later — exposing a gap: promotion proposals lived only in the `/memory review` conversation (ephemeral). A durable per-lesson flag fixes "decide later" today (before the MC app exists) and pre-wires the MC proposals page.
**Impact:** `factory/memory/{README.md,_lesson-template.md,LESSON-0001-*.md}` + `plugin/scripts/validate-memory.sh` (new `promotion` field), `plugin/agents/librarian.md` + `plugin/skills/{memory,learn}/SKILL.md` (set/list it), `plugin/.claude-plugin/plugin.json` (6.3.0). Mission Control: `frd-17-proposals-inbox.md` (promotions-queue criterion). Plugin decision-log: same date (v6.3.0).

## 2026-06-15 — Self-learning: always-on hybrid capture (raw inbox) + A.U.D.N. dedup + provenance
**What:** Made lesson capture GENERAL and automatic, per the owner's ask (capture across the whole workflow, not just builds) and a focused web-research pass. Three tiers (DR-047 updated): (1) **CAPTURE always-on** — a rule in every project's `.pandacorp/guide.md` and the factory `CLAUDE.md` to jot a one-line candidate to a gitignored raw inbox (`.pandacorp/run/lessons.md` per project, `factory/memory/_inbox.md` in the factory) whenever something durable shows up in ANY skill or plain conversation; (2) **REFINE** — the `librarian` drains the inbox + project traces into clean `factory/memory/` lessons with **A.U.D.N. dedup** (ADD/UPDATE/MERGE/NO-OP, so capture volume ≠ store growth) and **provenance** (owner-stated > ci-verified > agent-inferred), at skill boundaries + a scheduled sweep + on demand; (3) **CONSOLIDATE/PROMOTE** — `review` + `learn` + the owner gate. Added a required `provenance` field to the lesson schema + the deterministic validator.
**Why:** The owner clarified the vision (capture continuous and general, even outside skills) and was unsure about a build-only trigger. Research (Mem0 A.U.D.N. dedup, MemoryGraft poisoning, LangMem hot/background, Anthropic structured note-taking, Cursor ambient-vs-deliberate) converged on always-on cheap capture + decoupled refinement + dedup + provenance + a probation/owner gate. Key finding: "more capture is not better" — unverified self-reflections poison the store — so provenance + dedup + the eval/owner gate are the safety backbone. The Claude Code hooks audit confirmed hooks can append/nudge but cannot orchestrate the librarian, so refinement runs via skills + a scheduled job, not a background hook.
**Context:** Owner chose the cadence: always-on capture + refine on a scheduled sweep + the Mission Control memory-health reminder (FRD-17), NOT tied to `implement`. The inbox is gitignored/provisional; refined lessons stay committed English (DR-033). The librarian stays propose-only; promotion stays the owner's (DR-047 high-risk tier).
**Impact:** `factory/decisions/registry.yaml` (DR-047: 3-tier capture + provenance + A.U.D.N.), `CLAUDE.md` (rule 8 capture), `factory/memory/{README.md,_lesson-template.md,LESSON-0001-*.md}` (provenance), `.gitignore` (factory inbox). Plugin v6.2.0 (same date, plugin decision-log): `librarian.md` (inbox + A.U.D.N. + provenance), `skills/memory/SKILL.md` (drain inbox + hybrid cadence), `templates/shared/.pandacorp/guide.md.tpl` (project capture rule), `scripts/validate-memory.sh` (provenance). Pending: activate the scheduled sweep, prototype reconcile (DR-046).

## 2026-06-15 — Project structure: the .pandacorp/ integration layer + overlay versioning & auto-upgrade (DR-048)
**What:** Reorganized every project's factory-integration files into a single **`.pandacorp/`** folder (the project's analogue of `.github/`), separated from the product, and added **overlay versioning with automatic upgrade**. Committed inside `.pandacorp/`: `guide.md` (the managed Origin/write-gate/doc-map that `CLAUDE.md` imports), `status.yaml` (now with `overlay_version` + `created_with`), `idea-origin.md`, `README.md`, `verify.sh`. Gitignored: `.pandacorp/comms/` (Spanish narrative), `.pandacorp/inbox/` (bugs + decisions), `.pandacorp/run/` (runtime). `CLAUDE.md` is now THIN — it `@`-imports `@AGENTS.md` + `@.pandacorp/guide.md` and leaves a free zone for the owner. Product docs stay in `docs/`; the tool contracts (`CLAUDE.md`/`AGENTS.md`/`.claude/`) stay at the root. New skill **`/pandacorp:upgrade`** migrates an old layout, regenerates the managed layer non-destructively and bumps `overlay_version`; the mutating skills' preflight runs it automatically (silent for compatible bumps, announced for breaking ones).
**Why:** The owner asked, across the conversation, for (a) a clear committed-vs-gitignored criterion — the **autosufficiency test** (does a clone need it to continue?), (b) all Pandacorp machinery in one delimited place like `.github/`, (c) a thin `CLAUDE.md` that *references* a managed file so upgrades don't clobber the owner's notes, and (d) projects that stay on the latest overlay automatically. `.pandacorp/` already existed for `verify.sh`, so it was the natural home.
**Context:** Owner's design choices: autosufficiency as the committed/gitignored test (orthogonal to DR-009's language axis); `@import` works for `CLAUDE.md` (Claude Code native) but NOT portably for `AGENTS.md` (other agents), so `AGENTS.md` stays self-contained — which doubles as the graceful-degradation path for a plugin-less fork (DR-044); upgrade is silent-but-safe (managed files regenerate whole, breaking jumps announced). The detector/preflight/hook marker is now simply the presence of `.pandacorp/status.yaml`.
**Impact:** `factory/decisions/registry.yaml` (DR-048; DR-044/045 marker simplified). Plugin **v6.0.0** (breaking paths; same date, plugin decision-log): overlay restructured (`templates/shared/.pandacorp/`, thin `CLAUDE.md.tpl`, `OVERLAY_VERSION`), new `/pandacorp:upgrade`, preflight version-check, detector + hook rewritten, ~39 files path-migrated. Mission Control: FRD-01/02/04/14/16 paths + its own `.pandacorp/` (dogfood). Existing projects migrate via `/pandacorp:upgrade`. Pending: commit + `claude plugin update` + restart.

## 2026-06-15 — Self-learning loop: cross-project memory + harvest/retrieve/eval-gate (DR-047, alternative B)
**What:** Defined the factory's self-learning system and shipped its foundation (Phase 0). New policy **DR-047**: the factory accumulates durable engineering lessons in a new **`factory/memory/`** store (committed English know-how, like `factory/standards/`; typed: problem-solution | library-verdict | pattern | gotcha | anti-pattern), **harvested** at the existing ~15 capture points and **retrieved** by agents before design/blueprint/implement. Autonomy is **tiered by risk (alternative B, semi-autonomous)**: low-risk (store a lesson) auto-applies after a light eval-gate as a *candidate*; medium-risk (a SHOULD standard) auto only if it ships with its verifier; high-risk (a MUST standard, a skill/agent, a DR, any deletion, anything touching a project) always goes through the owner. `learn` becomes the *apply* stage of a closed loop (harvest → store → retrieve → propose → gate → promote → prune), no longer a manual point-tool. Phase 0 shipped: `factory/memory/` (README + lesson template + first seed lesson), DR-047, and the proposal `docs/proposals/09-self-learning-factory.md`.
**Why:** The owner wants the factory to get smarter and faster with use — documenting solved problems, recording which libraries worked/failed, growing skills/rules, self-suggesting improvements in Mission Control, and self-questioning/pruning what stops working ("solito solito"). A deep-research pass (25 claims, 24 at 3-0, primary sources) showed the proven path is **gradient-free externalized memory** (A-MEM, Letta/MemGPT, Voyager, ExpeL, Self-Refine) + **eval-gated self-modification** (DGM, AlphaEvolve), and that the governance edge (pruning stale lessons, anti-poisoning, a library-verdict memory) is **unsolved in the literature** — so an eval-gate + human gate is the responsible design. The factory already had the three knowledge planes (registry=policy, decision-logs=history, standards=know-how) and the capture points, but no cross-project memory and no closed loop; `learn` was 100% manual.
**Context:** The owner chose **alternative B** (semi-autonomous, eval-gated) over A (everything human-gated) and C (full DGM-style self-evolution, deferred as a north star). Key design choices: the store is committed English engineering know-how (not personal data, DR-033) so it propagates like a standard; harvested lessons are *candidates*, not confirmed truth (anti-poisoning, grounded in the ExpeL finding that reflections-on-reflections poison insight extraction); deletion is deprecation, never removal (DR-011/DR-007); the eval-gate reuses existing machinery (`learn` already requires a verifier, `skill-creator` already benchmarks skills). DR-047 operationalizes the long-aspirational DR-030 (recurring pattern → standard) by giving it a substrate and a loop. The autonomy/eval/human-gate shape is the constitution itself (§3 adversarial verification, §7 each intervention reduces future ones) applied to the factory's own evolution. The "Hermes Agent" the owner referenced is real (NousResearch: Memory/Skills/Crons pillars, self-documenting) but its claims are secondary/unverified — used as directional inspiration only.
**Impact:** `factory/decisions/registry.yaml` (DR-047), `factory/memory/` (new: `README.md`, `_lesson-template.md`, `LESSON-0001-*.md`), `docs/proposals/09-self-learning-factory.md` (new), `CLAUDE.md` ("What's here" gains a `factory/memory/` line). Pending (later phases, NOT this change): the harvester agent ("cronista") + capture hooks (Phase 1), retrieval injection into the agents (Phase 2), Mission Control **FRD-17** "Propuestas" inbox (Phase 3/5), the prune job (Phase 4). Mission Control's Manual decision-rules reference (FRD-08) will pick up DR-047 by derivation (DR-046); the prototype's hand-reconciled `CONFIG.rules` + a self-learning Concept page are pending until the system is built.

## 2026-06-15 — Mission Control's Manual stays in sync with the factory (DR-046)
**What:** New policy **DR-046**: Mission Control's Manual (the "Códice del gremio", [FRD-08](../mission-control/docs/frds/frd-08-documentation/frd.md)) is the operable, navigable surface of the factory's know-how and MUST stay in sync with it via two mechanisms — (1) the **Reference** catalogs (commands/agents/decision-rules/standards) are **derived** from the canonical source (plugin skill+agent frontmatter, `factory/decisions/registry.yaml`, `factory/standards/`), so a new/renamed/removed skill, agent, rule or standard appears automatically with no hand-copy; (2) the hand-authored **Guides/Concepts** are kept in sync by the documentation discipline — a change to how the factory is operated updates the affected Manual page in the same change. Encoded the rule in the root `CLAUDE.md` "Decision log" section (a paragraph after the canonical-doc discipline).
**Why:** The owner asked that every relevant change to the factory's workflow (new skills, new agents, flow changes) also be reflected in Mission Control's documentation/Manual, and that the future app keep it defined there rather than drifting. A re-audit proved the need: the prototype Manual had silently drifted — missing 2 skills (`adopt`, `review-launch`), 1 agent (`implementer`), 6 rules (DR-040..045) and 1 standard (`documentation`).
**Context:** Owner's choices — mechanism = derive (reference) + discipline (prose), not pure manual discipline; scope = add the rule AND reconcile the prototype now. The rule lives in policy (registry DR-046) + canonical doc (FRD-08) + factory operating discipline (`CLAUDE.md`), deliberately NOT in `factory/standards/documentation.md` (that standard propagates to product projects; Mission Control is the factory's own tool, not a product project).
**Impact:** `factory/decisions/registry.yaml` (DR-046), `CLAUDE.md` ("Decision log" section). Twin entry + the prototype reconciliation in Mission Control's decision-log (same date): `mission-control/docs/frds/frd-08-documentation.md` rewritten, `mission-control/prototype/index.html` (`CONFIG.{skills,agents,rules,estandares}`).

## 2026-06-15 — Skill write-gate + brownfield adoption (factory↔project boundary hardened)
**What:** Two new policies + a guard subsystem to harden the boundary between the factory and a project. (1) **Write-gate (DR-044)**: changes to a Pandacorp product project that touch app behavior, a canonical doc (PRD/FRD/blueprint/ADR/DESIGN) or state (`status.yaml`/work-orders) flow through the `/pandacorp:*` skills, not ad-hoc free-chat edits — with a PRAGMATIC frontier (reading/debugging and micro non-product edits stay exempt) and a ROUTER that classifies the owner's request and AUTO-INVOKES the right skill, announcing it (entry only; the skill's internal human-gates stay intact). (2) **Brownfield adoption (DR-045)**: the factory stays greenfield-first but now ACCEPTS external projects via the new `/pandacorp:adopt`; every mutating skill gained a PREFLIGHT that stops on a non-Pandacorp folder and points to adopt/spec instead of failing silently or hallucinating.
**Why:** Analysis of "what happens if I run a Pandacorp skill on a project that isn't in the factory" found two gaps: (a) no skill verified "is this a Pandacorp project?" — `implement` hit an empty queue, `iterate` invented PRD/FRDs and queued work-orders into a non-existent queue; (b) nothing stopped free-chat changes from bypassing the documented process → docs/state drift. The owner wanted changes to a Pandacorp project to always flow through the skills, plus a supported path for projects built outside the factory.
**Context:** Owner's design choices — frontier = pragmatic (not "everything by skill"); router = auto-invoke and announce (not propose-and-confirm); enforcement = instruction + a non-blocking warning hook (not a hard block, which would false-positive against the skills themselves). The Pandacorp marker is `docs/status.yaml` + "Origin — Pandacorp" in `CLAUDE.md` (reuses the scope check already in `block-dangerous.sh`; a shared `is-pandacorp-project.sh` formalizes it, distinguishing a product project from the factory, which has no `status.yaml`). Auto-invoke covers entering the skill only; the production (DR-004), money (DR-005), delete (DR-007) and external-comms (DR-008) gates still ask.
**Impact:** `factory/decisions/registry.yaml` (DR-044, DR-045). Plugin (same date, `plugin/docs/decision-log.md`, v5.1.0): `CLAUDE.md.tpl`/`AGENTS.md.tpl` (write-gate section), preflight in `implement`/`iterate`/`release`/`bug`/`new-version`, new `scripts/is-pandacorp-project.sh` + `scripts/warn-adhoc-write.sh` + the `Write|Edit` hook in `hooks/hooks.json`, new skill `plugin/skills/adopt/`. Mission Control: `frd-16-orphan-project-detection.md` (same date, its decision-log). Root `CLAUDE.md` operation table (added `/adopt`). Pending: commit + `claude plugin update` + restart.

## 2026-06-15 — Pipeline re-audit fixes: return-aware delivery, post-launch loop, state-model unification
**What:** Acted on the pipeline re-audit. (1) **State model unified**: idea-card status is now `discovered → recommended → in-pipeline → shipped | discarded` (retired `documented` as a card status); once `in-pipeline` the card freezes as a pointer and the board derives the middle columns from the project's `status.yaml` `phase` — fixing the two-disjoint-machines fracture (CLAUDE.md rule 5, `_idea-template.md`, FRD-01/02). (2) **Return-aware pipeline (DR-042)**: `spec`/`blueprint`/`release`/`product-manager`/`analytics`/`researcher` now read `return_type` — personal skips market/pricing/GTM, monetary/mixed get a demand gate before build + a unit-economics block + a launch plan, opportunity validates the asset and instruments the opportunity metric. (3) **Post-launch loop (DR-043)**: new `/pandacorp:review-launch` + portfolio business columns close the kill/double-down loop. (4) **Engineering refinements**: EARS on every criterion (constitution §1), FRD size cap, feasibility spike + FRD↔blueprint reconciliation, cross-family reviewer in deep (DR-015), independent fact-check of research before the PRD. (5) **Designer (constitution §16)**: bespoke per-domain identity; the RPG/"guild" look is Mission Control's only, never a product template.
**Why:** The audit found the flow strong on engineering but blind on the economic/market axis and fractured in its state model; the v4 i18n migration had translated the issues, not fixed them; and `return_type` was declared (DR-039) but the delivery pipeline never read it.
**Context:** Owner approved all audit recommendations and clarified the designer point (product apps are bespoke per domain, not RPG). State-model design: card = pre-project + terminal source of truth, project `phase` = the in-flight truth, board = derived view (no duplicated state across factory↔project).
**Impact:** `factory/constitution.md` (§1, §16), `factory/decisions/registry.yaml` (DR-042, DR-043; DR-015 note), `factory/standards/conventions.md` (gitignored card-frontmatter-enum carve-out), `factory/portfolio.{md,example.md}` (business columns), `CLAUDE.md` (rule 5, operation table). Plugin/ideas/Mission-Control: their decision logs (same date). Pending: commit + `claude plugin update` + restart.

## 2026-06-15 — Launch market & language is researched per product (not assumed Spanish)
**What:** Added a launch-market-&-language research dimension. During `/pandacorp:spec`, the `researcher` analyzes where to launch (which market/country first) and in which language(s) — single vs multi from the MVP — weighing market size/competition, customer-acquisition ease, ROI and the owner's country/language; the `product-manager` records the decision in the PRD (owner-approved). Products are always i18n-ready; the chosen launch locale sets the UI default (no longer "Spanish by default"). Mission Control and owner-facing tools stay Spanish regardless. `/pandacorp:onboarding` now captures the owner's `country`.
**Why:** The launch language is a market decision per product, not a fixed assumption. The owner's country matters for acquisition ease, but a bigger/higher-ROI market may justify another language or multi-language. No fixed default — case by case.
**Impact:** `plugin/skills/onboarding/SKILL.md` + `factory/profile.example.md` (`country`), `plugin/skills/spec/SKILL.md`, `plugin/agents/researcher.md`, `plugin/agents/product-manager.md`, `factory/standards/conventions.md` (Language/UI), `factory/standards/seo-i18n.md`, `factory/decisions/registry.yaml` (DR-041). Also fixed a stale "documents in Spanish" rule still present in constitution §15, product-manager and spec (a language-migration residual).

## 2026-06-15 — Solo-operator git workflow: direct push to main allowed (no mandatory PR)
**What:** Dropped the "feature branches; never push to main" rule. The solo operator commits and pushes to `main` directly; mandatory PRs/branches are gone (there is no second human to review). The quality gate is the `reviewer` in `/pandacorp:implement` + `.pandacorp/verify.sh`. A throwaway branch stays as an OPTIONAL tool for big/risky changes. Removed the direct-push-to-main block from `block-dangerous.sh`; kept the anti-catastrophe guards (force-push, delete main branch, `reset --hard`, `rm -rf`, `gh repo delete`).
**Why:** With a single operator, human PR review adds no value — it's pure friction. The agentic review during the build is the real gate.
**Impact:** `plugin/scripts/block-dangerous.sh`, `factory/constitution.md` (§11), `factory/standards/conventions.md`, `factory/decisions/registry.yaml` (DR-040), the project templates `CLAUDE.md.tpl`/`AGENTS.md.tpl`, the build agents (`implementer`, `backend-dev`, `frontend-dev`), the `iterate` skill, `mission-control/PLAN.md`. Applies to the factory and every scaffolded project.

## 2026-06-15 — Interaction language: the agent always talks to the owner in Spanish
**What:** Made explicit a second language axis. The ARTIFACT language follows git state (committed = English / gitignored = Spanish, DR-009). The INTERACTION language is separate: everything the agent says to the owner — in chat and inside any skill (questions, explanations, progress, recommendations, summaries) — is ALWAYS in Spanish, regardless of the artifact's language.
**Why:** The owner operates Pandacorp in Spanish; the "committed = English" rule must not be misread as "answer the owner in English." The agent writes English into committed files but addresses the owner in Spanish.
**Impact:** `CLAUDE.md` (rule #1), `factory/standards/conventions.md` (Language), `factory/decisions/registry.yaml` (DR-009), and the project templates `CLAUDE.md.tpl` / `AGENTS.md.tpl` (propagates to every project).

## 2026-06-15 — Language migration executed (whole repo → English; Spanish only in the comms layer/UI)
**What:** Executed the policy below on a dedicated branch (`refactor/i18n-english`, one commit per phase, tag `refactor-i18n-baseline`). Renamed Spanish folders/files to English with `git mv` (`fabrica`→`factory`, `estandares`→`standards`, `decisiones`→`decisions`, `docs/investigacion`→`docs/research`, `docs/propuestas`→`docs/proposals`, `mission-control/docs/diseno`→`design`, `constitucion`→`constitution`, `bitacora`→`decision-log`, `registro`→`registry`, `estado`→`status`, FRD/doc slugs, zone assets); updated ~306 cross-references; migrated machine enums/keys to English (idea status, project phase, frontmatter and status.yaml keys, owner-profile schema, build-mode tokens); translated ~95 committed docs to English via a subagent fan-out + verifier; split the status.yaml prose into a gitignored `summary.md`; bumped the plugin to 4.0.0.
**Why:** Implement the language policy (entry below) so the public repo reads as English while the owner keeps operating in Spanish.
**Context:** Spanish stays only where the policy allows: the Mission Control prototype UI copy, gitignored personal data, and the gitignored communication layer (`summary.md`, `decisions.md`, `iteration.md`, `progress.md`). Verified with an adversarial review: 154 tracked files preserved, `git log --follow` history intact, no content lost, personal data still gitignored, machine files parse, prototype renders with Spanish labels, `claude plugin validate` passes.
**Impact:** Repo-wide. Canonical rule in `CLAUDE.md` (rule #1), `factory/standards/conventions.md` (Language), `factory/decisions/registry.yaml` (DR-009); plugin in `plugin/docs/decision-log.md` (v4.0.0). Pending: merge to `main` (owner gate).

## 2026-06-15 — Language policy: committed = English / gitignored = Spanish
**What:** Change to the language rule for artifacts. **The state in git decides the language**: everything committed goes in ENGLISH —code, commits, **file and folder names**, and product/technical documents (PRD, FRD, blueprint, ADR, README, tests, and the project's `docs/decision-log.md`)—; everything gitignored goes in SPANISH —the communication with Pandacorp (project summary, `docs/decisions.md`, logs, activity, Mission Control feed, `docs/iteration.md`) and the owner's personal data—. `docs/status.yaml` is committed with only machine state in English (keys/enums/counters/SHAs) and its human-readable prose moves to the gitignored Spanish layer; Mission Control shows it in Spanish. The user's UI continues via i18n, Spanish by default.
**Why:** The owner wants to keep the industry standard (anyone who clones the GitHub repo sees everything in English) while still operating Pandacorp in Spanish. The previous rule ("documents in Spanish") was the opposite and would have forced on-the-fly translation in the interface; with this policy each artifact is born already in the correct language, without a translation layer. Resuming on another machine still works because what matters (FRD/PRD/work orders/`status.yaml`) is committed and in English; the Spanish layer is a regenerable local view (closing/advancing the phase before jumping to another machine protects the in-flight feedback in `iteration.md`).
**Context:** Owner's decisions in the conversation: (1) file names ALWAYS in English, in every project, regardless of the content's language; (2) the PROJECT decision log is the real history of the product → English + committed (the factory's is documented today in Spanish and will be translated in the sweep); (3) `status.yaml` committed with machine state in English + the prose to the Spanish layer. The Spanish layer is local-only: losing the old history when switching machines is acceptable as long as the durable parts are in the English docs.
**Impact:** Canonical docs updated TODAY (in Spanish, to be translated in the sweep): `CLAUDE.md` (rule #1), `factory/standards/conventions.md` (Language section), `factory/decisions/registry.yaml` (DR-009 rewritten). **PENDING** (large sweep, not yet executed): translate ~100 committed docs to English, rename ~40 files/folders with Spanish names, separate the prose from `status.yaml` into the gitignored layer, set up the convention for the local Spanish layer, and propagate to the templates (`CLAUDE.md.tpl`, `AGENTS.md.tpl`, the scaffold's `.gitignore`). Mission Control: its UI copy stays in Spanish by design (it is the owner's Spanish tool).

## 2026-06-15 — App rename: "cockpit" → "Mission Control" (and the RPG panel → "Party")
**What:** Owner's decision: the factory app is no longer called "cockpit" and is now called **Mission Control**; the per-project RPG panel previously called "Mission Control" is now **Party**. The folder `cockpit/` → `mission-control/` was renamed (with `git mv`) and the vocabulary was updated across the whole repo: paths (`cockpit/` → `mission-control/`), prose ("the cockpit" → "Mission Control"), the panel's canonical docs (`MISSION-CONTROL.md` → `PARTY.md`, `frd-06-*`), `.claude/launch.json`, and `.claude/hooks/decision-log-reminder.sh`. "Pandacorp" is kept as the name of the factory/brand.
**Why:** By convention, "Mission Control" names the center that supervises and commands the entire operation (NASA, macOS), not a sub-panel; the name was applied backwards and was being confused with the whole app. It stands as: Pandacorp (organization) → Mission Control (control center = the app) → Party (the agent crew of each mission).
**Context:** The owner requested the rename after noticing the inconsistency; he chose "Party" (an RPG term) for the panel, consistent with the UI (the neighboring subtab was already "Work orders"). The non-visible internal identifiers of the prototype were preserved (`cockpitDataDiagram`, slug `pandacorp-cockpit`, id `c-cockpit`, functions `mc*`/`missionBody`). The historical file name `docs/proposals/05-interfaz-cockpit.md` was renamed to `05-mission-control-interface.md`.
**Impact:** `CLAUDE.md`, `README.md`, `DECISION-LOG.md`, `factory/standards/{infra,external-services}.md`, `factory/decisions/registry.yaml` (DR-031), `factory/portfolio.example.md`, `plugin/skills/{implement,decide,recommend,codify,bug,blueprint}/SKILL.md`, `plugin/scripts/emit-event.sh`, `plugin/templates/shared/docs/status.yaml.tpl`, `docs/**`, the entire `mission-control/` folder, `.claude/launch.json`, `.claude/hooks/decision-log-reminder.sh`. Product doc: see [mission-control/docs/decision-log.md](../mission-control/docs/decision-log.md) (same date).

## 2026-06-14 — The factory opens as a public template (depersonalization + profile + portability)
**What:** We prepared the repo to upload it to **public** GitHub so that anyone can clone it and use it as their own factory. Three structural changes: (1) **depersonalization** — the hardcoded personal identity was removed (the owner's own name, ~167 occurrences in ~110 files), replacing it with the generic role "the owner"; (2) **owner profile** — a new skill `/pandacorp:onboarding` that interviews the owner and generates `factory/profile.md` (personal, gitignored), which the rest of the factory reads to personalize itself; versioned seeds `profile.example.md` and `portfolio.example.md`; (3) **portability** — the absolute paths to the factory were removed from skills/scripts/Mission Control; the root is resolved dynamically (cwd / repo root). Personal data (profile, portfolio, idea cards) is left gitignored.
**Why:** The owner wants to share the know-how without leaking what is his, and to have the repo work as-is on any machine. A factory written around one person and with absolute paths is not reusable.
**Context:** Owner's decisions — remove the own name (don't even leave it nor parameterize it with tokens); the onboarding interview as the personalization mechanism; exclude only idea cards + `portfolio.md` (the decision logs and `docs/` are kept as the "why" of the framework); history cleaned only of the personal cards (filter-repo); no LICENSE for now.
**Impact:** `README.md` (new), `.gitignore`, `CLAUDE.md`, `factory/profile.example.md` and `factory/portfolio.example.md` (new), `plugin/skills/onboarding/` (new), `plugin/scripts/scan-ideas.sh` and several skills (relative paths), `mission-control/PLAN.md`, `factory/decisions/registry.yaml` (DR-033). Pending: commit + `claude plugin update`.

## 2026-06-14 — Documentation discipline propagated to all projects (standard)
**What:** The two-layer rule (canonical doc + decision log) goes from being only the factory's to a **propagable engineering standard**: a new `factory/standards/documentation.md`; every project is born with a `docs/decision-log.md` (scaffold template); and the rule is injected into each project's `CLAUDE.md` and `AGENTS.md`.
**Why:** The owner asked that the discipline apply to EVERY project, not just the factory. The standards in `factory/standards/` are injected into each project via scaffold + agents; that is the correct channel for it to always be enforced.
**Context:** The distinction was clarified in the standard — `docs/decision-log.md` (durable history) ≠ `docs/iteration.md` (transient phase memory, DR-032) ≠ `registry.yaml` (policy). Enforcement: the `reviewer` checklist + phase gate; automatable in the future.
**Impact:** `factory/standards/documentation.md` (new), `factory/standards/README.md`, `factory/standards/structure.md`, `plugin/templates/shared/docs/decision-log.md.tpl` (new), `plugin/templates/shared/CLAUDE.md.tpl`, `plugin/templates/shared/AGENTS.md.tpl`, `plugin/skills/scaffold/SKILL.md`. Pending: commit + `claude plugin update`.

## 2026-06-14 — Documenting = canonical doc + decision log (two layers)
**What:** We refined the "document everything" discipline: each relevant change is documented in **two places**. (1) The **canonical doc** that owns the fact is updated to reflect the current truth (app behavior → the FRD; technical → blueprint/ADR; design → DESIGN/tokens; plugin → the skill/agent file; idea → its card). (2) The area's **decision log** records what changed and why, linking that doc.
**Why:** The owner clarified that a log is not enough: the documentation has to go **where it belongs**. The canonical doc answers "what is true now?" and the decision log "how did we get here and why?". The doc alone loses the why; the decision log alone leaves the doc out of date.
**Context:** Three distinct things remain and are not mixed — canonical doc (truth), decision log (history), `registry.yaml` (policy). Routing map of change→doc in `CLAUDE.md`.
**Impact:** `CLAUDE.md` (Decision Log section rewritten with the map and the two-layer rule), `DECISION-LOG.md` (How it is used).

## 2026-06-14 — Decision-log discipline: document everything, by area
**What:** We adopted a **per-area decision log** (Mission Control, plugin, ideas, factory) with an index in `DECISION-LOG.md` (root). Each relevant decision is noted with a date, with *what* and *why*. It is codified as a rule in `CLAUDE.md` + a reminder hook.
**Why:** Until now the *why* of decisions was scattered (commit messages, `registry.yaml`, or it was lost). The owner wants **everything** we decide to be recorded and visible, organized by area, so the thread can be picked up again even as time passes or the conversation is lost.
**Context:** The owner chose: scope = *decisions + the why* (not raw transcripts); organization = *one decision log per area* (not global nor hybrid); enforcement = *rule in CLAUDE.md + reminder hook*. Discarded: a single global chronological log (mixes topics) and dumping entire conversations (noise).
**Impact:** `DECISION-LOG.md`, `CLAUDE.md`, `mission-control/docs/decision-log.md`, `plugin/docs/decision-log.md`, `factory/ideas/decision-log.md`, `factory/decision-log.md`, hook in `.claude/settings.json`.

# Decision Log — Ideas

Decisions about the **idea base and its process** (scoring, discarding, discovery flow). Most recent on top. See the index and format in [DECISION-LOG.md](../../DECISION-LOG.md).

> Note: the content and status of **each idea** lives in its own card (`factory/ideas/<slug>.md`, frontmatter). This file holds decisions about **how we manage ideas**, not the ideas themselves.

## 2026-06-26 — `/discover` run: 2 net-new survivors (PandaSeña, PandaScript); collector-tracker space found saturated on the board
**What:** A `/discover` run (3 researchers, 2 streams, 40+ sourced links) produced **2 new `discovered` cards**: `panda-sena.md` (🏆 build — gestor de preventas seña/saldo, micro, PE-nativo) and `panda-script.md` (🏆 build — colección→guion de TikTok, micro, utilidad personal). Hero = PandaSeña. Plus an **extension proposal** for the owned app `cotizapdf` (puente cotización→factura SUNAT vía PSE), NOT carded — gated on verifying PSE/OSE terms (legal MEDIUM). Killed this run (reported, not carded): PandaPortfolio/collection-value, PandaGaps/gap-finder, PandaVariant, multi-category tracker, figure preorder/restock alert (**all re-surface concepts already on the board** — `panda-collector` discarded consolidated *binder+valor+alertas*; `collector-launch-tracker` = PandaWatch público; `funko-tracker`), PandaSwap (vitamin + network-effect), dev tools ContextBridge/PromptProof (Anthropic-absorbs-natively pattern, like the discarded `ai-token-budget-manager`), job tracker (no channel; folds into JobLeap).
**Why:** The collector-tracking/value/alerts/authenticator space is now **saturated on the owner's own board** — the only genuinely-new collector survivor was preventa *management* (distinct from launch *alerts*). The creator-tooling space (content scripts/planning) is wide open and high founder-fit. Curated to 2 strong cards rather than padding (avoids the "6 fragmentos" mistake `panda-collector` flagged).
**Impact:** New cards `factory/ideas/{panda-sena,panda-script}.md`. No skill/plugin change. Exclusion rule (DR 2026-06-25) applied: every board concept (any status incl. discarded) excluded.
**Outcome (same day):** Owner immediately **discarded PandaSeña** from Mission Control — *"es lo mismo que PandaTrack, pero versión simple"*. Lesson: PandaTrack (an owned app, not carded) already covers preorder tracking; PandaSeña should have been surfaced as an **extension of PandaTrack**, not a new standalone card. The miss: I didn't read PandaTrack's actual scope (it lives in a sibling folder, only named in the profile). **Rejection pattern distilled into `factory/profile.md` (DR-053):** don't propose simplified re-implementations of the owner's owned Panda* apps — check their real scope and propose an extension. PandaScript stands.

## 2026-06-26 — Card body format = the Propuesta-tab rendering contract (subsection markers)
**What:** `_idea-template.md`'s body is now a precise marker format the Mission Control Propuesta tab parses to render a rich memo: `### subsections` per labelled row, a `> **Badges:**` pipe line, a `> blockquote` testimonial under El problema, `- **Title** — desc` feature bullets + a `- ✓`/`- !` checklist mock under La visión, and a `| Riesgo | Mitigación |` table under Gaps. Keep the headings/markers verbatim. Plugin side (skills + version): `plugin/docs/decision-log.md` (same date, v9.12.0).
**Why:** Owner wanted the Propuesta tab pixel-faithful to the approved HTML; the old one-line-bullet body rendered flat. The format must come from the skill, so every new card looks right — a worked example lives at `factory/ideas/pandacheck.md`.
**Impact:** `factory/ideas/_idea-template.md` (body rewrite + rendering-contract comment). Consumers: `/discover`, `/new-idea`, Mission Control's `IdeaPitch`. Cross-ref: `plugin/docs/decision-log.md` v9.12.0 + `mission-control/docs/decision-log.md`.

## 2026-06-26 — `/discover` redesign approved (the "memo-pitch" direction) + 6 owner rules — pending implementation
**What:** Approved the direction to fix why discovery cards don't land: they're not *the owner's*, they're told *cold*, and they leave open exactly the doubts that deflate him (source-of-truth, legality, buildability, distribution). The redesign keeps the current `/discover` pipeline and adds verifiable gates + a **STRONG (hot→cold) memo-pitch** output rendered as an **HTML memo derived from the `.md`** (one fixed template, charts only where there's real data). Full proposal + a worked example live in [docs/proposals/10-discover-redesign-pitch.html](../../docs/proposals/10-discover-redesign-pitch.html) and [10b-sample-idea-memo.html](../../docs/proposals/10b-sample-idea-memo.html) (gitignored, Spanish, owner-facing). Researched + adversarially red-teamed via a 12-agent workflow (40+ sources). **The 6 owner rules that govern implementation:**
1. **Fold-in → propose an extension, don't hide.** Existing apps (built outside Pandacorp) need NO frozen scope; when an idea overlaps one, surface it as a *proposed feature-extension* of that app when beneficial — not as a near-duplicate standalone, not silently dropped.
2. **Output = HTML memo, but it MUST be visual** (like the proposal/PandaCheck example), not plain text — else not worth it. Conditional adoption.
3. **Complexity default = MICRO + SMALL** (cotizapdf, pandawatch). A LARGE app is only proposed when the pain is highly recurrent, with strong complaints, and strong monetization odds.
4. **Legal risk: HIGH → kill; LOW → present with disclaimer.** ("validate-with-disclaimer" exists only for low risk.)
5. **The 3 return paths have EQUAL priority** (hit / micro-apps / personal); the report's hero is chosen by pain strength + fit + buildability, not by return type.
6. **Market-reality filter applies to MONETARY ideas only (must work in LATAM).** A personal-utility idea (helps the owner's own collection) is welcome even if it doesn't monetize.
**new-idea:** since the owner already wants to build those ideas, it does NOT use the "sell" layer — it *documents what he wants and proposes how to improve it*, inheriting the quality gates (source-of-truth, legal, buildability) but not the pitch/persuasion capa.
**Why:** Owner: no recommended idea has made him "dream"; the doc didn't *sell* the idea, felt misaligned, too complex (the authenticator's true/false + legal), and lacked a distribution plan. He asked for an investor-pitch-style output, easy-to-build micro-app bias, and everything red-teamed.
**Impact:** **Skill side BUILT (2026-06-26):** `plugin/skills/discover/SKILL.md` (full rework — gates + memo-pitch hot→cold + the 6 rules), `plugin/skills/new-idea/SKILL.md` (quality gates without the sell layer), `factory/ideas/_idea-template.md` (new frontmatter `complexity_bucket`/`painkiller_class`/`ground_truth_source`/`legal_risk`/`prior_workaround`/`distribution_channel` + hot→cold body), plugin **v9.9.0** (`plugin/docs/decision-log.md`, same date). **Pending:** Mission Control's **Propuesta** tab (native React `<IdeaPitch>`, order Propuesta→Documentos→Campaña, FRD-02) — its own entry in `mission-control/docs/decision-log.md`; and an `explore` nudge pass. The standalone HTML memo derives from the card `.md` (no bespoke per-idea HTML).

## 2026-06-25 — `discard_reason` on the card: capture WHY, so discovery learns the rejection pattern
**What:** New card field `discard_reason` (set from Mission Control on discard, alongside `status: discarded`): the *why* of the rejection. `/pandacorp:discover` reads it across discarded cards to distil the owner's recurring **rejection patterns** and kills new candidates that fit one (not just exact repeats); durable patterns are distilled into the profile. The raw reason lives on the card; the generalized preference lives in `factory/profile.md` — same data, two granularities, no new file. Skill/plugin side + the MC capture (FRD-02, queued for the build): `plugin/docs/decision-log.md` (same date, v9.8.0).
**Why:** Owner wanted a discard to *teach* the factory ("no me recomiendes cosas con ese mismo patrón de descarte"), ideally captured in the app. Excluding the exact idea wasn't enough — the reason must generalize.
**Impact:** `factory/ideas/_idea-template.md` (`discard_reason` field). Consumers: MC (writes it on discard) + `/pandacorp:discover` + `/pandacorp:new-idea`. Cross-ref: `plugin/docs/decision-log.md` v9.8.0.

## 2026-06-25 — Discarded ideas: keep the card (`status: discarded`), never delete — it's the single do-not-recommend memory
**What:** A rejected idea must stay as a `status: discarded` card (the Descartado column), **never deleted**. That column is the single source of truth for rejected ideas and the do-not-recommend memory: `/pandacorp:discover` reads all cards (any status, incl. discarded) and excludes them, surfacing only genuinely NEW ideas; `/pandacorp:new-idea` checks discarded cards before reviving one. Restored the 4 previously-deleted discards as minimal `status: discarded` cards. **No separate graveyard ledger** — a first attempt added one and the owner correctly rejected it as duplicating the Descartado column. Skill/plugin side: `plugin/docs/decision-log.md` (same date, v9.7.0).
**Why:** Owner: discover re-recommended ideas already on the board and resurrected discarded ones, because **deleting a discarded card erased the memory it was rejected**. The fix is one source (the discarded status), one rule (don't delete) — not a parallel file.
**Impact:** Convention: discard = mark `status: discarded` (from Mission Control), don't delete. Restored `factory/ideas/{ai-token-budget-manager,freelancer-creative-crm,agency-report-generator,multichannel-seller-ai-assistant}.md` as discarded. Consumers: `/pandacorp:discover` + `/pandacorp:new-idea`. Cross-ref: `plugin/docs/decision-log.md` v9.7.0.

## 2026-06-23 — Card Return must validate the revenue rail in the owner's market (Perú/LATAM)
**What:** The template's Return section now requires checking that the affiliate program / payment processor / ad network actually exists and pays in the owner's country — and saying so if the model only works abroad (and whether it's *build-for-others*). The discovery-flow side (kill criterion #8 + market-reality research) is in `plugin/docs/decision-log.md` (same date, v9.3.0).
**Why:** A PandaShelf re-eval exposed US-lens monetization (affiliates that exclude Perú); cards must contextualize return to where the owner actually operates.
**Impact:** `factory/ideas/_idea-template.md` (Return section). Cross-ref: `plugin/docs/decision-log.md` v9.3.0.

## 2026-06-23 — Card = a decision memo: verdict frontmatter + 2-layer (glance/deep-dive) body
**What:** The idea-card format (`_idea-template.md`) is now a **1-page decision memo**. New frontmatter decision fields: `verdict` (build | validate | integrate), `the_bet` (one-line hook), `fold_into` (an existing owned app, if integrate), `why_now`, `kill_risk`, `validation_step`. The body is restructured into two layers: **At a glance** (verdict + la apuesta + storytelling problem + diferencial + why-now + founder-fit + #1 risk + 7-day test + a 5-axis scorecard) for deciding, and **Deep dive** (solución, features, similares, comparación, retorno, red team, evidencia, notas) for reading more. A `discard` never becomes a card (it's red-teamed out before carding). This is the idea-base/process side; the skill + plugin-version side is in `plugin/docs/decision-log.md` (same date, v9.2.0).
**Why:** Owner couldn't decide from a flat 9-card buffet — needed a verdict at a glance, the conviction hooks (why-you/why-now/what-winning-looks-like + an honest surviving risk), and awareness of apps he already owns (fold-in). The card had to stop merely informing and start enabling a yes/no.
**Impact:** `factory/ideas/_idea-template.md` (decision frontmatter + 2-layer memo). Consumers: `/pandacorp:discover` (curated, red-teamed) and `/pandacorp:new-idea` write the new fields; Mission Control can now render a **decision view** (verdict + the_bet on the chip, fold-in flagged) — it renders the card body as markdown, so the memo surfaces with no app change, and the new frontmatter is additive (old cards stay valid). Cross-ref: `plugin/docs/decision-log.md` v9.2.0.

## 2026-06-23 — Card anatomy enriched: storytelling problem + features + similares + differential value
**What:** The idea-card format (`_idea-template.md`) gains three sections after *Proposed solution* — **Features** (high-level dream bullets), **Similar (what already exists)** and **Comparison & differential value (why ours)** — and the **Problem** is now written as **storytelling** (the reader in the shoes of whoever suffers the pain). The discovery flow was updated to match (the `researcher` brings *similares* + the differential + dream features; the report gives a per-idea value pitch). This is the *idea-base/process* side of the change; the skill + plugin-version side is logged in `plugin/docs/decision-log.md` (same date, v9.1.0).
**Why:** Owner feedback during a `/discover` run — the cards informed but didn't *convince* (no competitive wedge, no emotional hook, flat feature lists). He wants to see the value, what already exists and why ours wins, features that make him dream, and the problem told so he can step into the user's shoes.
**Impact:** `factory/ideas/_idea-template.md` (3 new sections + storytelling Problem). Consumers: `/pandacorp:discover` and `/pandacorp:new-idea` write the new sections; Mission Control renders the card body as markdown, so the extra sections surface with no app change. Cross-ref: `plugin/docs/decision-log.md` v9.1.0.

## 2026-06-15 — Idea-status machine unified + cards migrated to the v4 schema
**What:** Retired `documented` as a card status; the machine is now `discovered → recommended → in-pipeline → shipped | discarded`. `new-idea`/`discover` create cards as `discovered` (not `documented` — a raw idea has no PRD yet); on handoff the card freezes at `in-pipeline` and the board derives the middle columns (documented/design/architecture/building) from the project's `status.yaml` `phase`, NOT the card status — the "Documentada" column = a project in phase `product`. Also migrated the two real cards (`funko-tracker`, `restaurant-table-ordering`) to the v4 frontmatter: valid `project_type` (`web`), `origin: owner`, added `return_type` + `profile_alignment` (they were still in the dropped `tipo` vocabulary, e.g. `project_type: ambas`/`monetizable`).
**Why:** The board promised six columns but no skill ever wrote the middle three to the card; `documented` meant two different things (raw card vs post-PRD); and the real cards carried invalid enum values that the board/skills can't parse.
**Impact:** `factory/ideas/_idea-template.md`, `funko-tracker.md`, `restaurant-table-ordering.md`, `factory/standards/conventions.md` (gitignored-frontmatter-enum rule). Board derivation: `mission-control/docs/decision-log.md` (FRD-01/02). Skills: `plugin/docs/decision-log.md` (v5.0.0).

## 2026-06-15 — Card frontmatter migrated to English keys/enums (UI labels stay Spanish)
**What:** As part of the repo-wide language policy, the idea-card frontmatter keys and enum values moved to English: keys `title/project_type/origin/status/difficulty/profile_alignment/return_type/likely_stack/evidence/created/project`; the `status` enum `discovered → documented → recommended → in-pipeline → shipped | discarded`. The board's consumers were synced (`ideas.base` groups by `status`, `scan-ideas.sh` parses `status:`, the Mission Control prototype maps the English keys to Spanish display labels). Personal cards (gitignored) keep their Spanish prose body; only their frontmatter keys/enums were migrated.
**Why:** Machine-read identifiers must be English (committed = English); the owner still sees Spanish labels in the board.
**Impact:** `factory/ideas/_idea-template.md`, `ideas.base`, `plugin/scripts/scan-ideas.sh`, the prototype. See `factory/decision-log.md` (same date).

## 2026-06-15 — Card schema: category (project_type) + return, dropping the binary tag
**What:** The card replaces the `tipo` tag (monetizable/personal/both) with two orthogonal axes: `project_type` (web/mobile/desktop/ai/claude-code/prompt-system/automation/cli/rework/other) and `return_type` (monetary/opportunity/personal/mixed), plus `profile_alignment`. `new-idea` always classifies both.
**Why:** The binary tag didn't distinguish WHAT kind of solution it is, nor did it capture that the return can be an opportunity and not just money. It aligns the card with the recommendation model (DR-039) and with the board tags (Mission Control FRD-02).
**Impact:** `factory/ideas/_idea-template.md`, `plugin/skills/new-idea/SKILL.md`. Reflected in `mission-control/` (FRD-02) and DR-039.

## 2026-06-15 — Recommendations based on the owner's profile (two streams + dual value)
**What:** `discover` and `recommend` now read `factory/profile.md` and propose ideas in two streams (~50/50): general high-return opportunities, and opportunities aligned to the profile (interests/assets) even if their monetary return is lower. "Return" is no longer just money: it includes opportunity (reach/network/positioning), learning and personal value. The scope is opened up to any technological solution (web/mobile app, rework, tooling/Claude Code project, prompt or prompt system, automation). The card adds `project_type`, `profile_alignment` and `return_type`; `new-idea`'s scoring weighs return and fit.
**Why:** The owner wants recommendations well aligned to him —his interests, goals and assets (e.g. an audience that opens doors)— without losing the big general opportunities. Not everything has to be an app or monetize heavily: what matters is a real problem with good return (monetary or opportunity).
**Impact:** `plugin/skills/{discover,recommend,new-idea,onboarding}/SKILL.md`, `factory/profile.example.md`, `factory/ideas/_idea-template.md`, `factory/constitution.md` (mission), `factory/decisions/registry.yaml` (DR-039), `mission-control/prototype/index.html`.

## 2026-06-14 — Ideas decision log created
**What:** The decision log about the idea base starts.
**Why:** Part of the factory's "document everything" discipline — see [factory/decision-log.md](../decision-log.md).

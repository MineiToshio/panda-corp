<!--
  INDEX.md — retrieval index over factory/memory/. One line per ACTIVE lesson
  (candidates and deprecated are excluded). Maintained by the librarian with
  DELTA edits ONLY — add/update/remove single lines, never regenerate the file
  wholesale (ACE context-collapse: a wholesale rewrite by a model erodes the
  accumulated detail). Hard cap: 150 lines.
  Format: - LESSON-NNNN · use when <trigger> · <one-line insight>
-->

- LESSON-0001 · use when designing or running the lesson harvester and deciding whether a reflection-style insight qualifies as a storable lesson · anchor every lesson to concrete falsifiable evidence; reflections-on-reflections hallucinate and poison the memory
- LESSON-0002 · use when an FRD gate fails after a patch and you must decide between rebuilding the work order or suspecting the reviewer's own gate test is defective · distinguish wrong-production-code from defective-gate-test; a rebuild discards correct work and cannot fix a bad test
- LESSON-0003 · use when a board/rollup derives live state from worker-written frontmatter during a parallel build wave and shows less in-flight work than exists on disk · the orchestrator must write IN_PROGRESS at dispatch; never rely on workers to self-report their own start
- LESSON-0004 · use when an automated conformance/sync step is about to overwrite a project config from a template or other single-source-of-truth copy · verify the source is truly ahead and re-run the FULL gate after syncing; blind overwrite can revert a fix
- LESSON-0005 · use when parsing or writing markdown frontmatter with gray-matter@4 in Node · pass a no-op options object to bypass gray-matter's content-keyed cache; stringify appends a trailing newline
- LESSON-0006 · use when returning a constant catalog/array of objects to callers or building a plain-object dictionary keyed by user-supplied strings in JS/TS · spread and Object.freeze are shallow; use per-entry copies and Object.create(null) dictionaries, verified by adversarial tests
- LESSON-0007 · use when adding ARIA roles or labels to non-semantic elements in a React project linted by Biome's a11y rules · no aria-label on bare spans, no role=group on divs, interactive roles need tabIndex, nav is not a tablist
- LESSON-0008 · use when writing a regex/marker parser (status fields, delimited lists) over markdown or freeform text · parse markers line-anchored to structural positions, never first-substring-anywhere; strip nested content before splitting on delimiters
- LESSON-0009 · use when selecting the most-recent ISO 8601 timestamp among strings that may come from more than one producer or offset · lexicographic comparison is only safe when every producer stamps the same offset; otherwise compare via Date.parse
- LESSON-0021 · use when a whole-project gate that asserts over all routes/units goes red because one unit is legitimately BLOCKED on an owner action · a shared gate over N units must quarantine a needs-owner-blocked node instead of red-locking unrelated work
- LESSON-0022 · use when an engine or orchestrator is about to set a done/release phase transition whose definition of done includes distinct mandated stages · gate the advance on evidence that EACH mandated stage ran; per-unit passes are not the whole-program audit
- LESSON-0027 · use when about to assert a project/feature's current state by citing a prior audit/proposal finding, before executing a fix/change plan derived from one, or repeating a carried-forward background/context claim across recurring sweeps, rather than checking the current code · an audit finding (or a fix plan/narrative claim built on it) is a snapshot, not a durable fact; re-verify against the live artifact every time — for a multi-item plan, red-team each item against the live file before executing
- LESSON-0034 · use when you launched or are about to verify a dev server from inside a git worktree and need the preview tools to reflect that worktree's code · preview_start/launch.json resolve to the MAIN checkout, not the worktree; boot the test harness's own webServer, or run your own dev server on a free port and curl-verify before handing the URL to the owner
- LESSON-0040 · use when a Playwright e2e suite fails en masse and more than one project/dev-server may be running on the same machine · rule out a foreign process reusing the port, an orphaned same-project lock, a localhost-vs-127.0.0.1 allowedDevOrigins mismatch, AND a long-lived server's own content-cache corruption before trusting it as a real regression
- LESSON-0042 · use when producing portfolio/case-study screenshots of an internal tool that a public audience will see · default to the design-phase static mock (with a few extra sample rows for density) instead of a live-data screenshot, to avoid exposing sensitive data and to look fuller
- LESSON-0043 · use when you find stray uncommitted files in a shared main checkout that don't obviously belong to your current session · evaluate provenance yourself (diff, coherence, timing) and decide commit/discard autonomously; reserve escalation for genuinely ambiguous or destructive cases
- LESSON-0047 · use when a preview/screenshot verification tool returns an unexpected result (timeout, black/blank image, or stale content) and you are about to conclude the UI itself is broken · rule out known tooling false-signal modes first (animated canvas, worktree launch.json, Turbopack root, below-fold reveal, prefers-color-scheme default, hidden Browser pane) before trusting it as a real defect
- LESSON-0049 · use when about to reduce a UI screenshot's file size with a palette-reducing tool (pngquant, `magick -colors 256`) before shipping it · keep UI screenshots as full-colour PNG source; palette reduction bands gradients and pixelates text, and the serving pipeline re-optimizes on delivery anyway
- LESSON-0069 · use when about to base a diagnosis, a claim to the owner, or a "done"/"verified" declaration on a doc, a past finding, a recognized pattern, or a self-review · a secondary representation of reality can silently diverge from it; insert one direct tool-mediated read of the live artifact first, always
- LESSON-0078 · use when checking whether a gate/verify script passed and its output is piped through another command · `$?` after a bare pipe is the LAST command's status, so a red gate reads green; capture the gate's own exit code (`pipefail`/`PIPESTATUS[0]`/un-piped)
- LESSON-0090 · use when a directory is gitignored BY DESIGN yet holds state whose loss would be a real incident · gitignored-by-design is not backed-up-by-default; sweep it into an external backstop and verify a write/regeneration trigger actually fires, and never jot a provisional note from inside a worktree
- LESSON-0099 · use when scaffolding or auditing a project's vitest.config.ts exclude list on a project that uses git worktrees or DR-107 preserved-test archives · exclude `.pandacorp/**` explicitly — any gitignored, code-bearing subdirectory under it (preserved-tests, the build engine's own gate-worktree) gets globbed and cross-contaminates module resolution otherwise
- LESSON-0102 · use when a script that bootstraps a git worktree copies/rewrites a gitignored config that embeds absolute filesystem paths · renaming identifiers is cosmetic; embedded absolute paths must be RETARGETED to the new location or every consumer silently keeps operating on the OLD checkout under a NEW name
- LESSON-0103 · use when `git add <path>` refuses with "paths are ignored by one of your .gitignore files" for a path you know is already tracked · naming a tracked path explicitly still runs the ignore check (even when `git check-ignore` reports nothing); use `git add -u --`, then `-f --`, never weaken .gitignore
- LESSON-0105 · use when a legitimate git commit or grep/inspection command is unexpectedly blocked as a dangerous redirect · block-dangerous.sh scans the whole command string with no quoting awareness, so a bare `>` or protected-looking path in TEXT is enough; sidestep with `commit -F`, `grep -nF --`, or the Read/Write tools
- LESSON-0109 · use when a legitimate command, canary or delegation prompt is blocked/degraded by a safety mechanism and you are tempted to weaken the gate · any gate reasoning over surface text will false-positive on resembling content; change how the intent is EXPRESSED, never the gate, and file a BL when it recurs across unrelated call sites
- LESSON-0113 · use when reviewing or shipping anything whose contract depends on a stated trigger ("this regenerates on commit", "this MUST happen before release", "check X before stage N") · a documented mechanism (or its documented TIMING) is a proposal, not evidence it's wired/accurate; require the installed call site as `path:line`, the wiring in the same change, or a tracked BL id
- LESSON-0119 · use when creating or debugging a scheduled/cron routine that must run headless with no human present to click through permission prompts · pre-seed a broad, wildcard-anchored permission allowlist (never per-version/per-exact-command) before relying on unattended execution; an unexplained silent stall is a permission prompt, not a scheduling bug
- LESSON-0123 · use when writing or generating any user-facing copy and deciding what punctuation/style rules keep it from reading as AI-generated · ban the em dash/en dash as sentence-level separators across all copy (UI, MDX, blog, metadata, including automated-generator output); it is the single most reliable AI-prose tell
- LESSON-0125 · use when standing up a NEW use of git-worktree isolation, or debugging a symptom that only reproduces inside a worktree/when a parallel session is active · audit all eight guarantees up front (dispatch fidelity, gitignored-state backstop, daemon environment, git-common-dir root resolution, test-glob hygiene, the worktree-setup.sh hook, shared-checkout landing serialization, branch freshness vs origin/main) — none is automatic just because DR-096 is in use
- LESSON-0129 · use when drafting first-person case-study copy that needs to explain a technical decision, a tradeoff, or something the author would do differently · favor concrete, legible, confident prose over cleverness/jargon; exactly one honest "I'd do it differently" confession per case study, other decisions led by the win
- LESSON-0144 · use when designing or reviewing a phase-inference/adoption step that stamps a project's phase from what artifacts exist on disk · artifact PRESENCE is not artifact QUALITY; re-run that phase's own independent readiness/quality gate at the point of inference, never borrow a presence check from a different concern
- LESSON-0155 · use when deciding whether to reuse, reset or clean a run-state path that could hold residue from an interrupted or crashed prior process · registered AND clean → safe to reuse; anything dirty/orphaned/unregistered/ambiguous → preserve and fall back to a slower-but-safe path, never force-remove
- LESSON-0167 · use when a skill-authoring or prompt-distillation task needs real example cases and the natural instinct is to ask the owner to recall or supply them · mine factory/memory, factory/backlog and the decision logs for already-vetted grounding examples first; human recall is worse than grep
- LESSON-0179 · use when designing or reviewing an orchestrator skill/agent (including the factory's own build-engine framing) that delegates work to subagents · write it as a standing, sticky loop with a hard per-action "orchestrate vs work" gate; a single unguarded "you finish it" step licenses the expensive brain to silently take over delegated work
- LESSON-0147 · use when a build engine or skill is about to hand a runtime-synthesized subagent brief that will render on a live dashboard or feed an in-run retry · inline role, boundary, owned files and how-to-run directly in the dispatch prompt itself; a pointer-spec prompt ("read the work order") starves both the cold subagent and any live observability surface
- LESSON-0152 · use when a prompt asks a model to produce a section a deterministic parser/gate will then parse (a heading, a fenced block, a required key) · name the exact heading/schema literally in the prompt and keep at least one REAL (non-mocked) canary in the matrix — a fully-mocked suite always speaks the parser's dialect by construction and cannot catch a wording mismatch
- LESSON-0199 · use this before navigating to any authenticated service via the Claude in Chrome extension, or when list_connected_browsers/switch_browser can't find an expected profile · the extension connects PER CHROME PROFILE, not per browser; verify the logged-in identity on the page itself, never assume the sole connected browser/profile is the right one
- LESSON-0204 · use when about to write or finalize first-person copy that makes a claim about who the owner IS or DOES · never infer/invent a biographical or identity claim from case studies or prior drafts — interview the owner for it and source every claim in a gitignored dossier
- LESSON-0205 · use when drafting positioning copy for a developer whose current practice leans heavily on AI-assisted/orchestrated work · keep proven hand-coding capability AND current AI-orchestrated practice equally visible across every surface; dropping either pole distorts the positioning
- LESSON-0206 · use when finalizing AI-drafted first-person persona copy before it ships · run an explicit red-team against a fixed criteria list (traceability, privacy, positioning balance, native-language phrasing, per-item section coherence, scannability, coherence, and concrete AI-tell signals like missing contractions/impersonal aphorisms) before the content gate
- LESSON-0216 · use when closing out an AC that describes a specific visual/branded asset (favicon, logo, icon) and the file already exists at the expected path · file presence is not content correctness; open/render the actual asset and compare it against what the AC describes before declaring it done
- LESSON-0217 · use when a public site's contact/footer section needs to surface a personal email or phone number · never render it as plain text in server-rendered HTML — it is a standing scraping target; use a contact form or a client-only/obfuscated reveal instead
- LESSON-0221 · use when drafting or reviewing case-study decision titles, a metrics/glance bar, or any numeric claim for a portfolio page · titles self-explanatory + under ~9 words, metrics bar holds impact only (never trivia), plus-before-number formatting, and every number sourced — never guessed
- LESSON-0223 · use when writing career/CV/LinkedIn copy and a fact can't be confirmed from the locally-cloned repos, or before labeling a capability with a buzzword like "agents" · mine internal docs/employer repo git history before marking a gap, trust the owner's word for skills absent from local repos, prefer LinkedIn on source contradictions, and grep for the defining code mechanism before using a loaded technical term
- LESSON-0224 · use when drafting or reviewing CV/LinkedIn career copy (bullets, company descriptions, job titles, bilingual EN/ES) · localize bilingual copy (never translate literally), frame credit as accountability not sole ownership, plain English over jargon, present tense for active companies, internally coherent bullets, and "Formal title (Lead Engineer)" not "(Tech Lead)" for a de facto lead role
- LESSON-0225 · use when preparing a case-study/portfolio page's OG image or title/description for LinkedIn Featured (or any social-preview card) · render the page's own distinguishing visual (not one shared generic template) into the OG image, and budget title copy to ~26 chars/one line and description to ~4 lines
- LESSON-0240 · use when an owner correction is the second (or later) instance of the same class of defect within a session/project · promote the fix to a standing documented policy (style guide, decision log, generator rewrite) instead of another point-fix — that is this owner's default expectation for recurring corrections
- LESSON-0246 · use when evaluating whether an assisted/automated workflow is succeeding, or prioritizing what to fix in one, and a faster/cheaper manual escape hatch exists alongside it · check ADOPTION first — a user routing around a slow/expensive assisted path is load-bearing evidence on par with an internal cost/time benchmark, even when every internal quality metric looks fine

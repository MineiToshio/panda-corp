# Decision log — Plugin pandacorp

Decisions about the plugin: skills, agents, hooks, templates and the factory flow. Most recent on top. See the index and format in [DECISION-LOG.md](../../DECISION-LOG.md).

> Reminder: after editing `plugin/`, commit and run `claude plugin update pandacorp@panda-corp` (see `CLAUDE.md`).

## 2026-07-03 — v9.56.0: `progress.md` writer contract — milestones, not gate-tool noise (BL-0014)

**What:** defined and documented the writer contract for `.pandacorp/comms/progress.md` — the owner-facing
milestone feed Mission Control's Summary tab renders (`readActivityLog`). It records what *advanced* (an
FRD/WO built or changed, a decision taken, an item blocked needing the owner) and explicitly BANS raw
gate-tool exit lines (`**Biome:** limpio. Exit 0.`, `**tsc:**`, `**knip:**`, `**madge:**`, `**Vitest:**`,
`**Smoke (Playwright):**`, `**Visual (Playwright):**` — the `verify.sh` recap shape). Landed in three places:
(1) `factory/standards/build-orchestration.md` §7 ("The build supervisor") — the canonical statement of the
contract, with the banned-pattern list and the required milestone shape; (2) `plugin/skills/implement/SKILL.md`
("Real-time documentation") — the writer-facing instruction for the engine/supervisor; (3)
`plugin/agents/reviewer.md` (verdict-per-FRD step) — the reviewer no longer transcribes the tool checklist into
`progress.md`, only a one-line milestone. Also corrected a stale claim in Mission Control's Manual
(`content/manual/concepts/estado-y-archivos.md`) that said MC doesn't read `progress.md` — it does, via
`readActivityLog`; the page now states the milestone contract. Proven with a noise-canary grep
(`^- \*\*(Biome|tsc|knip|madge|Vitest|Smoke|Visual)\b.*Exit`) that matches the old-style fixture (RED) and
does not match a fixture written under the new contract (GREEN), plus a milestone-positive check (an FRD id /
decision reference is present). **Out of scope:** Mission Control's own `readActivityLog` rendering/filtering
(a separate MC change if any residual noise ever slips through) — the fix is entirely on the writer side.
**Why:** the owner-facing "what happened" surface was reading like a CI log (evidence:
`mission-control/.pandacorp/comms/progress.md:24-30` during a real build) instead of a progress narrative,
burying the signal (decisions, blocks) the owner actually needs. MINOR (changed skill/agent behavior, DR-034),
bumped on top of the same-day v9.55.0 (below) since both branched from the same base.
See `factory/decision-log.md`.

## 2026-07-03 — v9.55.0: work orders now carry a required `## Summary` (BL-0015)

**What:** the `work-orders` skill (`plugin/skills/work-orders/SKILL.md`, step 2) now explicitly
requires a **filled `## Summary`** in every generated work order — one/two lines of "what this WO
delivers," not the template placeholder — pointing out that Mission Control's board
(`deriveWorkOrderSummary()`) reads exactly this section for the WO card, and falls back to the raw
file path when it's missing. `iterate`'s impact-triage steps (both the "small adjustment/fix" reopen
path and the "new feature/module" path) now call out keeping `## Summary` current when a WO is
reopened, created, or widened. `plugin/templates/docs/work-order-template.md`'s `## Summary` section
gained a callout explaining it is MC's read target, not filler. **Language decision: EN-doc route
(minimal), not the ES-comms replica.** The item's recommended alternative — a Spanish summary replica
under `.pandacorp/comms/` read by a new MC reader — would require a Mission Control source change
(a new comms reader wired into the WO summary field), which is out of scope for a plugin-only backlog
item; the committed WO doc is already English per the language rule (DR-009), and MC's existing
`deriveWorkOrderSummary()` already renders it with zero MC changes. **Follow-on filed, not built
here:** if the owner wants the Spanish-replica UX for WO summaries, it needs its own
`/pandacorp:change` inside `mission-control/` (new comms reader + wiring into the `summary` field) —
not done in this change. **Backfill: fix-forward, not applied.** ~118 existing work orders across
`mission-control/docs/frds/**/work-orders/` (the project that surfaced this bug) lack `## Summary`
today; backfilling them is a separate, larger undertaking (arguably MC's own project-level change) and
is explicitly optional per the backlog item — newly generated/edited WOs get it going forward.
**Proof:** MC's `deriveWorkOrderSummary()` regex was replicated verbatim against the updated WO
template — parses a real, non-placeholder paragraph (GREEN) and still returns `undefined` for a body
with no `## Summary` heading (RED control, the pre-fix state); MC's own existing reader unit tests
(`mission-control/src/lib/work-orders/_tests/work-orders.test.ts`, WITH/WITHOUT `## Summary` cases)
were read and confirmed to already cover this — untouched, since no MC source changed. MINOR, bumped
on top of the same-day v9.54.0 (below) since both branched from the same base. See
`factory/backlog/BL-0015-work-orders-lack-summary-section-mc-shows-path.md`.

## 2026-07-03 — v9.54.0: `/pandacorp:memory` invocation ergonomics (BL-0010)

**What:** two small UX fixes to `plugin/skills/memory/SKILL.md`, owner-raised (BL-0010). (1) **In-project
harvest default:** `harvest` step 1 now resolves the target as explicit `<project>` arg wins → else, if the
cwd is itself a Pandacorp project (`is-pandacorp-project.sh` against `.`), default to **the current
project** → else (running from the factory root), offer recent/just-shipped projects from
`factory/portfolio.md` as before. The factory-run sweep, an explicit `harvest <project>` from anywhere, and
the `/loop` portfolio sweep (always explicit targets) are unchanged — only the no-argument, in-project case
gained a default. (2) **Bare invocation no longer auto-harvests:** the `$ARGUMENTS` line now reads `status`
(default, read-only) · `harvest [<project>]` · `review` — a mode-less `/pandacorp:memory` lands on `status`
(step 11, already read-only) instead of silently firing `harvest` (a write). **Why:** harvest is the action
that WRITES; the owner found it one accidental keystroke away from a bare invocation, and the natural
in-project gesture ("harvest THIS project") required naming the project explicitly. MINOR (skill-invocation
behavior change), bumped on top of the same-day v9.53.0 (below) since both branched from the same base. BL-0010 closed. See `factory/backlog/BL-0010-memory-skill-invocation-ergonomics.md`.

## 2026-07-03 — v9.53.0: new skill `/pandacorp:implement-backlog` — works and closes `factory/backlog/` items

**What:** a new user-invocable skill, `plugin/skills/implement-backlog/SKILL.md`. It is the mechanism the backlog's own read-only note already referenced ("pídeselo a un agente, p. ej. «implementa BL-0007»") and the one Mission Control's Backlog tab now links to (see the same-day mission-control entry). Two modes: **one id** (`/pandacorp:implement-backlog BL-0007`) implements just that item directly, in the running conversation; **no argument** drains the WHOLE `open`/`doing` queue by dispatching **one subagent per item** (`Agent` tool, `isolation: "worktree"`, all in one message so they run in parallel), each subagent's model **sized to that item's complexity per CONV-12/DR-111** (never inherited from the parent conversation's tier — haiku for a one-line/mechanical fix, sonnet the default floor, opus for architectural/high-judgment items, Fable never automatic). Once every subagent returns, the orchestrating agent merges their branches into `main` **serialized, one at a time** (never in parallel — that avoids the exact collision the serialization exists to prevent), re-running `bash plugin/scripts/validate-backlog.sh` after each merge to catch an id/frontmatter collision immediately. Closing an item follows the item's own template contract: `status: done` + `closed:` + `closes:`, a `plugin/` semver bump + `claude plugin validate` when `plugin/` changed, a `factory/decision-log.md` entry when a standard/registry/constitution changed. `factory/backlog/` has no filing skill on the change-owner side (unlike a project's `/pandacorp:change`) — items are already filed by the librarian's routing, an audit, or hand-authored; this skill only works and closes them. **Why:** the owner asked how backlog items actually get implemented, confirmed there was no skill for it, and — after weighing "just do it directly" vs. a skill — chose the skill for the same reason CONV-11/CONV-12 exist: a mechanical rule enforced every time (the close-out checklist, the model-tier calculation) beats prose the agent might forget on a given pass, especially since a future agent with zero context from this conversation may run it. MINOR bump (new capability, no behavior change to existing skills). No OVERLAY_VERSION change — this skill is factory-internal (operates on `factory/backlog/`, which only exists in this repo), not injected into product projects.

## 2026-07-03 — v9.52.2: `/pandacorp:upgrade` becomes user-invocable

**What:** flipped `plugin/skills/upgrade/SKILL.md` frontmatter from `user-invocable: false` to `true`, so `/pandacorp:upgrade` now appears in the owner's `/pandacorp` autocomplete instead of only running as an internal engine (triggered by another skill's preflight, DR-048). Behavior is unchanged — same active-build guard, same non-destructive regeneration of the overlay — only its reachability changes. Updated the root `CLAUDE.md` "Internal engines" paragraph to drop `:upgrade` from the hidden list and note it is now directly runnable. **Why:** the owner wants to force a re-sync of a project's `.pandacorp/` overlay on demand, without waiting for another skill's preflight to notice drift. PATCH (visibility/access change, no new capability or behavior).

## 2026-07-02 — v9.52.0: subagent model-tier selection rule (CONV-12, DR-111)

**What:** a new propagated rule governing how the LEAD agent picks a subagent's model when it delegates WITHOUT specifying one — sibling to the language rule (CONV-1/DR-009) and the interaction-style rule (CONV-11/DR-110), same DR-051 propagation channel. Scoped to ad-hoc delegation only (the generic `Agent` tool, or a `Workflow`'s `agent()` outside the build engine): calculate the model tier from the SUBTASK's complexity (haiku/mechanical, sonnet/default floor, opus/high-complexity-judgment) instead of silently inheriting the parent conversation's tier. Explicitly does NOT reopen Pandacorp's own named agents (`plugin/agents/*.md` already pin `model:` in frontmatter) nor the build engine's own escalation (`pickWorkerModel`, DR-073/DR-108) — both already correct. **Fable is excluded from the automatic calculation entirely** — never auto-selected, only used on the owner's explicit request or after asking for confirmation first (owner correction during planning). Added to `factory/standards/conventions.md` ("Rule — subagent model selection", SHOULD/manual) and `factory/standards/rule-registry.md` (CONV-12 row, counts bumped to 117/86 manual). Concise pointers duplicated in `plugin/templates/shared/AGENTS.md.tpl` and `plugin/templates/shared/.pandacorp/guide.md.tpl` (new rule 11) — both regenerated non-destructively by `/pandacorp:upgrade`. Also added directly to panda-corp's own root `CLAUDE.md` (rule 11) for immediate effect in factory sessions. Registered as `DR-111`. **Why:** the owner reported deep-research-style fan-outs (and other ad-hoc delegations) burning the token budget by running every subagent at the parent session's own (sometimes Opus/Fable) tier, with no complexity-based calculation. MINOR + OVERLAY 8.57.0 → 8.58.0. See `factory/decision-log.md`.

## 2026-07-02 — v9.51.0: owner-facing interaction-style rule (CONV-11, DR-110)

**What:** a new propagated rule governing how the LEAD agent replies to the owner in chat (factory and every product project) — sibling to the existing language rule (CONV-1/DR-009), same propagation channel (DR-051). Added to `factory/standards/conventions.md` ("Rule — interaction style", SHOULD/manual) and `factory/standards/rule-registry.md` (CONV-11 row). Concise pointers duplicated in `plugin/templates/shared/AGENTS.md.tpl` (new paragraph under "Language & interaction") and `plugin/templates/shared/.pandacorp/guide.md.tpl` (new rule 10) — both regenerated non-destructively by `/pandacorp:upgrade`. Also added directly to panda-corp's own root `CLAUDE.md` (rule 10) so the factory's own sessions pick it up immediately, without waiting on an upgrade run. **Deliberately NOT mirrored into `plugin/templates/rules/code-conventions.md`**: that file is read by coding subagents (backend-dev, frontend-dev, reviewer, etc.) via `docs/rules/`, and this rule's scope is explicitly the owner↔lead-agent channel only — mirroring it there would leak an owner-chat rule into subagent-facing docs, which precedent (CONV-1's interaction bullet is likewise absent from that file) already avoids. Registered as `DR-110`. **Why:** owner feedback (2026-07-02) — conversations blended progress narration with the real answer, replies defaulted too verbose/technical, and proposals were rubber-stamped instead of red-teamed. MINOR + OVERLAY 8.56.0 → 8.57.0. See `factory/decision-log.md`.

## 2026-07-02 — v9.52.1: scheduled routines get a canonical, versioned definition in the repo

**What:** NEW `plugin/docs/routines.md` — the canonical definition (taskId, cron, description, full
prompt, recreate instructions) of the two machine-local scheduled routines: `pandacorp-memory-review`
(the DAILY loop-v2 sweep: harvest per project → review → status, with the >=20-notes / >=7-days /
harvest-orphan thresholds) and `pandacorp-review-launch` (weekly DR-043). The installed copy at
`~/.claude/scheduled-tasks/` is a DEPLOYMENT of this file; on divergence, this file wins.
`skills/memory/SKILL.md` (cadence trigger 2) and DR-047's nota now point at it. **Why:** the owner
caught the gap (2026-07-02): the routines lived ONLY on the machine — nothing in the repo said what
the sweep must do or how to recreate it, so a lost machine (or a reader of the repo) lost the loop's
motor; he also read the routine's pre-v2 weekly prompt (review-only) and rightly flagged that a
sweep without per-project harvest never grows the store. The v2 prompt already harvests per project;
now the repo proves it. PATCH.

## 2026-07-02 — v9.50.0: standards catalog upgrade — new gates, pruned rules, new rule files, stacks B/C real (proposal 24)

**What:** the plugin side of the standards-catalog upgrade (docs/proposals/24, approved by the owner). (1) **New fail-closed gates in stack A** (`verify.sh` + canaried in `canary.sh`): data-layer isolation (ORM outside `src/queries/` = RED), API error contract (4xx/5xx without `problem()` = RED), and the **Header-Scan Gate** (`e2e/headers.spec.ts`, NEW — the check `web-security.md` promised but never shipped), tiered by `deploy_target` read via `_target.ts`'s new `readDeployTarget()` (external=hard / internal=advisory, the DR-074 pattern); canonical helper snippets (`problem()`, `safeFetch`, rate-limit middleware, `headers()` config) added to `STACK.md`. (2) **Rule library pruned for context budget** (Anthropic guidance: bloated always-loaded rules degrade compliance): the 5 `always` files went ~236 → 169 body lines by deleting everything Biome/tsc/verify.sh already enforce (one pointer line per file); no judgment rule lost. (3) **Six new rule files** (`api-design`, `error-handling`, `resilience`, `ai-implementation` [always], `data-modeling` [prisma], `auth` [better-auth]) + react/nextjs enriched with caching/forms/Suspense discipline — operative forms of the new canonical standards. (4) **Stacks B (Hono) and C (FastAPI, absorbing D) are real golden-path guides** with an honest "Pending template machinery" checklist; `stack-d-scraper/STACK.md` is now a pointer to C. (5) **Skills/agents wired to the named checks**: `architecture` install step applies the SEC-4 release cooldown + installs `headers.spec.ts`; `security-auditor` gained the CSP-enforce ratchet, safeFetch and PII-in-logs named checks; `upgrade` conformance set is now SEVEN e2e files + `_target.ts` additive back-fill + **the post-resync canary is BLOCKING**; `sync-portfolio` gained the overlay-drift watch (quiet projects surface). **Why:** the audit found ~18 MUSTs with no mechanism and an over-budget always-loaded layer; this closes both. MINOR + OVERLAY 8.55.4 → **8.56.0**. Evidence/context: `docs/proposals/24-standards-catalog-audit-2026-07-02.md`, `factory/standards/rule-registry.md`.

## 2026-07-02 — v9.49.1: capture is in-the-moment; the Stop hook is a context RE-SCAN backstop, never a memory quiz

**What:** owner feedback on v9.49.0's capture hook ("no me gusta que pregunte al final si me
acuerdo — la conversación pasa en tiempos distintos y no me voy a acordar; el agente debe capturar
en el momento en que le voy diciendo las cosas"). Two changes: (1) rule 8 (factory `CLAUDE.md` +
project `guide.md.tpl`, OVERLAY 8.55.3 → 8.55.4) now mandates capture **IN THE SAME TURN** the
event happens (an owner correction, a fix-after-failure, a library verdict, a gotcha) — deferring to
session end is explicitly forbidden ("deferred capture is lost capture"); (2)
`capture-lessons-reminder.sh` is reframed as the BACKSTOP: when it fires (once per session, only if
the inbox has no recent notes) it instructs the agent to **re-scan the full conversation in its
context, turn by turn** — a read-back of what slipped, never "try to remember" and never a question
to the owner. **Why:** the owner's memory must never be a dependency of the loop; the agent's
context holds the whole conversation, so the honest mechanism is same-turn capture + a mechanical
re-scan net. PATCH.

## 2026-07-02 — v9.49.0: self-learning loop v2 — the loop turns by itself, retrieval is measured, lessons are validated (proposal 23)

**What:** the full loop-v2 plan (owner-approved, `docs/proposals/23-self-learning-loop-v2.md`) lands in one
release. **F1 the motor:** `implement`'s close-out harvest is now a REQUIRED 3-sub-step (harvest →
`count-lesson-citations.sh` → `last_harvest:` stamp in `status.yaml`; no stamp = incomplete close-out); the
`pandacorp-memory-review` scheduled task became a DAILY threshold sweep (works only when ≥20 pending notes /
≥7 days / an un-harvested `release` project — silent otherwise; marker `factory/memory/_last-sweep`,
gitignored); NEW Stop hook `capture-lessons-reminder.sh` (once per session, Pandacorp contexts only, ≥6 user
turns) forces the did-anything-durable-happen ask before finishing. **F2 measured retrieval:** NEW
`factory/memory/INDEX.md` (one line per active lesson + `trigger:` "use this when …"; librarian delta-edits
only — ACE), `trigger:` required by `validate-memory.sh` and backfilled on all 22 lessons; all 7 builder
agents + `guide.md.tpl` switch from "Grep the store (and hand-edit counters)" to "read INDEX first + CITE
`LESSON-NNNN` in the durable artifact"; NEW `count-lesson-citations.sh` greps citations per project and
updates `times_applied`/`applied_in` deterministically (tested: idempotent, validator-clean). **F3
validity:** librarian gets DEFAULT-REJECT (no evidence anchor → discard) + failure-lessons-by-contrast;
`candidate → active` now requires CROSS-corroboration (a different build, or owner/CI provenance); prune's
"never retrieved" criterion FROZEN until ≥3 projects measured. **F4 the ladder:** ≥3 citing projects
auto-queue `promotion: proposed` (the escalator); `learn`'s gate made explicitly tiered (SHOULD + deterministic
verifier auto-applies with notification; MUST/DR/skill always owner); reflection pass (≥3 same-domain lessons →
one synthesized pattern, never reflections-on-reflections). FRD-17's close (Propuestas + memory-health) filed
in Mission Control's change queue. **Why:** two consecutive audits (2026-06-22, 2026-07-01) found the loop
"prescribed but not wired" — all 22 lessons at `times_applied: 0`, zero scheduled jobs, close-out harvest
prose-only; the owner's pain ("I must run /memory and /learn by hand") was the missing motor. Grounded in
Letta sleep-time compute, Mem0, Devin suggested-knowledge, EDV (10% polluted memories measurably hurt),
ReasoningBank, ACE, Voyager. **Impact:** `skills/{memory,learn,implement}/SKILL.md`, `agents/librarian.md` +
7 builder agents, `scripts/{count-lesson-citations.sh,capture-lessons-reminder.sh,validate-memory.sh}`,
`hooks/hooks.json`, `templates/shared/.pandacorp/guide.md.tpl` (OVERLAY 8.55.2 → 8.55.3),
`factory/memory/{README.md,_lesson-template.md,INDEX.md,LESSON-*}`, `factory/decisions/registry.yaml`
(DR-047 rewritten), MC Manual `concepts/autoaprendizaje.md`, scheduled task updated (machine-local). MINOR.

## 2026-07-02 — v9.48.0: merge-queue keeps the worktree — removal belongs to ExitWorktree (BL-0023)

**What:** `templates/shared/.pandacorp/merge-queue.sh` no longer deletes the worktree/branch after a
successful merge — the invoking Claude session STANDS inside that directory, and deleting a live
session's cwd dangles it (next message dies with "Path … does not exist"; every background task is
killed with the forced restart — owner-reported, recurring). New contract printed by the script:
call `ExitWorktree(action: remove)` (harness-level — cleans AND restores the session cwd), outside
`git worktree remove` as fallback. `pending-work.sh` recalibrated: merged+clean survivors are "♻
removable" session shells, never pending (no false "⎇ pendientes"). Docs re-worded: DR-096 registry
default, build-orchestration.md "Parallel manual sessions", guide.md.tpl. MINOR (contract change of
a shared tooling script).

**Why:** removal was owned by the wrong layer — only the harness knows the session's cwd binding.
Full analysis: `factory/backlog/BL-0023-merge-queue-deletes-live-session-cwd.md`.

## 2026-07-02 — v9.47.0: e2e template gains the `server-env.json` deterministic-data hook

**What:** `templates/stack-a-nextjs/e2e/playwright.config.ts` now injects an OPTIONAL project-local
`e2e/server-env.json` into the webServer environment (relative values resolve against the project
root; a `PORT` entry moves the e2e server to its own port; when the file exists the server is never
reused). MINOR: new compatible capability — projects can pin their data sources (fixture roots,
frozen event files) so live data never moves gate pixels. First consumer: Mission Control
(`mc-e2e-fixture-factory-root`, after the 2026-07-02 incident where a completed build's earned
XP/level REDDED an unrelated merge's visual gate). Without the file the config behaves exactly as
before.

**Why:** Visual baselines coupled to live data drift with every real build — the gate must compare
against frozen inputs so any RED is code. The indirection keeps the canonical config verbatim
(DR-059/DR-076) while the per-project data pinning lives in a never-overwritten sidecar.

## 2026-07-01 — Build engine: literal NUL bytes in globToRe made the Workflow tool unlaunchable · v9.46.2 · overlay 8.55.2

**What:** `templates/shared/.claude/workflows/pandacorp-build.js` used two **literal NUL bytes**
(`\x00`) as the sentinel in its glob→regex helper (`globToRe`: `**` → NUL → `.*`). Claude Code's
Workflow permission dialog rejects any script containing hidden control characters, so EVERY
`/pandacorp:implement` launch failed with "script contains control characters" (hit on Mission
Control, 2026-07-01). Replaced the raw bytes with the 6-char escape sequence `\u0000` — byte-clean
source, identical runtime semantics (verified: `**` still crosses `/`, single `*` doesn't).

**Why:** Fix-forward at the source (DR-076): the engine template must be launchable through the
approval dialog; a raw control byte in committed source is also invisible to review. Overlay bumped
so every project's `.claude/workflows/pandacorp-build.js` regenerates on next upgrade.

## 2026-07-01 — Back-port: "Forbidden pattern — docs/proposals/" section into the documentation rule template · v9.46.1 · overlay 8.55.1

**What:** `templates/rules/documentation-and-decisions.md` gains the "Forbidden pattern —
`docs/proposals/` in a project" section (a pending change always goes to `.pandacorp/inbox/changes/`;
a project-level `docs/proposals/` file is invisible to the build engine and never gets picked up).
Overlay bumped 8.55.0 → 8.55.1 so the rule re-syncs to every project on its next upgrade (DR-051).

**Why:** DR-094's loud back-port detector fired during Mission Control's overlay upgrade (8.51.1 →
8.55.1): MC's copy carried this section in-project (commit a497af0) while the template lacked it — the
verbatim conformance overwrite would have silently clobbered it. The content is generic to all
projects, so per DR-076 the fix belongs at the source (the template), not in-place.

## 2026-07-01 — Granularity calibration from real-build evidence: DR-100 floor+ceiling with numbers, artifacts completeness, mandatory Build Plan table, fix-forward mandate, track.jsonl gap fixes · v9.46.0

**What:** The owner-approved Fase-3 of the granularity audit (two mining passes over the
personal-page-v2 + Mission Control builds; findings quoted in the amended docs). Five levers:
(1) **DR-100 calibrated bounds** — target ~25–50 min / ~1.5–4k LOC per WO; ceiling ~4k LOC (MC WOs
>6k LOC averaged 3.0 gate rejects vs 0.8 below 3k; the 7k-LOC one took 5 gate attempts / 20h); floor
~20 min (the gate costs a FIXED ~9 min, so ppv2's FRD-08 paid 127% overhead) — in
`build-orchestration.md` §2, DR-100, `architect.md` (rule 7 + readiness gate check 9),
`product-manager.md` (FRD floor: a tiny feature merges into the one it extends; built product → the
change queue). (2) **Artifacts completeness** — readiness gate check 5b: each WO's `artifacts` globs
must cover EVERY file it will write, not merely be disjoint (an undeclared output is invisible to
DR-060 and can collide ACROSS FRDs under global waves); also DR-100 (3b). (3) **Mandatory Build Plan
DAG table** in `blueprint-template.md` (WO · deps · artifacts · foundation · parallel-with) — the
planner parses structure, not free prose. (4) **Fix-forward mandate (DR-073)** — a bounded fault
nameable at file:line with a ≤~30-line fix MUST take the findings/patch exit, never a bare failure or
blocked_reason 'error' (80% of ppv2's first-gate fails had ≤6-min fixes; the revert path cost ~1.5h
of 2.2h rework) — in the gate prompt + DR-073. (5) **track.jsonl instrumentation** — the gate now
appends `review_end` with its verdict on EVERY exit (reopen/blocked/fail, not only pass — failed
reviews were invisible in the durable timeline); `verifyPatched` appends `review_end pass` + `frd_end`
(it is the certifying stamp of the patched path); `revertAndReopen` appends one `wo_reopen` line per
WO (reopen_count resets on pass — DR-072 C2 — so the timeline under-reported rework). Scheduler
harness re-run after the prompt edits: 4/4 fixtures green.

**Why:** calibrate the spec→architecture→implement chain with measured numbers instead of adjectives,
so the autonomous build converges faster without weakening any gate. The floor exists for gate
economics only — with global waves (BL-0021) small FRDs no longer cost parallelism.

**Impact:** `factory/standards/build-orchestration.md`, `factory/decisions/registry.yaml`
(DR-100, DR-073), `plugin/agents/{architect,product-manager}.md`,
`plugin/templates/docs/blueprint-template.md`, `plugin/templates/shared/.claude/workflows/
pandacorp-build.js` (+ MC overlay re-sync), Manual `el-pipeline.md`. Validation: next real build's
track.jsonl vs the ppv2 baseline (first-pass rate, rework share, review_end coverage).

## 2026-07-01 — Engine: GLOBAL WAVE scheduler — cross-FRD parallel builds, gates serialized at wave boundaries (BL-0021) · v9.45.0

**What:** The build engine's main loop is no longer a sequential `for (const f of plan.frds)`. Each
wave is now picked from the READY work orders of **all** FRDs — `dependsOn` satisfied (dep committed →
`IN_REVIEW`/`VERIFIED`; a dep outside the schedule counts as satisfied, mirroring the old intra-FRD
rule), artifacts disjoint per DR-060 **checked across the whole wave** (cross-FRD overlaps serialize),
capped at the mode's wave size and the remaining agent budget (DR-070). The per-FRD gates queue when a
feature's build WOs are all committed and run **serialized at wave boundaries** — waves are synchronous
barriers, so a gate's whole-project checks always see a QUIET tree. Every prior invariant preserved:
foundation-first waves + completeness gate (DR-057/065), Option B single serialized commit writer,
DR-069 safe-points (now at every wave/gate boundary — same cadence), budget/agents/maxFrds brakes,
blocks health breaker, the full DR-072/073/107 + BL-0001 convergence flow (extracted verbatim into
`gateAndConverge`), and resume semantics (an all-IN_REVIEW FRD gates first; drained-change FRDs enroll
into the RUNNING schedule).

**Why:** Owner-confirmed 2026-07-01 (BL-0021): after personal-page-v2's foundation verified, SIX
independent 1-WO FRDs were simultaneously ready and the engine built them single-file — ~4.5h of
queue in `powerful` mode (wave 8). DR-100 right-sizing produces small FRDs, which had turned the
intra-FRD wave into a no-op: the mode's parallelism was theoretical. Estimated saving on that run's
mid-section: ~4.5h → ~2h.

**Verification:** simulation harness (stubbed agent/parallel runtime) over 4 fixtures — `parallel`
(ppv2 shape: wave 2 carries 6 WOs of 6 FRDs concurrently, 7 gates serialized, 0 gates with builds in
flight), `overlap` (cross-FRD artifact collision → different waves), `resume` (all-IN_REVIEW FRD gates
first), `reopen` (gate reject → patch → independent verify → dependent WO still builds). All GREEN.

**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (scheduler rework),
`plugin/skills/implement/SKILL.md`, `factory/standards/build-orchestration.md` (§2 global waves),
`factory/decisions/registry.yaml` (DR-050 amended), Mission Control overlay copy re-synced +
`content/manual/concepts/construccion-desatendida.md` (Paralelismo — oleadas globales), BL-0021 → done.

## 2026-07-01 — Engine: per-WO `wo_commit` Party event at the green commit · v9.44.0

**What:** `commitWOGreen` (the serialized mech git writer) now also appends a `wo_commit` line
(`{event, at, project, frd, wo, state:"IN_REVIEW"}`) to `~/.claude/dashboard-events.ndjson`, next to
the `wo_end` it already writes to `.pandacorp/track.jsonl`. Completes the Party producer contract
(BL-0020 added `gate` + `achievement` in v9.41.0): every WO **state transition** now lands one
dashboard event. Mission Control's own overlay copy (`mission-control/.claude/workflows/`) re-synced
from the template (it was ~260 lines behind — pre-BL-0020, so a MC build would have emitted none of
the Party events).

**Why:** Mission Control's Party (FRD-06, reworked today) derives the scene STRUCTURE from the
work-order frontmatter and uses a fresher-event signal to refresh that read mid-session
(`router.refresh`). The IN_REVIEW stamp alone appends no event, so a WO finishing while the owner
watched never walked forge→tribunal until a manual reload — the "muñequitos no se mueven" half of the
owner's Party audit. Fire-and-forget printf, same pattern as the BL-0020 emitters.

**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (WO_COMMIT_EVENT +
commitWOGreen), `mission-control/.claude/workflows/pandacorp-build.js` (overlay re-sync). Consumer
side documented in Mission Control FRD-06/FRD-01 + its `docs/decision-log.md` (same change).

## 2026-07-01 — `design` skill: canvas loop driven by a stateful procedure — relay automation, on-disk tracker, canvas journal, closing sweep, engine routing (DR-109) · v9.43.0

**What:** Phases A+B+D of the design-skill audit (`docs/proposals/22-design-skill-audit-2026-07-01.md`).
**NEW** `plugin/skills/design/references/canvas-procedure.md` — the numbered, stateful algorithm that now
drives the whole EXPLORE+Claude-Design path (the ~700-word Step-0 mega-paragraph is condensed to a summary +
pointer): outbound prompts by the best transport (claude-in-chrome browser → `pbcopy` clipboard, one Cmd+V
per round → manual) with every prompt persisted to `docs/design/canvas/prompts/`; **inbound by POLLING**
`DesignSync list_files` until stabilization (never ask the owner "¿ya generó?"); an on-disk
`docs/design/canvas/tracker.md` (system + every `ui:true` screen enumerated up front + the closing sweep)
that the **Step-9 advance gate now checks** — a skipped screen is structurally impossible on any model
(Sonnet-safe: state in files, not attention); per-round journaling (`canvas/log.md` + `iteration.md`,
DR-032 extended to external rounds); a **closing bidirectional component-reconciliation sweep** before the
manifest bridges to `components.md`; and the **engine routing table** in Step 0 (Claude Design default on
greenfield EXPLORE; HTML fallback; extract on ADOPT-VISUAL; in-repo on brownfield/post-freeze). **Why:** on
personal-page-v2 the owner was the message bus in both directions, screens were skipped until asked
page-by-page, components used by pages never entered the system gallery, and the canvas rounds left no
journal trace ("1 clean round" over a many-round reality). **Impact:** `plugin/skills/design/SKILL.md`
(Step 0 routing + procedure pointer, Rules condensed, Step-9 tracker check), **NEW**
`plugin/skills/design/references/canvas-procedure.md`, `factory/standards/design.md` (§1c relay/tracker/
sweep/routing, §11, §12, §13), `factory/decisions/registry.yaml` (+DR-109), MC Manual `el-pipeline`
narrative. No rule-library/OVERLAY change (factory-internal design guidance, like DR-058/DR-101). Phase C
(claude-in-chrome autopilot spike) runs next in an interactive session; phase E validation rides the next
UI project (once Opus, once Sonnet). Plugin v9.42.0→**v9.43.0** (MINOR). Activation: commit +
`claude plugin update pandacorp@panda-corp` + restart.

## 2026-07-01 — `/discover` v2: verified-source playbook + rotating lenses + two-phase funnel (DR-039 reworked) · v9.42.0

**What:** Full rework of `plugin/skills/discover/SKILL.md` after the owner rejected/shrugged at every
recommendation to date. (1) **Sources:** new `plugin/skills/discover/sources.md` — a live access-tested
mining playbook (2026-07-01): G2/Trustpilot (403), X-search (402), Google Play (JS) and trend listicles
are OUT as generators; IN (all verified fetchable): Chrome Web Store 1★ reviews, Ask HN via the Algolia
API, public Canny boards (vote counts), AlternativeTo, Microns.io as the willingness-to-pay oracle,
Apple App Store reviews+charts, Google product forums, pain-point-SEO autocomplete gaps; Reddit only via
WebSearch with tested query mechanics. Includes the platform-hostility map (never WhatsApp/LinkedIn/
Spotify-API/YouTube-ads) and the verified Perú payments table (Polar/Paddle/Gumroad yes; Stripe/
ExtensionPay no). (2) **Lenses replace the A/B 50/50 streams:** app-enhancement (extensions from
mainstream-app complaints) · owner identities beyond the saturated collector niche · own-itch/QoL
(seeded by owner-pasted TikTok themes — the highest-signal source the agent can't read) · incumbent
challenger (AI-first rebuild of ONE workflow and/or 80/20 unbundling; structural wedge only); the
collector lens is DORMANT (board-saturated). (3) **Two-phase funnel:** Phase 1 = 8-12 cheap one-line
teasers (persisted to `factory/ideas/_drafts/discover-<date>.md`), the owner picks 2-3; Phase 2 =
deep-dive + the hard gates + memo-pitch cards ONLY for the picks; reactions to passed-over teasers are
distilled into profile attraction/rejection patterns (DR-053). Unattended runs stop at the sheet.
`researcher.md` gains rule 8 (playbook-only mining + teaser-vs-deep-dive depth scoping). Gates 3.0-3e
kept; 3a is now lens-aware (challenger fit = "can he reach the first 10 users") and 3d counts
platform-ToS hostility.
**Why:** Board evidence: every general-stream card was discarded as `profile_alignment: low` generic
B2B (the sources were B2B directories — two not even fetchable), and the profile stream had drained the
collector niche into duplicates ("es lo mismo que PandaTrack"). The June memo-pitch redesign fixed
presentation, not generation; and the owner's taste entered only AFTER the full spend. Diagnosis + live
source audit + extension/challenger research in `docs/proposals/21` (owner approved 2026-07-01).
**Impact:** `plugin/skills/discover/{SKILL.md,sources.md}`, `plugin/agents/researcher.md`,
`factory/decisions/registry.yaml` (DR-039 default reworked in place), `factory/profile.md` (attraction
patterns + lens state), Manual synced (DR-046): `content/manual/concepts/{tu-perfil,el-pipeline}.md`,
`src/lib/manual/skill-flows.ts` (discover flow), `src/app/manual/manualPages.tsx`. MINOR (behavior
change, compatible). Cross-ref: `factory/ideas/decision-log.md` (same date).

## 2026-07-01 — Implement-speed audit phases 3+4: builder context packs + cheap mechanical tier (DR-108) + Party events (BL-0020, BL-0002) · v9.41.0 (OVERLAY 8.54.0→8.55.0)

**What:** The remaining two phases of the owner-approved implement-speed plan. **Phase 3 (DR-108, engine):**
the planner returns per WO its `path` + `acText` (the verbatim EARS AC lines that WO owns) and `woCtx()`
injects them into every builder/test-writer prompt — one reader hands off to N builders (they got only a
one-line `wo.summary` before, re-read the same docs N times, and still missed AC scope the gate caught
late); the solo builder now runs its OWN self-test + Status Note via the shared `SELFTEST` contract (one
spawn per WO instead of two — the trust boundary is the FRD gate, never the self-test; split mode keeps a
separate closer); mechanical steps (`commit:*`, the new `dispatch:*`, `safe-point`, `sync-rollups`,
`archive-changes`, `notify-end`, `ensure-stopped`) run on the `MECH` tier (default haiku,
`args.mechModel` overrides). **Phase 4 (engine → Party):** the engine now emits the two event types
Mission Control's FRD-06 always consumed but never received (0 in a 13MB stream): `achievement` (one line
per WO set VERIFIED, from BOTH stampers — the FRD gate and `verifyPatched`) and `gate` (at gate open, with
top-level `frd`) — closes BL-0020's producer side; the MC-side trophy fallback (derive from `woStates`
when no events) is filed `ready` in MC's own queue (`mc-party-trophies-frontmatter-fallback.md`). BL-0002
closed: the ENGINE stamps `IN_PROGRESS` on the whole wave at dispatch (cheap tier), so the board never
shows "En progreso: 0" over a busy build (LESSON-0003 promoted, like LESSON-0002 in phase 2).
**Why:** phase-3 evidence — every WO paid two same-model spawns + a sonnet commit agent, and the
subagent-prompt audit showed builders constructing from a one-liner while ~15-25k input tokens per wave
went to redundant re-reads; phase-4 evidence — the Party consumed `achievement`/`gate` that no producer
ever wrote, which is exactly the "no conecta" the owner saw (the live snapshot was NOT stale).
**Impact:** DR-108 in the registry; BL-0002/BL-0019/BL-0020 closed; LESSON-0003 promoted;
`build-orchestration.md` §5+§6 + `implement` SKILL.md + Manual `el-pipeline` synced (DR-046). MINOR;
OVERLAY 8.54.0→8.55.0 (engine changed). Model policy unchanged where it matters: sonnet floor + opus
escalation for real work; haiku only executes scripts.

## 2026-07-01 — Implement-speed audit phases 1+2: scoped e2e gate (DR-106) + reject-cycle convergence budget (DR-107) · v9.40.0 (OVERLAY 8.53.0→8.54.0)

**What:** The owner-approved fix batch for "the build is too slow and it deletes whole work orders instead of
patching" (evidence: personal-page-v2, ~14h45m, 11 full serialized Playwright runs at the gates, WO-01-004
discarded twice — once 1,397 lines over a one-shot patch failure, once a 1-LINE fix over a trivial TS2345 in
the patch's OWN new test file). **Engine (`pandacorp-build.js`, OVERLAY-bumping):** `attemptPatch` gains a
self-repair budget (fix reds its own edits introduced, ≤2 internal cycles) + a discriminated give-up verdict
(`cause: 'code' | 'gate-test-defective'` + `defectiveTests` in `REPAIR_SCHEMA`) with the undo-own-edits
contract; NEW `repairGateTest` (independent reviewer-role, judge model) repairs a genuinely-defective
reviewer test instead of discarding a correct build (BL-0001/LESSON-0002) — an upheld test falls back to the
normal revert; `revertAndReopen` preserves reviewer-authored / Status-Note-referenced test files to
`.pandacorp/run/preserved-tests/<wo>/` (the builder prompt restores them as the rebuild's RED baseline); the
gate-reject routing gains ONE bounded IN-RUN RETRY (rebuild reopened WOs from the clean base on opus + one
re-gate, in the same run — skipped at the agent ceiling / reopen cap, no second patch cycle); the Visual QA
pass and the hardening security stage re-verify with `--since` (the one FULL suite per pass stays at
close-out / notify-end). **Template (`stack-a-nextjs/verify.sh`, OVERLAY-bumping):** `--since` mode now runs
only `e2e/smoke.spec.ts` + `e2e/shell.spec.ts` (the browser face of the DR-072 blocking lenses); the full
`e2e/` run (visual + responsive) stays unscoped-mode only; fail-closed spec-presence checks hold in both
modes (DR-106). **Agents/skills:** `reviewer.md` adversarial tests must be satisfiable (viewport
conventions; the `gate-test-defective` handback documented); `implement/SKILL.md` per-FRD loop rewritten for
the new flow. `reopen_count` remains the single budget; DR-072's split gate untouched.
**Why:** Both personal-page-v2 reverts were procedurally correct under the old contracts — the contracts were
the defect: a one-shot all-or-nothing patch, a revert that destroyed test evidence, and a next-pass-only
retry that re-paid the run's fixed overhead per reopened WO.
**Impact:** DR-106 + DR-107 added and DR-073 marked refined-by in `factory/decisions/registry.yaml`;
`factory/standards/build-orchestration.md` §5+§6 + `factory/standards/quality.md` (DR-015) updated; BL-0001
closed, BL-0017/BL-0018 filed and closed; LESSON-0002 `promotion: approved`; Manual narrative
`concepts/el-pipeline.md` synced (DR-046). MINOR (engine/template behavior change, backward-compatible);
OVERLAY 8.53.0→8.54.0 (engine + verify.sh are overlay files — projects pick it up via `/pandacorp:upgrade`).
Phases 3 (builder context packs + cheap-model mechanical steps) and 4 (Party `achievement`/`gate` events)
follow in the same audit plan.

## 2026-07-01 — Audit-20 execution: the full end-to-end process-audit fix batch · v9.39.0 (OVERLAY 8.52.1→8.53.0)
**What:** The owner approved 100% of `docs/proposals/20-factory-process-audit-2026-07-01.md`; this is the
plugin side of the whole batch (commits `1ba9f5c`, `edc2614`, `186298f`, `9e82425`, `470a861` + this bump).
**Engine** (`pandacorp-build.js`, OVERLAY-bumping): a fail-closed **Hardening phase** (security-auditor +
analytics with evidence artifacts) gates `phase: release` in BOTH close-out paths (BL-0012; the fail-safe
close never touches `phase`); an **in-engine DR-069 safe-point check at every FRD boundary** (rethink stop,
ready-queue drain via a reusable `processChange` + re-plan of new FRDs into the running loop, answered
decisions flip BLOCKED→PLANNED); a **close-out archive step** (DR-069 §7 done+`shipped_sha`+move to
`done/`); the patcher no longer self-certifies — a **`verifyPatched` independent reviewer** is the only
stamper of VERIFIED/`last_green_sha` (DR-073 amend); baseline clears `rethink_pending`; every judge-model
spawn is COST-weighted. **Skills:** `/bug` files `status: ready` (was un-drainable `pending`) + archives as
`done` + redirects to `/change`; `/upgrade` gains a HARD active-build guard and `change`/`bug`/`iterate`
preflights defer it mid-build; `/implement` preflight now REALLY re-checks the gates (asserts the new
`readiness_gate`/`grounding_gate` frontmatter stamps + a marker grep); `architecture` runs DR-100 via a
FRESH agent and owns the DRAFT→ACTIVE flip (new step 9b2, stamps); `spec` owns the PRD/FRD flip at the
owner's ok + records the demand signal durably; `work-orders` no longer auto-advances `phase`; `/decide`
unblocks WOs mechanically; `learn`/`memory` gain the backlog next-free-id + drain ritual. **Templates:**
`status.yaml.tpl` `pending_changes` (dead `pending_bugs` removed); `guide.md.tpl`/`AGENTS.md.tpl` route the
owner via `/change`; PRD/events/research templates gain the DR-035/041/042/043/074 sections review-launch
reads; WO template gains `difficulty`/`reopen_count`; blueprint template gains the gate stamps;
components-inventory gains the DR-075 nav-contract slot; `decision-log.md.tpl` drops `operation`; stacks
B/C/D carry the DR-105 PROVISIONAL banner; stack-a fixed (pnpm/Prisma/DR-090/DR-040/canary+DR-100 prose).
**Agents:** DR-047 RETRIEVE + `times_applied` wired into all 7 builder agents; `analytics`/`devops`
frontmatter `:blueprint`→`:architecture` (the Manual derives from it); architect's readiness-gate section
rewritten. **Hooks/scripts:** `block-dangerous.sh` — hard-reset allowlisted only for an explicit hex SHA
(DR-104) + mechanical DR-004 production-deploy gate (vercel --prod/flyctl/railway/wrangler), 6-case test
matrix green; NEW `validate-backlog.sh` (id-uniqueness + next-free-id, mirror of `validate-memory.sh`);
`scan-ideas.sh` Englished. Rules re-mirrored (quality gains 4 HARD gates + numeric bars; typescript
ESLint→Biome; web-security hstspreload owner gate).
**Why:** audit-20's headline was promise-without-mechanism — canonical docs asserting enforcement no code
implements. This batch makes each promise either real (cabled) or corrected (the claim removed).
**Version:** MINOR v9.38.2→**v9.39.0** (new capabilities: hardening phase, safe-point drain, independent
patch verify, validators, mechanical gates). **OVERLAY 8.52.1→8.53.0** (engine + status.yaml.tpl +
guide/AGENTS tpls + decision-log tpl changed — projects pick it up on their next `/pandacorp:upgrade`).
Factory-side story: `factory/decision-log.md` (2026-07-01). Activation: commit +
`claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — `worktree-bootstrap.sh` step 3 no-op fixed: `PANDACORP_FACTORY_ROOT` now actually written · v9.38.2 (OVERLAY 8.52.0→8.52.1)
**What:** Step 3 ("Secrets & data are REFERENCED, not copied") had a bare `:` no-op guarded by an MC/factory-worktree detection condition, with a comment claiming "the runner exports `PANDACORP_FACTORY_ROOT`" — nothing actually did. `config.ts::resolveFactoryRoot()` falls back to `path.resolve(cwd, "..")` when the env var is unset, which in a fresh Mission Control worktree resolves to that worktree's own near-empty `factory/` (git only checks out tracked files — `factory/profile.md`/`portfolio.md` are gitignored and never copied), tripping the onboarding gate (`OnboardingGate.tsx`) on every fresh `next dev` spawn and reding the whole e2e suite (every route fails "no visible app shell"). Fixed: step 3 now writes a worktree-local, gitignored `.env.local` (`PANDACORP_FACTORY_ROOT=$MAIN_WT`) next to whichever app's `package.json` it finds (`$WORKTREE/mission-control` first, else `$WORKTREE` itself), mirroring the pattern already used for `launch.json` in step 2.
**Why:** Discovered and only worked around (not fixed) in a same-day session — a worktree-local `.env.local` was hand-created to get `verify.sh`'s e2e suite green, then deleted before merging so it wouldn't leak into other runs. That meant the workaround had to be repeated by hand, by whoever noticed, every time — exactly the footgun DR-096 (worktree self-isolation) exists to prevent.
**Verification:** Created a throwaway worktree, ran the fixed `worktree-bootstrap.sh`, confirmed it wrote `mission-control/.env.local` with `PANDACORP_FACTORY_ROOT=<main checkout>`, confirmed the file is gitignored (`.env.*` pattern), and confirmed `resolveFactoryRoot()`'s resolution logic now points at the main checkout where `factory/profile.md` actually exists (so the onboarding gate would not trip).
**Impact:** `plugin/templates/shared/.pandacorp/worktree-bootstrap.sh` (canonical), `mission-control/.pandacorp/worktree-bootstrap.sh` (MC's local copy, synced directly rather than waiting for the next `/pandacorp:upgrade`), `plugin/templates/OVERLAY_VERSION` (8.52.0→**8.52.1**), `plugin/.claude-plugin/plugin.json` (**v9.38.2**, PATCH — fix, no behavior-shape change). MC's `.pandacorp/status.yaml` `overlay_version` stays on its own pre-existing drift track (8.51.0→8.51.1 — MC's overlay was already behind the template before this fix; see `mc-overlay-behind-template` in `mission-control/.pandacorp/inbox/changes/`), not the template's new 8.52.1. See `mission-control/docs/decision-log.md` for the project-side entry. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — Close a decision in place, never just append a narrative entry (real incident) · v9.38.1
**What:** Found via `/pandacorp:decide 2026-06-21-2`: three decisions in Mission Control's own `decisions.md` had been resolved DAYS ago (the owner answered in conversation, or a repair pass made them moot) but still parsed as mechanically "pending" — because the resolution was recorded as a NEW sibling entry prepended above the original, and the original block's own heading/`- **Estado:**` was never touched. Root cause (per an Explore-agent investigation): no skill documented WHO edits the original block when a decision is resolved outside the strict `/pandacorp:decide` happy path. Fix: `plugin/skills/implement/SKILL.md`'s "Decision points" section gained an explicit rule — any resolution (owner-answered OR an agent resolving autonomously mid-build) MUST edit the decision's OWN heading + Estado line in place; a new narrative entry is welcome as EXTRA context, never a substitute. `plugin/skills/decide/SKILL.md`'s "Record the outcome" step (already correct for its own path) was strengthened to spell out "mark it" unambiguously, plus a new instruction: if `/pandacorp:decide` finds a decision a newer sibling entry already resolved, close the original with that same outcome instead of asking the owner again.
**Why:** The owner traced this live while running `/pandacorp:decide` on a genuinely stale decision and asked "shouldn't `/pandacorp:implement` also clean these up once they're decided, so they don't sit orphaned in Mission Control?" — exactly right; the gap was real and reproduced immediately (all 3 Mission Control decisions confirmed via WO state + git history, then closed in place, see `mission-control/.pandacorp/inbox/decisions.md`, gitignored, no commit).
**Manual sync (DR-046):** `content/manual/guides/g-feedback.md` — added a line clarifying resolutions are always recorded on the original block.
**Impact:** `plugin/skills/implement/SKILL.md` (new "Closing out a decision" rule), `plugin/skills/decide/SKILL.md` (step 4 strengthened), `plugin/.claude-plugin/plugin.json` (**v9.38.1**, PATCH — clarifies/strengthens existing skill instructions, no new capability). No `OVERLAY_VERSION` bump (skill-only). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — DR-103 routing wired into `librarian`, `/memory` AND `/learn` (memory is not an action queue) · v9.38.0
**What:** Wired the DR-103 durability routing rule into the self-learning loop at two points:
1. **Harvest path** — the `librarian` agent (`plugin/agents/librarian.md`, new step 0) and `/pandacorp:memory` (`plugin/skills/memory/SKILL.md`, harvest step 2 + a new Rules bullet) classify every candidate BEFORE writing to `factory/memory/`: a **durable, retrievable lesson** → memory; an **actionable defect/change to the factory's own tooling** (a `gap ·` note, a to-fix item) → `factory/backlog/` as a `BL-*` item; a **dual-nature** item is SPLIT (defect → backlog, generalizable lesson → memory, linked by id); a **product-project** defect → that project's `.pandacorp/inbox/changes/`, never the factory backlog.
2. **Front-door router** — `/pandacorp:learn` (`plugin/skills/learn/SKILL.md`, new **step 0** + a Rules bullet + the entry-point note) now classifies any "change the factory" request across the four planes and REDIRECTS: an actionable factory-tooling defect/change → filed as a `BL-*` in `factory/backlog/` (NOT forced into a standard); a durable lesson → `factory/memory/`; a research/strategy doc → `docs/proposals/`; a product-project change → `/pandacorp:change`. A dual-nature request splits (fix → backlog, durable rule → codified here). This closes the gap where `/learn` was the only factory front door but had no route for a plain fix/defect. `CLAUDE.md` gains a "Changing the factory itself — route by plane" table.
**Why:** The owner saw LESSON-0002 in Mission Control's promotions queue (`/pandacorp:learn LESSON-0002`) and recognized it was a build-engine bug to FIX, not a lesson to retrieve — and asked whether `/learn` should know where to route a change. Deep-research confirmed (unanimous): a durable learning memory and an actionable defect queue are two stores with opposite lifecycles, and conflating them degrades retrieval ("temporal memory contamination"). The factory had no plane-3 home for its own tooling; DR-103 adds `factory/backlog/` and this routes into it from both the harvest path and the `/learn` front door.
**Impact:** `plugin/agents/librarian.md`, `plugin/skills/memory/SKILL.md`, `plugin/skills/learn/SKILL.md`, `CLAUDE.md`. Canonical doctrine: `factory/decisions/registry.yaml` (DR-103), `factory/backlog/README.md`, `factory/memory/README.md`. **No OVERLAY change** — factory-internal (these skills/agents operate on factory stores; projects already separate `.pandacorp/run/lessons.md` from `.pandacorp/inbox/changes/`). Plugin v9.37.0→**v9.38.0** (MINOR — new routing capability). Factory-side history + the lesson migration (L-0002/03/04 split into BL-0001/02/03) + audit-19 extraction: `factory/decision-log.md` (2026-06-30). MC Manual narrative (a "three planes / factory backlog" concept page + the `/learn` router in the el-plugin concept) is a tracked follow-up. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — `/pandacorp:decide` checks staleness + new OBSOLETO status; "Aprobar" one-click removed · v9.37.0
**What:** Three owner-driven changes to the decision flow, same day as the id-scoping change below:
1. **Staleness check.** Before presenting a pending decision (whether listing all or scoped to one id), the skill now computes its age (today minus the heading's date) and, at **7 days or older**, flags it explicitly and actively judges relevance (reads the work order/file the decision references, checks if the codebase already moved past it) before asking for an answer — never silently walks a stale decision as if it were fresh.
2. **New terminal status `OBSOLETO`** (alongside `RESUELTO`), recorded when the owner confirms a flagged decision no longer applies — distinct from `RESUELTO` so the history is honest about which decisions were actually answered vs dropped. Never invented by the skill on its own; the owner always confirms (same "don't decide for them" rule). `decisions-inbox-template.md`'s `Estado` comment updated to document both terminal values.
3. **Removed the "Aprobar la recomendación" one-click** from Mission Control's Summary tab (owner: a pre-baked approve button let them record an answer without the context a real `/pandacorp:decide <id>` conversation provides). The recommendation text still renders as context; there is no longer a copyable "Aprobado: …" command. Mission Control also now shows an "hace N días" age hint per pending card and a distinct "Obsoleta" tag for `OBSOLETO`-status cards.
**Why:** Owner: decisions sitting a week+ old likely no longer reflect the current codebase, and answering them cold risks recording a stale/irrelevant decision; a way to recognize and drop them (without inventing a fake answer) was missing. Separately, the approve one-click bypassed the context needed to actually approve something responsibly.
**Manual sync (DR-046):** `content/manual/guides/g-feedback.md` — the `/pandacorp:decide` bullet now mentions the 7-day staleness check and the OBSOLETO outcome.
**Impact:** `plugin/skills/decide/SKILL.md` (staleness step + OBSOLETO recording + rules), `plugin/templates/docs/decisions-inbox-template.md` (Estado comment), `plugin/.claude-plugin/plugin.json` (**v9.37.0**, MINOR). Mission Control companion change (same day): `src/lib/docs/activity.ts` (`DecisionPoint.date`/`.status` 3-way, `OBSOLETE_KEYWORDS`), `tab-summary.tsx` (ApproveButton removed, age hint, Obsoleta tag), `docs/frds/frd-04-project-workspace/frd.md`. No `OVERLAY_VERSION` bump (skill-only change, template stays reference-only).

## 2026-06-30 — `/pandacorp:decide` accepts a decision id to scope to ONE pending decision · v9.36.0
**What:** Owner request after noticing Mission Control's Summary tab told them to run `/pandacorp:decide` with no way to say WHICH decision — invoking it cold walks every pending decision, one by one (confirmed in the skill's own prior contract). `/pandacorp:decide` now recognizes an optional leading **id** in `$ARGUMENTS` (e.g. `/pandacorp:decide 2026-06-21-1` or `/pandacorp:decide 2026-06-21-1 "yes, show the costs"`) and scopes directly to that one decision instead of listing/asking about every pending one. The id is **derived, never written by the agent**: `<YYYY-MM-DD>-<n>` for a date-prefixed `## YYYY-MM-DD ... — <title>` heading (`n` = 1-based count of headings sharing that EXACT date, pending + resolved both count so an id never shifts when a sibling resolves), or `legacy-<n>` for an `OPEN:/CLOSED:/RESOLVED:` heading (no date). The SAME derivation is implemented in Mission Control (`mission-control/src/lib/docs/activity.ts`, `DecisionPoint.id`, same-day companion change), so an id copied from there always resolves to the same block here.
**Why:** Two-sided fix needed in lockstep — neither half alone solves the owner's problem: Mission Control can SHOW an id, but without the skill understanding it, the copied command is just decoration; the skill can ACCEPT an id, but without Mission Control deriving and displaying the same one, the owner has nothing to copy. Implemented directly (not via `/pandacorp:learn`'s skill-creator eval-gate) — a scoped, owner-directed argument-parsing extension to an existing skill, not a new skill/capability warranting the full authoring+benchmark pipeline.
**Manual sync (DR-046):** `content/manual/guides/g-feedback.md` updated — the `/pandacorp:decide` bullet now mentions the per-card `<id>`-scoped command.
**Impact:** `plugin/skills/decide/SKILL.md` (id derivation + `$ARGUMENTS` scoping rule + "how it looks for the owner"), `plugin/templates/docs/decisions-inbox-template.md` (explanatory comment — no template SHAPE change, ids are derived from the existing heading, no new field to write), `plugin/.claude-plugin/plugin.json` (**v9.36.0**, MINOR — new compatible capability). No `OVERLAY_VERSION` bump (skill-only change, no template injected into projects beyond the comment-only `decisions-inbox-template.md`, which is read-as-reference by agents, not copied verbatim into a project at scaffold time). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — Internal-engine skills hidden from the slash menu (`user-invocable: false`) · v9.35.0
**What:** Added `user-invocable: false` to the frontmatter of the **6 skills that are not meant to be typed by the owner** — `bug`, `iterate`, `scaffold`, `work-orders`, `upgrade`, `new-version`. They now disappear from the `/` autocomplete menu but stay fully invokable by the model and by other skills (the documented mechanism — only flag that hides a skill from the menu while keeping it callable). The owner's slash menu drops from 24 to ~18 entries, showing only the skills the owner actually runs. `bug`/`iterate` were already documented as the internal engines behind `/change`; `scaffold`/`work-orders` are invoked by `spec`/`architecture`; `upgrade` is auto-triggered by each skill's preflight (DR-048); `new-version` is an optional milestone command the owner has never used (the day-to-day is `/change`) — kept (not deleted) so the capability survives, just hidden.
**Why:** Owner pain: typing `/pandacorp` surfaced a 24-item list with no signal about which are owner-facing vs internal engines, so he couldn't tell what was safe to pick. Hiding the engines fixes the menu noise at the source. Considered and REJECTED a rename `pandacorp:`→`corp:` (purely cosmetic): measured blast radius 332 files / 1826 occurrences across plugin + factory + Mission Control (incl. ~100 tests asserting the exact command string) + the served MC snapshot needing a redeploy + plugin reinstall + existing projects left stale — high risk/effort for a 5-keystroke gain, and the menu noise (the real pain) is already solved by the hiding. Project + plugin keep the name `pandacorp`.
**Trade-off:** the 6 skills can no longer be typed directly by the owner (the `bug`/`iterate` direct aliases are gone from the menu). In practice none are run by hand — the front door is `/change`, and `upgrade` self-triggers. If a manual run is ever needed (e.g. regenerate work-orders, force an upgrade), flip the flag or have the agent invoke it.
**No flow/concept change (DR-046):** the pipeline, gates and the "one door = `/change`" model are unchanged — this is a visibility-only edit. The Manual's Reference catalog auto-derives from skill frontmatter, so the hidden skills drop out on the next MC build with no hand-copy; no narrative concept page describes these engines as owner commands, so none needs rewriting.
**Impact:** `plugin/skills/{bug,iterate,scaffold,work-orders,upgrade,new-version}/SKILL.md` (+`user-invocable: false`), `plugin/.claude-plugin/plugin.json` (**v9.35.0**, MINOR), `CLAUDE.md` (the *How the factory is operated* "Optional/internal" line notes they are hidden). No OVERLAY change (no template touched). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — Gate canary EXTENDED to every fail-closed gate (DR-079) · v9.34.0 (OVERLAY 8.50.0→8.51.0)
**What:** Grew `canary.sh` from the v1 three checks to **all eight** fail-closed gates (owner: "cover everything"): biome (`--error-on-warnings`), the test-placement structure guard, the DR-100 readiness gate, **tsc** (a strict type error, isolated via a temp tsconfig that compiles ONLY the fixture — so a pre-existing project error can't mis-attribute), **madge** (a circular-import pair, scoped to the sandbox), **vitest** (a deliberately-failing test, scoped), **knip** (an orphan unimported file), and **Playwright spec discovery** (`playwright test e2e/ --list` must find smoke/visual/responsive/shell — proving the config still wires them so the e2e layer can't pass vacuously, the DR-075 rot; fast, no browser/webServer boot). Each check still GENERATES its fixture under a `__canary__/` sandbox (or temp tsconfig), asserts the gate REDs, and cleans up — no broken files persist.
**Intentionally NOT canaried:** a full Playwright browser RUN (too slow/flaky on demand — discovery + the fail-closed missing-harness checks + DR-059 conformance cover its rot modes) and `doc-lint.sh` (advisory — it never reds). So coverage is now complete for what's sensible to canary.
**Why:** Owner go-ahead to extend the canary to everything. Closes the meta-gap for every gate that can silently rot (a rule disabled, an exit code swallowed, a spec undiscovered), not just the cheap three.
**Verification:** Ran it against Mission Control — **8/8 gates rejected their broken input** (exit 0, sandbox cleaned). Ran a **negative test** too: feeding a CLEAN biome fixture made the canary correctly report that gate ROTTED and exit non-zero — proving the canary is not a tautology (it genuinely catches a gate that stopped failing). Plus `claude plugin validate`, `bash -n`, registry YAML valid.
**Impact:** `plugin/templates/stack-a-nextjs/canary.sh` (rewritten), `plugin/skills/upgrade/SKILL.md` (`--canary` coverage note), `plugin/templates/OVERLAY_VERSION` (8.50.0→**8.51.0**), `plugin/.claude-plugin/plugin.json` (**v9.34.0**, MINOR). Canonical: `factory/decisions/registry.yaml` (DR-079 extended) + `factory/standards/quality.md`. MC overlay synced to 8.51.0 (see `mission-control/docs/decision-log.md`). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — Índice de templates: digests listados + "Generated by" del rename architecture · v9.33.2
**What:** Doc-only en `plugin/templates/docs/README.md` (el índice de templates), a raíz del rename `/blueprint`→`/architecture` y del nuevo digest de arquitectura: (1) la columna "Generated by" de las filas `blueprint-template`/`work-order-template`/`architecture-template`/`adr-template`/`events-template` decía `blueprint` (el skill viejo) → ahora `architecture`; (2) se añadieron las dos filas de digests que faltaban en el índice — `spec-digest-template.md` → `.pandacorp/comms/spec-resumen.md` (powers MC's Spec tab, gen. por `spec`) y `architecture-digest-template.md` → `.pandacorp/comms/arquitectura-resumen.md` (powers MC's Arquitectura tab, gen. por `architecture`). El nombre de archivo `blueprint-template.md` y la ruta `blueprint.md` NO cambian (el artefacto se conserva).
**Why:** Cierre de consistencia documental tras el rename del skill + el tab de Arquitectura. El índice afirma listar "every recurring document"; los digests faltaban y los generadores estaban stale.
**Files:** `plugin/templates/docs/README.md`, `plugin/.claude-plugin/plugin.json` (v9.33.2). PATCH (doc-only, sin cambio de comportamiento ni de OVERLAY). MC Manual (el-pipeline + mission-control-por-dentro) actualizado en paralelo (`mission-control/docs/decision-log.md`). Activación: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-30 — `warn-adhoc-write.sh` scope gap fixed: fábrica ahora también aislada · v9.33.1
**What:** El hook PreToolUse `warn-adhoc-write.sh` tenía una guarda de alcance `[ -f "$cwd/.pandacorp/status.yaml" ] || allow` que lo hacía silenciar inmediatamente cuando el `cwd` era la raíz de la **fábrica** (`panda-corp`). El directorio `.pandacorp/` de la fábrica solo tiene `run/`, no `status.yaml`, por lo que el recordatorio de isolación (DR-096) y el registro de sesión (DR-099) eran no-ops en la fábrica. Split del alcance en dos: (1) write-gate DR-044 (routear por un skill) sigue siendo solo para proyectos con `status.yaml`; (2) isolation reminder + session attribution ahora detectan también la fábrica via `factory/constitution.md` y se activan en ambos contextos. El mensaje de isolación es independiente del write-gate, no concatenado.
**Why:** Una conversación que trabajaba en `factory/standards/` y `plugin/skills/` dejó WIP sin commitear en el checkout principal, bloqueando el merge-queue de otra conversación que trabajaba en `mission-control/`. Root cause: el hook tenía gap de alcance para la fábrica. La fábrica tiene exactamente el mismo problema de sesiones paralelas que los proyectos.
**Impact:** `plugin/scripts/warn-adhoc-write.sh` (split de alcance), `plugin/.claude-plugin/plugin.json` (**v9.33.1**, PATCH — fix de comportamiento). No cambia el overlay (el hook vive en el plugin, no en el template). Activación: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-30 — Gate canary BUILT (DR-079) — `canary.sh` ships + is wired into architecture/upgrade · v9.33.0 (OVERLAY 8.49.0→8.50.0)
**What:** The DR-079 gate canary stops being vacuous — it ships as a real harness. NEW `plugin/templates/stack-a-nextjs/canary.sh` (installed VERBATIM to `.pandacorp/canary.sh`); `verify.sh --canary` already execs it. It proves each fail-closed gate STILL goes RED on a deliberately-broken input (the anti-cheat companion to constitution principle 24). It keeps NO broken files in the repo: each check GENERATES its broken input under a `__canary__/` sandbox, feeds it to the gate, asserts the gate REJECTS it, and cleans up; `canary.sh`'s exit code is the verdict (0 = all canaried gates went RED; 1 = a gate rotted). **v1 covers** the cheap, deterministic, config-rot-prone gates: biome (`biome check … --error-on-warnings` on a forbidden-`any`/mis-formatted file), the test-placement structure guard (a stray `*.test.ts` outside `_tests/`), and the DR-100 readiness gate (a `[NEEDS CLARIFICATION]` marker in a `status: ACTIVE` doc). **Deferred** (need a whole-project compile or a running app): tsc, vitest, knip, and the Playwright smoke/visual/responsive/shell layer — not canaried yet, never falsely RED. Wired into the flow: `architecture` Step 6 installs `{verify.sh,canary.sh}` verbatim; `upgrade` adds `canary.sh` to the conformance byte-diff/overwrite set (a pre-DR-079 project gets it installed first time) and the `--canary` note reflects the shipped harness.
**Why:** Owner go-ahead to build the canary (the P1-6 backlog item from the factory-flow audit). Conformance (DR-059/076) proves the gate FILES match the template; the canary proves they still FUNCTIONALLY reject — closing the "a gate can be present but disabled / its exit code swallowed" meta-gap.
**Impact:** NEW `plugin/templates/stack-a-nextjs/canary.sh`; `plugin/skills/{architecture,upgrade}/SKILL.md` (install + conformance wiring); `plugin/templates/OVERLAY_VERSION` (8.49.0→**8.50.0**); `plugin/.claude-plugin/plugin.json` (**v9.33.0**, MINOR — new capability). Canonical: `factory/decisions/registry.yaml` (DR-079 amended) + `factory/standards/quality.md` (advisory list — canary no longer "vacuous"). MC's overlay synced (canary.sh installed, overlay→8.50.0; see `mission-control/docs/decision-log.md`). **Validated by RUNNING it against Mission Control: 3/3 gates rejected their broken input, exit 0, sandbox cleaned** + `claude plugin validate` + `bash -n` + registry YAML valid. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — Foundation work orders use the owning FRD's id, never a `WO-00-*` prefix · v9.32.0
**What:** Clarified the work-order numbering convention so the `architecture` skill / `architect` stop inventing a fictional `00` FRD for foundation/shell work orders. Foundation WOs belong to a REAL owning FRD (almost always the platform-shell feature **FRD-01**) and are numbered as that FRD's own correlatives (`WO-01-001`, `WO-01-002`, …) with `parent: FRD-01` + **`foundation: true`**; the Wave-0/foundation fact lives in the `foundation` frontmatter + the Build Plan/`dependsOn`, NEVER in the id. A `WO-00-*` id is a defect the readiness gate (DR-100) rejects.
**Why:** On `personal-page-v2` the architect numbered FRD-01's four foundation WOs `WO-00-001/WO-00-002/WO-01-001/WO-01-002` — the owner caught that `00` isn't a real FRD. Root cause: the foundation-wave guidance described scheduling but not numbering, so the agent conflated the *wave* (a separate dimension) with the *id*.
**Files:** `plugin/skills/architecture/SKILL.md` (foundation-wave step → explicit numbering rule), `plugin/templates/docs/work-order-template.md` (`id` comment + new optional `foundation:` frontmatter field), `plugin/.claude-plugin/plugin.json` (v9.32.0). No `OVERLAY_VERSION` change (skill + doc-template only; not injected into project overlay). No new DR — a precision clarification of the existing `WO-NN-MMM` convention (pairs with DR-057 foundation-wave + DR-100 readiness gate). DR-046: a wording/precision refinement, no flow/gate/concept change → no Manual narrative edit (the skills Reference auto-derives). Activation: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-30 — Doc-lint louder + proactive; Responsive Gate two-tier (off-target = no-break advisory) · v9.31.0 (OVERLAY 8.48.0→8.49.0)
**What:** Owner directives on two audit follow-ups:
- **doc-lint advisory but loud + proactive (DR-077 amended).** `templates/shared/.pandacorp/doc-lint.sh` still ALWAYS exits 0 (never blocks — no red-lock), but now reports its findings PROMINENTLY by default (a banner + the full list, not a quiet one-liner) and instructs the agent to **FIX** the surfaced drift (missing frontmatter keys / broken REQ→WO links) in the same pass when touching docs — not merely report it. A hard/blocking doc-lint was explicitly NOT adopted (would block legit work).
- **Responsive Gate two-tier (DR-074 amended).** `templates/stack-a-nextjs/e2e/{responsive.spec.ts,_responsive-helper.ts}`: split the checks into a FUNCTIONAL tier (content clipped off-canvas + `<main>` occluded = a feature you can't reach) and a COSMETIC tier (overflow-that-scrolls + sub-24px tap targets). ON the targeted platform both run BLOCKING (unchanged). OFF the targeted platform — a desktop-only app at the mobile width, a mobile-only app at a desktop width — only the FUNCTIONAL tier runs via the new `reportBreakageOnly` (loud test-annotation + `console.warn`, **never throws**): a real break that hides functionality is surfaced, cosmetic imperfection off-target is accepted as low-priority, and the desktop-retrofit anti-red-lock guarantee holds (this tier can't fail the gate). `DESKTOP_WIDTH` lives in the synced `responsive.spec.ts` (not the per-project `_target.ts`).
**Why:** Owner: (2) "don't just mention a broken doc — fix it; but don't block the build." (3) "for a desktop-only app, mobile is low-priority and ugly-is-fine, but nothing should BREAK and all functionality must be reachable" (and the reverse for mobile-only on desktop).
**Impact:** `plugin/templates/shared/.pandacorp/doc-lint.sh`, `plugin/templates/stack-a-nextjs/e2e/{responsive.spec.ts,_responsive-helper.ts}`, `plugin/templates/OVERLAY_VERSION` (8.48.0→**8.49.0**), `plugin/.claude-plugin/plugin.json` (**v9.31.0**, MINOR — compatible gate-behavior change). MC's overlay copies synced to 8.49.0 (see `mission-control/docs/decision-log.md`). Canonical doctrine: `factory/decisions/registry.yaml` (DR-074/DR-077 amendments) + `factory/standards/{quality.md,design.md}` (see `factory/decision-log.md`). Validated: `claude plugin validate`, `bash -n doc-lint.sh`, MC `tsc --noEmit` (typechecks the synced e2e) + `biome` on the two specs — all green. REMAINING confidence step: a live Playwright run of the advisory tier against a real off-target app (the advisory design bounds the risk — it cannot red-lock). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-30 — Factory-flow audit fixes: gate hardening + overlay sweep · v9.30.0 (OVERLAY 8.47.0→8.48.0)
**What:** Resolved the plugin-side findings of the factory-flow audit (`docs/proposals/19-factory-flow-audit-2026-06-30.md`):
- **Stop-gate attribution by repo-relative paths, not basenames (P1-9).** `scripts/verify-before-stop.sh` previously compared touched/dirty/failing files by **basename** (`foo.ts`), so two same-named files in different folders cross-attributed — it could silence an owned red or block on a foreign one. Now it normalizes all three sets to the **repo-root frame** (touched = absolute→strip root; git porcelain = already root-relative; gate output = cwd-relative→also emit the cwd-prefixed candidate) before intersecting. Fail-closed: with no git root it can't attribute → it BLOCKS (never a silent allow). Verified the basename-collision case (a foreign `b/util.ts` no longer attributes to my `a/util.ts`) and the owned case still blocks.
- **Build-engine fail-open edges closed (P1-8)** in `templates/shared/.claude/workflows/pandacorp-build.js`: (1) the **foundation-completeness gate** treated a null/invalid verdict as complete (`!fc || fc.complete` → fan out) — now only an explicit `complete === true` fans surfaces out; a no-verdict is a bounded retry then escalate (fail-closed). (2) **`maxAgents` could overshoot mid-wave** by (wave-1) because the brake was checked only at the wave boundary — the wave width is now capped to the remaining agent budget (`min(P.wave, maxAgents - agentSpawned)`), so the in-engine brake holds even if the supervisor is dead. (3) **undeclared `artifacts` now force serialization** instead of being read as "no overlap" (`artifactsOverlap` returns true when either WO declares none) + a per-wave warning lists the WOs being serialized for missing artifacts.
- **Script robustness (P2-14):** `templates/shared/.pandacorp/pending-work.sh --json` now emits via `jq` (safe escaping of unusual branch/path names) with a quote/backslash-escaping manual fallback; `templates/shared/.pandacorp/merge-queue.sh` adds an **early busy-main preflight** (fast `exit 12` hand-back before the multi-minute rebase+gate) while keeping the authoritative `git merge --ff-only` recheck.
- **`operation` phase swept from overlay templates (P0-3):** `templates/shared/.pandacorp/status.yaml.tpl` (phase enum) and `templates/shared/.pandacorp/guide.md.tpl` (pipeline line) no longer list the obsolete `operation` phase (DR-085 folded it into `release`) — new projects stop inheriting the stale vocabulary.
- **`/pandacorp:bug` frontmatter fix (P1-11):** the description said `.pandacorp/inbox/bugs/` while the body uses the unified `.pandacorp/inbox/changes/` queue — reconciled (and notes it's reachable via the `/pandacorp:change` front door, DR-069).
**Why:** The audit's recurring theme is enforcement drifting from doctrine: fail-open edges, basename collisions, manual JSON and stale templates let a build look green or mis-attribute while the doctrine says fail-closed. These close those gaps without adding new process.
**Impact:** `plugin/scripts/verify-before-stop.sh`, `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/templates/shared/.pandacorp/{pending-work.sh,merge-queue.sh,status.yaml.tpl,guide.md.tpl}`, `plugin/skills/bug/SKILL.md`, `plugin/templates/OVERLAY_VERSION` (8.47.0→**8.48.0** — overlay template content changed; projects re-sync via `/upgrade`), `plugin/.claude-plugin/plugin.json` (**v9.30.0**, MINOR — build-engine behavior changed compatibly). Mission Control's installed overlay copies were synced to 8.48.0 (see `mission-control/docs/decision-log.md`). Validated: `claude plugin validate`, `bash -n` on every touched script, `node --check` on the workflow. P1-7 (a hard write-gate block) was deliberately NOT implemented — the owner already rejected a hard PreToolUse block in the registry (a hook can't tell a significant change from a micro-edit; the persistent nudge is the chosen enforcement). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-29 — Skill renombrado `/pandacorp:blueprint` → `/pandacorp:architecture` + emite el digest de arquitectura · v9.29.0
**What:** (1) `git mv plugin/skills/blueprint → plugin/skills/architecture`; H1 `# /pandacorp:architecture`; el skill se invoca ahora como `/pandacorp:architecture` (el slug = el dir, sin prefijo). Se actualizó CADA referencia VIVA al comando en el repo: skills que lo nombran como next/prev step (scaffold/spec/design/implement/work-orders), `templates/stack-*/STACK.md`, `templates/docs/work-orders-readme-template.md`, `templates/rules/README.md`, `scripts/verify-before-stop.sh`, `factory/decisions/registry.yaml` (el puntero canónico vivo + los shorthands de DR-032/100/102), `README.md`/`CLAUDE.md`/`factory/standards/infra.md`, y en Mission Control la UI + el mapa next-step + la narrativa del Manual. **NO se tocó** el artefacto `blueprint.md` (per-FRD), `blueprint-template.md`, la sección "Build Plan" ni los ids `BP-NN` (parte de la jerarquía `FRD > FDD > design-tokens > blueprint > work order`). El AGENTE `pandacorp:architect` (namespace distinto) queda igual. Las entradas históricas de los decision-logs se dejaron intactas. (2) NUEVO paso 9c: el skill EMITE `.pandacorp/comms/arquitectura-resumen.md` (español, owner-facing, gitignored) siguiendo el nuevo `templates/docs/architecture-digest-template.md` — stack table, modelo de datos condicional, comunicación/servicios, y una ficha por FRD; el DAG/ADRs/env NO se duplican (MC los lee en vivo).
**Why:** El comando "blueprint" describía mal la fase, que produce la arquitectura completa; alinearlo con `phase: architecture`. El digest da paridad con `/spec` (su `spec-resumen.md`) para que MC muestre la arquitectura nativamente (DR-046).
**Files:** `plugin/skills/architecture/SKILL.md` (renombrado de `blueprint/`, H1 + paso 9c), **NUEVO** `plugin/templates/docs/architecture-digest-template.md`, plugin templates/scripts arriba, `plugin/.claude-plugin/plugin.json` (v9.29.0). Fábrica: `factory/decisions/registry.yaml`, `factory/decision-log.md`. Mission Control: ver `mission-control/docs/decision-log.md` (la pestaña Arquitectura). MINOR (comando renombrado). **Sin cambio de OVERLAY** (ningún archivo de `templates/shared` referenciaba el comando — solo `blueprint.md` el doc, que se conserva). El catálogo de skills del Manual auto-deriva del dir renombrado. Activación: commit + `claude plugin update pandacorp@panda-corp` + reiniciar.

## 2026-06-30 — Un merge bloqueado DEBE llegar al dueño, en la conversación (refinamiento DR-099) · v9.28.2
**What:** Reforzado el "loud merge hand-back" de DR-099 a DOS canales: (a) `merge-queue.sh` dispara la notificación de escritorio (ya lo hacía en el template) Y (b) **el AGENTE DEBE avisar del merge bloqueado EN LA CONVERSACIÓN** (qué se bloqueó, por qué, que el trabajo está preservado en su rama, y qué se necesita) — sin irse a hacer parches en silencio. Explícito que esto es el entregable PROPIO de la sesión y por tanto EXENTO de la regla (3) de aislamiento (que silencia los rojos de OTRAS sesiones). Editado: `guide.md.tpl` (párrafo DR-099) + comentario del `merge-queue.sh` template.
**Why:** Un cambio de Party-liveness se construyó verde en un worktree pero el merge-queue se devolvió repetidamente (el build paralelo de FRD-10 dejó `main` en rojo por SUS propios exports muertos; luego un hueco de entorno del e2e en worktree). El dueño NUNCA SE ENTERÓ — el agente se quedó callado (sobre-aplicando "no narres otras sesiones") y se fue a parchear en `:1987`/`:3100`; el dueño lo descubrió al preguntar y advirtió que el trabajo terminado podía PERDERSE. Además se detectó que la copia de `merge-queue.sh` de Mission Control estaba DESACTUALIZADA (su `fail()` no tenía la notificación del template) → ni el canal (a) funcionaba.
**Files:** `plugin/templates/shared/.pandacorp/guide.md.tpl` (párrafo DR-099) + `plugin/templates/shared/.pandacorp/merge-queue.sh` (comentario), `plugin/.claude-plugin/plugin.json` (v9.28.2). Factory: `factory/decisions/registry.yaml` (DR-099) + `factory/standards/build-orchestration.md` §9 + `factory/decision-log.md`. Proyecto MC (efecto inmediato): `mission-control/.pandacorp/{merge-queue.sh (fail() sincronizado), guide.md}`. Sin OVERLAY change (guía/regla; los proyectos re-sincronizan la guía en el próximo `/upgrade`). Activación: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-29 — Back-port: `knip.json` canónico reconoce el `src/proxy.ts` de Next 16 (DR-076/DR-059) · v9.28.1
**What:** Añadido `src/proxy.ts` a los `entry` del `knip.json` canónico (`plugin/templates/stack-a-nextjs/knip.json`), conservando `src/middleware.ts` (compat Next ≤15). Next 16 renombró la convención del middleware a `proxy.ts`; sin esa entrada el `proxy.ts` real de un proyecto no se traza (hint no-fatal "no matches" sobre el `middleware.ts` obsoleto). Propagado en el MISMO cambio al proyecto vivo `personal-page-v2` (su copia conformance-checked ya coincide con la plantilla). `OVERLAY_VERSION` 8.46.0→8.47.0 para que el resto de proyectos Next lo tomen en el próximo `/pandacorp:upgrade`.
**Why:** DR-076 — un fichero de gate conformance-checked se arregla EN LA FUENTE (la plantilla), nunca in-place, o `/upgrade` lo revierte y la plantilla queda obsoleta para todos. Detectado al aterrizar `personal-page-v2` al stack Next 16 instalado (misma clase de bug de currency que DR-102).
**Files:** `plugin/templates/stack-a-nextjs/knip.json` (+`src/proxy.ts`), `plugin/templates/OVERLAY_VERSION` (8.47.0), `plugin/.claude-plugin/plugin.json` (v9.28.1). Proyecto: `personal-page-v2/knip.json` + `.pandacorp/status.yaml` (overlay 8.47.0). Sin nuevo DR (aplicación de DR-076/DR-059). Activación: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-29 — `blueprint` skill: repo-grounding validation gate + grounded artifacts (versiones/.env.example/AGENTS.md/NFRs explícitos) (DR-102) · v9.28.0
**What:** Se extiende `/blueprint` (fase de arquitectura) con la mitad de fidelidad-externa que a DR-100 le faltaba. Cinco cambios: **(1) GATE DE VALIDACIÓN repo-grounding** (nuevo paso 9b, antes de que cualquier doc pase a ACTIVE; re-chequeado por el preflight de `/implement`) — un reviewer fresco valida cada afirmación concreta de `architecture.md` + blueprints + work orders contra el scaffold REALMENTE INSTALADO (rutas de archivo, APIs/config del framework vigentes para la major instalada vía los docs version-matched del AGENTS.md, versiones de deps instaladas-o-pinneadas-en-un-WO, garantías duras verificadas contra un `pnpm build` real). **(2) Pinnear versiones EN EL PUNTO DE INSTALACIÓN** (instalar a la versión elegida o escribir el `pnpm add pkg@version` exacto en el WO de fundación), nunca solo en prosa. **(3) Generar `.env.example`** como artefacto estándar del blueprint. **(4) Generar/refrescar `AGENTS.md`** apuntando a los docs empaquetados por versión del framework (patrón create-next-app). **(5) NFRs explícitos por-WO** en el completeness-20% (seguridad + performance + a11y + reduced-motion).
**Why:** En `personal-page-v2`, justo después de que `/blueprint` produjera un plan fuerte y limpio según DR-100 E instalara el stack en verde, una auditoría adversarial encontró 4 BLOQUEANTES — todos "context blindness" contra el stack real (los docs decían `middleware.ts`, Next 16 lo renombró `proxy.ts`; faltaba `setRequestLocale` → `[locale]` salía DINÁMICO, rompiendo la garantía $0/estático; mismatch del atributo de tema; versiones de deps solo en prosa). Deep-research verificada (22 fuentes, 25 afirmaciones verificadas adversarialmente): validar artefactos contra el repo real antes de codegen es el reductor de errores #1 (arXiv:2604.05278), AGENTS.md→docs por versión reduce errores de API obsoleta (Next.js /evals), y los NFR se omiten sistemáticamente (~14%, arXiv:2511.12884). Caveat honrado (SSRN 6515898): el VOLUMEN/calidad de spec por sí solo NO reduce defectos — esto ATERRIZA el plan, no lo infla.
**Files:** `plugin/skills/blueprint/SKILL.md` (paso 6: pins al instalar + `.env.example` + `AGENTS.md` version-matched; nuevo paso 9b repo-grounding gate), `plugin/templates/docs/work-order-template.md` (completeness-20% nombra NFRs explícitos), `plugin/.claude-plugin/plugin.json` (v9.28.0). Fábrica: `factory/decisions/registry.yaml` (DR-102, extiende DR-100), `factory/decision-log.md`. Guía interna de la fase de arquitectura (la consumen `/blueprint` + agentes architect/reviewer) — **sin cambio en la rule-library/OVERLAY** (no inyecta regla por-proyecto). Manual MC: catálogos de skills/DR auto-derivan; las fases visibles del pipeline + gates del dueño no cambian (9b es un check interno del architect dentro de la fase de arquitectura existente, como el readiness gate de DR-100), así que sin edición de página narrativa. Activación: commit + `claude plugin update pandacorp@panda-corp` + reiniciar.

## 2026-06-28 — `design` skill: Claude Design EXPLORE path, tanda 2 — set completo de pantallas, peso=importancia, contenido real, playbooks, rúbrica (DR-101) · v9.27.0
**What:** Segunda tanda de aprendizajes del canvas de Claude Design (de `personal-page-v2`), ENCIMA de DR-058 §1c (no se repite: conexión real, brief exhaustivo anti-omisión, placeholders de imagen, motion obligatorio, breakpoints por `target_platforms`, pull-and-review, autoría de tokens). Siete reglas. **(1) Cadencia de DOS ETAPAS, nunca parar en el landing:** Etapa 1 genera el *sistema* (el dueño lo aprueba), luego Etapa 2 genera **TODAS** las pantallas de los FRD con UI — **enumeradas por el skill desde los FRDs** (`ui:true` → su(s) pantalla(s)) — una por una con pull-and-review; el dueño nunca debe tener que preguntar "¿dónde están las otras páginas?". **(2) Peso = importancia (jerarquía/proporción):** la acción/conversión primaria recibe un tratamiento prominente (el `cta-band` compartido), nunca un enlace de una línea infradimensionado (el CTA de contacto salió como un enlace minúsculo); sin abrir con un hero sin contexto — primero título+contexto+at-a-glance (los reclutadores ojean <60s). **(3) UN `cta-band` de cierre compartido** reusado en cada página (refuerza DR-062) — un "let's talk" ad-hoc por página es defecto de jerarquía Y de cohesión. **(4) Contenido real, nunca inventado** — listas/cards/stack/archivo desde las fuentes REALES del dueño, longitudes balanceadas; placeholders solo donde el dato está genuinamente pendiente y marcados visiblemente. **(5) Prompts explícitos** — nombra el elemento + la acción + lo que se queda ("Borra el elemento ENTERO incluyendo su texto y enlace; reemplázalo por nada"). **(6) Playbooks por superficie** (case study / Now / blog índice / blog post / contacto / projects-archivo / stack) capturados una vez. **(7) Rúbrica de revisión por pantalla** que el agente corre (tirando de cada fichero real, no de memoria) antes del gate del dueño.
**Why:** Petición del dueño vía `/pandacorp:learn` (tanda 2). La guía del canvas seguía demasiado laxa: solo el landing se generaba hasta que el dueño pedía página por página; el CTA de conversión salía como enlace de una línea; los prompts eran ambiguos (un "quita la nota" vago dejaba texto residual); la calidad por superficie había que re-investigarla en vivo. Lección fuente: memoria `claude-design-skill-improvements` de `personal-page-v2` (ahora `promotion: approved`).
**Files:** `plugin/skills/design/SKILL.md` (procedimiento EXPLORE → cadencia 2 etapas + disciplina de prompts + jerarquía + contenido real; regla DR-058 + nueva regla DR-101), **NUEVO** `plugin/skills/design/references/surface-playbooks.md`, `plugin/.claude-plugin/plugin.json` (v9.27.0). Fábrica: `factory/decisions/registry.yaml` (DR-101; notas de DR-058/DR-062 extendidas), `factory/standards/design.md` (§9 jerarquía, §10 contenido real, §11 cadencia 2 etapas + prompts, §12 rúbrica; §7 + cta-band de cierre; checklist §1c extendido; tabla de wiring §9→§13), `factory/decision-log.md`. Guía interna de la fase de diseño (la consumen el skill `design` + agentes designer/reviewer) — como DR-058/DR-062, **sin cambio en la rule-library/OVERLAY** (no inyecta regla por-proyecto). Manual MC: catálogos de standards/DR auto-derivan; el flujo visible + gate visual del dueño no cambian, así que sin edición de página narrativa. Activación: commit + `claude plugin update pandacorp@panda-corp` + reiniciar.

## 2026-06-28 — blueprint + work-orders precisos para build IA: readiness gate, marcador bloqueante, right-sizing, anti-omisión (DR-100) · v9.26.0
**What:** Endurecidos `/blueprint` + `/work-orders` (y `/spec` donde tocaba) para que los docs salgan precisos —ni más ni menos— y el build 100% IA sea ideal. Siete piezas: **(1) Readiness gate** (cohesión estilo BMad `bmad-check-implementation-readiness`): antes de que un blueprint pase a `ACTIVE` —y re-chequeado en el preflight de `/implement`— el architect verifica fail-closed: cada REQ→componente, cada AC cubierto por EXACTAMENTE UN work order, data model sin TBD, `dependsOn` acíclico+completo, artifacts de wave disjuntos por diseño, foundation completa, contratos `docs/api/<wo>.md` materializados antes del consumidor, y sin `[NEEDS CLARIFICATION]` residual. **(2) Marcador `[NEEDS CLARIFICATION: q]`** (Spec Kit): una pregunta abierta que CAMBIA el build bloquea (distinto de `[ASSUMPTION]`, decisión tomada); cableado fail-closed en `verify.sh` (marcador en doc `ACTIVE` = RED; DRAFT puede llevarlo). **(3) Right-sizing con techo:** la política COARSE (DR-050) es el suelo; el techo ahora es concreto (no revisable de una sentada / fuera de la zona fiable de contexto / mezcla >1 preocupación → partir). **(4) Anti-sobre-especificación:** si un blueprint/WO "se lee como código", sobra; especificar a la altitud (contratos/shapes); un ejemplo canónico > enumerar cada caso. **(5) Completitud 20%** (anti-omisión): la IA entrega el 80% happy-path y omite el 20% de producción (error por failure mode / validación más allá de tipos / estados vacío-parcial / observabilidad / seguridad) — el FRD nombra los que aplican, el Status-Note del WO enumera AC met/unmet (surfacing, NO auto-certificación — regla 4 intacta), el reviewer lo verifica (un faltante BLOQUEA). **(6) Contratos estructurados** (input/success/errors/invariants). **(7) Calibración EARS** (>~3 precondiciones → tabla).
**Why:** Petición del dueño (voz): que blueprint y work-orders salgan SIEMPRE precisos para que la implementación con IA sea ideal, con libertad para tocar `spec`. Deep-research (28 fuentes, 125 claims, 24 confirmados con verificación adversarial 2/3, 1 refutado) cruzada con el mapa interno de las skills: el pipeline FRD→blueprint→work-orders está validado por Kiro/BMad/Spec Kit/EARS — las "bones" son correctas; el hueco era ENFORCEMENT (la precisión vivía como prosa, no como gate). Es la **contraparte de build de DR-095** (que arregló el lado spec/FRD). Un dato concreto se REFUTÓ en verificación y no se usa; el principio "una preocupación por tarea" que tocaba se sostiene por sí solo (curse-of-instructions + mezclar tipos de trabajo es anti-patrón) y es lo que codifica el techo de la work order.
**Files:** `plugin/skills/{blueprint,work-orders,spec}/SKILL.md`, `plugin/agents/{architect,product-manager,reviewer}.md`, `plugin/templates/docs/{frd,work-order,blueprint}-template.md`, `plugin/templates/stack-a-nextjs/verify.sh` (residual-ambiguity gate), `plugin/.claude-plugin/plugin.json` (v9.26.0). Factory: `factory/decisions/registry.yaml` (DR-100), `factory/standards/{build-orchestration.md §2/§3b, quality.md}`, `factory/decision-log.md`. Manual: `concepts/{el-pipeline,el-equipo}.md` (catálogos de skills/agents/DR auto-derivan). Activación: commit + `claude plugin update pandacorp@panda-corp` + reiniciar.

## 2026-06-27 — Silencio al commitear con WIP ajeno (DR-099 mecanismo 3, endurecido) · v9.25.0
**What:** La regla de aislamiento de conversación (DR-099 mec. 3) ahora dice explícito qué hacer al COMMITEAR cuando `git status` muestra ficheros que esta sesión no tocó (WIP de otra sesión): `git add` SOLO de los ficheros propios (nunca `git add -A`) y commitea — sin preguntarle al dueño si commitear, sin mencionar los ficheros de la otra sesión. Commitear solo lo propio es correcto, no necesita permiso y deja intacto lo ajeno (DR-093 no-sweep); pararse a narrarlo o preguntar es el ruido que la regla elimina.
**Why:** Petición del dueño — una sesión de PersonalPage V2, a mitad de un fix de plugin, corrió `git status`, vio el WIP de `TimelineView.tsx` de otra sesión en el checkout compartido de la fábrica, y SE DETUVO a narrarlo + preguntar "¿commiteo solo mis 5 archivos o lo dejo?". Es el MISMO ruido cruzado, pero de fuente DISTINTA al volcado del Stop-gate que se arregló antes hoy (aquello era el hook; esto es la narración del agente al commitear). DR-099 decía "no narres otras sesiones" pero no detallaba el momento del commit, así que el agente preguntaba por defecto. Esto cierra el hueco. Sigue siendo regla BLANDA (comportamiento del agente, no cableada en hook) — el fix duradero es el aislamiento en worktree (ahí `git status` solo muestra lo tuyo); y cualquier regla solo llega a una sesión tras `claude plugin update` + `/upgrade` + reiniciar.
**Files:** `factory/decisions/registry.yaml` (DR-099 mec. 3), `factory/standards/build-orchestration.md` (§9), `plugin/templates/shared/.pandacorp/guide.md.tpl`, `plugin/.claude-plugin/plugin.json` (v9.25.0). Overlay live: `mission-control/.pandacorp/guide.md`; Manual `concepts/estandares-y-reglas.md`. Canonical/why: `factory/decision-log.md`.

## 2026-06-27 — `design` skill: hardened Claude Design EXPLORE path (DR-058)
**What:** Rewrote the EXPLORE-path Claude Design bullet + the DR-058 rule in `skills/design/SKILL.md`: correct connection facts (owner-driven canvas is the generator; `DesignSync` tool = file sync only; one-time `/design-login`; `/design-sync` push-only; `/design` may be absent; never assert commands from the filesystem), the procedure (create project → upload ALL context → ONE exhaustive prompt → pull-and-review → integrate), token authorship, and a pointer to the new `factory/standards/design.md` §1c **anti-omission completeness checklist** (every component + every state + image placeholders + mandatory motion layer + breakpoints by `target_platforms`).
**Why:** Real-session learnings from `personal-page-v2`: the old text assumed a non-existent agent-driven generation flow, and a thin brief let Claude Design ship a flat, half-empty system (no menu/avatar/cards/social/forms/states).
**Impact:** `plugin/skills/design/SKILL.md`; `plugin/.claude-plugin/plugin.json` v9.23.0→v9.24.0 (MINOR). Factory side: `factory/standards/design.md` (§1c), `factory/decisions/registry.yaml` (DR-058), `factory/decision-log.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-27 — El Stop gate se calla ante un rojo ajeno (DR-099 mecanismo 4) · v9.23.0
**What:** `verify-before-stop.sh` ahora **atribuye** el rojo antes de avisarte. `warn-adhoc-write.sh` registra cada edición de la sesión en `.pandacorp/run/sessions/<session_id>.touched` (runtime, gitignored); al fallar el gate, compara los ficheros sucios + culpados (por basename) contra ese registro, y si **ningún** fichero que esta sesión tocó está implicado, **deja terminar en silencio** (lo apunta en `run/foreign-red.log` para PULL, sin stderr) en vez de volcar el fallo de otra sesión a la conversación. Fail-closed: un rojo que toca algo que la sesión editó, o que no puede atribuir (sin registro), **sigue bloqueando**.
**Why:** Petición del dueño — en un checkout compartido el gate de programa-completo falla por el WIP en vuelo de otra sesión, y el Stop hook seguía volcándote ese fallo ajeno ("el gate está rojo por el FRD-21 de la otra sesión…"). Eso le estresaba y no le servía: "si no es de esta sesión, ni me lo menciones; solo interrúmpeme si de verdad necesita mi decisión". DR-099 ya decía manejar el rojo ajeno en silencio, pero el hook lo contradecía. Esto le da dientes en el gate. El caso que sí necesita decisión (un conflicto irresoluble) lo sigue sacando el hand-back ruidoso (mecanismo 2), no el gate.
**Files:** `plugin/scripts/verify-before-stop.sh`, `plugin/scripts/warn-adhoc-write.sh`, `plugin/.claude-plugin/plugin.json` (v9.23.0). Factory: `factory/decisions/registry.yaml` (DR-099 mecanismo 4), `factory/standards/build-orchestration.md` (§9). Overlay: `guide.md.tpl` + `mission-control/.pandacorp/guide.md`; Manual `concepts/estandares-y-reglas.md`. Canonical/why: `factory/decision-log.md`. Probado: harness de 8 casos + test e2e del hook (ajeno→silencio, propio→block).

## 2026-06-27 — merge-queue: limpiar el manifiesto huérfano tras fusionar (.json) · v9.22.2
**What:** `merge-queue.sh` (plantilla + materializado MC, byte-idénticos) borraba el manifiesto del worktree como `run/worktrees/$BRANCH`, pero `worktree-bootstrap.sh` lo escribe como `$BRANCH.json` → tras cada merge quedaba un `.json` huérfano acumulándose. Corregido a `$BRANCH.json`.
**Why:** Basura de runtime que se acumulaba. Impacto bajo: ni el indicador "⎇ pendientes" de MC ni `pending-work.sh` leen ese directorio (ambos derivan de `git worktree list` + `git branch --no-merged`), así que NO causaba fantasmas — pero la limpieza debía ser correcta. Detectado al dogfoodear la cola dos veces en una sesión (quedaron 2 `.json` huérfanos).
**Files:** `plugin/templates/shared/.pandacorp/merge-queue.sh`, `mission-control/.pandacorp/merge-queue.sh`, `plugin/.claude-plugin/plugin.json` (v9.22.2).

## 2026-06-27 — Plantilla del spec-digest: orden problema-primero + reglas de autoría de cards · v9.22.1
**What:** `templates/docs/spec-digest-template.md` actualizado para que el skill `/spec` genere un digest que rinde escaneable en el tab Spec de Mission Control: **El problema primero** (como lista de bullets, no párrafo), **Hipótesis de valor** como UN statement corto (callout), y la regla clave de las cards `- **Título** — descripción` (el paréntesis va DENTRO de la descripción, nunca entre el `**` y el `—`). Refleja la mejora del render (MC: parser tolerante + kinds `bullets`/`highlight`, alcance→checklist, fuera sin tachado).
**Why:** Al cambiar el contenido del digest, el render quedó feo (usuarios/métricas "puro título" por un bullet mal autorado, hipótesis muro de texto, fuera tachado). Se arregló el render en MC y se ajustó la plantilla para que la PRÓXIMA pasada del skill genere directamente el formato bueno.
**Impact:** `templates/docs/spec-digest-template.md` (contrato de render + mini-ejemplo). Lado MC: `lib/spec/spec.ts`, `SpecDigest.*`, `frd-02` AC-02-011.2 (decision-log de Mission Control). PATCH (ajuste de plantilla, sin cambiar la capacidad del skill). v9.22.0→v9.22.1.

## 2026-06-27 — El gate visual respeta target_platforms (DR-074) · v9.22.0
**What:** `visual.spec.ts` deja de comparar una foto de ancho mobile en proyectos solo-escritorio: el test visual por superficie hace early-return (pase vacuo) para el "project" mobile de Playwright cuando `target_platforms` no incluye mobile, cableado sobre el MISMO `TARGETS_MOBILE` que ya usa `responsive.spec.ts`. Early-return y no `test.skip` (Biome `noSkippedTests`).
**Why:** MC es solo-escritorio pero el gate visual seguía sacando fotos a 390px — el layout de escritorio aplastado, ruido que fluctuaba con el contenido/tiempo y ponía el gate en rojo una y otra vez (esta sesión incluso bloqueó un merge ajeno). La responsive ya se saltaba; ahora `target_platforms` elige los anchos para AMBOS gates y mata la clase de falsa alarma de raíz.
**Files:** `plugin/templates/stack-a-nextjs/e2e/visual.spec.ts` (plantilla) + `mission-control/e2e/visual.spec.ts` (materializado, idéntico, DR-076); borradas `mission-control/e2e/visual.spec.ts-snapshots/*-mobile-mobile-darwin.png`; re-bendecida `portfolio-desktop`; `plugin/.claude-plugin/plugin.json` (v9.22.0). Canonical/why: `factory/decision-log.md`.

## 2026-06-27 — Reforzar la regla de aislamiento paralelo (DR-099) · v9.21.0
**What:** El aislamiento de DR-096 deja de ser una regla *blanda*. Tres mecanismos: (1) el hook PreToolUse de escritura (`warn-adhoc-write.sh`) avisa de aislarse cuando se edita código de producto **directo en el checkout principal** (git-dir sin `/worktrees/`) y sin build activo — proactivo, atribuible, no-bloqueante (un hook no puede distinguir fiable un cambio significativo de un micro-edit, así que recuerda en vez de atrapar; se suprime dentro de un worktree y durante un build, DR-060); (2) `merge-queue.sh` dispara una **notificación de escritorio** en todo merge que no puede aterrizar (conflicto 10 / gate rojo 11 / main ocupado 12); (3) regla de **aislamiento de conversación** — un rojo ajeno se maneja en silencio, no se narra al dueño lo que hacen otras sesiones (estado cross-sesión es PULL vía Mission Control), nunca "hecho" hasta estar en main.
**Why:** El agente construyó FRD-21 directo en el checkout principal ("el árbol está quieto") en vez de un worktree, dejando WIP sin comitear que puso en rojo el gate de otra sesión — justo lo que DR-096 evita, pero era una regla blanda que el agente racionalizó (y ni estaba viva en esa sesión ya en marcha). El dueño pidió cablearla.
**Files:** `plugin/scripts/warn-adhoc-write.sh`, `plugin/templates/shared/.pandacorp/merge-queue.sh`, `plugin/templates/shared/.pandacorp/guide.md.tpl`, `plugin/.claude-plugin/plugin.json` (v9.21.0), `plugin/templates/OVERLAY_VERSION` (8.46.0). Factory: `factory/decisions/registry.yaml` (DR-099), `factory/standards/build-orchestration.md` (§9). Canonical/why: `factory/decision-log.md`.

## 2026-06-27 — Los cambios directos avanzan el estado del work order (DR-097) · v9.20.0
**What:** Regla del overlay (propagable): quien implementa un cambio **directamente** (a mano, dentro o fuera de `/implement`) es dueño de mover el `implementation_status` del WO a la realidad — `IN_PROGRESS` mientras construye, `VERIFIED` en cuanto el gate verde (`.pandacorp/verify.sh`, scripts/CI objetivos) pasa limpio. Nunca dejar trabajo terminado y en verde varado en `IN_REVIEW`; el tablero deriva la columna Kanban de ese campo.
**Why:** El dueño terminó FRD-21 a mano y los WO quedaron en `IN_REVIEW` (se veían como no-hechos) porque el agente sobre-aplicó la regla 4 de la constitución (trató al `reviewer` como obligatorio para VERIFIED). La regla 4 prohíbe la **auto-evaluación**; el gate `verify.sh` ES el verificador objetivo, y estaba verde. El trabajo manual debe llegar a VERIFIED con el gate verde, no esperar a un reviewer que nadie corre. El dueño pidió codificarlo.
**Impact:** `plugin/templates/shared/.pandacorp/guide.md.tpl` (+ `mission-control/.pandacorp/guide.md` vivo); fábrica: `factory/decisions/registry.yaml` (DR-097). v9.19.0→**v9.20.0** (MINOR); `OVERLAY_VERSION` →**8.45.0**. Pairs con DR-096.

## 2026-06-26 — Sesiones manuales en paralelo: aislamiento por worktree + merge queue serializada + visibilidad (DR-096) · v9.19.0
**What:** Estándar propagable para el workflow del dueño de **N conversaciones en paralelo lanzadas a mano** (fuera de `/implement`). Una sesión que va a hacer un **cambio de código no-trivial** (la misma frontera del write-gate; los micro-edits siguen in-tree) **se auto-aísla en su propio git worktree** — el agente llama a `EnterWorktree` él mismo, sancionado por una regla permanente del overlay (SIN skill orquestador). Nuevos scripts ship por plantilla: `worktree-bootstrap.sh` (pnpm install hardlink, `launch.json` autoPort vía `jq` que quita `--port`, secretos referenciados, DB por worktree o `PANDACORP_FACTORY_ROOT`) y `merge-queue.sh` (lock atómico `merge.lock` en el git dir compartido → rebase sobre main actual → gate de integración → ff-merge → quita el worktree; conflicto no auto-resoluble o gate rojo = devuelto). `verify-before-stop.sh` gana la atribución "no-es-mío" (lista ficheros sucios + instruye a no enmascarar WIP ajeno). Manifiesto de worktrees activos (gitignored) para visibilidad.
**Why:** Dolor del dueño: lanzar agentes en paralelo reventaba el gate — la sesión A termina limpia pero su `verify.sh` whole-program (tsc/knip/visual leen TODO el árbol) sale rojo por el WIP en vuelo de B, y A, ciega a B, intenta arreglar código que no escribió. Verificado en vivo: el Stop hook salió rojo por un diff visual de `documentacion`-mobile causado solo por el `globals.css`/`Banner` sin comitear de una sesión paralela. No se puede acotar un gate whole-program (tsc lo es) → solo el aislamiento hace cierto "mi gate solo ve mi trabajo". Distinto de `/implement` (colisión-safe por construcción, DR-060/086, NO usa worktrees); este es el caso no-coordinado (operacionaliza DR-093). Con grounding de research (merge-queue, locks atómicos, `CREATE DATABASE … TEMPLATE`, store hardlink de pnpm, el techo del conflicto semántico).
**Impact:** `plugin/templates/shared/.pandacorp/{merge-queue.sh,worktree-bootstrap.sh,pending-work.sh}` (nuevos), `plugin/scripts/verify-before-stop.sh`, `plugin/templates/shared/.pandacorp/guide.md.tpl`; lado fábrica: `factory/standards/build-orchestration.md` (§7 visibilidad ampliada) + `factory/decisions/registry.yaml` (DR-096 §1-4). **Visibilidad (§7):** invariante "worktree que sobrevive = sin mergear" + `pending-work.sh` (une worktrees vivos con `git branch --no-merged`, antigüedad/estado, `--json`/`--notify`) + spec del indicador global "N pendientes" en el shell de MC (primario) + detalle en el resumen del proyecto (secundario). v9.17.0→**v9.19.0** (2× MINOR); `OVERLAY_VERSION` →**8.44.0**. Follow-ups: UI de pendientes en MC (vía `/iterate`), `/pandacorp:merge` opcional. Proposal: `docs/proposals/18`.

## 2026-06-26 — `/spec`: clarification gate (preguntar antes de generar, calibrado por origen) + FRDs completos-no-verbosos (DR-095) · v9.17.0
**What:** Dos refuerzos al product phase para que los docs sean el input ideal de los agentes de build:
- **Clarification gate (paso 4 nuevo en `spec`):** antes de escribir PRD/FRDs, el `product-manager` INFIERE todo lo posible (card/research/perfil/estándares) y lo deja como `[ASSUMPTION: … — fuente]` visibles, y luego pregunta **solo los huecos materiales** (en español, agrupados por lente: problema/scope · usuarios/flujos · datos&reglas · edge cases/errores · métricas · non-goals, sin relleno). **Flag de modo** `--ask`/`--auto`/`--infer`, default por `origin`: `new-idea` (`origin: owner`) → **ask** (siempre pregunta y espera); `discover` (`origin: discovery`) → **auto** (infiere; pregunta solo lo clave que el research no resolvió); `infer` nunca pregunta. Se registra en la sección **Assumptions & open decisions** del PRD.
- **FRDs completos-no-verbosos:** el FRD es el contrato del agente, así que el template ahora exige **data model, edge cases & error states, non-goals, ejemplo trabajado, boceto de contrato** — pero completo-no-verboso (la over-specification satura el attention budget del modelo; el motor ya pasa un slice por agente, DR-056). PRD se queda delgado.
**Why:** El owner pidió hacer la documentación "perfecta" y traer el patrón de preguntas de PandaTrack (`create-frd-package`: "no crear la FRD hasta resolver la ambigüedad crítica"). Deep-research (27 fuentes, 24/25 claims) validó: spec-driven dev (Kiro/Spec Kit/EARS) es la forma correcta; la ambigüedad → código incorrecto; el modo de fallo de over-specification (lost-in-the-middle) es real → "lo más detallado posible" es incorrecto. El stat "66%/d>1.2" de preguntas-de-clarificación fue REFUTADO, así que se enmarca como palanca de alineación, no número. Calibración del owner: new-idea siempre pregunta; discover infiere y solo pregunta lo clave; + flag explícito.
**Impact:** `plugin/skills/spec/SKILL.md` (paso 4 gate + flag + pasos 5/6 renumerados a 5-10), `plugin/agents/product-manager.md` (reglas 11/12 + SOP), `plugin/templates/docs/frd-template.md` (data model/edge cases/ejemplos/contrato), `plugin/templates/docs/prd-template.md` (Assumptions & open decisions), `factory/decisions/registry.yaml` (DR-095). **MINOR → v9.17.0.** Sin cambio de `OVERLAY_VERSION` (skill+agente+plantillas de doc, no toca el overlay). **Manual (DR-046):** la Reference auto-deriva el skill/DR; cambia el FLUJO del product phase → narrativa de `el-pipeline` diferida con el resto del track del Manual de MC. Lado fábrica: `factory/decision-log.md`. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-26 — `/spec` genera el "spec digest" que alimenta el tab Spec de Mission Control · v9.16.0
**What:** El skill `/pandacorp:spec` ahora produce, como nuevo paso 7 del product phase, `.pandacorp/comms/spec-resumen.md` — un resumen de alto nivel en **español** del PRD + research + cada FRD (una card por FRD con resumen de una línea + bloques Overview / User stories / Reglas de negocio / Fuera de alcance / Open questions). Mission Control lo renderiza nativamente en el **tab "Spec"** (entre Propuesta y Documentos), visible solo si el archivo existe. Nueva plantilla `plugin/templates/docs/spec-digest-template.md` con el contrato de marcadores exacto que parsea el tab; se regenera en cada iteración de `spec`.
**Why:** El owner pidió una vista visual, escaneable y en español del spec dentro de Mission Control (como la pestaña Propuesta pero para PRD/research/FRDs), construida e iterada en vivo esta sesión (tab + componente `SpecDigest` + parser `lib/spec/spec.ts` + lector server-only + modal de detalle por FRD). Este cambio cablea el skill para que GENERE el dato que el tab consume, en todo proyecto futuro.
**Impact:** `plugin/skills/spec/SKILL.md` (paso 7 nuevo + renumerado 8/9), `plugin/templates/docs/spec-digest-template.md` (nueva). **MINOR → v9.16.0.** Sin cambio de `OVERLAY_VERSION` (skill + plantilla de doc, no toca el overlay de proyectos). **Manual (DR-046):** la Reference auto-deriva el skill; adición menor de flujo (un artefacto nuevo), narrativa diferida con el resto del track del Manual de MC. Lado Mission Control (el tab + componentes) se formaliza en `mission-control/docs/` (FRD-02 + decision-log) y se commitea. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-26 — Visual coherence at the VALUE level, not just structural patterns (DR-062 reforzado) · v9.15.0
**What:** Strengthened the cross-surface cohesion rule so it explicitly binds at the **token/value level** (colors, type sizes, spacing, radii, card/block styles), not only the structural pattern (page title, tabs, chips). A new page/section/tab must **read its siblings and reuse their values**, and the factory **prefers a shared primitive/token map over a copied `*.styles.ts` constant** so two surfaces can't drift.
**Why:** Live incident this session — Mission Control's new "Spec" tab (SpecDigest) and the existing "Propuesta" tab (IdeaPitch) diverged in title size, description colour, block tints and card title colour (two separate style files re-deriving the same look); the owner saw a visible jump switching tabs and asked to make coherence a binding rule.
**Impact:** `factory/standards/design.md` §7 (canonical, value-level clause + the incident), `plugin/templates/rules/styling-and-ui.md` (new "Visual coherence" section — injectable rule projects inherit). **MINOR → v9.15.0.** Injectable rule changed → `OVERLAY_VERSION` bump owed on the next overlay release (deferred, like prior rule-text refinements). **Manual (DR-046):** the standards/rules Reference auto-derives; this refines an existing concept (DR-062), no new flow/gate → no narrative page change. MC-side fix (IdeaPitch restyled to match SpecDigest) logged in `mission-control/docs/decision-log.md`. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-26 — `/pandacorp:implement`: build parcial por FRD (`frds`) y por change de la cola (`change`) + gate de dependencias · v9.14.0 → v9.15.0
**What:** El skill `/pandacorp:implement` ahora soporta dos modos de build parcial: (A) **por FRD** (`/pandacorp:implement frd-05-settings`) y (B) **por change de la cola** (`/pandacorp:implement change:add-export-button`). Cambios en el engine:
- `PLAN_SCHEMA.unsatisfiedDeps` — el planner reporta las deps del subconjunto pedido que NO están VERIFIED; gate de parada temprana que aborta con mensaje claro en español.
- `const CHANGE` + fase **Process Change** (antes de Plan): cuando `args.change` está seteado, un agente busca el archivo en `.pandacorp/inbox/changes/`, verifica `status: ready`, lo procesa via iterate/bug engine (crea/actualiza FRDs+WOs con `implementation_status: PLANNED`), y devuelve los FRDs afectados que se convierten en el filtro `ONLY` para el resto del build. No archiva la change — la archiva el gate de FRD cuando verifica, igual que el drain normal.
- `let ONLY` (antes `const`) — mutable para que Process Change pueda sobreescribirlo.
- `args.change` y `args.frds` son mutuamente excluyentes; `change` gana.
- Log line actualizada para mostrar `· change <name>` o `· frds <list>` según el modo.
**Why:** El owner quería implementar partes del proyecto de forma aislada, sin correr todo el build. Antes solo había build completo. Un build parcial sin chequeo de deps produce errores crípticos en el gate; un build de change sin procesamiento previo deja los FRDs sin crear.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (meta.phases, inputs, PROCESS_CHANGE_SCHEMA, PLAN_SCHEMA, Process Change phase, dep gate, log), `mission-control/.claude/workflows/pandacorp-build.js` (ídem, copia instalada), `plugin/skills/implement/SKILL.md` (args `frds` y `change` documentados), `mission-control/content/manual/concepts/el-pipeline.md` + `content/manual/guides/g-modo.md` (Manual DR-046). **MINOR (v9.13.0→v9.14.0)**. Proyectos existentes necesitan `/pandacorp:upgrade` para la copia del workflow.
**Refinamiento v9.15.0 (mismo feature):** normalización de inputs en el workflow JS — `frds` acepta folder name / `docs/frds/<folder>` / path-to-frd.md; `change` acepta slug / slug.md / ruta relativa completa. `normalizeFolder()` + `.split('/').pop().replace(/\.md$/, '')` hacen la conversión antes de que el agente los use. Manual comprehensivo añadido: nueva guía `g-implement-parcial.md` (flujo detallado de los tres modos, gate de deps, tabla de cuándo usar cada uno) + tabla de resumen en `el-pipeline.md` § Build.

## 2026-06-26 — `/discover` dedup hardened: never re-recommend, deduped by PROBLEM not title/solution (Gate 3.0) · v9.13.0
**What:** Added a new **Gate 3.0 (DEDUP GATE)** to `/discover` — the FIRST, hard, rendered gate, before founder-fit. For each candidate it names the nearest existing card + the pain each addresses, and **drops any candidate whose CORE PAIN is already on the board in any status (incl. discarded), regardless of a different name, angle, solution, tech, or re-scope**. A genuine improvement/re-scope **updates the existing card, never spawns a second**. Reinforced the EXCLUSION SET build (Step 0 — record each card's *pain*, not just its title/solution), Step 4 (re-check pain before writing any card), and the "Never re-recommend" rule (dedup by problem).
**Why:** The owner caught a duplicate on the board: `pandacheck` ("anti-fake checklist") and `collectible-authenticator` ("AI visual authenticator") are the SAME pain (*"is my collectible real?"*) — the first is a rename/re-scope of the second, yet both became cards. `/discover` was deduping by title/solution, so a re-framed pain leaked through. Owner: "no me repitas ideas; si ya está en el tablero (descartada o no), no me la propongas." The fix makes dedup pain-level and explicit/auditable.
**Impact:** `plugin/skills/discover/SKILL.md` (Step 0 EXCLUSION SET, new Gate 3.0, Step 4, Rules). **MINOR (v9.12.2→v9.13.0)** — new explicit gate in the discover flow. Manual (DR-046): the Reference catalog auto-derives from the skill frontmatter (description unchanged → no hand-copy); this is internal gate-enforcement hardening, not a new user-facing concept/flow, so no concept-narrative page changed. Follow-up candidate (not done): mirror the pain-level dedup into `/new-idea` (it can also revive a discarded card). Board cleanup recommended to owner: `pandacheck` (build) supersedes `collectible-authenticator` (validate) — collapse to one.

## 2026-06-26 — doc-lint advisory gate: EXIT-trap so it can never flaky-RED the verify gate · v9.12.2
**What:** `plugin/templates/shared/.pandacorp/doc-lint.sh` now installs `trap 'exit 0' EXIT` right after `set -euo pipefail`, enforcing its documented "ADVISORY — ALWAYS exits 0" contract no matter how the process terminates. Mirrored verbatim into the live project copy `mission-control/.pandacorp/doc-lint.sh` (kept byte-identical → no `/upgrade` drift).
**Why:** A `/discover` run's Stop hook RED-locked on `.pandacorp/verify.sh` even though only `factory/ideas/*.md` had changed. Root cause: `doc-lint.sh` flakily exited non-zero under output redirection — a reader that closes a pipe early (`grep -q`/`head -1`) SIGPIPEs its writer, and under `pipefail`+`inherit_errexit` that killed the script *before* its final `exit 0`. `verify.sh` invokes it without `|| true`, trusting the always-0 contract, so the flake propagated to the gate. The EXIT trap is the robust, contract-honouring fix (the script still prints every advisory finding — nothing is silenced). Reproduced the flaky non-zero under redirection, then verified green ×10 + full `verify.sh` exit 0 (60 e2e pass).
**Impact:** `plugin/templates/shared/.pandacorp/doc-lint.sh` + `mission-control/.pandacorp/doc-lint.sh` (identical). **PATCH (v9.12.1→v9.12.2)** — a gate-robustness fix, no skill/flow/behavior change. Defense-in-depth follow-up (not done here): also guard the call site in `plugin/templates/stack-a-nextjs/verify.sh` with `|| true`.

## 2026-06-26 — Gold-standard worked example so generated cards render identically · v9.12.1
**What:** Added `plugin/templates/idea-card-example.md` — a COMPLETE, proven-renders-rich idea card (the PandaCheck memo) that `/discover` and `/new-idea` now **mirror** when writing a card. The blank `_idea-template.md` gives the contract; this gives a filled gold reference to copy, so the agent reproduces the exact markers every time (badges line, `###` subsections, problema `> quote`, feature bullets + `- ✓`/`- !` mock, `| Riesgo | Mitigación |` table, evidence `desc — [src](url)`). It lives in `plugin/templates/` (NOT `factory/ideas/`), so it never shows on the board. Both skills also now state explicitly that **colours/charts/bold are applied AUTOMATICALLY by the IdeaPitch component from the markers — the agent never "paints" them** (so the output is consistent, not LLM-variable on styling).
**Why:** Owner asked for assurance that skill-generated proposals will look exactly like PandaCheck (colours, charts, mock, bold). The format was specified, but a filled gold example is the strongest guardrail against LLM format drift — the residual risk that a marker is dropped degrades one element gracefully, never the whole card.
**Impact:** `plugin/templates/idea-card-example.md` (new), `plugin/skills/discover/SKILL.md` (Step 7 — mirror the example), `plugin/skills/new-idea/SKILL.md` (Step 5 — mirror the example). **PATCH (v9.12.0→v9.12.1)** — a reliability guardrail + reference fixture, no behavior/flow change (Manual narrative unchanged; the el-pipeline memo concept still holds; Reference auto-derives). Cross-ref: the format itself is the 2026-06-26 v9.12.0 entry below.

## 2026-06-26 — Idea card body: precise rendering-contract format for the Propuesta tab · v9.12.0
**What:** Pinned the EXACT memo format the idea card body must follow so Mission Control's **Propuesta** tab renders it rich (not flat markdown). `factory/ideas/_idea-template.md` rewritten: the body is now `### subsections` under `## 🔥 De un vistazo` (La apuesta · El problema [PAS prose + a `> blockquote` testimonial] · Por qué ahora · Por qué tú · La visión [4 `- **Title** — desc` bullets → feature grid + an optional `**Vista previa — url**` + `- ✓`/`- !` lines → UI mock]) and `## ❄️ Profundizar` (Mercado · Distribución · Gaps y fuente de la verdad [+ a `| Riesgo | Mitigación |` table → red/green cards] · Riesgo legal · Red team [pre-mortem] · El ask · Evidencia), plus a `> **Badges:** a | b | c` line (pipe-separated → coloured hero pills) and the scorecard table. A "rendering contract" comment documents each marker. `/discover` (Step 7) and `/new-idea` (Step 5) updated to write these markers verbatim.
**Why:** Owner reviewed the live Propuesta tab and wanted it pixel-faithful to the approved HTML demo (`docs/proposals/10b-*.html`): full sections, the feature grid, the checklist mock, red/green risk↔mitigation tables, coloured critical text, the badges — and "the SKILL must emit it this way, not just the example card". The old template used flat one-line bullets, which the component couldn't render as rich blocks. The component (`IdeaPitch`) parses these markers — see `mission-control/docs/decision-log.md` (same date).
**Impact:** `factory/ideas/_idea-template.md` (rewritten body + rendering contract), `plugin/skills/discover/SKILL.md` (Step 7 markers), `plugin/skills/new-idea/SKILL.md` (Step 5 markers). **MINOR (→v9.12.0)** — a precise output-format capability. Cross-ref: `factory/ideas/decision-log.md` (template) + `mission-control/docs/decision-log.md` (IdeaPitch renderer). MC side ships a worked example card `factory/ideas/pandacheck.md`. Manual: Reference auto-derives; the el-pipeline narrative already covers the memo concept (no flow/gate change).

## 2026-06-26 — Enforcement batch: single-source state, concurrency, back-port detector (DR-092/093/094) · v9.11.0
**What:** **DR-092** reviewer quality lens + `clean-code.md` rule: reject a second independent derivation of shared derived state (compute once in a cached resolver). **DR-093** `build-orchestration.md` concurrency section: isolate concurrent sessions (own worktree/branch), never sweep/edit another session's WIP, append-only shared logs. **DR-094** `upgrade/SKILL.md`: loud back-port detector before the gate-config overwrite.
**Why:** Live failures this session — re-derived guild level (NV3/NV1), parallel session breaking the shared gate 4×, MC biome fix nearly reverted by upgrade.
**Impact:** `plugin/agents/reviewer.md`, `factory/standards/{clean-code,build-orchestration}.md`, `plugin/skills/upgrade/SKILL.md`, `factory/decisions/registry.yaml`. **MINOR v9.10.0→v9.11.0**. `OVERLAY_VERSION` 8.42.2→8.42.3 (clean-code rule changed). Factory side: `factory/decision-log.md`. Proposal 17 P2.6/P2.7/P2.8.

## 2026-06-26 — Resilient visual-fidelity oracle (fallback chain) + UI-FRD oracle enforcement (DR-091) · v9.10.0
**What:** `reviewer.md` per-route fidelity lens gets a **fallback chain** so it never silently no-ops: per-FRD `mocks/<route>` → ELSE the FRD's `visual_source` (global prototype view/shard, rendered live + A/B) → ELSE a `ui: true` FRD with no oracle is a surfaced finding (stays advisory, DR-072). `doc-lint.sh` (template) gains a UI-FRD oracle-completeness check (flags `ui:true` with no `fdd.md`, or empty `mocks/` and no `visual_source`).
**Why:** Verified root cause (proposal 17) of MC's hand-fixed fidelity gaps: MC was never sharded, so the `mocks/`-gated per-route check skipped 17 surfaces while the gate stayed green. The fallback uses the global prototype the FRDs already declare; the doc-lint check catches the un-sharded surface.
**Impact:** `plugin/agents/reviewer.md`, `plugin/templates/shared/.pandacorp/doc-lint.sh`, `factory/standards/build-orchestration.md`, `factory/decisions/registry.yaml` (DR-091). **MINOR v9.9.0→v9.10.0** (new reviewer capability). `OVERLAY_VERSION` 8.42.1→8.42.2 (doc-lint changed, DR-051) → propagates to projects on next `/upgrade`. Manual Reference (DR catalog) auto-derives; narrative unchanged (gate behavior nuance, not a new flow). Factory side: `factory/decision-log.md`.

## 2026-06-26 — `/discover` + `/new-idea` redesign: memo-pitch (hot→cold) output + quality gates · v9.9.0
**What:** Reworked the ideation skills so ideas "make the owner dream AND decide" (he reported none has). `/discover` now: biases to **easy-to-build micro/small apps** (a `large` only with a near-sure monetary case, else re-scope/kill); adds **1-2★ reviews** + **The Mom Test** (past-behavior `prior_workaround`) as primary pain sources; runs new pre-card **gates** — a **founder-fit PRE-FILTER** (Paul Graham's 3 traits, before scoring), **painkiller/vitamin/candy** (H/M/L label with cited evidence, never a gauge), **complexity bucket** (micro/small/large + risk accelerators, no day count), and **ground-truth + legal** (kill question "where does the truth come from?"; legal HIGH→kill / LOW→disclaimer); and renders output as a **memo-pitch ordered hot→cold** (Klaff STRONG): la apuesta · problema as PAS · por qué tú · la visión (the prize) on top; mercado (honest SOM) · **distribución** · gaps+ground-truth+legal · red-team/pre-mortem · el ask below — audited by **SUCCESs + the YC Email Test** before showing. `/new-idea` runs the same **quality gates but NOT the sell layer** — it documents the owner's own idea and proposes improvements (no idea-hunting, no agitation). The card `.md` stays the source of truth; Mission Control's new **Propuesta** tab renders the memo natively (see `mission-control/docs/decision-log.md`).
**The 6 owner rules** (from the proposal review, recorded in `factory/ideas/decision-log.md` 2026-06-26): (1) don't freeze owned-app scope — overlap → propose an **extension**, not a hidden/near-duplicate card; (2) output = visual HTML memo (native React in MC); (3) default bucket micro+small, large only with strong recurrent pain + strong complaints + strong monetization; (4) legal HIGH→kill, LOW→disclaimer; (5) the 3 return paths are EQUAL priority (hero chosen by pain+fit+buildability, not return); (6) market-reality only for monetary ideas — a personal-utility idea is welcome without monetization.
**Why:** Owner feedback: ideas felt misaligned (not *his*), were told cold (no dream), were too complex with unaddressed source-of-truth + legal (the authenticator), and lacked a distribution plan. Researched + adversarially red-teamed via a 12-agent workflow (40+ sources); proposal in `docs/proposals/10-discover-redesign-pitch.html` (+ `10b` worked example).
**Impact:** `plugin/skills/discover/SKILL.md` (full rework), `plugin/skills/new-idea/SKILL.md` (gates without sell), `plugin/skills/explore/SKILL.md` (founder-fit + ground-truth/buildability nudges — pending), `factory/ideas/_idea-template.md` (new frontmatter `complexity_bucket`/`painkiller_class`/`ground_truth_source`/`legal_risk`/`prior_workaround`/`distribution_channel` + hot→cold body). **MINOR (v9.8.3→v9.9.0)** — new compatible capabilities. Manual: Reference catalogs auto-derive; the hand-authored `concepts/el-pipeline` narrative updated in the same change (DR-046). Cross-ref: `factory/ideas/decision-log.md` (2026-06-26 owner rules) + `mission-control/docs/decision-log.md` (Propuesta tab, FRD-02).

## 2026-06-25 — Back-port: `stack-a-nextjs/biome.json` ignores `*.py` + `trailingCommas: all` (DR-076) · v9.8.3
**What:** Back-ported two in-place biome fixes that lived in Mission Control's `biome.json` but not in the template, so the `/pandacorp:upgrade` conformance overwrite (DR-059) would have **reverted** them: (1) `"!**/*.py"` added to `files.includes` (MC has real `.py` helper scripts — `crop-avatars.py`, `embed-images.py`, `.pandacorp/run/*.py` — that biome must not try to process), (2) `"trailingCommas": "all"` in the JS formatter. Bumped `templates/OVERLAY_VERSION` 8.42.0 → **8.42.1** so the change propagates to every project on the next upgrade (DR-051).
**Why:** DR-076 — canonical gate-config files are edited at the SOURCE, never in-place, because conformance overwrites them: a fix made only in a project is silently reverted on the next `/upgrade` AND leaves the template stale for everyone (the same way MC's `knip@6` fix was once lost). Surfaced while preparing MC's overlay upgrade (8.36.1 → 8.42.1): the dry-run diff caught MC's `biome.json` ahead of the template; the owner chose the DR-076 path (back-port, then upgrade). Both fixes are generic-good defaults, so they belong in the template, not a project-local exception.
**Impact:** `plugin/templates/stack-a-nextjs/biome.json` (+2 generic fixes), `plugin/templates/OVERLAY_VERSION` (8.42.0→8.42.1). **PATCH (v9.8.2→v9.8.3)** — template gate-config fix, no skill behavior change. After this, MC's `/pandacorp:upgrade` overwrite of `biome.json` is a byte no-op (template == project). Other projects pick it up on their next upgrade.

## 2026-06-25 — `implement`: review worktrees live under the canonical review-worktrees root (DR-090) · v9.8.2
**What:** Updated `plugin/skills/implement/SKILL.md` (review-worktree bullet): the green-snapshot test worktree now lives under **`/Users/Shared/review-worktrees/<project>`** (a `Shared`-level sibling of `Proyectos/`, named after the project, NOT inside `Proyectos/` and NOT inside `.claude/`), instead of the old `../<project>-review` sibling. Both the create and the per-FRD refresh commands use the new path.
**Why:** Same cleanup as DR-089 for deployments: keep `Proyectos/` for source checkouts only; every derived worktree gets its own predictable `Shared`-level root. The owner found `<project>-review` cluttering Proyectos confusing. `.claude/` was rejected (tool-internal — Claude Code's worktree sweep could delete a deliberately-persistent review worktree).
**Impact:** `plugin/skills/implement/SKILL.md` (+DR-090 path). **PATCH (v9.8.1→v9.8.2)** — path convention, no flow/behavior change. Standard/policy: `factory/standards/infra.md` (Worktrees → review-worktree root) + `factory/decisions/registry.yaml` (DR-090). MC side (the snapshot command it shows): `mission-control/src/lib/snapshot/snapshot.ts` + tests + FRD-14 AC-14-001.1/.2 + Manual "probar" guide — see `mission-control/docs/decision-log.md`. Manual: DR + standards catalogs auto-derive; the hand-authored "probar" guide updated in the same change.

## 2026-06-25 — `release`: local deployments follow the canonical-root rule (DR-089) · v9.8.1
**What:** Added a pointer in `plugin/skills/release/SKILL.md` (internal-deploy step): if an internal project gets an **always-on local deployment**, it MUST follow **DR-089** — placed under `/Users/Shared/local-deployments/<project>/` (outside `Proyectos/`, named after the project, no `-live` suffix), as a production build in an isolated git worktree served via launchd on the reserved port. Points to `factory/standards/infra.md` (Local deployments) + Mission Control's `serve.sh`/`deploy-local.sh` as the reference implementation.
**Why:** MC's always-on deploy had been a worktree named `panda-corp-live` mixed into `Proyectos/`, which confused the owner and poisoned the visual gate. The new factory rule (DR-089) needs a hook in the skill that actually performs releases so future internal deploys land in the right place.
**Impact:** `plugin/skills/release/SKILL.md` (+DR-089 pointer). **PATCH (v9.8.0→v9.8.1)** — guidance only, no skill/agent behavior change. Policy/standard live in `factory/decisions/registry.yaml` (DR-089) + `factory/standards/infra.md`; migration + MC-side changes in `factory/decision-log.md` + `mission-control/docs/decision-log.md`. Manual: DR + standards catalogs auto-derive; no flow/gate changed → no narrative edit.

## 2026-06-25 — `discover` learns from discard REASONS: capture the why, kill matching rejection patterns · v9.8.0
**What:** Closed the feedback loop so a discard *teaches* the factory, not just removes a card. Two parts:
- **Capture (Mission Control, FRD-02):** on discard, MC now also captures **why** — a quick-tag set + free text — and writes it to the card's new `discard_reason` frontmatter field (an extension of the single allowed write, `status: discarded`). Spec'd + queued for the MC build (it's app behavior → the iterate/change flow); the field is documented in `_idea-template.md`.
- **Learn (discover):** Step 0 now reads each `discarded` card's `discard_reason` (+ the profile anti-patterns) and distils the recurring **rejection patterns** (the generalizable reasons — "saturated/strong-incumbent", "B2B sales-heavy", "doesn't leverage my channel", "US-only monetization", "not interested in topic X"). New **kill criterion #9**: a candidate that fits a pattern the owner already rejected is killed *even if the idea itself is brand-new*. Step 1 tells the researchers to avoid those patterns; durable patterns are distilled into `factory/profile.md` (DR-053) so `recommend` + conversation also weigh them. `new-idea` warns before capturing something that fits a rejection pattern.
**Why:** Owner — *"yo descarto una idea, pero ¿hay forma de que recibas el porqué y no me recomiendes cosas con ese mismo patrón de descarte? De ser posible dentro de la app."* Excluding the exact discarded idea (v9.7.0) wasn't enough — he wanted the factory to generalize from the *reason* and stop proposing the same kind of thing. Reused the existing planes (the reason on the card = raw per-idea signal; the generalized preference in the profile = durable) — one data, two granularities, no new file/duplication (the graveyard lesson).
**Impact:** `plugin/skills/discover/SKILL.md` (Step 0 reason→patterns, Step 1 avoid-patterns, Step 3 kill #9, rule), `plugin/skills/new-idea/SKILL.md` (rejection-pattern warning), `factory/ideas/_idea-template.md` (`discard_reason` field — logged in `factory/ideas/decision-log.md`). MC `discard_reason` capture (FRD-02) queued for the MC build (separate). `plugin/.claude-plugin/plugin.json` **v9.7.1 → v9.8.0** (MINOR — new learning capability, backward-compatible; `discard_reason` is optional/additive). **No `OVERLAY_VERSION` change**. **Manual (DR-046):** Reference auto-derives; discovery-flow refinement — narrative deferred to the MC Manual track. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-25 — Visual baseline template: mask live data lists with `[data-volatile]` (DR-088) · v9.7.1
**What:** Added `[data-volatile]` to the exclusion selector in the stack template's visual gate (`plugin/templates/stack-a-nextjs/e2e/visual.spec.ts`), generalizing the per-region exclusions (dashboard-banners / activity-log / summary-text, DR-076). Any region whose content is live, variable-length data that changes the page height is marked `data-volatile` in the component (recommended `style={{display:'contents'}}` so the live layout is unchanged) and masked from the `fullPage` baseline — the baseline asserts the page CHROME, not the data-driven list. The template comment documents the convention so every future project gets it.
**Why:** Mission Control's gate (dogfood) kept failing falsely on `/board` and `/proposals` — baselines coupled to live factory data + cold/warm reflow drifted the page height (e.g. `/proposals` 9769→10203px in one session) and broke the screenshot diff though the routes rendered fine, forcing a manual re-bless on almost every idea-base change. A height-drifting list isn't a fidelity regression. Conformance-checked file (DR-059) → the fix belongs in the template and re-syncs via `/pandacorp:upgrade`; back-ported in the same change.
**Impact:** `plugin/templates/stack-a-nextjs/e2e/visual.spec.ts` (template, the canonical source). Policy: `factory/decisions/registry.yaml` **new DR-088** + `factory/decision-log.md`. MC application + re-blessed baselines: `mission-control/docs/decision-log.md` (2026-06-25). **v9.7.0 → v9.7.1** (PATCH — template conformance file, no skill/agent behavior change). **No `OVERLAY_VERSION` change** (the visual-spec is propagated/conformance-checked, not part of the `.pandacorp/` overlay version). **Manual (DR-046):** the DR catalog auto-derives; no flow/gate/concept changed → no narrative edit. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-25 — `discover` never re-recommends: exclusion via the `discarded` status (one source of truth, NO separate ledger) · v9.7.0
**What:** Fixed `/discover` re-surfacing ideas already on the board or already discarded. Step 0 builds an **EXCLUSION SET** from **every** existing card `factory/ideas/*.md` in ANY status — including **`discarded`** (the Descartado column). Step 4 changed from "deduplicate (merge evidence)" to "**exclude — don't re-surface**": any candidate matching the set is dropped for the whole run (not presented, not carded). Step 1 passes the exclusion set to the researchers (net-new only); Step 7 presents only NEW ideas + a transparent "(omití N ya en base, incl. descartadas)" line; `new-idea` checks discarded cards before reviving. The key invariant added as a rule: **discarding = `status: discarded`, NEVER delete the card** — the Descartado column IS the durable do-not-recommend memory; deleting a discarded card erases that memory and the idea resurfaces. Red-team auto-kills are reported per-run, not persisted (only the owner discards, via MC — the human gate).
**Why:** Owner — *"me recomiendas cosas que ya estaban (SpecPilot, el agregador); no me repitas, ni las que están en investigación ni las descartadas."* Root cause: the old dedup merged evidence but still re-presented existing cards, and **deleting a discarded card erased the memory that it was rejected** (the token-budget idea re-appeared every run). A first fix introduced a separate `_graveyard.md` ledger; the owner correctly rejected it as **duplicating the Descartado column** (same purpose, two sources). Corrected design: the `discarded` status is the single source — discovery reads and excludes it; the only rule needed is *don't delete, mark discarded*. Restored the 4 previously-deleted discards as `status: discarded` cards so the memory + column are intact.
**Impact:** `plugin/skills/discover/SKILL.md` (Step 0/1/3/4/7 + 2 rules), `plugin/skills/new-idea/SKILL.md` (discarded-cards check). Restored `factory/ideas/{ai-token-budget-manager,freelancer-creative-crm,agency-report-generator,multichannel-seller-ai-assistant}.md` as `status: discarded` (logged in `factory/ideas/decision-log.md`). No `_graveyard.md` (rejected as duplication). `plugin/.claude-plugin/plugin.json` **v9.6.1 → v9.7.0** (MINOR). **No `OVERLAY_VERSION` change**. **Manual (DR-046):** Reference auto-derives; flow refinement — narrative deferred to the MC Manual track (mid-re-anchor). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-25 — Dependency-capture rubric in the work-orders skill (DR-087a) · v9.6.1
**What:** Added a "what counts as a dependency" rubric to `plugin/skills/work-orders/SKILL.md` (pointing at `build-orchestration.md`): `dependsOn` must capture the **architectural couplings** (A reads/renders-based-on/is-built-on/extends B), not just sequential build order — a UI surface depends on its libs + the design-system foundations it uses + the data readers it renders + features whose data/engine it renders. Only evidence-based couplings, never speculative.
**Why:** Auditing Mission Control's real dependency graph showed deps were under-captured (READMEs listed only partial build order → a sparse, misleading DAG). Guidance fixes it for future projects.
**Impact:** `plugin/skills/work-orders/SKILL.md` + the canonical standard `factory/standards/build-orchestration.md`. **v9.6.0 → v9.6.1** (PATCH — guidance only, no behavior change). After committing: `claude plugin update pandacorp@panda-corp`.

## 2026-06-24 — Work-order `dependsOn` frontmatter (DR-087): real dependency DAG, no fabricated chain · v9.6.0
**What:** Added a **`dependsOn: [WO-NN-MMM]`** field to the work-order frontmatter template, the work-orders README template ("Depends on" column ↔ frontmatter), and the `work-orders` skill (which now populates it). It is the machine-readable mirror of the Build Plan DAG / README "Depends on" column — the real upstream WOs a work order depends on (`[]` for a root, cross-FRD allowed), never a fabricated chain. Mission Control's dependency DAG (FRD-12) reads it.
**Why:** MC's DAG showed a fake linear chain (every WO ← one other) because the deps lived only in the README prose and the DAG invented a sequential fallback. Making deps machine-readable frontmatter (like `artifacts`) is the durable fix the owner chose.
**Impact:** `plugin/templates/docs/work-order-template.md`, `plugin/templates/docs/work-orders-readme-template.md`, `plugin/skills/work-orders/SKILL.md`; standard `factory/standards/build-orchestration.md` + registry `DR-087`. **v9.5.0 → v9.6.0** (MINOR — new compatible field; absent → `[]`). **`OVERLAY_VERSION` 8.41.0 → 8.42.0** (template + skill changed → projects pick it up on `/upgrade`). MC reader/DAG + backfill land under `mission-control/`. After committing: `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — Build engine writes `.pandacorp/track.jsonl` (durable build-timeline data for the Observabilidad timeline, FRD-12) · v9.5.0
**What:** Building on DR-086, the engine appends a per-project timing log to `.pandacorp/track.jsonl` (committed machine-state) at the points it already touches: `wo_start` (build begins, both the single-impl and split paths), `wo_end` (in `commitWOGreen`, with state — staged + committed with the WO), and `review_start`/`review_end`/`frd_end` (the FRD gate — staged + committed there). New `TRACK` helper mirrors the existing `EMIT` (one append per transition, fire-and-forget). Durable, unlike the global `dashboard-events.ndjson` (rotates; live Party view).
**Why:** The MC Observabilidad timeline needs precise, durable per-WO/FRD durations for every future build; git gives only finish times (distorted for parallel waves) and the event stream is ephemeral. Recording at the engine's existing transitions (DR-086's per-WO commit is the natural `wo_end`) is a small in-place add. Already-built projects (e.g. MC) have no track → the MC timeline falls back to git structure (no fine durations) — inherent, the data wasn't captured at build time.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (TRACK helper + the four append points + track.jsonl staging), `factory/standards/build-orchestration.md` §2, `plugin/templates/shared/.gitignore` (track.jsonl committed), `factory/decisions/registry.yaml` DR-086 (the data layer it enables). `plugin.json` **v9.4.0 → v9.5.0** (MINOR — new capability). **`OVERLAY_VERSION` 8.40.0 → 8.41.0** (`templates/shared` engine changed → projects get it on `/upgrade`). The MC reader (`lib/build-track`) + `TimelineView v2` (FRD-12) land in `mission-control/` separately. **Manual (DR-046):** the timeline-concept narrative lands with the MC view (FRD-12). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — Build engine: commit each work order the instant it greens (finer save points, DR-086) · v9.4.0
**What:** The build engine (`pandacorp-build.js`) now commits **each work order the moment its self-test passes**, via a single SERIALIZED git writer (a promise-chain — one git op at a time) that stages only that WO's own disjoint `artifacts` + its work-order .md, instead of **batching the commit after the whole wave**. So a kill/cancel/internet-drop keeps every already-green WO (committed → `IN_REVIEW` → skipped on resume, never rebuilt) and only the still-building (uncommitted) WOs are redone — a mid-wave cut no longer loses the whole wave (up to 8 WOs in powerful mode). Single-writer serialization preserves the no-`index.lock` / no-merge invariant (DR-060) even while sibling WOs are still writing. `last_green_sha` still advances only at the FRD gate (the verified anchor); per-WO commits are finer RESUME points via the frontmatter.
**Why:** Owner (*"descartar trabajo al 99% por un corte es un desperdicio — commitemos más pronto"*), after a 5-agent + red-team investigation (workflow `build-resume-policy`) confirmed RESTART-from-last-commit is the right policy (resume into partial multi-agent state is unsafe — no merge path) and the correct lever is finer save points. The engine already committed per-wave; the only gap was the batched timing. Now also matches what MC's Manual (`concepts/construccion-desatendida`) already described.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (new `commitWOGreen` in `buildWO`; batched after-wave commit removed; build/self-test prompts), `factory/standards/build-orchestration.md` §2, `factory/decisions/registry.yaml` (new **DR-086**), `factory/decision-log.md`. `plugin.json` **v9.3.0 → v9.4.0** (MINOR — backward-compatible engine improvement). **`OVERLAY_VERSION` 8.39.0 → 8.40.0** (`templates/shared` engine changed → projects get it on `/upgrade`). Restart policy reaffirmed, not changed (refines DR-060/067). **Manual (DR-046):** DR catalog auto-derives; the narrative `concepts/construccion-desatendida` already describes per-WO commit → no narrative edit. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — `discover` market-reality check: evaluate monetization/distribution in the owner's country (Perú/LATAM), never a US default · v9.3.0
**What:** Added an **owner's-market reality check** to discovery. Step 0 now reads the owner's **country/market** from the profile; Step 1's researcher must verify, per idea, that the **revenue rail** (affiliate program, payment processor, ad network) AND the customer base actually exist and *pay* from that country — not a US default. New **kill criterion #8** ("doesn't work in the owner's market") is **kill-or-reframe**: if the monetization premise silently assumes US/abroad infra, either *reframe* (build-for-others — owner is the builder, NOT the first monetizing user; name that validation risk) and/or find the in-market model (local affiliates, TikTok Live, sponsorships, own store), or *kill* only if money was the sole value and no in-market model exists. New rule + `new-idea` aligned + the template's Return section now asks to validate the rail in-market.
**Why:** Owner caught the gap on a live re-eval of PandaShelf (the discovery hero): the card assumed US-style affiliate monetization (Entertainment Earth / Tokyo Otaku Mode) without checking Perú. Research confirmed his instinct — **Amazon Associates excludes Perú**, Japan proxies (ZenMarket/Buyee) pay only store credit, local geek stores have no formal affiliate programs; only Buscalibre (5%, books only), MercadoLibre Afiliados PE and (via PayPal, low-conversion) HLJ/Entertainment Earth partly work. A US-lens evaluation would keep recommending a monetization that doesn't pay the owner. The factory must always contextualize to where the owner actually operates.
**Impact:** `plugin/skills/discover/SKILL.md` (Step 0 country, Step 1 market-reality input, Step 3 kill criterion #8, new rule), `plugin/skills/new-idea/SKILL.md` (step 3), `factory/ideas/_idea-template.md` (Return section in-market validation — logged in `factory/ideas/decision-log.md`). `plugin/.claude-plugin/plugin.json` **v9.2.0 → v9.3.0** (MINOR — new evaluation dimension, backward-compatible). **No `OVERLAY_VERSION` change**. **Manual (DR-046):** Reference auto-derives; the market-check is a refinement of the kill-gate concept already noted for the deferred MC Manual reconciliation. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — `discover` becomes a DECISION engine: red-team kill-gate + explicit verdict + fold-in detection + curated shortlist · v9.2.0
**What:** Reframed `/discover` from "inform" to "**help the owner decide**". Builds on v9.1.0 (which added the value-selling sections) with a full decision layer:
- **Red-team kill-gate (curation).** Every candidate runs an adversarial pre-mortem against explicit **kill criteria** (no distribution to first 10 · vitamin-not-painkiller · no observable demand · well-funded incumbent without a defensible wedge · too complex for the weeks window · not founder-fit · market = "everyone"). Failures are **killed** — they never become cards, only a one-line "descartadas y por qué" in the report. A valid problem with a strong incumbent + no wedge, or no distribution, is now a *discard*, not a card.
- **Explicit asymmetric verdict** per survivor: **🏆 build / 🧪 validate (7-day test) / 🔌 integrate (into an app the owner already owns)** — new frontmatter `verdict` + `the_bet` + `fold_into` + `why_now` + `kill_risk` + `validation_step`.
- **Fold-in detection.** New Step 0 reads `factory/portfolio.md` + the owner's existing project folders so an idea can be judged *standalone* vs *a feature of something he already has* (prefer integrate over a near-duplicate).
- **Curate, don't buffet.** ~4 survivors total (≈2 aligned + 2 general, profile-weighted), replacing the old "~3-5 per stream" pile.
- **Card = a 1-page decision memo, TL;DR-first** (`_idea-template.md` restructured): *Veredicto + la apuesta* on top → *De un vistazo* (storytelling problem · diferencial · why-now · founder-fit · #1 risk · 7-day test · scorecard) → *Profundizar* (solución, features, similares, comparación, retorno, red team, evidencia, notas).
- **Report = a decision view**: the hero + a scannable verdict table + "descartadas y por qué" + "integrar en lo que ya tienes". The researcher streams now also return the kill-gate/scorecard inputs (demand strength, incumbent strength, distribution/founder-fit, why-now, effort-weeks). `new-idea` got the matching alignment (verdict + fold-in check + the conviction hooks + the memo body).
**Why:** Owner feedback after reading a 9-idea `/discover` run: *"veo la información y no sé qué decisión tomar… ninguna me llama, necesito ese wow factor que me diga: esta es."* The flat list of 9 was a buffet (the brain optimizes for not-being-wrong, not for deciding); there was no curation, no explicit verdict, no conviction hooks (why-you / why-now / what-winning-looks-like / an honest surviving risk), and no awareness of the apps he already owns (he asked whether an idea like the collection storefront could fold into his existing PandaTrack). Grounded in a research pass on decision frameworks (RICE/ICE/weighted-scoring + Gary Klein pre-mortem + Amazon/VC 1-page memos) and a read of his sibling apps (pandatrack/buytrack/manga-watch/cotizapdf).
**Impact:** `plugin/skills/discover/SKILL.md` (rewritten: Step 0 inventory, Step 1 richer researcher output, Step 3 kill-gate, Step 5 verdict+scorecard+fold-in, Step 6 curated memo cards, Step 7 decision-view report, new rules + description), `plugin/skills/new-idea/SKILL.md` (steps 3/4/5 aligned). The card-format/template side is logged in `factory/ideas/decision-log.md` (same date): `factory/ideas/_idea-template.md` (decision frontmatter + 2-layer memo). `plugin/.claude-plugin/plugin.json` **v9.1.0 → v9.2.0** (MINOR — richer skill behavior, backward-compatible; old cards stay valid, the new frontmatter is additive/optional). **No `OVERLAY_VERSION` change** (skills + the versioned idea template only). **Manual (DR-046):** the commands Reference auto-derives the updated `discover`/`new-idea` descriptions; the *concept* of discovery-as-decision (kill-gate + verdict) is worth a line in the Manual's discovery/pipeline narrative when MC's Manual is next reconciled — noted, deferred to the MC track (mid-re-anchor). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — `discover`/`new-idea` cards sell the value: similares + differential + dream features + storytelling problem · v9.1.0
**What:** Enriched the discovery output so every idea card *convinces*, not just informs. The `researcher` streams now return, per opportunity, the **similares** (real existing products/competitors, named + link), the **differential / wedge** (concretely why ours is better/different and where the value is — a me-too with no edge is flagged weak), and **dream features** (3-6 aspirational capabilities). The card body (`discover` step 4 + `_idea-template.md`) gains three sections after *Solución* — **Features**, **Similares (qué ya existe)**, **Comparación y valor diferencial (por qué el nuestro)** — and the **Problema is rewritten as storytelling** (a concrete scene that puts the owner in the shoes of whoever suffers the pain). The report (step 5) now gives a one-line **value pitch** per idea so the wedge is visible without opening the card. `new-idea` (the shared crystallizer) got the matching light touch (research the *similares* + differential in step 3; fill the new template sections in step 5) so the two card-creating skills stay consistent.
**Why:** Owner feedback on a live `/discover` run — the proposals read as flat feature lists and didn't land: *"no veo nada que me convenza… hazme soñar"*. He wanted to (1) see the **value**, (2) see **what already exists** and **why ours adds more**, (3) get high-level **features that make him dream**, and (4) feel the **problem in the user's shoes** (storytelling). The cards captured pain + evidence but never the competitive wedge or an emotional hook, so nothing stood out as "wow".
**Impact:** `plugin/skills/discover/SKILL.md` (step 1 researcher output + step 4 card anatomy + step 5 report + 2 new rules), `plugin/skills/new-idea/SKILL.md` (step 3 + step 5). The card-format/template side is logged in `factory/ideas/decision-log.md` (same date): `factory/ideas/_idea-template.md` (three new sections + storytelling Problem). `plugin/.claude-plugin/plugin.json` **v9.0.0 → v9.1.0** (MINOR — richer skill output, backward-compatible; existing cards stay valid, the new sections are additive). **No `OVERLAY_VERSION` change** (skills + the versioned idea template only — no `templates/shared` or `stack-*`). **Manual (DR-046):** the commands Reference auto-derives the `discover`/`new-idea` descriptions (unchanged → catalog already current); this refines card *content*, not a pipeline/team/plugin **concept**, so **no hand-authored narrative page is the matching target** — nothing to sync. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-23 — Lifecycle redefined: `operation` folds into `release`, the audit moves into construction, `deploy_target` for internal-vs-external (DR-085) · v9.0.0
**What:** The plugin-side encoding of the owner's pipeline redefinition (full rationale in `factory/decision-log.md` + `factory/decisions/registry.yaml` DR-085). The lifecycle is now **six phases** (research → product → design → architecture → implementation → release), and the skills are reframed:
- **`release/SKILL.md`** — release now **= deploy/launch** (internal OR external) + the launch plan, NOT "audit + deploy". The security audit, quality gates and telemetry/metrics verification are **removed** from here (they moved into construction). It branches on **`deploy_target: internal | external`** (a new `status.yaml` field, not a phase): internal = run the in-house tool as-is (e.g. `127.0.0.1`, no production gate / landing / GTM); external = staging → human production gate (DR-004) → prod + launch plan. Sets `phase: release` (the launched/terminal phase) + the card → shipped/lanzada.
- **`implement/SKILL.md`** — added the **final hardening step** as construction's last act (once every FRD is `VERIFIED`): security audit (OWASP + ASI Top 10), telemetry/metrics verification, quality close-out. The build is not "done" until it's secure, instrumented and quality-gated; it ends at `phase: release`. No `operation` phase.
- **`review-launch/SKILL.md`** — reframed as the **post-launch loop UNDER the release phase**, not a separate phase; precondition changed `phase: operation` → **`phase: release`**. Reaching `release` (internal or external) triggers eligibility.
- **`adopt/SKILL.md`** — phase enumeration drops `operation` (valid: product|design|architecture|implementation|release); a launched/live project maps to `release` + records `deploy_target`; the portfolio-row condition and next-step report updated.
- **`new-version/SKILL.md`** — a shipped project sits at `phase: release`; implement runs the hardening; release deploys/launches.
**Why:** Owner — *"release y operación son lo mismo; el concepto es un release de software, interno o externo; la auditoría/calidad/seguridad/métricas es el último paso de la construcción, no una fase posterior."* The old split (release=audit+deploy vs operation=live) had no clean way to express an in-house tool that is genuinely launched but never externally hosted (like Mission Control), and it deferred hardening to a separate phase. Folding `operation` into `release`, moving hardening into construction, and tracking internal-vs-external as `deploy_target` aligns the plugin with how the owner ships and with Mission Control's six Campaña rooms.
**Impact:** `plugin/skills/{release,implement,review-launch,adopt,new-version}/SKILL.md`; `factory/decisions/registry.yaml` (new DR-085 + DR-043 reframed); `factory/standards/{build-orchestration.md §5b/§6, observability.md, infra.md}`; root `CLAUDE.md` (operation list + rule-5 board mapping). `plugin/.claude-plugin/plugin.json` **v8.45.0 → v9.0.0** (MAJOR — redefines the lifecycle, a breaking conceptual change incompatible with the old phase vocabulary). **No `OVERLAY_VERSION` change** (skills + standards + registry only — no `templates/shared` or `stack-*` touched, so no project re-sync needed). **Mission Control** (the 6-room Campaña + phase→column derivation + status.yaml `deploy_target` reads) is owned by the other agent under `mission-control/` — not touched here. **Manual (DR-046):** the Reference catalogs (commands + DR) auto-derive the updated skills + DR-085; the hand-authored pipeline-concept narrative is part of the MC Manual, deferred to its owner. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-22 — `sync`: gap classification is DIRECTIONAL — never document a bug or an unbuilt feature down into the spec (DR-081 refinement) · v8.45.0
**What:** Refined `/pandacorp:sync` so a code↔doc gap is classified by **direction** before anything is written. Only **code-ahead-of-doc** is documented (the code outran the spec → write the cascade); a **deliberate** behaviour change updates the doc (confirmed at the intent gate). A gap where the **doc is right** is NEVER closed by editing the doc down to the code: a **bug** (the code contradicts a correct doc) is **routed to `/pandacorp:change`/`bug`** to fix the code, the doc untouched; a **documented-but-unbuilt** feature (specified, never implemented *"por diferentes motivos"*) is **marked pending** (the PRD feature-landscape / the work order's `implementation_status`), the spec untouched. The intent gate now sorts each divergence into one of **five actions** (document / update-doc / it's-a-bug / pending / experiment) — and it's the only place a bug is told apart from an on-purpose change, which the code alone can't reveal. So `sync` and `change`/`bug` are complementary: `sync` documents code-ahead-of-doc, `change`/`bug` fixes code-behind-doc, and `sync` **detects-and-routes** the latter.
**Why:** Owner question — *"what if the gap isn't new functionality but a bug, or a feature that's documented but was never implemented?"* The skill as first shipped assumed every gap is closed by documenting the code — which would have **blessed a bug as spec** or **erased wanted-but-unbuilt work**. The fix encodes the asymmetry: the code is the oracle of what the app DOES, while the FRD/PRD stays the oracle of what it SHOULD do — the spec is degraded only by a confirmed *deliberate* change, never by broken or absent code.
**Impact:** `plugin/skills/sync/SKILL.md` (step 3 direction-first + step 5 five-action gate + a new rule + the description), `factory/decisions/registry.yaml` (DR-081 default — directional clause), `plugin/.claude-plugin/plugin.json` v8.44.0→**v8.45.0** (MINOR — new handling capability). **No `OVERLAY_VERSION` change** (skill only). **Manual (DR-046):** the commands Reference auto-derives the updated `sync` description; the hand-authored narrative for the reverse flow stays deferred with the rest (MC mid-re-anchor). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-22 — `adopt` registers BOTH planes correctly: board card always, portfolio row once building · v8.44.0
**What:** Refined `/pandacorp:adopt` step 7 (factory registration) so an adopted project lands in **both** Mission Control reads without desync. (1) **Always** create the retroactive idea card `factory/ideas/<slug>.md` — this is what puts the project on the **board** (it freezes as a pointer; the board derives its column from the project's `status.yaml` phase). (2) Add the `factory/portfolio.md` row **only once the build has started** (inferred phase `implementation`+) — the portfolio is the index of projects *en obra/lanzados*; an `architecture`-only adoption lives on the board via its card until `/pandacorp:implement` begins. (3) Fixed the **path convention**: the card's `project:` must be a path `resolveProjectPath` understands (factory-root-relative — `../<slug>` for a sibling, `<slug>` for a project *inside* the factory like Mission Control — or absolute), **never a bare slug**, because the board and the portfolio both resolve via the factory root, not a hardcoded `../`.
**Why:** Owner hit the symptom: Mission Control showed in the **portfolio but not the board**, and (once given a card) landed in the wrong column. Root cause = two reads from two sources with no bridge for a project without a card (the board reads `factory/ideas/`, the portfolio reads `factory/portfolio.md`), plus the **board resolved project paths differently from the portfolio** (`factoryRoot/../slug` vs the canonical `resolveProjectPath`) — so a project living *inside* the factory (MC) never resolved. The board-side resolver bug is fixed in the Mission Control app (see its decision log); this entry makes `adopt` register projects so the two planes stay in sync going forward. MC itself got a hand-authored card (gitignored idea data) to close the immediate gap.
**Impact:** `plugin/skills/adopt/SKILL.md` (step 7 + description), `plugin/.claude-plugin/plugin.json` v8.43.0→**v8.44.0** (MINOR — behaviour change to a skill). **No `OVERLAY_VERSION` change** (skill only — no `templates/`). **Manual (DR-046):** the commands Reference auto-derives the updated `adopt` description; the hand-authored guide narrative was synced in the same change — `content/manual/guides/g-adoptar.md` + the rendered `manualPages.tsx` adopt page (registration step + closing note now say "board always, portfolio once building"). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-22 — New skill `/pandacorp:sync`: the reverse documentation flow (code→docs) for hand-edits (DR-081) · v8.43.0
**What:** A new skill that reconciles a project's documentation **backwards** from changes made directly in the code — the inverse of `iterate`, and **exhaustive by design** (the owner's standard: plenty of well-made docs over too little, nothing unnoticed). The owner hand-edits an app (notably Mission Control) to move fast / iterate visually, outside the `spec→design→implement` / `change` flow; `sync` brings that reality back into the docs. It works in **two modes** — *warm* (changes already in context, still verified against the real diff) and *cold* (no context → a **FULL AUDIT**: enumerate every app surface from the code, compare each to its owning doc, find every code↔doc gap; fan out for a large app). For each blessed change it propagates the **whole cascade**, not just the first-level doc — behaviour→FRD **+ its work order(s) + fdd**; architecture/tech→blueprint **+ ADR + architecture.md + affected work orders**; UI→fdd **+ design tokens/components**; scope→PRD — across **both `docs/` and `.pandacorp/`** (the `comms/summary` Spanish narrative + `status` counters), and records the two-layer `docs/decision-log.md` entry (**asking the Why**, which the diff lacks). It sits behind an **INTENT GATE** (the owner marks each change deliberate=document vs experimental=don't bless — the independent oracle the skill itself lacks); everything written is **marked reconciled-from-code** (provenance, like `adopt`'s as-built). **External design mirror:** if the change moved the shared design system and it has Claude Design provenance (`_ds_manifest.json`, DR-058), `sync` **detects + flags + offers** a re-sync via `/design-sync` — it **never auto-pushes** (an outward-facing write to an external beta service behind the owner's login; the repo's `docs/design` stays the source of truth, DR-054). Closes with the advisory `doc-lint` + a lesson/profile capture (DR-047/053).
**Why:** Owner-asked. The forward pipeline had no incremental reverse path — `adopt` is one-time/full-project/brownfield, `iterate`/`change` go doc→code. The design is anchored on the process-audit meta-pattern (DR-078/079/080 + the `factory-process-audit-2026-06-22` memory): the inverse flow **inverts the oracle** (the code becomes the oracle, the doc conforms — constitution §24), so `sync` **documents but never verifies** — the owner supplies the missing intent oracle at the gate, and provenance-marking stops an accident being blessed as design. The existing `remind-manual-sync` hook (DR-046) already recognised this gap for the MC Manual; `sync` generalises the fix to every doc. Scope was **sharpened the same day by owner feedback** — make it exhaustive (the full cascade, never leave the work order or fdd stale), cover both trees, work cold (a full app↔docs audit), and resolve the owner's open Claude-Design question (researched → detect+offer, never auto-push: the repo is the source of truth, the mirror is refreshed only via `/design-sync` behind the login gate).
**Impact:** `plugin/skills/sync/SKILL.md` (new), `factory/decisions/registry.yaml` (DR-081), `CLAUDE.md` (the inside-project operation list), `plugin/.claude-plugin/plugin.json` v8.42.0→**v8.43.0** (MINOR — new skill; the working tree shows **v8.44.0** because the parallel `adopt` board/portfolio fix bumped on top). **No `OVERLAY_VERSION` change** (a skill only — no `templates/shared` or `stack-*` touched, so no project re-sync needed). v1 scoped to **modo proyecto** by owner choice; *modo fábrica* (plugin/standards hand-edits) deferred to a possible v2, routed to `learn` for now. **Manual (DR-046):** the Reference catalogs (commands + DR) auto-derive `sync`/DR-081 so the catalog is current; the hand-authored narrative page for the new reverse flow is **deferred** with the rest of the MC Manual narrative (MC is mid-re-anchor) — offered to the owner. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-22 — Process-audit fixes wired into the plugin: DR-078/079/080 + self-learning close-out harvest · v8.42.0
**What:** The plugin-side wiring of the 2026-06-22 process audit (full rationale in `factory/decision-log.md` + the `factory-process-audit-2026-06-22` memory). Constitution §24 (independent-oracle verification) + DR-078 (fail-loud readers) + DR-079 (gate canary) + DR-080 (fidelity provenance + builder-blind acceptance suite) + the DR-047 self-learning activation, made operative:
- **`agents/implementer.md`**: RED tests for any artifact-reader must include a real + a malformed fixture and the reader must fail loud (DR-078); the Forbidden list now bars editing the reviewer's acceptance suite or any blessed baseline (DR-080) and returning a silent `[]`/`null` from an unparseable reader (DR-078).
- **`agents/reviewer.md`**: authors the builder-blind happy-path acceptance suite + an adversarial malformed-fixture test (DR-078/080); records the bless ORACLE (prototype shard + Layer-B sign-off + `prototype_blessed_at`) in the same commit (DR-080).
- **`skills/implement/SKILL.md`**: close-out now runs a `librarian` harvest of the just-built project (DR-047 — the loop was dormant: 1 graduated lesson vs 103 inbox notes).
- **`skills/upgrade/SKILL.md`**: runs `verify.sh --canary` after gate-config conformance (DR-079).
- **`templates/stack-a-nextjs/verify.sh`**: a `--canary` mode (DR-079) + a bless-provenance advisory (DR-080), both no-red-lock (vacuous / non-blocking).
- **`templates/rules/{quality-and-testing,typescript}.md`**: the project-facing fail-loud-reader / `NonEmpty` rule (DR-078).
- **`templates/docs/fdd-template.md`**: a `prototype_blessed_at` frontmatter field + a bless-provenance note (DR-080).
**Versioning:** `plugin.json` v8.41.0→**v8.42.0** (MINOR — new compatible capabilities: the canary gate, the fail-loud rules, the acceptance-suite/provenance discipline, the close-out harvest). `OVERLAY_VERSION` 8.38.0→**8.39.0** (templates/rules + stack-a `verify.sh` + `fdd-template` changed → projects re-sync on `/upgrade`; the new `verify.sh` stages are advisory/vacuous, so no green project red-locks). **Deferred:** the advisory `data-presence.spec.ts` template + the VLM fidelity judge (see factory log). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-21 — Change-queue completion: verify-then-ARCHIVE, never hard-delete (DR-069 2nd amendment) · v8.41.0
**What:** When a queued change lands, the build **archives** it instead of leaving it in the active queue (clutter) or hard-deleting it. On completion: (1) **independently** verify the durable record exists (generator ≠ verifier — the landing commit touched the canonical doc *and* a `docs/decision-log.md` entry references the change), (2) stamp `status: done` + `shipped_sha` + `shipped_at`, (3) **move** the file to `changes/done/` (a move, never an `rm`). The active `changes/` shows pending-only; the Spanish audit trail survives in `done/` (MC reads it for "recently shipped"). Durable record missing → don't archive, leave in place + flag the doc gap. **Never auto-delete** `done/`; pruning is a separate owner-gated, reversible action.
**Why:** Owner asked "delete done items to keep the folder clean?"; a red team (pro-delete / pro-keep / web-researcher) converged: the GOAL is right, hard-delete is the wrong METHOD here — `changes/` is **gitignored** (delete = irreversible, no git recovery), the decision-log is **not a guaranteed superset** of the change file (curated-English-technical vs raw-Spanish-intent + classification + `draft→ready` provenance), and "done" is build-emitted (deleting on an unverified self-report violates generator ≠ verifier). Industry: Kanban/Jira/Linear/GitHub archive-or-close not delete; event-sourcing append-only; SQS/Sidekiq delete-on-done only because a separate system-of-record exists; the universal rule is commit-before-retire. Archive = 100% of the clean-folder goal, 0% irreversible risk.
**Impact:** `plugin/skills/change/SKILL.md`, `plugin/skills/implement/SKILL.md` (step-3 drain), `factory/standards/build-orchestration.md` §8, `factory/decisions/registry.yaml` (DR-069 amended — clause 7 + nota), `plugin/.claude-plugin/plugin.json` v8.41.0 (MINOR — new lifecycle capability). **No `OVERLAY_VERSION` change** (skills + a standard only). **Deferred:** the MC Manual narrative (DR-046) — folded into the queued "Manual narrative sync" item. Pending: commit + `claude plugin update`.

## 2026-06-21 — Gate-template fidelity: back-port the stale biome/knip/verify.sh + a back-port discipline hook (DR-076) · v8.40.0
**What:** Fix for four follow-ups the MC re-anchor upgrade surfaced — the canonical gate templates had drifted BEHIND real projects, and conformance "overwrite from template" would have REGRESSED a project that was ahead. **(a) Back-ported the UNIVERSAL biome/knip deltas (red-team A, tested on MC):** `biome.json` += `css.parser.tailwindDirectives:true` (without it biome PARSE-ERRORS on `@theme` in every Tailwind-v4 `globals.css`), += `!.pandacorp/**`, += `style.useExportType:"error"`, and KEEP `complexity.noImportantStyles:"off"` (the `prefers-reduced-motion` WCAG reset the factory's own `accessibility.md` mandates — NOT MC-pixel-art-specific, the proposal-v1 misclassification; dropping it RED-locks a fresh project's a11y CSS); `knip.json` → `knip@6` schema, DELETE the `_pandacorp` key (knip v6 rejects it → hard error at cold start), `ignoreDependencies:["tailwindcss","@axe-core/playwright"]`. **(b)** `@axe-core/playwright` (the DR-074 dep, imported only in `e2e/_responsive-helper.ts` which knip's playwright plugin doesn't trace) → `ignoreDependencies`, NOT pulling `e2e/` into knip `project` (tested: that adds a NEW false-positive). **(c)** `verify.sh` runs the e2e gates in ONE `playwright test e2e/` invocation (was four `pnpm test:X` → one webServer boot, ~4× faster), gating on the SPEC FILE not the npm script — a CLEANUP, NOT the fix for the observed `ERR_CONNECTION_REFUSED` (red-team C: that is a mid-invocation dev-server death from `.next`/process contention; fixed by the deferred worktree isolation). **(d)** softened the DR-075 shell wording (`build-orchestration.md` §3 + `design.md` §5b): don't AUTO-EMIT an orphan `frd-NN-app-shell` at greenfield shard time, but a deliberately-authored app-shell FRD rooted in the PRD (a brownfield re-anchor) is correct, with `foundation:true` WOs. **Root-cause fix:** the conformance MECHANISM stays UNCHANGED (red-team B: "diff-and-flag-on-ahead" erodes DR-059's binary guarantee and isn't mechanically decidable) — instead a **discipline rule** (canonical gate files edited at the SOURCE template, never in-place) enforced by a new `PreToolUse` hook **`remind-backport-gate-config.sh`** (cloned from `remind-manual-sync.sh`, wired in `hooks.json`).
**Why:** `/pandacorp:upgrade` of MC (8.33.0→8.36.1) would have regressed MC's already-fixed `biome.json`/`knip.json` (the template still had `knip@5` + the v6-rejected `_pandacorp` key; the MC fix landed in-place at `b6c2fa7` and was never back-ported — red-team B git-forensics: a discipline violation, not a mechanism failure). Red-team-hardened (3 opus agents, 2 ran empirical biome/knip tests on MC, proposal 15 v2): three decisive corrections — keep `noImportantStyles:off` (A), reject the conformance-mechanism change (B), and the `ERR_CONNECTION_REFUSED` is contention not the 4-invocation race (C).
**Impact:** `plugin/templates/stack-a-nextjs/{biome.json,knip.json,verify.sh,STACK.md}`, `plugin/scripts/remind-backport-gate-config.sh` (new) + `plugin/hooks/hooks.json`, `plugin/skills/upgrade/SKILL.md` (Rules), `factory/standards/{quality.md,build-orchestration.md,design.md}`, `factory/decisions/registry.yaml` (DR-076), `plugin/.claude-plugin/plugin.json` v8.40.0 (MINOR — new hook), `plugin/templates/OVERLAY_VERSION` 8.37.0→8.38.0 (templates changed) → projects re-sync the corrected biome/knip/verify.sh on `/upgrade`; MC already carries the corrected biome/knip (kept during its upgrade) so it does NOT regress. Refines DR-059/074/075. **Deferred (flagged):** factory-side "template-never-behind-a-project" CI check; the `biome.project.json` opt-out (YAGNI); the `.next`-contention worktree fix (the real (c) fix); biome `recommended:true`→`preset` (deprecated, future major); the MC Manual narrative (DR-046). Pending: commit + `claude plugin update`.

## 2026-06-21 — Template library: second pass — agent-created docs + top-level README (DR-077 follow-up) · v8.39.0
**What:** An audit pass over every recurring file confirmed all 12 doc templates are referenced by their creating skill, and closed the remaining gaps. Added **4 templates** for recurring docs that had none: `api-contract-template.md` (per-backend-WO `docs/api/<wo-id>.md`, DR-060 — the highest-volume untemplated doc), `components-inventory-template.md` (`docs/design/components.md`, DR-057), `changes-readme-template.md` (the `.pandacorp/inbox/changes/README.md` queue index) and `decisions-inbox-template.md` (`.pandacorp/inbox/decisions.md`). Wired each to its creator — crucially the **agent-created** ones reference the template in the **AGENT's prompt**, not a skill: `backend-dev`→api-contract, `designer`(+`design`)→components-inventory; `change`/`bug`→changes-readme; `decide`→decisions-inbox. Added a **top-level `plugin/templates/README.md`** indexing all four template kinds (docs/rules/shared/stack-*) and answering "how is the template honored?" by creator class — skill-created (skill names the path) / agent-created (agent prompt names the path) / hand-authored-or-tool-generated (format documented + advisory doc-lint) — plus the explicit list of **intentionally-freeform** docs (design-decisions, technical-assumptions, voice-and-tone, references, a11y-report, security-audit, launch-plan, comms/*, run/lessons) that get no rigid template by design.
**Why:** Owner's second-pass request: ensure no constantly-created file lacks a template, all creators reference it, and — for docs created OUTSIDE a skill — a mechanism to ensure the template is used. The audit surfaced that several docs are created by build AGENTS (backend-dev, designer), not phase skills, so the enforcement point is the agent prompt — a distinction the first pass left implicit.
**Impact:** `plugin/templates/docs/{api-contract,components-inventory,changes-readme,decisions-inbox}-template.md` (new) + `plugin/templates/docs/README.md` (4 rows + creator note) + `plugin/templates/README.md` (new, top-level), `plugin/agents/{backend-dev,designer}.md`, `plugin/skills/{design,change,bug,decide}/SKILL.md`, `plugin/.claude-plugin/plugin.json` v8.39.0 (MINOR — more templates). **No `OVERLAY_VERSION` change** (new templates live in `templates/docs/`, consumed at generation; `shared/`/`stack-*` unchanged). Extends DR-077. **Deferred:** the MC Manual narrative (DR-046). Pending: commit + `claude plugin update`.

## 2026-06-21 — Complete the document-template library + a doc-lint; no per-doc version stamp, no migration engine (DR-077) · v8.38.0
**What:** Filled the missing document templates so every recurring doc a project generates has one **thin** base format in `plugin/templates/docs/`: **FDD, platform `architecture.md`, ADR, events plan, research, design-system (root `DESIGN.md`), per-FRD work-orders `README`, and the owner-channel change-request card** — joining the existing PRD/FRD/blueprint/work-order, with a `README.md` indexing them + the policy. Each is thin with explicitly-optional sections (an LLM pads every mandatory section into noise). Every doc-generating skill now POINTS at its template (the template is the single structural contract): `spec`→research, `blueprint`→architecture/adr/events, `design`→design-system/fdd, `work-orders`→work-orders README, `change`/`bug`→change-request. Added a shipped, conservative **doc-lint** (`plugin/templates/shared/.pandacorp/doc-lint.sh`, invoked from `verify.sh`): required frontmatter keys + the `REQ→AC→WO` stable-ID spine resolves; no `docs/` ⇒ vacuous pass. **Explicitly NOT built:** a per-document `template_version` stamp (a third version axis re-creates the state-fracture; provenance already lives in `status.yaml` `created_with`/`overlay_version`) and a structural migration engine (LLM docs have no deterministic common ancestor — retrofit = REGENERATE via the owning skill; reserve in-place edits for rare hand-edited docs).
**Why:** Owner-asked ("a templates folder so all our docs share a format, versioned, with old samples migratable"). A 10-agent research + red-team workflow (4 web angles + steelman + 4 adversarial opus lenses + a verdict judge) returned **do-reduced, high confidence, red team unanimous**: copier/cruft DO stamp+migrate but for FLEETS with deterministic regeneration; the AI-spec tools (spec-kit/Kiro/Tessl) all chose regenerate-or-flag, never auto-migrate. In-repo ground-truth killed (b)/(c): 1 project in the portfolio, the doc templates' whole git history additive (0 breaking structural bumps), 0 migration events. The highest-leverage piece the owner's proposal OMITTED was the doc-lint, which `verify.sh` confirmed was absent.
**Impact:** `plugin/templates/docs/{adr,architecture,fdd,events,research,design-system,work-orders-readme,change-request}-template.md` + `README.md` (new), `plugin/templates/shared/.pandacorp/doc-lint.sh` (new), `plugin/templates/stack-a-nextjs/verify.sh` (doc-lint call), `plugin/skills/{spec,blueprint,design,work-orders,change,bug}/SKILL.md` (template pointers), `factory/decisions/registry.yaml` (DR-077), `factory/standards/structure.md` (Templates note), `plugin/.claude-plugin/plugin.json` v8.38.0 (MINOR — new templates + doc-lint capability), `plugin/templates/OVERLAY_VERSION` 8.36.1→8.37.0 (shared changed: `doc-lint.sh`) → projects get the lint on `/upgrade`. Refines DR-049/048/059. **Note:** DR-076 was concurrently taken by the in-flight proposal 15 (gate-template fidelity) → this took **DR-077** to avoid the collision. **Deferred:** the MC Manual narrative (DR-046) — MC is mid-re-anchor. Pending: commit + `claude plugin update`.

## 2026-06-21 — Change-queue readiness gate: `status: draft | ready` (DR-069 amendment) · v8.37.0
**What:** The change queue (DR-069) gains a **readiness gate**. `status` becomes `draft | ready | done`: **`ready`** → the build drains+builds it; **`draft`** → captured for visibility but the owner is still working it out (or it depends on something undone / needs a design pass / is infra the build doesn't implement) → **the build SKIPS it untouched**; **`done`** → built, set by the build. **The supervisor's drain (`implement` step 3) takes ONLY `ready`, skips `draft`.** `/pandacorp:change` defaults a clear, actionable request to `ready`; "just note it" → `draft`. The owner promotes a draft with a one-line frontmatter edit.
**Why:** Owner-driven: *"que el implement solamente tome las ideas que ya están listas, las que están draft no, porque todavía las estamos trabajando."* The owner wanted to queue four MC items — two cooked (Prompt-2 observability consumer, Manual sync), two still being shaped (a changes/decisions view, the Party rework) — and build only the cooked two. The pre-existing single `pending` status had no hold switch. This is the "I'm still thinking" vs "go build it" separator.
**Impact:** `plugin/skills/change/SKILL.md`, `plugin/skills/implement/SKILL.md` (step-3 drain), `factory/standards/build-orchestration.md` §8, `factory/decisions/registry.yaml` (DR-069 amended — clause 6 + enum + nota), `plugin/.claude-plugin/plugin.json` v8.37.0 (MINOR — new queue capability). **No `OVERLAY_VERSION` change** (skills + a standard only). **Deferred:** the MC Manual narrative (DR-046) — folded into the queued "Manual narrative sync" item. Pending: commit + `claude plugin update`.

## 2026-06-21 — App shell as a captured foundation + Shell-Presence Gate (fidelity, not consistency) (DR-075) · v8.36.0
**What:** The fix for "a whole nav menu went missing and passed green" — two factory-level gaps left after DR-074. **(B-structural)** The per-FRD shard (DR-056) drops the **persistent app shell** (topbar/nav/footer/layout frame) because it belongs to no FRD. Fix: the shell is captured as a **foundation work order** (NOT a new FRD — that would orphan the `REQ-NN-MMM→WO` spine and the engine has no FRD-ordering primitive), in BOTH `design` (new **Step 8a**) AND `adopt` (step 6 — the brownfield path where MC actually lost it). `design`/`designer` register the `AppShell`/`Nav` in `components.md` + record the prototype's nav contract; `blueprint` schedules it in the existing foundation wave (built first, surfaces mount into `app/layout.tsx`) — the foundation-completeness gate then enforces "no surface until the shell is green" for free, zero engine change. **(A-enforcement)** The visual gate proves *consistency* (self-baseline), not *fidelity*. Fix: a **VERBATIM** `e2e/shell.spec.ts` (joins the DR-059/074 byte-diffed set → **six** e2e files; `verify.sh` gains a fail-closed `test:shell` shim) that asserts the app against the **prototype-anchored** nav contract `e2e/shell.ts` (per-project seed, created-if-missing, never byte-diffed). It iterates `SURFACES` minus author-declared `SHELL_EXEMPT` (**not `BLESSED`** — the MC failure is a route that shipped blessed without the shell), settle-first like the responsive helper, and per route asserts: the shell landmark is VISIBLE, every top-level destination is a VISIBLE in-shell link to its correct path, each destination 2xx-resolves, and on mobile (via `_target.ts`) the nav is reachable. Empty contract (no shell) ⇒ vacuous pass. The `reviewer` gets ONE advisory whole-app sentence (the block is the deterministic gate; shell *resemblance* is a punch-list nit, DR-072 preserved). **Retrofit:** `upgrade` adds `shell.spec.ts` to the byte-diffed set + seeds `shell.ts` **EMPTY** (never a filled contract — a filled nav on an app with no shell would red-lock all commits; filled in the same change that builds the shell).
**Why:** MC shipped with no global menu (the prototype's topbar Inicio·Tablero·Portfolio·Documentación absent) and passed 112/112 GREEN — continuation of `mc-design-fidelity-failure`. The owner diagnosed why it was MC specifically: MC was **adopted** (brownfield), and `adopt`'s shard had no shell-capture step. Red team (3 adversarial opus agents: minimalism / gate-reliability / ship-retrofit, proposal 14) reshaped v1: shell-as-FRD → foundation WO (cheaper + better-enforced, kills the spine collision); A2 semantic blocking gate → one advisory sentence (its blocking subset ⊆ A1, and a blocker re-opens DR-072 churn); the nav contract must be PROTOTYPE-anchored not `routes.ts`-derived (MC: 8 SURFACES vs 6 nav tabs — deriving false-reds AND moves the consistency trap up a level); iterate `SURFACES` not `BLESSED` (gating on blessed inherits the exact blind spot); A1 must assert visible/in-shell/href-correct/2xx + settle-first or it false-negatives a dead nav / false-reds a hydrating one; Piece C (responsive-shell) was scope creep DR-074 cut → dropped (the shell rides the shipped responsive gate; only the mobile-nav-reachability check rides inside A1).
**Impact:** `plugin/templates/stack-a-nextjs/e2e/{shell.spec.ts,shell.ts}` (new) + `e2e/playwright.config.ts` (comment), `plugin/templates/stack-a-nextjs/{verify.sh,STACK.md}`, `plugin/skills/{design,blueprint,upgrade,adopt}/SKILL.md`, `plugin/agents/{designer,reviewer}.md`, `factory/standards/{design.md §5b,quality.md,build-orchestration.md §3/§5}`, `factory/decisions/registry.yaml` (DR-075), `plugin/.claude-plugin/plugin.json` v8.36.0 (MINOR — new e2e capability), `templates/OVERLAY_VERSION` 8.35.0→8.36.0 (templates changed) → projects get the gate on `/upgrade` (back-filled EMPTY `shell.ts`). Refines DR-054/055/056/057/072/074. **Deferred:** the MC Manual narrative (DR-046, concepts/el-pipeline + estandares-y-reglas). **Next (separate):** re-anchor MC under the new gate (build the missing shell + responsive + reconcile). **v8.36.1 (PATCH, verification follow-up):** independent verification (generator≠verifier) caught that (B1) `test:shell` runs under both playwright projects so the structural checks would false-red a correct responsive app's mobile hamburger nav — fixed by forcing a desktop viewport in the structural checks (1)+(2); and (N3) test (3) "destination resolves" is now gated on `blessed` so a not-yet-built nav target can't red the gate mid-build. Same verification also surfaced + fixed latent biome-2.5 failures in the DR-074 e2e templates (import order, format, a behavior-preserving cognitive-complexity refactor of the occlusion scan). Re-verified: biome clean + tsc strict clean on the full e2e set in a real project (mission-control).

## 2026-06-21 — Platform target field + shipped smart responsive gate (DR-074) · v8.35.0
**What:** A tracked `target_platforms` product field + a SHIPPED, smart, fail-closed Responsive Gate the machinery reads — the real fix for MC shipping non-responsive while passing GREEN (smoke only checks "renders", visual only "matches its OWN baseline" — neither asserts the app WORKS at a phone width). Enforcement ships as VERBATIM stack templates (the DR-059 mechanism), NOT prose threaded through skills. **New e2e templates** (`templates/stack-a-nextjs/e2e/`): `_responsive-helper.ts` (`assertResponsive(page, url, {mobileWidth})` — settles first: mobile viewport → block `**/api/live**` → `domcontentloaded` → `fonts.ready` → `<main>` visible; then per-scroll-root **overflow** + **silent off-canvas clip**, both honoring the author-declared `data-scroll-x="intentional"` escape hatch for legit kanban/DAG/wide-table scroll-x, `+1` sub-pixel tolerance; **tap targets** via axe `target-size` SC 2.5.8 / **24px**; **`<main>` not occluded** by a fixed bar unless `scroll-margin-top` reserves space, WCAG 2.4.11); `responsive.spec.ts` (runs the mobile checks ONLY when `target_platforms` includes mobile, via `_target.ts` reading status.yaml — else a vacuous pass); generalized `smoke.spec.ts`/`visual.spec.ts` (route-driven via `./routes`); `playwright.config.ts` (desktop+mobile projects). `routes.ts` + `_target.ts` are PER-PROJECT seeds (created-if-missing, NEVER byte-diffed). **`verify.sh`** gains a fail-closed `test:responsive` shim. **`status.yaml.tpl`** gains `target_platforms: responsive` (default for NEW projects). **Skills:** `spec` (PRD records the target platform one-liner + sets the field); `blueprint` (installs the 5 verbatim e2e files + seeds routes.ts/_target.ts if missing + `test:responsive` script + `@axe-core/playwright` devDep); `upgrade` (conformance-checks the 5 verbatim e2e files — byte-diff/overwrite, NEVER routes.ts; adds the devDep + script; **back-fills `target_platforms: desktop` for existing projects, NOT responsive** — the red-lock fix); `adopt` (sets `target_platforms: desktop` for brownfield). **Standards:** `design.md` §4b, `quality.md` (the gate in the gates list + wires the previously-advisory axe target-size 2.5.8 into the gate), `build-orchestration.md` §5.
**Why:** MC verified at 390px overflows on every page (GuildBar, kanban, portfolio; the workspace has no mobile baseline at all) yet shipped 112/112 VERIFIED GREEN. Red team (3 adversarial opus agents): the failure is NON-ENFORCEMENT, not a missing field (the rule already existed and MC shipped non-responsive anyway); the naïve `scrollWidth<=width` gate is both unreliable (false-reds MC's legit scroll-x kanbans → implementers game it with `overflow:hidden`, creating clip bugs) and non-shipping (the specs were authored per-project, in no stack template — exactly why MC's gate was lenient). v2 = ship the gate as a verbatim template + make it smart (author-declared escape hatch) + cut the per-stage ceremony. A rating pass added 3 must-fixes, all applied: routes.ts excluded from conformance (per-project — byte-diffing it = data loss); retrofit back-fills `desktop` not `responsive` (else the new mobile gate red-locks all commits on a non-responsive existing project); the gate reads `target_platforms` to choose which widths to assert.
**Impact:** `templates/stack-a-nextjs/e2e/{_responsive-helper,responsive.spec,smoke.spec,visual.spec,routes,_target}.ts` + `e2e/playwright.config.ts` (new), `templates/stack-a-nextjs/{verify.sh,STACK.md}`, `templates/shared/.pandacorp/status.yaml.tpl`, `plugin/skills/{spec,blueprint,upgrade,adopt}/SKILL.md`, `factory/standards/{design.md,quality.md,build-orchestration.md}`, `factory/decisions/registry.yaml` (DR-074), `plugin/.claude-plugin/plugin.json` v8.35.0 (MINOR — new capability), `templates/OVERLAY_VERSION` 8.34.0→8.35.0 (templates changed) → projects get the gate on `/upgrade` (back-filled `desktop`). Refines DR-055/056/059/071. **Deferred:** the MC Manual narrative (DR-046, concepts/el-pipeline + estandares-y-reglas). Pending: commit + `claude plugin update`.

## 2026-06-21 — Adaptive repair: patch-in-place before rebuild + adaptive model escalation (DR-073) · v8.34.0
**What:** `templates/shared/.claude/workflows/pandacorp-build.js`: the engine now **patches a localized gate reject in place before reverting**, and **escalates the worker model to opus** when a WO is hard or has failed. **(1) Patch-first.** `frdGate`'s reject branch no longer reverts/changes status/touches `reopen_count` for a specific-WO CORRECTION fail — it leaves the WO `IN_REVIEW` and returns **`findings: [{ wo, finding (file:line), failingTest (RED-proven), files }]`** (added to `FRD_GATE_SCHEMA`). New **`attemptPatch(frd, findings, reviewIds)`** (opus, `xhigh`): fix ONLY the named finding on the EXISTING build, make the RED test pass, then **re-gate WHOLE-PROJECT** (full FRD adversarial+integration + a whole-project `knip`+`biome`+`tsc`, NOT `--since` — red-team-A: a dead export must not slip to a sibling FRD), commit + `VERIFIED` + reset `reopen_count:0` ONLY on whole-project-clean; else change nothing. New **`revertAndReopen(frd, reopenIds)`** (the old DR-070 revert moved out of the gate): set `PLANNED`, **increment `reopen_count`**, surgical `git checkout last_green_sha -- <files>` + `git rm` newly-created (never a hard reset), commit. The reject handler (was line 425) now: patch → VERIFIED-if-green, else revert+reopen. **(2) Adaptive model.** New `pickWorkerModel(wo)` floors at `P.worker`, escalates to opus on `difficulty=='high'` (HYBRID a-priori, owner decision) OR `reopen_count>=1` (empirical); `buildWO`'s be/fe/build calls use `woModel` + `effort:'high'` when opus (test-writer + selftest stay at `P.worker`); escalations logged `⤴ opus: <wo>`. **Cost-weighted brake (red-team-B):** new `COST(m)` (opus=3) weights `agentSpawned` so `maxAgents` brakes on a token-proxy. **(3) HYBRID difficulty.** `PLAN_SCHEMA` WO items + the plan-agent prompt now carry/populate `difficulty` + `reopen_count` from the WO frontmatter; `skills/work-orders/SKILL.md` adds `difficulty: low|medium|high` (default medium) with a rubric; `agents/reviewer.md` + `skills/implement/SKILL.md` describe patch-first + escalation. **SINGLE counter:** `reopen_count` is the only budget (the patch is a sub-step of one reject cycle, NOT a `patch_attempts` axis — kills the nesting/non-termination the red team flagged).
**Why:** MC Phase-2's WO-07-005 (the complex Configuración surface) churned **4 gate cycles**, each a different progressively-smaller finding (5 EARS → reverse cross-nav → dead-code knip → dead Back button); every reject discarded a ~99%-correct build and the rebuild re-introduced a new micro-bug. It converged only when a human recovered the good build from git and **patched the one bug** (~5 min vs 4 multi-agent passes). Owner: "no tiene sentido descartar todo por un 1% que falla." A complex surface also got a sonnet implementer regardless of difficulty/failure history. Red-team-hardened (3 adversarial opus agents, proposal 12 v2): patch must re-gate whole-project (A), opus escalation must be cost-weighted (B), one unified budget (no nesting).
**Impact:** `templates/shared/.claude/workflows/pandacorp-build.js` (syntax-checked via async-wrap), `agents/reviewer.md`, `skills/{implement,work-orders}/SKILL.md`, `factory/standards/build-orchestration.md` §6 (gate-reject patch-first + model selection) + §9 (single budget), `factory/decisions/registry.yaml` (DR-073), `plugin/.claude-plugin/plugin.json` v8.34.0 (MINOR — new capability), `templates/OVERLAY_VERSION` 8.33.0→8.34.0 (engine changed) → MC needs `/upgrade` (its `/implement` preflight does it). Refines DR-070 (revert becomes the fallback) + DR-072 (reopen_count is the single budget). **Deferred:** MC Manual narrative (DR-046, concepts/el-pipeline). Pending: commit + `claude plugin update`.

## 2026-06-21 — Convergence: split gate (correction blocks, fidelity advisory) + end Visual QA pass (DR-072) · v8.33.0
**What:** `pandacorp-build.js`: **`frdGate` is split** — CORRECTION (smoke/tests/types/lint/acceptance-criteria/security/no-dup/gross-structural) BLOCKS; visual-fidelity NITS are ADVISORY (appended to `.pandacorp/comms/visual-punch-list.md`, never reopen). New **end-of-build Visual QA phase** (over the FRDs built that run, outside the loop: render → compare to mock → punch-list → fix cheap gaps directly → re-verify → residual for the owner; punch-list + bounded polish, never reject-to-rebuild). New **`MAX_REOPENS`** (3) non-progress stop (a WO reopened too often → BLOCK needs-owner, via a `reopen_count` frontmatter field the gate increments). **`effort: 'xhigh'`** on the gate/repair/close-out. **R3:** `repairFoundation` verifies `last_green_sha` is an ancestor (`git merge-base --is-ancestor`) before a hard reset — orphaned SHA → surgical restore or escalate, never wipe verified work. **R2:** loud warning if `args` arrived as a non-object (the unbounded-run bug). `plugin/agents/reviewer.md`: fidelity advisory + gross-only + the Visual QA role + the reopen-cap. `factory/standards/build-orchestration.md` §9 is the canonical convergence model.
**Why:** Overnight runs churned (build→reject-on-fidelity→rebuild, ~1 net FRD/run). Research (deep-research workflow + forensics): convergence is a gate-engineering problem; noisy visual/LLM judges over-reject as hard blockers; the fix is splitting the gate + consolidating fidelity into a non-blocking end pass, not lowering quality. Realistic target = a gate-green working draft + a short punch-list, not a hands-off pixel-perfect product.
**Impact:** `templates/shared/.claude/workflows/pandacorp-build.js` (syntax-checked via async-wrap), `agents/reviewer.md`, `factory/standards/build-orchestration.md` §9, `factory/decisions/registry.yaml` (DR-072), `plugin/.claude-plugin/plugin.json` v8.33.0 (MINOR). `OVERLAY_VERSION` bumps (the engine — an injected overlay file — changed) → MC needs `/upgrade` (its `/implement` preflight does it). **Deferred:** Manual narrative + `quality.md` xref. Pending: commit + `claude plugin update`.

## 2026-06-21 — Gate harness: domcontentloaded not networkidle for real-time apps (DR-071) · v8.32.1
**What:** `templates/stack-a-nextjs/STACK.md` — the canonical smoke + visual gate (DR-055/056) used `waitUntil: "networkidle"`, which **never settles when the page holds an open SSE/EventSource/websocket/long-poll** → the navigation times out → the gate **hangs on every real-time route**. This is what made MC's Phase-2 build **churn for hours with 0 gate passes** (every live surface opens an EventSource). Fix: **smoke** navigates with `domcontentloaded` and uses the existing `toBeVisible("main,h1")` as the readiness signal (SSE stays live); **visual** additionally `page.route("**/<live>**", r => r.abort())` before navigating (determinism — no streaming pixels), then `domcontentloaded` + the readiness wait + `fonts.ready` before `toHaveScreenshot`. The MC instance `e2e/{smoke,visual}.spec.ts` fixed in the same change.
**Why:** The build's own log had diagnosed it but DR-070 (its autonomous fix) addressed the budget cap + sibling poisoning, not this — so it kept churning. DR-071 is the missing piece that lets a real-time surface pass the gate. A pure gate-config bug that breaks ANY project with a live transport.
**Impact:** `templates/stack-a-nextjs/STACK.md`, `factory/decisions/registry.yaml` (DR-071), `plugin/.claude-plugin/plugin.json` v8.32.1 (**PATCH** — config fix, no skill/engine behavior change, `OVERLAY_VERSION` unchanged), + `mission-control/e2e/{smoke,visual}.spec.ts`. Refines DR-055/056. Pending: commit + `claude plugin update`.

## 2026-06-21 — Build engine: honest agent cap + gate-reject reverts its code (DR-070) · v8.32.0
**What:** Two engine fixes (`templates/shared/.claude/workflows/pandacorp-build.js`) after MC's Phase-2 pass-1 ran **68 agents / ~4.95M tokens for only +1 net FRD** with the `maxAgents=40` cap doing nothing and clean siblings blocking. **(1) Honest, fine-grained agent cap.** `agentSpawned` now increments on EVERY spawn (added `baseline`, `plan`, close-out) and the ceiling is checked at a `capHit()` helper at TWO safe points — the per-FRD-loop top (existing) AND the top of each **build-wave** inside an FRD (new). A pass with a few large FRDs used to inflate the count inside one FRD and overshoot the ceiling arbitrarily (the cap was only ever tested between FRDs); now it brakes at the next wave boundary, leaving that FRD's built WOs IN_REVIEW (resumable) and skipping its gate. **(2) Gate-reject reverts its own code (the global-gate entanglement fix).** The FRD gate's `verify.sh --since` scopes only TESTS; biome/tsc/knip run WHOLE-PROJECT. A rejected surface's code was committed by the wave-writer (Option B) and LEFT in the tree on reopen/block, so its dead exports/warnings turned EVERY later FRD's global gate RED — clean siblings (FRD-11, FRD-14) blocked on a sibling's mess, not their own merit. Now on reopen/block the gate/repair agent **reverts the rejected WO's files to `last_green_sha`** (surgical `git checkout <sha> -- <files>` + `git rm` for newly-created files — NEVER a whole-tree hard reset, which would discard verified siblings) and commits the revert with the status change. The supervisor (`implement` skill) ALSO gains a reliable EXTERNAL agent-file-count brake independent of the in-engine counter.
**Why:** The owner's budget guardrail silently failed (a 40-cap run hit 68 agents / ~5M tokens) and the build degraded as it progressed (siblings poisoned via the global gate). Both recur every pass if unfixed; the owner chose "fix the engine first" before relaunching MC Phase-2.
**Impact:** `templates/shared/.claude/workflows/pandacorp-build.js` (capHit + wave-boundary check + accurate counting + gate/repair revert-on-reject), `skills/implement/SKILL.md` (supervisor external agent-count brake — to be wired), `factory/standards/build-orchestration.md` (how a run stops + reject-cleans-the-tree), `factory/decisions/registry.yaml` (DR-070), `templates/OVERLAY_VERSION` 8.28.0→8.29.0 (engine changed), `plugin/.claude-plugin/plugin.json` v8.32.0 (MINOR). Validated: source `node --check` passes (CommonJS context; the workflow runtime allows the top-level return/await the ESM check rejects). Pending: commit + `claude plugin update`.

## 2026-06-20 — One owner door (`/change`) + a durable classified change queue (DR-069) · v8.31.0
**What:** New skill **`skills/change/SKILL.md`** — the single owner front door for ANY change or bug: it classifies (bug vs feature) + assigns a Kanban **class of service** (`expedite`/`standard`) and files one file to `.pandacorp/inbox/changes/` (Spanish, gitignored). It is **detection-free + doc-free** (writes one file, never edits docs, no build-detection) — so it can't mis-detect a running build and corrupt it. `skills/implement/SKILL.md`: the per-FRD loop's safe-point step now **drains `changes/` and routes** (expedite-first / standard-FIFO; autonomous → integrate via the `iterate`/`bug` engine then the FRD gate; needs-owner → `decisions.md` + defer & continue; structural/new-design → `rethink_pending` + guide). `skills/bug/SKILL.md`: writes to the unified `changes/` (`type:bug`) + becomes reachable via `/change` (still a direct alias + the bug engine). `skills/iterate/SKILL.md`: reframed as the integration engine `change`/the build invoke. `factory/standards/build-orchestration.md` §8 is the canonical spec.
**Why:** Inject changes mid-build (builds run hours/days). Research-grounded (Kanban classes of service; HITL checkpoints; injection-queue-drained-at-iteration-boundaries). Answers the owner's two fears: detection can't corrupt (capture is detection-free + the queue is durable + fencing/DR-067 = safe-when-wrong); and **one memorable door** routes bug/feature internally while direct verbs stay direct. No engine JS change — the supervisor (an agent that reads files) drains the queue at safe points; the engine stays deterministic.
**Impact:** new `skills/change/SKILL.md`; `skills/{implement,bug,iterate}/SKILL.md`; `factory/standards/build-orchestration.md` §8; `factory/decisions/registry.yaml` (DR-069); `plugin/.claude-plugin/plugin.json` v8.31.0 (MINOR). No template/overlay change → `OVERLAY_VERSION` unchanged. **Deferred:** Mission Control's view of pending `changes/`+`decisions` + the Manual narrative (DR-046) — MC is mid-build. Pending: commit + `claude plugin update`.

## 2026-06-20 — Build-lifecycle honesty: running:false on every exit + stop-the-loop (DR-068) · v8.30.0
**What:** `skills/implement/SKILL.md` gets two hardenings after a build sat dead ~1h showing `running:true` + a stuck spinner. **(1) Guaranteed shutdown:** the supervisor writes `running:false` + removes the build lock as its **last act on EVERY exit** (not only a clean loop-end) — because a KILLED run never reaches the engine's own close-out (which already sets `running:false` on a normal end). A stale `running:true` (heartbeat past TTL) is never trusted as alive — readers cross it with recency (DR-066) and the next writer auto-corrects it. **(2) Stop the LOOP vs stop a run:** stopping a run just makes the supervisor relaunch the next pass; to halt the whole build the owner sets `rethink_pending:true` / `.pandacorp/run/stop`, checked at each safe point AND before every relaunch — now surfaced explicitly. `factory/standards/build-orchestration.md` §6 gets "Build-lifecycle honesty".
**Why:** Owner saw a phantom running build (measured dead 59m) and earlier it "came back" after he stopped it. No engine edit — reading the engine showed it already has a normal-end close-out + fail-safe (the earlier "engine doesn't guarantee running:false" claim came from a misfired grep; corrected by reading — see the `verify-before-telling` rule).
**Impact:** `skills/implement/SKILL.md`, `factory/standards/build-orchestration.md` §6, `factory/decisions/registry.yaml` (DR-068), `plugin/.claude-plugin/plugin.json` v8.30.0 (MINOR). Pairs with DR-066 + DR-067. Pending: commit + `claude plugin update`.

## 2026-06-20 — No-stash recovery + supervisor watches git-tree health (DR-067) · v8.29.0
**What:** Two enforced corollaries of the resumable, single-writer/no-merge build design, after a build wedged ~1h on a self-inflicted git conflict. `skills/implement/SKILL.md`: **(1)** new Rule — **never `git stash`** to preserve in-flight work across kill/relaunch/stop (it's resumable from frontmatter+commits; a later stash-pop onto a moved tree conflicts); recover a dirty/conflicted tree ONLY by restoring to `last_green_sha`; the **baseline self-heal** now restores a dirty/conflicted tree + drops stale build stashes + clears leftover temp `preview-wo*` pages first. **(2)** the **Monitor watches `git status --porcelain`** each tick — any `UU`/unmerged path or `<<<<<<<` marker is a broken tree → emits an alarm; the **React** step restores a broken tree to last green (bounded auto-repair, DR-065). `factory/standards/build-orchestration.md` §6 gets "Working-tree health & no-stash recovery" as the canonical statement.
**Why:** The MC Phase-2 orchestrator improvised `git stash` on kill/relaunch (not in the skill); applying a stale `frd02-lacampana` stash onto an already-`VERIFIED` FRD-02 left `<<<<<<<` markers in `Button.tsx` → 130 gate errors → build wedged ~1h while the supervisor (blind to `git status`) reported "healthy". Complements DR-066: liveness was made honest, but a tree can be **broken while alive** — same failure class. Owner: *"make sure this never happens again; the supervisor/subagents should detect and resolve problems themselves."*
**Impact:** `skills/implement/SKILL.md`, `factory/standards/build-orchestration.md` §6, `factory/decisions/registry.yaml` (DR-067), `plugin/.claude-plugin/plugin.json` v8.29.0 (MINOR). MC's own working-tree cleanup deferred (the Phase-2 build owns `mission-control/`); the hardened baseline self-heal cleans the stash/preview cruft on the next relaunch. Pending: commit + `claude plugin update`.

## 2026-06-20 — Observability fidelity: producer heartbeat + consumer recency rule (DR-066) · v8.28.0
**What:** New factory standard so any monitor never reads a self-reported flag as proof of life (DR-066). Producer side wired into the plugin: **engine** `templates/shared/.claude/workflows/pandacorp-build.js` now advances `last_event_at` + `updated_at` in `status.yaml` at every safe point (the sync-rollups pass and the FRD gate), and the `EMIT` `AgentWorking` stream append is documented as the producer's positive heartbeat. **Supervisor** `skills/implement/SKILL.md`: on every Monitor tick AND on the time-driven `ScheduleWakeup` beat it appends a positive `SupervisorTick` heartbeat event to `~/.claude/dashboard-events.ndjson` and advances `supervisor_heartbeat` — so "sin señal" (no events at all) genuinely means hung, not merely quiet between long agents (the exact audit failure). **Template** `templates/shared/.pandacorp/status.yaml.tpl` now declares `run_started_at`/`supervisor_heartbeat`/`last_event_at` with the rule "NEVER read `running` alone — cross it with heartbeat recency; en vivo / datos de hace X / sin señal". Standard text in `factory/standards/{observability,quality,build-orchestration}.md`.
**Why:** MC's read layer had `running: true` + a frozen `supervisor_heartbeat` while the build advanced (heartbeat was only event-driven → froze between quiet agents). A monitor's worst failure is showing a dead process as alive. Owner choices (2026-06-20): graded 3-band freshness; gate = near-real-time via push/watch when cheap, ≤30 s worst-case polling.
**Impact:** `templates/shared/.claude/workflows/pandacorp-build.js`, `skills/implement/SKILL.md`, `templates/shared/.pandacorp/status.yaml.tpl`, `templates/OVERLAY_VERSION` (8.27.0→8.28.0), `.claude-plugin/plugin.json` (v8.28.0). Factory: `standards/{observability,quality,build-orchestration}.md`, `decisions/registry.yaml` (DR-066), `factory/decision-log.md`. **Deferred:** the MC **consumer** UI side + Manual narrative (DR-046) — `mission-control/` is owned by the parallel Phase-2 build window. Pending: commit + `claude plugin update`.

## 2026-06-20 — Five MC-dogfood process improvements: phase-aware Stop gate, functional reconciliation, complete-foundation gate, bounded auto-repair · v8.27.0
**What:** Five general engine/standard improvements (DR-063/064/065 + DR-057 extended). **(1) Phase-aware Stop gate (DR-063):** `scripts/verify-before-stop.sh` now no-ops while a build is active — it checks a freshness lock `.pandacorp/run/build.lock` (`find -mmin -10`); fresh → skip the strict full-suite gate (the engine owns enforcement per-FRD + close-out), stale/absent → run it strictly. The `implement` skill's supervisor creates/refreshes/removes the lock (launch + heartbeat + close). `templates/stack-a-nextjs/verify.sh` gains a clarifying comment (hook = phase-aware, verify.sh = always strict). **(2) Bidirectional reconciliation (DR-064):** `design` (Step 8b + advance-gate check), `adopt` (design-migration step 5) and the `designer` agent now reconcile prototype↔FRD *functionality* both ways (Group A → update FRD, Group B → build into prototype / flag) before the prototype is canonical. **(3) Foundation-completeness gate (extends DR-057)** and **(4) bounded auto-repair (DR-065)** in `templates/shared/.claude/workflows/pandacorp-build.js`: a `foundationCompletenessGate()` derives the foundation as the UNION from `components.md` and blocks fan-out until it's complete+green; the FRD gate returns `missingFoundation:[…]` when a surface fails for a missing primitive, and the engine runs `repairFoundation()` (reset → add primitive → rebuild → retry, `FOUNDATION_REPAIR_CAP=2`) instead of escalating.
**Why:** Each was suffered in the Mission Control Phase-2 build: the Stop gate blocked every turn of the background build (and any session parked at the cwd in the shared repo); the re-anchor reconciled only the look while behaviour diverged; the foundation was declared ready while incomplete → flat surfaces; the engine detected the root cause but stopped to ask instead of auto-resolving the fix it already knew. Owner: "the implement skill should be smart enough to resolve it like you did" — autonomy without loops/budget burn.
**Impact:** `scripts/verify-before-stop.sh`, `skills/{implement,design,adopt}/SKILL.md`, `agents/designer.md`, `templates/shared/.claude/workflows/pandacorp-build.js`, `templates/stack-a-nextjs/verify.sh`, `templates/OVERLAY_VERSION` (8.23.0→8.27.0), `.claude-plugin/plugin.json` (v8.27.0). Factory: `standards/{quality,build-orchestration,design}.md`, `decisions/registry.yaml` (DR-063/064/065 + DR-057 extended), `factory/decision-log.md`. Owner choices (2026-06-20): Stop gate = full no-op during an active build; auto-repair cap = 2. #1's hook is plugin-scoped → no `upgrade` change needed. Pending: commit + `claude plugin update`.

## 2026-06-20 — Manual-sync reminder hook enforces DR-046 · v8.26.0
**What:** New non-blocking `PreToolUse` (Write|Edit) hook `scripts/remind-manual-sync.sh`, wired in `hooks/hooks.json` alongside the write-gate. When a conversation edits the factory's **operable surface** — `plugin/skills/`, `plugin/agents/`, `factory/standards/`, `factory/decisions/registry.yaml`, `factory/constitution.md` — it injects context reminding the model to reflect the change in Mission Control's Manual (`content/manual/`) in the SAME change. It clarifies that the Reference catalogs auto-derive (don't hand-copy) and only the hand-authored narrative (Tutorial/Guides/Concepts) needs the manual update. Scoped to the factory repo only (git-root marker: `factory/constitution.md` + `mission-control/content/manual/`); fires regardless of cwd or active skill (so `/memory` and `/learn` trigger it too). NEVER blocks (exit 0). No template/overlay change → `OVERLAY_VERSION` unchanged. **MINOR → v8.26.0.**
**Why:** DR-046 mandated the Manual stay in sync with the factory, but nothing enforced the narrative half — most factory changes happen in free conversation (or in `/memory`/`/learn`), not through a skill, so it relied on the agent remembering. This hook makes it reliable, using the same hook pattern as the write-gate. Owner decision (2026-06-20): start as a reminder now (Mission Control's own build is mid-flight at 55/78 WOs; a hard block would fight the build's commits), harden to a Stop-style hard block once that build finishes.
**Impact:** `plugin/scripts/remind-manual-sync.sh` (new), `plugin/hooks/hooks.json`, `plugin/.claude-plugin/plugin.json`. Factory: `factory/decisions/registry.yaml` (DR-046 nota), `factory/decision-log.md`. Manual: `mission-control/content/manual/concepts/hooks-gates-seguridad.md`. Pending: commit + `claude plugin update`; later — harden to a hard-blocking Stop variant when MC's build is done.

## 2026-06-19 — "One cohesive application" standard (DR-062) · v8.25.0
**What:** `design` skill + `designer` + `reviewer` now enforce cross-surface visual cohesion (DR-062): standardize the framing patterns across every surface — ONE page-title block (H1 = nav label + optional subtitle), one section-header/tab/chip/panel/empty-state language; a surface inventing its own variant is a cohesion defect; titles/section-headers/tabs are first-class entries in `docs/design/components.md`; the reviewer checks a screen doesn't look like a different app from its siblings. Canonical in `factory/standards/design.md` §7; registry DR-062. Design-time guidance, no `OVERLAY_VERSION` change. **MINOR → v8.25.0.**
**Why:** Owner reviewed the MC prototype and found surfaces that felt like different apps (bespoke "Códice" header, missing page titles on Inicio/Tablero/Portfolio) and asked to recalcar "feels like ONE app" in the design skill. Menu-vs-title question resolved: align (H1 = nav label).
**Impact:** `plugin/skills/design/SKILL.md`, `plugin/agents/{designer,reviewer}.md`, `plugin/.claude-plugin/plugin.json`. Factory: `factory/standards/design.md` §7, `factory/decisions/registry.yaml` (DR-062), `factory/decision-log.md`. Pending: commit + `claude plugin update`.

## 2026-06-19 — Canonical gate-config bugs fixed (bash-3.2 portability + biome scope) · v8.24.1
**What:** First real install of the DR-059 canonical gate (into Mission Control) exposed two template bugs. `stack-a-nextjs/verify.sh`: `shopt -s inherit_errexit` is bash 4.4+ → aborted on macOS' bash 3.2; now `shopt -s inherit_errexit 2>/dev/null || true`. `stack-a-nextjs/biome.json`: unscoped, so it linted `.claude/` machinery + `docs/` mocks; now `files.includes: ["**", "!.claude/**", "!docs/**", "!public/**"]`. Config-only PATCH; no rule change → `OVERLAY_VERSION` unchanged. **PATCH → v8.24.1.**
**Why:** The canonical gate must run on the owner's machine and lint only product code; the MC dry-run proved neither held (the Stop gate failed at `shopt`, then on the linted machinery).
**Impact:** `plugin/templates/stack-a-nextjs/{verify.sh,biome.json}`, `plugin/.claude-plugin/plugin.json`. Refines DR-059. Pending: commit + `claude plugin update`.

## 2026-06-19 — Demo-only prototype controls become a marked factory convention (DR-061) · v8.24.0
**What:** The `designer` agent (rule 4) + the `design` skill (operating rules) now require that any **demo-only control** a prototype adds to *preview states* (mode/effort pickers, play/pause, reset, state togglers) — which won't exist in the real read-only/skill-driven app — be wrapped in a **dashed-border block + uppercase `DEMO` tag + a one-line note of how the real thing works**, with any real value (e.g. effort) shown as **read-only data** in a real surface. Canonical pattern lives in `factory/standards/design.md` §6; registry **DR-061** added. No template/overlay change (design-time guidance), so `OVERLAY_VERSION` is unchanged. **MINOR → v8.24.0.**
**Why:** Owner saw the Mission Control Party prototype's effort picker + power/reset buttons read as if MC could control the factory (it's read-only; builds launch via `/pandacorp:implement`) and asked to standardise the demo-marking across every generated mockup.
**Impact:** `plugin/agents/designer.md`, `plugin/skills/design/SKILL.md`, `plugin/.claude-plugin/plugin.json`. Factory: `factory/standards/design.md` §6, `factory/decisions/registry.yaml` (DR-061), `factory/decision-log.md`. Prototype applied: `mission-control/prototype/party-proposal.html` + spec. Pending: commit + `claude plugin update`.

## 2026-06-19 — Verification pass: the DR-056/057/059/060 fixes made REAL (engine + blockers + cross-feature review) · v8.23.0
**What:** Red-team audit + research found the DRs were declared-but-not-runnable; fixed. `pandacorp-build.js`: planner emits `artifacts`+`foundation`; real regex `globsOverlap` (11/11 unit tests); `pickDisjointWave` + foundation-only wave-0 (engine-enforced); **Option-B commit** — workers never call git, one serialized post-wave writer (no worktrees, no index.lock race, no merge); per-WO `docs/api/<wo>.md`; Status Note carries decisions; reuseRef tolerant when components.md is absent; close-out runs a **cross-feature integration review**. `verify.sh`: `--since <sha>` scopes vitest. `reviewer.md`: baseline-blessing (B before A; missing baseline on a new route = blessed, not RED) + cross-feature seam check. `design`/`designer`/`adopt`: produce + existence-gate `docs/design/components.md`. `upgrade`: conformance covers the toolchain (knip/madge/playwright + test:smoke/visual + strict tsconfig). `STACK.md`: baseline-blessing + `--since` documented. **`OVERLAY_VERSION` 8.22.0 → 8.23.0**. **MINOR → v8.23.0**. Refines DR-056/057/059/060.
**Why:** Owner wanted the process verified (not trusted) before MC Phase 2; the audit found 5 build-blockers + the cross-feature seam gap; the worktree idea was reconciled (rejected for our partitioned shape, per Claude Code agent-teams docs).
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/templates/stack-a-nextjs/{verify.sh,STACK.md}`, `plugin/agents/{reviewer,implementer,designer}.md`, `plugin/skills/{design,adopt,upgrade}/SKILL.md`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory: `factory/standards/build-orchestration.md`, `factory/decision-log.md`. Pending: commit + `claude plugin update`; then the controlled dry-run (2-FRD throwaway, watched).

## 2026-06-19 — Process-audit fixes: gate-config conformance (DR-059) + parallel-WO collision-proofing (DR-060) · v8.22.0
**What:** **DR-059** — canonical gate config installed verbatim + conformance-checked. NEW `plugin/templates/stack-a-nextjs/{biome.json,verify.sh,knip.json}` (MAXIMUM fail-closed: `biome check --error-on-warnings`, complexity=error, react/next/test domains + a11y on, knip + smoke + visual fail-closed). `blueprint` step 6 installs them byte-for-byte; `upgrade` adds a **conformance check** that regenerates/fails on drift; `STACK.md` corrected (Biome does NOT auto-activate domain rules) + references the canonical files. **DR-060** — `pandacorp-build.js`: `artifacts` added to `PLAN_SCHEMA`; new `pickDisjointWave` serializes wave-parallel WOs whose declared `artifacts` overlap (generalized banner-collision fix); per-WO contract `docs/api/<wo-id>.md` in the backend/frontend build prompts (replaces shared docs/api.md); implementer/backend read deps' `## Status Note`; self-test sets IN_REVIEW + commits code+frontmatter atomically with lock-retry. Docs/agents wired by the artifacts workstream (`build-orchestration.md` §2, `architect`, `work-orders`, `backend-dev`, `frontend-dev`, work-order/blueprint templates). **`OVERLAY_VERSION` 8.21.0 → 8.22.0** (engine changed). **MINOR → v8.22.0**.
**Why:** Exhaustive process audit before MC Phase 2 found: MC's verify.sh enforced almost none of the standards (DR-055/056 + ~14 rules silently off); the engine could co-schedule colliding WOs. Owner chose MAXIMUM fail-closed and fix-before-build.
**Impact:** `plugin/templates/stack-a-nextjs/{biome.json,verify.sh,knip.json,STACK.md}`, `plugin/skills/{blueprint,upgrade,work-orders}/SKILL.md`, `plugin/agents/{architect,backend-dev,frontend-dev}.md`, `plugin/templates/docs/{work-order,blueprint}-template.md`, `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory: `factory/standards/{quality.md,build-orchestration.md}`, `factory/decisions/registry.yaml` (DR-059, DR-060), `factory/decision-log.md`. Pending: commit + `claude plugin update`; then MC Phase 1/2 (which also wires MC's own gate config + engine copy). Follow-up: per-agent worktree for the commit race.

## 2026-06-19 — Component reuse/coherence (foundation-first + inventory) + Claude Design adoption (DR-057, DR-058) · v8.21.0
**What:** DR-057 — stop parallel agents duplicating components: **engine** (`pandacorp-build.js`) gains `reuseRef`, injected into the implementer + frontend-dev prompts — read `docs/design/components.md` (the living component inventory) before creating any UI component; reuse → adapt → create-only-if-new; append new shared components to the inventory. **`blueprint`** Build Plan schedules a **foundation wave** (shared primitives: Button, Banner/Alert, Card, Chip, Modal) FIRST, before feature WOs fan out; **`work-orders`** require feature WOs to list reused inventory components + depend on the foundation WO; **`implementer`/`frontend-dev`** get the "check before you create" gate; **`reviewer`** quality lens rejects re-implemented primitives + flags sibling visual inconsistency; **`clean-code.md`** (injectable rule) gains the component-inventory section. DR-058 — **`design` + `designer`** now PREFER Claude Design (claude.ai/design via `/design-sync` + `DesignSync`) for the EXPLORE path (its `_ds_manifest.json` seeds the inventory), push-up option for ADOPT-VISUAL, HTML as fallback. **`OVERLAY_VERSION` 8.20.0 → 8.21.0** (engine + clean-code rule changed). **MINOR → v8.21.0**.
**Why:** Owner spotted two near-identical warn banners (PluginSyncBanner/OrphansBanner) built by two non-communicating agents → duplicated, incoherent, no shared primitive. Research (deep-research, 3-0): a machine-readable component inventory + foundation-first raises reuse; Claude Design (verified real + present as the `DesignSync` tool here) provides exactly that.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/agents/{implementer,frontend-dev,reviewer,designer}.md`, `plugin/skills/{design,blueprint,work-orders}/SKILL.md`, `plugin/templates/rules/clean-code.md`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory: `factory/standards/{build-orchestration.md,design.md}`, `factory/decisions/registry.yaml` (DR-057, DR-058), `factory/decision-log.md`. Pending: commit + `claude plugin update`; MC's two banners → a shared `Banner` primitive in Phase 2.

## 2026-06-19 — 100% visual fidelity for AI implementers: shard + engine-injection + in-loop + two-layer gate (DR-056) · v8.20.0
**What:** Implemented DR-056 across the plugin. **Engine** (`templates/shared/.claude/workflows/pandacorp-build.js`): new `designRef(frd)` interpolated into the implementer + frontend-dev build prompts — for a web build it injects the binding visual refs (open the WO's `## Visual reference`; read `fdd.md` + `mocks/` + tokens) + the in-loop directive (render→screenshot→compare→correct ≤3) — fixing the root cause that the engine passed only a one-line summary. **`design` Step 8 + `designer`**: SHARD the approved whole-app prototype into one self-contained mock per UI FRD (`docs/frds/<frd>/mocks/`) + write `fdd.md` + set `ui:`/`visual_source`; whole-app prototype home = `docs/design/prototype/`; complete-extraction (every layer). **`adopt` step 6**: orchestrates the chain for brownfield (extract→place→shard→stage). **`work-orders` + `work-order-template`**: the `## Visual reference` is FILLED (exact mock + token slice + fdd pointer), since the WO body is the agent's only design context. **`implementer` + `frontend-dev`**: the in-loop fidelity loop before IN_REVIEW. **`reviewer` + `build-orchestration.md` §5 + `stack-a-nextjs/STACK.md`**: the two-layer visual-fidelity gate — Layer A deterministic Playwright `toHaveScreenshot` (config + determinism block + `verify.sh` fail-closed stage) + Layer B VLM mock-judge (reviewer, different model, named divergences, escalate-on-uncertainty). **`OVERLAY_VERSION` 8.19.0 → 8.20.0** (engine template changed). **MINOR → v8.20.0** (new capability, additive).
**Why:** Owner demanded a guarantee that agents reproduce his polished HTML 100% visually. Audit: the engine never passed the design to the builder; per-FRD fdd/mocks didn't exist; verification never compared to the mock. Research (3 web agents): scope+ground+in-loop is the design-to-code playbook; a layered (pixel + VLM) gate is the only reliable verifier.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/agents/{implementer,frontend-dev,reviewer,designer}.md`, `plugin/skills/{design,adopt,work-orders}/SKILL.md`, `plugin/templates/docs/work-order-template.md`, `plugin/templates/stack-a-nextjs/STACK.md`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory side: new `factory/standards/design.md` §5–6, `factory/standards/build-orchestration.md` §5, `factory/decisions/registry.yaml` (DR-056), `factory/decision-log.md`. Pending: commit + `claude plugin update`; then Mission Control execution (Phase 0/1/2).

## 2026-06-18 — Design anchor: detect-then-confirm, don't ask blind (DR-054 flow refinement) · v8.19.1
**What:** Refined the DR-054 anchor flow in `design` and `adopt` per the owner: instead of asking the owner up-front "do you have a design?", the skills now **scan the repo FIRST** for an existing visual (a `prototype/` or HTML mockups, `docs/design/` artifacts, a token/CSS system in the stylesheets, screenshots, the live styled UI), and **if something is found, present the list and let the owner confirm/correct** ("encontré esto, ¿es tu referencia? ¿falta alguna, o no son estas?"). Only if the repo has **nothing** do they ask whether it lives elsewhere (Figma, another folder); if there is genuinely none → EXPLORE path. `adopt` step 2 now inventories design assets; step 3(d) presents-and-confirms. PATCH → **v8.19.1** (refines the just-added v8.19.0 capability; no overlay file touched, `OVERLAY_VERSION` stays 8.19.0).
**Why:** Owner's call — detecting and confirming is a better UX than a blind question: the factory does the looking, the owner just validates. A blind "do you have a design?" misses the prototype that is right there in the repo (exactly how Mission Control's `prototype/` got orphaned).
**Impact:** `plugin/skills/design/SKILL.md` (Step 0 rewritten to scan→confirm→else-ask), `plugin/agents/designer.md` (rule 3), `plugin/skills/adopt/SKILL.md` (step 2 inventory + step 3d confirm), `plugin/.claude-plugin/plugin.json`. Factory: `factory/standards/design.md` §1, `factory/decisions/registry.yaml` (DR-054 default), `factory/decision-log.md`. Pending: commit + `claude plugin update`.

## 2026-06-18 — Design fidelity (anchor on the approved visual) + the Preview Smoke Gate · v8.19.0
**What:** Implemented DR-054 + DR-055 across the plugin. **`design`** gains **Step 0 (anchor check)** — ask the owner if an approved visual exists; if yes, ADOPT-VISUAL path (extract faithfully → `design-tokens.json`+`DESIGN.md`+`design-decisions.md`, stage assets incl. iframes, **skip** the 3-direction Step 4, gate = fidelity check) — and a **deliverable-existence check** at the advance gate (no advancing, even on owner ok, without DESIGN.md + design-tokens.json + design-decisions.md + per-UI-FRD fdd.md+mocks). **`designer`** rewritten: fidelity-not-novelty when a visual is approved; per-feature `fdd.md`+`mocks/` now REQUIRED for UI. **`adopt`** gains a **design-adoption step** (step 3 asks about an approved visual; step 6 migrates it into the frozen contract + stages assets) — the exact step whose absence orphaned Mission Control's `prototype/`. **Downstream fidelity:** `frd-template` (`ui:`/`visual_source` + Visual-contract section), `blueprint-template` (cite mocks as the UI visual spec + assets to stage), `work-order-template` (`## Visual reference` + fidelity AC), and `spec`/`blueprint`/`work-orders` skills + `implementer`/`frontend-dev` agents (must REPRODUCE the mock, not approximate). **Preview Smoke Gate (DR-055):** `reviewer` gains a **4th lens (runtime/visual)** — render every UI route, fail on console error/pageerror/blank, compare screenshot vs mock; `stack-a-nextjs/STACK.md` ships `playwright.config.ts` + `e2e/smoke.spec.ts` + a **fail-closed** `verify.sh` stage (missing harness = RED); `quality-and-testing.md` (injected rule) adds the definition-of-done bullet; `build-orchestration.md` §5 flipped to mandatory/default-on. **`OVERLAY_VERSION` 8.18.0 → 8.19.0** (rule file changed → projects gain the smoke definition-of-done on auto-upgrade). **MINOR → v8.19.0** (new capability; additive — existing projects' `verify.sh` is regenerated only on the next blueprint/upgrade that touches the stack).
**Why:** Mission Control dogfood: adopted with an owner-approved prototype as the 100% visual target, but the prototype was never migrated (adopt had no design step), the design brief demoted it and invented a fresh palette (design had no anchor path), the frozen artifacts were never produced, fidelity was never enforced (use-tokens passed every gate; party views shipped emoji not the contracted sprites), and nothing opened a browser so a console-error-spewing, mockup-divergent UI passed 112/112 VERIFIED. Owner: "bastaba con entrar una vez a la app."
**Impact:** `plugin/skills/{design,adopt,spec,blueprint,work-orders}/SKILL.md`, `plugin/agents/{designer,implementer,frontend-dev,reviewer}.md`, `plugin/templates/docs/{frd,blueprint,work-order}-template.md`, `plugin/templates/rules/quality-and-testing.md`, `plugin/templates/stack-a-nextjs/STACK.md`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory side (same change): **new `factory/standards/design.md`**, `factory/standards/build-orchestration.md` §5, `factory/decisions/registry.yaml` (DR-054, DR-055), `factory/decision-log.md`. Pending: commit + `claude plugin update pandacorp@panda-corp`; then Vía B (Mission Control remediation) under the corrected process.

## 2026-06-18 — Collision-proof dev-port allocation: central ledger, scaffold reserves, blueprint materializes launch.json · v8.18.0
**What:** Implemented DR-022's "the factory records it" — which had no actual record. **`scaffold` (new step 4c)** reserves the next free **10-wide block from base 4000** in a new central ledger `factory/ports.yaml` (gitignored, personal), records `slug → base` and writes `dev_port_base` into the project's `.pandacorp/status.yaml`. **`blueprint` (step 6)** materializes the ports once the stack/dev-command exist: app = `base+0` into **`.claude/launch.json`** (so the preview/dev server starts straight from Claude Code) + app/DB/service ports into `.env` per the block offsets (Postgres `+2`, Redis `+4`; review worktree `+1`/`+3`/`+5`); it self-heals a `dev_port_base: 0` (pre-convention project) by allocating a block then. **`status.yaml.tpl`** gains `dev_port_base: 0`. **`OVERLAY_VERSION` 8.17.0 → 8.18.0** → existing projects gain the field on auto-upgrade. **MINOR → v8.18.0** (new capability, backward-compatible).
**Why:** Owner builds several apps in parallel and wants a guarantee — not a probability — that ports never collide. A hash of the slug collides by the birthday problem (~30% at a few dozen projects), so allocation comes from a ledger (single source of truth, like the portfolio): sequential reservation = zero collisions by construction, and a project keeps its block forever so re-runs are deterministic. `launch.json` is the format the harness preview tool reads (`.claude/launch.json` with `runtimeExecutable`/`runtimeArgs`/`port`).
**Impact:** `plugin/skills/scaffold/SKILL.md` (step 4c), `plugin/skills/blueprint/SKILL.md` (step 6), `plugin/templates/shared/.pandacorp/status.yaml.tpl`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory side (same change): `factory/standards/infra.md` (Port convention rewritten), `factory/decisions/registry.yaml` (DR-022), `factory/ports.yaml` (new ledger, seeded `panda-corp: 4000`), `.gitignore` (ledger ignored), `.claude/launch.json` (dogfood: panda-corp → 4000), `factory/decision-log.md`. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-18 — Living owner profile: onboarding bootstrap from history + continuous capture · v8.17.0
**What:** Implemented DR-053 in the plugin. **`skills/onboarding/SKILL.md`**: new **step 2 (bootstrap)** — detect the owner's past Claude Code transcripts (`~/.claude/projects/*/`), **ask yes/no**, and on yes run the cheap funnel (layer 0 distilled auto-memory → layer 1 mechanical `jq` of only the owner's typed turns, 0 model tokens → layer 2 parallel agents extracting structured personal signal), then present "this is what I already know about you" and interview from there; plus a living-profile note up top and in Rules. **`templates/shared/.pandacorp/guide.md.tpl`**: new **rule 9** — capture durable personal facts into the factory profile at `{{FACTORY_PATH}}/factory/profile.md` (read-first, dedupe), kept distinct from engineering lessons (rule 8) and decisions. `{{FACTORY_PATH}}` is already stamped by scaffold, so the product project already knows where to write. **`OVERLAY_VERSION` 8.16.0 → 8.17.0** → existing projects gain guide.md rule 9 on auto-upgrade. **MINOR → v8.17.0** (new capability, backward-compatible).
**Why:** Owner wants the factory to know him without re-interviewing from scratch and to keep learning him as they talk, for personalization only — separate from the engineering self-learning loop (DR-047) and the decision log (three distinct planes). Measured first: a transcript is ~0.4% human-typed text, so the bootstrap strips ~99.6% of the bulk mechanically before any model reads.
**Impact:** `plugin/skills/onboarding/SKILL.md`, `plugin/templates/shared/.pandacorp/guide.md.tpl`, `plugin/templates/OVERLAY_VERSION`, `plugin/.claude-plugin/plugin.json`. Factory side (same change): `CLAUDE.md` rule 9, `factory/profile.example.md`, `factory/decisions/registry.yaml` (DR-053), `factory/decision-log.md`. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-18 — Rules-injection system: docs/rules/ library, selective copy, upgrade propagation · v8.13.0
**What:** Implemented the engineering-rules injection+propagation system (factory DR-051). New **`plugin/templates/rules/`** — a project-injectable rule library, 13 files split by purpose/technology (`code-conventions`, `project-structure`, `quality-and-testing`, `documentation-and-decisions` = `always`; `react`, `nextjs`, `styling-and-ui`, `web-performance`, `web-security`, `prisma`, `i18n-next-intl`, `analytics-and-errors` = tech-gated), each with an `applies_when` frontmatter token + a `README.md` manifest. **Templates:** `shared/CLAUDE.md.tpl` now imports `@docs/rules/README.md`; `shared/AGENTS.md.tpl`'s inlined durable-conventions block is replaced by a `docs/rules/` pointer (ends the two-copies drift). **Skills:** `scaffold` copies the `always` rules into `docs/rules/` at birth and defines the canonical `docs/rules/README.md` generation spec (directive + one `@import` per file + table); `blueprint` (step 6b) copies the tech-gated files matching the approved stack; `adopt` injects the set matching a brownfield's inferred stack; `upgrade` re-syncs `docs/rules/` verbatim from the plugin (the source of truth) and fixes the CLAUDE/AGENTS references; `learn` carries the mandatory propagation contract (update `rules/*.md` + wire hard-enforcement + bump `OVERLAY_VERSION`). **Hard enforcement:** `stack-a-nextjs/STACK.md` lists the lint rules to wire into `verify.sh` (`noArrayIndexKey`, `useExhaustiveDependencies`, `noExplicitAny`, barrel-import bans, a11y). **`OVERLAY_VERSION` 8.7.0 → 8.13.0** (templates/shared + new rules) → existing projects auto-upgrade and gain `docs/rules/`. MINOR bump → **8.13.0** (new capability, backward-compatible: upgrade migrates the AGENTS block non-destructively).
**Why:** Mission Control ignored `core/modules` though it was in the embed — proving a hand-condensed, drift-prone, non-hard-enforced embed isn't enough. The plugin (`${CLAUDE_PLUGIN_ROOT}`) is the only location skills reach machine-independently, so the library lives there (not `factory/rules/`); projects get FILES they can follow with or without Pandacorp. Owner's model (PandaTrack-style per-tech rule files + README-as-loader, copy only what the stack needs).
**Impact:** `plugin/templates/rules/**` (new), `plugin/templates/shared/{CLAUDE,AGENTS}.md.tpl`, `plugin/skills/{scaffold,blueprint,upgrade,adopt,learn}/SKILL.md`, `plugin/templates/stack-a-nextjs/STACK.md`, `plugin/templates/OVERLAY_VERSION`. Policy DR-051 + canonical `factory/standards/README.md` (`factory/decision-log.md`, same date). Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-18 — Biome is the single lint+format standard (no Prettier/ESLint) + version policy · v8.16.0
**What:** Stack default is now **Biome only** for format + lint (Prettier and ESLint dropped). `stack-a-nextjs/STACK.md`: Biome install (`@latest`, domains + a11y group + `useSortedClasses`), hard-enforcement section remapped to Biome rule names (`noArrayIndexKey`, `useExhaustiveDependencies`, `useHookAtTopLevel`, `noExplicitAny`, `noImportCycles`, `noBarrelFile`/`noReExportAll`, `useImportType`, `noParameterAssign`, `useMaxParams`, `noExcessiveCognitiveComplexity`, `a11y`, `noDangerouslySetInnerHtml`, `noGlobalEval`, `noFocusedTests`/`noSkippedTests`), `verify.sh` drops the `|| pnpm lint` ESLint fallback. Escape hatch = minimal ESLint for Testing-Library/full-next rules only. Injectable rules updated to Biome names / CI-reviewer (`code-conventions`, `styling-and-ui`, `web-security`, `clean-code`, `quality-and-testing`). Added the **version policy** (latest for new; compatible-major for old) to `code-conventions.md`. DR-052. MINOR → **8.16.0**; `OVERLAY_VERSION` 8.15.0 → 8.16.0.
**Why:** Owner standardized on Biome (the running MC iterate flagged a plan/stack ESLint↔Biome mismatch). Research-backed; `next lint` removed in Next 16. Version policy per owner.
**Impact:** `plugin/templates/stack-a-nextjs/STACK.md`, `plugin/templates/rules/{code-conventions,styling-and-ui,web-security,clean-code,quality-and-testing}.md`, `plugin/templates/OVERLAY_VERSION`. Canonical/policy: `factory/standards/{stack,quality}.md` + `registry.yaml` DR-052 + `factory/decision-log.md`. Pending: commit + `claude plugin update`. (Mission Control untouched — build in progress; re-syncs via upgrade later.)

## 2026-06-18 — `src/` mandatory (owner override) · v8.15.0
**What:** `project-structure.md` now mandates all app code under `src/` (was "pick one, default root-level"); `@/* → ./src/*`; config + non-code assets (`public/`, `content/`, `docs/`) stay at root. Updated `code-conventions.md` (alias) and the README catalog. MINOR → **8.15.0**; `OVERLAY_VERSION` 8.14.0 → 8.15.0.
**Why:** Owner overrode the earlier optional verdict — wants code/config separation enforced everywhere, not per-project. Trade-off accepted: Mission Control migrated to `src/` in the same push.
**Impact:** `plugin/templates/rules/{project-structure,code-conventions,README}.md`, `plugin/templates/OVERLAY_VERSION`. Canonical: `factory/standards/structure.md` + `factory/decision-log.md`. Pending: commit + `claude plugin update`.

## 2026-06-18 — Rule library expanded (research-driven): +clean-code, +typescript, +accessibility, 7 files extended · v8.14.0
**What:** Folded curated, non-contradicting best practices from web research into `plugin/templates/rules/`. New files: `clean-code.md` (`always`), `typescript.md` (`typescript`, new token), `accessibility.md` (`web-ui`, absorbs the a11y section formerly in `styling-and-ui.md`). Extended: `react.md` (React 19 primitives), `nextjs.md` (caching/streaming/metadata), `styling-and-ui.md` (Tailwind v4 `@theme`, no arbitrary values), `web-performance.md` (runtime perf), `web-security.md` (injection + supply-chain), `code-conventions.md` (deps + git hygiene), `project-structure.md` (source-root choice + route groups), `quality-and-testing.md` (test discipline + knip in DoD). README manifest updated (catalog + `typescript` token). `src/` deliberately NOT mandated (official Next.js docs: optional; would break Mission Control). MINOR → **8.14.0**; `OVERLAY_VERSION` 8.13.0 → 8.14.0 so existing projects re-sync via `/pandacorp:upgrade`.
**Why:** Owner requested a research pass to add high-value rules (clean code, scalability, readability, AI-friendliness, file-size limits, SRP/SoC, reuse) before resuming the Mission Control compliance migration, with the constraint of no contradictions. Sources: Google eng-practices, Clean Code, Fowler/Metz, official Next/React/TS/Tailwind docs, OWASP, OpenSSF, WCAG 2.2, web.dev, Testing Library.
**Impact:** `plugin/templates/rules/**` (16 files), `plugin/templates/OVERLAY_VERSION`. Canonical/policy: `factory/standards/structure.md` + `factory/decision-log.md` (same date), under DR-051's propagation contract. Pending: commit + `claude plugin update`.

## 2026-06-18 — Tech-gated library conventions added to the stack-a template · v8.12.0
**What:** Added a **"Library conventions"** section to `plugin/templates/stack-a-nextjs/STACK.md` — the tech-gated half of the rule harvest from the sibling projects (PandaTrack + jobresearch-app). These ride with stack-a's default libraries and only apply if the project actually uses them: **Prisma** (queries in the data layer, `getXByY`/`createX`/`updateX`/`deleteX` naming, DI of the client, `$transaction` for atomic multi-step writes), **next-intl** (hooks in components, server helpers only outside React), **PostHog** (event names centralized in `POSTHOG_EVENTS`, declarative `data-ph-event` delegate), **Sentry** (capture unexpected errors only, one helper, PII-redacted), and **design system** (one canonical component per pattern, promote core→modules). MINOR bump → **8.12.0** (additive guidance in a template; backward-compatible).
**Why:** Owner asked to mine the sibling projects' working rules and standardize the keepers. Library-specific rules belong in the stack template — the factory's natural per-stack gate — so they only reach projects on that stack, not every project. The general/durable half (React composition, performance, Server-Action authz) went to `factory/standards/` (`factory/decision-log.md`, same date).
**Impact:** `plugin/templates/stack-a-nextjs/STACK.md` (new Library conventions section). General half: `factory/standards/patterns.md` + `conventions.md` + `performance.md`. Pending: commit + `claude plugin update pandacorp@panda-corp`.

## 2026-06-18 — Party telemetry enriched: AgentWorking now carries frd/phase/activity/mode · v8.11.0
**What:** The build engine's `EMIT(role, wo)` Party event emitted only `{role, wo}`, too thin for a faithful Party view (it couldn't tell which FRD, build-vs-review, the deep relay sub-step, or the run mode). Enriched `EMIT(role, wo, ctx)` so `AgentWorking.data` now also carries **`frd`** (the feature), **`phase`** (`build`|`review`), **`activity`** (the sub-step — `test`|`backend`|`frontend`|`selftest` in the deep relay, `implement` otherwise, `gate`/`repair` in review) and **`mode`** (the run's mode). All six call sites pass context (the deep split's three roles, the single implementer, the self-test, the gate, the repair). Fields are **optional and additive** → fully backward-compatible: consumers reading `{role, wo}` are unaffected. MINOR bump → **8.11.0**.
**Why:** Part of the Party redesign (the faithful build/pipeline views) — the views must run on the engine's REAL telemetry, not a demo. Today's `{role, wo}` can't show the deep relay (`test-writer → backend-dev → frontend-dev`), the FRD title, or build-vs-review without inventing it. This is the engine-side prerequisite documented in `mission-control/prototype/party-redesign-spec.md` §7; the view-side consumption (`lib/events.ts`, FRD-06) lands when the redesign is implemented. (Hand-off/contract events — `HandoffWritten`, `ContractPublished` — are specced there too but deferred: the view can infer hand-offs from WO state + Build Plan deps for v1.)
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (EMIT + 6 call sites), synced to `mission-control/.claude/workflows/pandacorp-build.js` (identical). Syntax checked; emitted line verified valid JSON. No state/flow change; the next build simply writes richer events. Canonical design: `mission-control/prototype/party-redesign-spec.md`.

## 2026-06-17 — FRD status is a DERIVED rollup; engine syncs it (no more stale frd.md) · v8.10.0
**What:** The FRD/blueprint `implementation_status` was only written when ALL its work orders reached `VERIFIED` — so a fully-built feature awaiting its gate kept showing its initial `PLANNED` (FRD-06: 11 WOs `IN_REVIEW`, but `frd.md` still `PLANNED`, looking "not started"). Fix: the FRD status is explicitly a **DERIVED rollup** of its work orders (the atomic source of truth), and the engine now (1) **syncs every FRD/blueprint rollup when it plans** — a cheap frontmatter-only pass that corrects drift from an interrupted run, crash or manual edit *before* building — and (2) **re-derives & persists the rollup after each gate**, not only on all-`VERIFIED`. Rollup order: `VERIFIED` iff all are; else `BLOCKED` > `PLANNED` > `IN_PROGRESS` > `IN_REVIEW`. On any FRD↔WO discrepancy, the work orders win. MINOR bump → **8.10.0**.
**Why:** Owner caught a real contradiction (FRD-06 reported first as "verified", then as "PLANNED"). The `frd.md` field was an un-maintained cache of a derived value, so it lied about progress.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (new `sync-rollups` step + gate rollup). Rule: `factory/standards/build-orchestration.md` §1 + `factory/decisions/registry.yaml` DR-050 + `factory/decision-log.md`. Applied to Mission Control (engine copied; 3 FRDs resynced).

## 2026-06-17 — Reliable spend brake: maxAgents counted inside the engine · v8.9.0
**What:** The token brake was unreliable. `maxSpend` reads `budget.spent()` (output tokens of the turn), which (a) under-counts the heavy subagent work and (b) goes unenforced if the supervisor dies — both observed today: an MC run reached ~6M tokens with `maxSpend` set to 2M, after an app restart **orphaned** the run (supervisor dead, workflow still going, nothing internal capping it). Fix: **`maxAgents`** — the engine counts the subagents it spawns (`agentSpawned`: +2/+4 per work order built, +1 per FRD gate, +1 per repair pass) and stops at a safe point when it reaches the cap. Counted INSIDE the engine, so it brakes reliably regardless of `budget.spent()` semantics and survives a dead supervisor. `maxAgents` is now THE overnight guardrail; `maxSpend` is demoted to a secondary ceiling. Aligned across engine (`pandacorp-build.js`), the `implement` skill, and `build-orchestration.md` §6. MINOR bump → **8.9.0** (engine change; OVERLAY bumps as projects upgrade).

## 2026-06-17 — Supervisor: announce each FRD + review worktree for live review · v8.8.0
**What:** Two owner-facing additions to the supervisor (the owner couldn't tell when a feature finished, and feared "discover at the end it's all wrong"). (1) **Announce each FRD milestone**: on every FRD verified, the supervisor sends a visible+push message — the FRD name, one line on what it does, the route/page to try it, X/Y done. (2) **Review worktree**: the supervisor keeps a worktree pinned at `last_green_sha` (its OWN `node_modules` — a symlink breaks Next's HMR) and updates it (`git checkout`) each FRD; the owner reviews the latest green version there (a Claude Desktop session in that worktree has a native Preview — the build session's Preview can't point at an external worktree, confirmed) or asks for an on-demand dev server. No permanent dev server (Next's watcher is flaky across `git checkout`). MINOR bump to **8.8.0**; skill-only, no engine/OVERLAY change.
**Why:** The owner needs progress visibility mid-build (when did FRD-X finish? can I see it?) — not silence until the end. Milestone announcements + a review worktree give "see each feature as it lands" without touching the running build. The owner also decided this session to **KEEP per-FRD mutation rigor** (NOT reduce it to milestones) — the speed win already came from focused gates (v8.7.0); cutting rigor would feed the very fear this addresses.
**Impact:** `plugin/skills/implement/SKILL.md` (Announce-each-FRD bullet + Review-worktree bullet), `plugin/.claude-plugin/plugin.json` → 8.8.0. No engine/OVERLAY change. Activation: commit + `claude plugin update` + restart.

## 2026-06-17 — Supervisor heartbeat must be VISIBLE & periodic · v8.7.1
**What:** Clarified the supervisor's Health heartbeat in the `implement` skill: it must be a **visible message to the owner every ~20–30 min, even with no anomaly**, scheduled SEPARATELY via a `ScheduleWakeup` timer (the Monitor only wakes on events, so a healthy-but-quiet build went >1h silent and the owner reasonably thought it was stuck). Each heartbeat reports WOs done/total, the FRD in progress, rough output spend, and "all green". Doc-only PATCH to **8.7.1**; no engine/OVERLAY change.
**Why:** During the v8.5.0 MC run the supervisor watched correctly (no false alarms after its v2 self-fix) but never spoke for over an hour — silence reads as "stuck". A periodic latido is the difference between "trust it's running" and "is it dead?". Surfaced by the owner mid-run.
**Impact:** `plugin/skills/implement/SKILL.md` (Health heartbeat bullet), `plugin/.claude-plugin/plugin.json` → 8.7.1. Activation: commit + `claude plugin update` + restart.

## 2026-06-17 — Faster & scalable build: powerful default + cold-start trim + focused gates · v8.7.0
**What:** Three speed/scale improvements (owner feedback mid-run: "too slow, the 3700-test gates won't scale"). (1) **`powerful` is now the default mode** (wave 8, was `balanced`/4) — more work orders in parallel, no quality change. (2) **Cold-start trimmed**: the baseline **skips `verify.sh` when `HEAD == last_green_sha`** (tree already known-green), and the **planner reads only WO frontmatter** (status/id/deps/title) + the Build Plan, NOT every WO's full body (the implementer reads its own) — cuts the ~16-min "silent" cold start that each pass paid. (3) **Focused gates**: the per-FRD gate runs `verify.sh --since <last_green>` — biome+tsc globally but only the vitest tests **affected since the last green** (`vitest run --changed`), not the whole suite (3700+ and growing) every gate; the **full suite runs once at close-out** as the regression net. MINOR bump to **8.7.0**; `OVERLAY_VERSION` → 8.7.0 (the engine changed).
**Why:** Mid-run the owner saw it crawl with gates re-running 3700 tests each (heading to 7000+). Focused gates move per-gate cost from O(total tests) to O(tests-touched); the cold-start trims remove repeated dead time across the multiple passes a build takes. Trade-off (owner-approved): a behavior regression in an *unrelated* feature could slip to close-out — mitigated by global tsc/biome each gate + the full suite at close-out.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (MODE default, baseline skip, planner frontmatter-only, gate `--since`, close-out full suite), `plugin/skills/implement/SKILL.md` (modes + gate), `factory/standards/build-orchestration.md` (§5), `plugin/agents/architect.md` (verify.sh `--since`, DR-019), `plugin/.claude-plugin/plugin.json` + `OVERLAY_VERSION` → 8.7.0. **Pending:** the current MC run uses v8.5.0; copy the v8.7.0 engine + add `--since` to MC's `verify.sh` once it finishes (the engine's `--since` call degrades gracefully on an older verify.sh — runs full). Activation: commit + `claude plugin update` + restart.

## 2026-06-17 — Concurrent-run guard: clarify the supervisor relaunch · v8.6.1
**What:** One-sentence clarification to the `implement` skill's relaunch-for-multiple-passes instruction (v8.5.0), now that the concurrent-run guard exists (v8.6.0): the supervisor must relaunch the **workflow** directly (`Workflow({ name: 'pandacorp-build' })`), NOT re-invoke `/implement` — it already holds the run lock, so re-invoking the skill would trip its own concurrent-run guard and abort itself. Doc-only PATCH to **8.6.1**; no engine/OVERLAY change.
**Why:** Caught reviewing the v8.6.0 guard against the v8.5.0 relaunch loop — the two interact, and "relaunch" was ambiguous enough that an agent could re-invoke the skill and hit its own lock (a self-abort on pass 2). Surfaced from the owner's question comparing the lock vs `progress.md`.
**Impact:** `plugin/skills/implement/SKILL.md` (one sentence in the relaunch bullet), `plugin/.claude-plugin/plugin.json` → 8.6.1. Activation: commit + `claude plugin update` + restart.

## 2026-06-17 — Concurrent-run guard: heartbeat-based TTL lock · v8.6.0
**What:** Added a concurrent-run guard to prevent two `/pandacorp:implement` runs from colliding on the same project. Without a guard, two simultaneous runs cause race conditions on the work-order frontmatter (double-pickup of the same `PLANNED` WO), `status.yaml` corruption (both writers clobber each other's state) and conflicting git commits. A simple boolean `running: true` flag is insufficient — if a run is interrupted (internet cut, crash), the flag is never cleared and permanently blocks future runs ("zombie lock"). **The solution is a TTL lock via supervisor heartbeat**: `status.yaml` gains two new fields — `run_started_at` (set once at launch) and `supervisor_heartbeat` (written by the supervisor on every Monitor tick, ~2 min). The `implement` preflight reads these: if `running: true` AND heartbeat < 10 min old → ABORT with a clear message; if heartbeat ≥ 10 min old (or absent) → stale lock, auto-clear and proceed. The supervisor's existing Monitor loop gains one instruction: write `supervisor_heartbeat: <ISO timestamp>` on each tick. No changes to `pandacorp-build.js` or OVERLAY_VERSION — the guard lives entirely in the skill (the agent layer). MINOR bump to **8.6.0**.
**Why:** Owner identified that nothing prevents running `implement` twice on the same project (easy mistake when distracted), and a simple `running: true` flag would create a worse problem (zombie locks after crashes). The TTL-heartbeat pattern is the standard solution: the supervisor's liveness IS the lock — if the supervisor dies, the lock auto-expires in ≤10 min without any manual intervention.
**Impact:** `factory/standards/build-orchestration.md` (new §9 "Concurrent-run guard"), `plugin/skills/implement/SKILL.md` (preflight block + launch section + supervisor Monitor instruction), `plugin/.claude-plugin/plugin.json` → 8.6.0. No `pandacorp-build.js` or OVERLAY_VERSION changes. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-17 — Overnight-test fixes: cap counts reopens + internal spend ceiling · v8.5.0
**What:** The first real overnight run of the v8.4.0 engine (Mission Control, supervised) validated the core (per-FRD gate, BLOCKED skip, reopen-without-loop, clean consumption — 28 agents, 0 re-work, ~400 real tests added) AND caught a bug the static audit missed. THREE fixes to `pandacorp-build.js`: (1) **`maxFrds` now counts features PROCESSED** (`built + blocked + reopened`), not just built/blocked — a gate that REOPENS a feature's WOs didn't count, so chained reopens slipped past the cap and the run crept to extra features (the supervisor stopped it by hand). (2) **`maxSpend`** — an ABSOLUTE output-token ceiling via `budget.spent()` that works WITHOUT a `+Nk` turn directive: the real guardrail for unattended/overnight runs (until now the only token brake was the supervising agent). (3) **No rebuild of `IN_REVIEW`** — WOs left `IN_REVIEW` by an interrupted run go straight to the gate instead of being rebuilt (`doneIds` counts IN_REVIEW for dep resolution). The `implement` skill documents `maxSpend` as the overnight guardrail + the supervisor's **relaunch-for-multiple-passes** loop (reopens defer to the next run; relaunch until nothing pending or `maxSpend` hit) + `running:true` on launch. MINOR bump to **8.5.0**; `OVERLAY_VERSION` → 8.5.0.
**Why:** The owner left the engine on a supervised overnight test; the agent caught the scope-creep, stopped it ("better to stop short than burn tokens"), and reported it. Fixing the cap-counting + an internal spend ceiling make "run and walk away" genuinely safe without a human as the only brake. The overnight test surfaced what the audit couldn't — validating the "supervise the first real run" discipline.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/skills/implement/SKILL.md`, `factory/standards/build-orchestration.md` (§6), `plugin/.claude-plugin/plugin.json` + `OVERLAY_VERSION` → 8.5.0. Copied into Mission Control for the next run. Activation: commit + `claude plugin update` + restart.

## 2026-06-16 — Coarser work orders + no same-file wave-siblings · v8.4.0
**What:** Owner directive: future projects should generate **coarser**, better-grouped work orders without creating problems. Reinforced the WO-generating guidance — `work-orders` skill (step 3: "group generously — when in doubt, merge") + `architect` agent (step 7) + the `build-orchestration.md` standard (§2) — with a hard rule learned from auditing Mission Control: **no same-file wave-siblings** — two work orders that build in the same wave must NOT write the same file/module (parallel implementers collide; e.g. MC FRD-10's four WOs all creating `lib/achievements.ts`). Merge such siblings into one coarse WO (preferred) or serialize them with a dependency. The rule by itself nudges granularity coarser, the right way. MINOR bump to **8.4.0**; `OVERLAY_VERSION` → 8.4.0.
**Why:** Owner wants future projects coarser without the collision problem. The same-file failure mode was found by an impartial reviewer while confirming MC's (correct) decision to keep its fine WOs — so the fix is a factory rule + a memory lesson (DR-047 in `factory/memory/_inbox.md`), not just an MC patch.
**Impact:** `plugin/skills/work-orders/SKILL.md`, `plugin/agents/architect.md`, `factory/standards/build-orchestration.md` (§2), `plugin/.claude-plugin/plugin.json` + `OVERLAY_VERSION` → 8.4.0. Activation: commit + `claude plugin update` + restart.

## 2026-06-16 — Exhaustive audit pass: engine fixes + coherence cleanup · v8.3.0
**What:** A 4-auditor review (engine execution, doc coherence, old-flow traces, MC inventory) before trusting an overnight run surfaced fixes, all applied. **Engine** (`pandacorp-build.js`): the FRD gate now reviews ONLY the work orders built this cycle (never re-touches an already-`VERIFIED` WO → kills a latent re-work path that DR-050 was meant to remove); `consecutiveBlocks` no longer resets on a `reopen` (the health breaker stays honest); a fail-safe guarantees `.pandacorp/status.yaml running:false` on close (no phantom "running" build in Mission Control); `lowBudget` + `maxConsecutiveBlocks` are overridable via `args`; the self-test commit retries on a parallel `index.lock`. **Coherence** (old per-WO flow erased from the agents/constitution an agent reads first): `reviewer.md` (runs once per FRD, not per WO), `implementer.md` (state = frontmatter `IN_REVIEW`, never a body checkbox; never self-`VERIFIED`), `architect.md` (coarse WOs + writes the Build Plan, not atomic "one deploy unit" slices), `constitution.md` (coarse slice + per-FRD review), `registry.yaml` DR-013 (reviewer at the FRD gate). **Discoverability**: the supervisor is now canonical in `build-orchestration.md` (§7), and the standard is wired into the indexes a new agent reads first (`factory/standards/README.md`, `CLAUDE.md`). Plus small skill fixes (pro-mode wave, `balanced` listed, deep-wave note). MINOR bump to **8.3.0**; `OVERLAY_VERSION` → 8.3.0.
**Why:** The owner is about to leave a build running overnight and asked for an exhaustive check that everything is both correctly implemented AND documented "so any future agent or conversation understands all of this." The audit confirmed the core was coherent but caught a re-work bug and several old-flow descriptions in the agent instructions/constitution that would mislead a fresh agent. Decided + executed this session.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js`, `plugin/agents/{reviewer,implementer,architect}.md`, `plugin/skills/implement/SKILL.md`, `factory/constitution.md`, `factory/standards/{build-orchestration.md,structure.md,README.md}`, `factory/decisions/registry.yaml` (DR-013), `CLAUDE.md`, `plugin/.claude-plugin/plugin.json` + `OVERLAY_VERSION` → 8.3.0. **Still pending for PART 2 (Mission Control migration):** MC's own v1 engine (`mission-control/.claude/workflows/pandacorp-build.js`) and the Manual prototype (`mission-control/prototype/index.html`) still describe the old per-WO flow — both corrected when MC migrates. Activation: commit + `claude plugin update` + restart.

## 2026-06-16 — Freno por salud+gasto, repair-antes-de-bloquear, BLOCKED con motivo, sin ntfy · v8.2.0
**What:** The owner reviewed the v8.1 guardrail and refined how a build STOPS. (1) The build now **runs to completion** by default — `maxFrds` is no longer a per-run cap of 6; it defaults to **no cap** (Infinity) and is **opt-in for supervised TEST runs only**. A run stops only by: nothing left, the **budget ceiling** (the real token guardrail, set via the run's budget directive), a **health breaker** (3 non-`external` blocks in a row → something is systemically wrong), or work that **needs the owner**. Rationale: "stop after N features" protects neither tokens nor progress — one feature can cost 10x another. (2) **Repair before block** (the owner's rule): a failing work order or FRD gate first triggers a **repair pass** (a strong-model agent diagnoses + fixes, re-verifying with `verify.sh`); only if the fix is out of reach does the feature go `BLOCKED`. (3) **`BLOCKED` carries a `blocked_reason`**: `needs-owner` (a human must act — env var/secret, external account, product decision; also logged to `.pandacorp/inbox/decisions.md`), `external` (internet/upstream, retried later, doesn't trip the breaker), or `error` (unresolved fault). (4) **ntfy removed** (the app is poor): notifications are now macOS desktop (`osascript`) from the engine + `PushNotification` to the phone from the supervising agent. MINOR bump to **8.2.0**; `OVERLAY_VERSION` → 8.2.0.
**Why:** The owner is about to run unattended builds and wants the build to *finish the work*, not stop at an arbitrary count, while staying safe against runaway token spend and silent stalls. Health+budget stops + repair-before-block + a typed block reason give "run to completion unless something is genuinely wrong or needs me." Replaces the v8.1 hard `maxFrds=6` cap. Decided with the owner this session.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (`maxFrds`→Infinity default, `attemptRepair`, `consecutiveBlocks` health breaker, `blocked_reason`, `NOTIFY` desktop-only), `plugin/skills/implement/SKILL.md`, `plugin/templates/docs/work-order-template.md` (`blocked_reason` field), `factory/standards/build-orchestration.md` (§6 "How a run stops"), `factory/decisions/registry.yaml` (DR-050 refined), `plugin/.claude-plugin/plugin.json` + `plugin/templates/OVERLAY_VERSION` → 8.2.0. Reaches a project via `/upgrade` or the part-2 MC migration (the engine runs from the PROJECT's copy). Activation: commit + `claude plugin update` + restart.

## 2026-06-16 — Build supervisor + token guardrail + phone channel · v8.1.0
**What:** Hardened the build for unattended/overnight runs (owner feedback: a v7.3 run went >1h unwatched with zero signal, and the owner is about to leave the machine building overnight on a Max plan). (1) **Hard per-run cap `maxFrds`** (default 6) in `pandacorp-build.js`: a single run builds at most N features then stops at a safe point, so one unattended run can NEVER build the whole app and burn the week's tokens — independent of the budget circuit breaker. (2) **Dual-channel notifications**: the engine fires `osascript` (Mac) AND `ntfy` (phone, via `.pandacorp/run/ntfy-topic`) on every alarm (baseline-unrepairable, FRD blocked, run end). (3) The `implement` skill now mandates the **build supervisor** at launch: a `Monitor` live-watch (~2-min bash poll — no tokens, only events wake the agent) + 15-min health heartbeat + react (unstick / stop+notify-phone) + **self-learning of AVOIDABLE stalls** (DR-047: classify uncontrollable vs avoidable, capture/fix the avoidable) + a session budget ceiling. (4) Aligned the `implement` SKILL body to the v2 per-FRD flow + frontmatter state, and the gate's `reopen` is handled WITHOUT a retry loop. MINOR bump to **8.1.0**; `OVERLAY_VERSION` → 8.1.0.
**Why:** Make "run and walk away" actually safe: the cap stops a single run from running away on tokens, the supervisor catches stalls/errors the moment they happen and tells the owner on their phone, and avoidable stalls feed the self-learning loop so they don't recur. Closes the monitoring gap the owner hit. Decided with the owner this session.
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (`maxFrds` cap, `NOTIFY` dual-channel, `reopen` handling), `plugin/skills/implement/SKILL.md` (rewritten to v2 + the supervisor section), `plugin/templates/OVERLAY_VERSION` → 8.1.0, `plugin/.claude-plugin/plugin.json` → 8.1.0. The engine reaches a project via `/upgrade` or the part-2 MC migration (it runs from the PROJECT's copy of `pandacorp-build.js`, not the cache). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — Document-driven build orchestration: state, plans and tests in the docs · v8.0.0 (MAJOR · DR-050)
**What:** Rebuilt the build flow around **DR-050** after the Mission Control dogfood showed ~76% of the build effort was re-work. **State now lives in the work-order/FRD/blueprint frontmatter** (`status` + `implementation_status: PLANNED→IN_PROGRESS→IN_REVIEW→VERIFIED`, +`BLOCKED`) — the engine's single source of truth; it builds only `PLANNED`/`IN_PROGRESS` and **never rebuilds `VERIFIED`** (kills the re-work). **Work orders are coarse** (one view/capability, not atomic). **The Build Plan (the WO DAG) lives in each per-FRD `blueprint.md`** and the engine reads it instead of re-inferring dependencies. Each closed WO writes a **`## Status Note` hand-off**. The engine **builds FRD by FRD and reviews/tests per FRD** (a WO runs only its own fast self-test → `IN_REVIEW`; one review + integration gate over the whole FRD → `VERIFIED`), dropping review cost from O(WOs) to O(FRDs); a **functional/browser** layer (preview tools) is designed in, opt-in. Added **document templates** (`plugin/templates/docs/`) — the factory had none. MAJOR bump to **8.0.0**; `OVERLAY_VERSION` → 8.0.0.
**Why:** MC built with the pipeline ran ~8h / ~944 subagents for ~37 work orders with the owner blind to stalls. Forensics: state wasn't in the data (the engine rebuilt finished WOs every relaunch), atomic WOs + per-WO review/verify + runtime dependency inference. This recovers what **PandaTrack** already did and **DR-049 left behind**. Owner-approved (`docs/proposals/11-build-orchestration-v2.md`); decided across the session (`IN_REVIEW` not `IMPLEMENTED`, iterate reopens WOs, per-FRD build/test).
**Impact:** new `plugin/templates/docs/{prd,frd,blueprint,work-order}-template.md`; `plugin/skills/{work-orders,blueprint,spec,iterate,new-version}/SKILL.md`; `plugin/templates/shared/.claude/workflows/pandacorp-build.js` rewritten to the per-FRD loop; `plugin/templates/OVERLAY_VERSION` → 8.0.0; `plugin/.claude-plugin/plugin.json` → 8.0.0. Factory: `factory/standards/build-orchestration.md` (new canonical), `factory/decisions/registry.yaml` (DR-050), proposal 11 adopted. **Pending (part 2):** Mission Control migration (frontmatter on its WOs, mark the 37 built `VERIFIED`, re-group the ~77 remaining into coarse WOs + Build Plans, relaunch) + a pass over the `implement` skill body + Manual (DR-046). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — Faster build: high parallelism + selective verify + native notifications · v7.3.0
**What:** Three changes to the build engine (`pandacorp-build.js`), driven by the Mission Control build running ~6h at ~5 WO/h with the owner blind to stalls. (1) **Higher parallelism**: `powerful` → `wave: 8` with `split: false` (one full-stack implementer per WO, faster than test→backend→frontend), `balanced` → `wave: 4`. (2) **Selective verify** (the big win): per-WO safe points run biome + tsc + ONLY the WO's own tests, not the whole suite; the FULL `verify.sh` runs once per WAVE — a barrier that repairs cross-WO regressions — instead of once per WO. Cost drops from O(WOs × suite) to O(WOs × WO-tests + waves × suite). (3) **Native owner notifications**: the engine fires a macOS `osascript` desktop notification on the freeze, baseline-unrepairable, wave-verify-unrepairable and end-of-run paths, so the owner learns when the build blocks, can't self-repair, or finishes — no more silent stalls. The end-of-run notify ALWAYS fires (built/blocked/pending counts). MINOR bump to **7.3.0**; `OVERLAY_VERSION` → **7.3.0**.
**Why:** Owner feedback after the MC dogfood: the build was far too slow (the global per-WO verify dominated; only 3 WO ran in parallel) and gave no signal when something needed attention (a blocked WO or pending decision never reached them). Selective verify + higher wave target ~3-4× throughput; the notifications close the visibility gap until Mission Control's live view exists. Decided with the owner ("máximo" speed + "notificación nativa + yo vigilo").
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (PROFILES waves/split, `verifyWO` selective, per-wave full-suite barrier, osascript notifications; re-copied to `mission-control/.claude/workflows/`), `plugin/templates/OVERLAY_VERSION` → 7.3.0, `plugin/.claude-plugin/plugin.json` → 7.3.0, `mission-control/.pandacorp/status.yaml` `overlay_version` → 7.3.0. Trade-off: a selective per-WO verify can miss a cross-WO regression until the wave barrier — acceptable, the barrier catches it cheaply. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — pandacorp-build self-heals a red baseline (deadlock breaker) · v7.2.0
**What:** Added a **Baseline self-heal phase** to the build engine (`pandacorp-build.js`), running BEFORE the Plan. `verify.sh` is global, so a single test left red by a prior run — an adversarial test the reviewer added after a WO closed/was-blocked, broken code from a WO that exhausted its 2 review cycles, or a committed regression — freezes the WHOLE build: every new WO fails its own verify gate on someone else's broken test, the ready-queue drains, and the run stalls silently as a phantom "empty queue". The new phase runs `verify.sh` first; if green it changes nothing; if red it spawns an `implementer` (judge model) to fix the PRODUCTION code (never weaken tests) until the suite is green and commit — or, if it genuinely can't, return red and STOP for the owner instead of stalling. MINOR bump to **7.2.0**; `OVERLAY_VERSION` → **7.2.0** (machinery changed compatibly; existing projects pick it up on `/upgrade`).
**Why:** Building Mission Control hit this deadlock 3× (WO-17-001 `parseProjects`, WO-05-001 work-order status parse) — each time the owner's session had to repair the baseline by hand before the build could move. The engine should do that itself. Root cause is structural (a global verify gate + the reviewer adding adversarial tests in a step separate from the WO), captured in `factory/memory/_inbox.md`; this is the engine-side mitigation. Decided with the owner ("arreglo el motor primero").
**Impact:** `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (new `phase('Baseline')` + repair agent; re-copied to `mission-control/.claude/workflows/`), `plugin/templates/OVERLAY_VERSION` → 7.2.0, `plugin/.claude-plugin/plugin.json` → 7.2.0, `mission-control/.pandacorp/status.yaml` `overlay_version` → 7.2.0. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — blueprint preflight + project-namespaced Party events · v7.1.0
**What:** Two compatible improvements surfaced while preparing Mission Control to be built with the pipeline. (1) **`blueprint` gained the DR-045/048 preflight** that `implement`/`iterate`/`bug` already had: it confirms the `.pandacorp/status.yaml` marker and, if `overlay_version` is behind `OVERLAY_VERSION`, runs `/pandacorp:upgrade` first — so blueprint never runs against a stale or absent overlay (it was the only mutating skill missing this). (2) **Party events are now namespaced by project**: `emit-event.sh` and the `pandacorp-build.js` `EMIT` add a `project` field (the project folder via `basename "$PWD"`) so Mission Control's Party/observability panels can attribute events and tell concurrent builds apart. The field is additive — old consumers ignore it. MINOR bump to **7.1.0**; `OVERLAY_VERSION` → **7.1.0** (the `build.js` machinery changed compatibly), so existing projects pick up the namespaced emitter on their next `/pandacorp:upgrade`.
**Why:** Materializing Mission Control's overlay (documented by hand inside the factory, never scaffolded) surfaced both gaps: blueprint would not auto-upgrade an out-of-date overlay, and the shared `~/.claude/dashboard-events.ndjson` stream had no way to attribute an event to a project. Both were captured in `factory/memory/_inbox.md`. See `mission-control/docs/decision-log.md` (2026-06-16, overlay materialization).
**Impact:** `plugin/skills/blueprint/SKILL.md` (preflight block), `plugin/scripts/emit-event.sh` (+`project`), `plugin/templates/shared/.claude/workflows/pandacorp-build.js` (`EMIT` +`project`; re-copied to `mission-control/.claude/workflows/`), `plugin/templates/OVERLAY_VERSION` → 7.1.0, `plugin/.claude-plugin/plugin.json` → 7.1.0, `mission-control/.pandacorp/status.yaml` `overlay_version` → 7.1.0 (machinery already current). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-16 — Feature-centric docs structure (the FRD as a self-contained module) · v7.0.0 (MAJOR)
**What:** Rewrote the whole flow to the **feature-centric** documentation model (DR-049): each FRD is a self-contained module folder `docs/frds/frd-NN-<slug>/` (`frd.md` + conditional `fdd.md`/`mocks/` + per-feature `blueprint.md` + `work-orders/`), under a thin product layer (`docs/product/{prd.md,architecture.md,research.md}`) and a global design system (`docs/design/` = the PDD). The old **by-type** layout (flat `docs/frds/frd-NN.md`, ONE global `docs/blueprint.md`, ONE global `docs/work-orders/`, `docs/prd.md`) is gone. Two architecture layers: platform `docs/product/architecture.md` (stable, one) vs per-FRD `blueprint.md` (large → a `blueprints/` folder, not "pillars"). Work orders now live **under their FRD**; `implement`'s build walks `docs/frds/*/work-orders/` and orders by each WO's `Dependencies` (topological), instead of a flat global glob. Stable numeric folder-derived IDs `REQ-NN-MMM → AC-NN-MMM.K → CMP-NN-<slug>/IF-NN-<slug> → WO-NN-MMM`, with the source-of-truth hierarchy `FRD > FDD > design-tokens > blueprint > work order` restated in every blueprint/WO header. `docs/{design,adr,analytics/events.md,reviews,decision-log.md}` stay global. **MAJOR bump to 7.0.0** (breaking doc paths); `OVERLAY_VERSION` → 7.0.0 (the overlay templates changed). `upgrade` gained a docs-migration step (2b) that reshapes an old by-type project into the new layout (relocate-only, IDs preserved).
**Why:** The owner observed the flat model caps at scale (a single blueprint monolith; growth forced a mid-project flat→nested migration) and adopted the JobLeap arrangement after a benchmark (JobLeap vs PandaTrack). Feature-centric gives locality of change — a new feature is a new folder, nothing else moves. See `factory/decision-log.md` (2026-06-16) + `docs/proposals/10-feature-centric-doc-structure.md`.
**Impact:** Skills rewritten to the new paths/model: `spec`, `scaffold`, `blueprint`, `work-orders`, `implement`, `iterate`, `design`, `adopt`, `upgrade`, `new-version`, `release`, `review-launch`, `memory`. Party agents: `product-manager`, `architect`, `designer`, `copywriter`, `implementer`, `frontend-dev`, `librarian` (analytics/reviewer/security-auditor unchanged — their paths stay global). Templates: `shared/.pandacorp/guide.md.tpl` (doc-map), `shared/AGENTS.md.tpl`, `shared/.claude/workflows/pandacorp-build.js` (per-FRD work-order discovery + cross-FRD ordering), `templates/OVERLAY_VERSION` → 7.0.0. Plugin `plugin.json` → 7.0.0. Canonical: `factory/standards/structure.md` ("Documentation structure") + `documentation.md` + DR-049 (`registry.yaml`). Mission Control docs migrated to the new layout + Manual (DR-046). Existing projects migrate via `/pandacorp:upgrade` (step 2b). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Durable promotions queue: the `promotion` field · v6.3.0
**What:** Rule-promotion approval is now durable, not conversation-only. Lessons carry a **`promotion`** field (none/proposed/approved/rejected): `/pandacorp:memory review` sets `proposed` (with target + rationale), `/pandacorp:memory status` lists the queue, `/pandacorp:learn` sets `approved`/`rejected`. Owner reviews the full list and decides whenever. Single source of truth on the lesson; `/memory status` + MC FRD-17 are views. Added `promotion` to the schema, template, seed lesson and `validate-memory.sh`. MINOR bump to **6.3.0**.
**Why:** Owner asked about the approval flow + "decide later"; proposals were ephemeral (only in the review conversation). See `factory/decision-log.md` (same date).
**Impact:** `plugin/agents/librarian.md`, `plugin/skills/{memory,learn}/SKILL.md`, `plugin/scripts/validate-memory.sh`, `plugin/.claude-plugin/plugin.json` (6.3.0). Factory: `factory/memory/*`. Mission Control: `frd-17` (promotions-queue criterion). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Self-learning: always-on hybrid capture + A.U.D.N. dedup + provenance · v6.2.0
**What:** Made lesson capture general and automatic (DR-047 updated). **Capture (Tier 1)** is always-on: a rule in `templates/shared/.pandacorp/guide.md.tpl` (projects) and the factory `CLAUDE.md` jots one-line candidates to a gitignored raw inbox (`.pandacorp/run/lessons.md` / `factory/memory/_inbox.md`) on ANY skill or conversation. **Refine (Tier 2)**: `librarian` now drains the inbox first, dedups with **A.U.D.N.** (ADD/UPDATE/MERGE/NO-OP — capture volume no longer bloats the store) and sets **provenance** (owner-stated > ci-verified > agent-inferred); `/pandacorp:memory harvest` documents the hybrid cadence (boundary + scheduled `/loop` + on demand). Added a required `provenance` field to the lesson schema, template, seed lesson and `validate-memory.sh`. MINOR bump to **6.2.0** (new compatible capability).
**Why:** Owner wanted capture across the whole workflow, not just builds, and was wary of over-frequent triggers; research (Mem0/MemoryGraft/LangMem/Cursor/Anthropic) showed always-on capture is safe only with dedup + provenance + a gate — "more capture is not better" (self-reflections poison). Claude Code hooks can append/nudge but can't run the librarian, so refine is skill/schedule-driven. See `factory/decision-log.md` (same date).
**Impact:** `plugin/agents/librarian.md`, `plugin/skills/memory/SKILL.md`, `plugin/templates/shared/.pandacorp/guide.md.tpl`, `plugin/scripts/validate-memory.sh`, `plugin/.claude-plugin/plugin.json` (6.2.0). Factory: `registry.yaml` (DR-047), `CLAUDE.md`, `factory/memory/*`, `.gitignore`. Pending: activate the scheduled sweep, prototype reconcile (DR-046). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — sync-portfolio: recovery instructions on broken path · v6.1.1
**What:** Step 4 of `/pandacorp:sync-portfolio` now provides concrete recovery instructions when a project path is not found on disk. If the portfolio row has a `repo:` URL → prints the exact `git clone <repo> <path>` command and tells the owner to re-run `sync-portfolio`. If there is no repo → warns that there is no remote backup and suggests `/pandacorp:spec`. No automatic clone or write — read-only as always.
**Why:** The previous behavior only marked `⚠️ path not found` and asked the owner, leaving them without any next step. The gap: you delete (or lose) a project folder and the skill told you something was wrong but not how to fix it.
**Impact:** `plugin/skills/sync-portfolio/SKILL.md` (step 4). Also `mission-control/docs/frds/frd-03-portfolio.md` — added acceptance criteria for the same case in the Portfolio panel (badge + recovery command, once MC is built as Next.js). PATCH bump to **6.1.1** (no new capability, behavioral improvement in existing skill).

## 2026-06-15 — Self-learning loop engine: librarian agent + /pandacorp:memory + retrieval wiring · v6.1.0
**What:** Implemented the factory-side engine of the self-learning loop (DR-047, Phases 1/2/4 + the apply stage). **New agent `librarian`** (the "cronista"): harvests evidence-anchored CANDIDATE lessons from a project's capture points (the v6.0.0 `.pandacorp/` paths: `inbox/bugs/`, `comms/progress.md`, `inbox/decisions.md`, plus `docs/reviews/`, `docs/blueprint.md`/`adr/`, the launch verdict) into `factory/memory/`, deduping/corroborating, propose-only. **New skill `/pandacorp:memory`** (factory-side): `harvest` (Phase 1, with the DR-047 low-risk eval-gate that auto-activates a corroborated candidate), `review`/prune (Phase 4, deprecate-never-delete + promote proposals), `status`. **Retrieval wiring (Phase 2)**: `architect`, `backend-dev`, `frontend-dev` and `implementer` now consult `factory/memory/` before choosing libraries / solving problems (apply `library-verdict`s, avoid `anti-pattern`s/`gotcha`s, cite the `LESSON-NNNN`). **`learn` is now the apply/promote stage**: a second entry point promotes a corroborated memory lesson into a standard/DR/skill under the eval-gate + owner gate, back-linking the lesson. MINOR bump to **6.1.0** (new agent + new skill, backward compatible — 6.0.0 projects need no change).
**Why:** Close the loop the factory was missing (harvest → store → retrieve → propose → gate → promote → prune) so it gets smarter/faster with use, with the eval-gate + human gate the research showed is the only safe governance (see `factory/decision-log.md` 2026-06-15 DR-047 + `docs/proposals/09-self-learning-factory.md`). Built on the v6.0.0 `.pandacorp/` paths (orthogonal to that migration).
**Impact:** `plugin/agents/librarian.md` (new), `plugin/skills/memory/SKILL.md` (new), `plugin/skills/learn/SKILL.md` (promote-from-memory entry + Rules bullet), `plugin/agents/{architect,backend-dev,frontend-dev,implementer}.md` (retrieval rule), `plugin/.claude-plugin/plugin.json` (6.1.0). Pending (next waves): auto-harvest hook in the build workflow (`pandacorp-build.js`) + SubagentStop (Phase 1 automation), Mission Control **FRD-17** "Propuestas" inbox (Phase 5) + prototype reference reconcile for `librarian`/`memory`/DR-047 (DR-046), a deterministic `factory/memory/` schema validator, designer retrieval. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Project .pandacorp/ layer + overlay versioning & /upgrade · v6.0.0 (BREAKING)
**What:** MAJOR restructure of the project overlay (DR-048). The template now ships a `.pandacorp/` integration folder: moved `docs/status.yaml`→`.pandacorp/status.yaml` (+ `overlay_version`/`created_with`), `docs/iteration.md`→`.pandacorp/comms/iteration.md`; new `.pandacorp/{guide.md,README.md}.tpl`; `CLAUDE.md.tpl` is now thin (`@AGENTS.md` + `@.pandacorp/guide.md` imports + owner free zone); `AGENTS.md.tpl` self-contained; new `.gitignore` (ignores `.pandacorp/{comms,inbox,run}/`); new `templates/OVERLAY_VERSION` (6.0.0). New skill **`/pandacorp:upgrade`** (migrates old layout → `.pandacorp/`, regenerates the managed layer, bumps version, commits on its own — silent for compatible, announced for MAJOR). Preflight in the 5 mutating skills now checks `overlay_version` and auto-runs upgrade; the marker simplified to `.pandacorp/status.yaml`. Detector (`is-pandacorp-project.sh`) + write-gate hook (`warn-adhoc-write.sh`) rewritten for the new marker/exemptions. ~39 files path-migrated (skills/agents/standards/FRDs/build engine). MAJOR bump to **6.0.0** (breaking paths for existing projects — migrated by `/upgrade`).
**Why:** See `factory/decision-log.md` (same date, DR-048) — one delimited `.pandacorp/` like `.github/`, autosufficiency as the git criterion, thin `CLAUDE.md` via `@import`, automatic silent-safe upgrade.
**Impact:** `plugin/templates/` (shared overlay restructured + `OVERLAY_VERSION`), `plugin/skills/{upgrade (new),implement,iterate,release,bug,new-version,scaffold,spec,adopt,decide,design,blueprint,review-launch,work-orders,sync-portfolio,learn}`, `plugin/scripts/{is-pandacorp-project,warn-adhoc-write}.sh`, `plugin/agents/{product-manager,reviewer,test-writer}.md`, `plugin/.claude-plugin/plugin.json` (6.0.0). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Write-gate + preflight guards + /adopt (brownfield) · v5.1.0
**What:** Hardened the factory↔project boundary in the plugin. **Project template**: `CLAUDE.md.tpl` gained a "How changes are made — work through the skills" section (write-gate DR-044: pragmatic frontier + auto-invoking router, internal gates intact); `AGENTS.md.tpl` gained the matching durable-convention bullet — so every project, new or adopted, is born with it. **Preflight (DR-045)**: `implement`, `iterate`, `release`, `bug`, `new-version` now check the Pandacorp marker (`docs/status.yaml` + "Origin — Pandacorp") at start and STOP → adopt/spec on a non-Pandacorp folder, instead of empty-queue/hallucinating. **Hook**: a non-blocking `PreToolUse` `Write|Edit` nudge (`warn-adhoc-write.sh`) reminds when a product file is edited outside a skill; suppressed for non-projects, the gitignored Spanish layer, deps/config, and when `.pandacorp/skill-active` is present. **Shared detector**: `is-pandacorp-project.sh`. **New skill `/pandacorp:adopt`**: the brownfield mirror of `scaffold` — injects the overlay without clobbering, infers the real phase from the code, reconstructs minimal as-built docs (`product-manager`/`architect`), registers a retroactive idea card + portfolio row, human gate before writing. MINOR bump to **5.1.0** (new skill + new compatible capability).
**Why:** See `factory/decision-log.md` (same date) for the full rationale — running a mutating skill on an external folder failed silently or hallucinated, and free-chat changes bypassed the documented process.
**Impact:** `plugin/templates/shared/{CLAUDE.md.tpl,AGENTS.md.tpl}`, `plugin/skills/{implement,iterate,release,bug,new-version}/SKILL.md` (preflight), `plugin/skills/adopt/SKILL.md` (new), `plugin/scripts/{is-pandacorp-project,warn-adhoc-write}.sh` (new), `plugin/hooks/hooks.json` (Write|Edit matcher), `plugin/.claude-plugin/plugin.json` (5.1.0). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Skill naming convention made explicit
**What:** Codified the (previously implicit) skill-naming convention: every skill is `plugin/skills/<kebab-slug>/SKILL.md` with **no `pandacorp:` prefix** in the directory or frontmatter; it is invoked as `/pandacorp:<slug>` (the plugin namespace adds the prefix) and the body H1 is `# /pandacorp:<slug>`. Verified `review-launch` already conforms — **no skill was renamed**.
**Why:** The owner asked to confirm `review-launch` followed the standard and to write the rule down if it didn't exist. The convention was only an implicit sub-bullet in `learn`; now it's explicit and discoverable.
**Impact:** `CLAUDE.md` (Plugin maintenance — new "Skill naming convention" note), `plugin/skills/learn/SKILL.md` (step 5 made prescriptive + a Rules bullet). Rides on the pending plugin bump.

## 2026-06-15 — Return-aware pipeline, /review-launch skill, state-model & designer fixes · v5.0.0
**What:** Implemented the pipeline re-audit fixes across the plugin. **New skill `/pandacorp:review-launch`** (DR-043: reads real metrics vs the value hypothesis, recommends kill/hold/double-down; runs on demand or as a `/loop` job). **Return-aware delivery (DR-042)**: `spec` (demand pre-check BEFORE scaffold + research scoped by `return_type` + independent fact-check before the PRD), `blueprint` (per-active-user cost for unit economics + reads the designer's `technical-assumptions.md` + feasibility spike + FRD↔blueprint reconciliation), `release` (return-aware launch/distribution plan), `product-manager` (return-aware PRD + unit-economics block + measurable hypothesis & numeric kill-signals + FRD size cap + EARS on every criterion), `analytics` (activation milestone + opportunity metric), `researcher` (scope by `return_type`), `reviewer` (cross-family in deep mode). **State model unified**: `new-idea`/`discover` write `discovered` (was `documented` — a raw idea has no PRD), `spec` handoff writes `in-pipeline` (matches `scaffold`), fixed the phantom `building` idea-state wording in `iterate`/`implement`, aligned `recommend`/`scaffold`. **Designer**: bespoke per-domain identity, RPG/"guild" look reserved for Mission Control, emits `technical-assumptions.md`. `sync-portfolio` leaves the portfolio business columns to review-launch. MAJOR bump to **5.0.0** (flow + idea-status-enum changes).
**Why:** The audit found the flow strong on engineering but blind on the economic axis and fractured in its state model; the v4 i18n pass had translated the issues, not fixed them; and the expanded mission (`return_type`) was declared but never read by the delivery pipeline.
**Impact:** `plugin/skills/{review-launch (new),spec,blueprint,release,new-idea,discover,recommend,scaffold,iterate,implement,sync-portfolio,design}/SKILL.md`, `plugin/agents/{product-manager,analytics,researcher,reviewer,designer}.md`, `plugin/.claude-plugin/plugin.json` (5.0.0). Factory/ideas/Mission-Control decision logs: same date. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Launch-language research + direct-push-to-main workflow · v4.0.0
**What:** `onboarding` now captures the owner's `country`; `researcher`/`spec`/`product-manager` gained a launch-market-&-language research dimension (DR-041 — recommend per product, no fixed default, recorded in the PRD); the build agents (`implementer`/`backend-dev`/`frontend-dev`), `iterate` and `block-dangerous.sh` were updated for the direct-push-to-main workflow (DR-040).
**Why:** See `factory/decision-log.md` (same date) for the full rationale.
**Impact:** `plugin/skills/{onboarding,spec,iterate}`, `plugin/agents/{researcher,product-manager,implementer,backend-dev,frontend-dev}`, `plugin/scripts/block-dangerous.sh`, `plugin/templates/shared/{CLAUDE.md.tpl,AGENTS.md.tpl}`. Folded into the same 4.0.0 release (branch not yet shipped) — no extra version bump.

## 2026-06-15 — Language policy migration: committed = English / gitignored = Spanish · v4.0.0
**What:** Executed the language policy (DR-009) across the whole repo. Plugin impact: all skill/agent docs and `STACK.md` translated to English; templates updated — `CLAUDE.md.tpl`/`AGENTS.md.tpl` now state the new rule and use English doc paths/enums, `status.yaml.tpl` is machine-state-only (English) with the Spanish narrative moved to a gitignored `docs/summary.md`, `decision-log.md.tpl` is English, `iteration.md.tpl` stays Spanish (it seeds a gitignored Spanish comms file). `pandacorp-build.js` translated to English and its build-mode tokens migrated `equilibrado|potente|profundo → balanced|powerful|deep` (display labels stay Spanish; synced in `implement` SKILL, FRD-11, the prototype). Idea/profile/status frontmatter keys and enums migrated to English. Bumped MAJOR to **4.0.0** (DR-034) — breaking for existing projects (enum/path/key changes).
**Why:** Anyone cloning the public repo sees everything in English (industry standard) while the owner keeps operating Pandacorp in Spanish (UI + the gitignored communication layer).
**Impact:** `plugin/.claude-plugin/plugin.json` (4.0.0), `.claude-plugin/marketplace.json`, `plugin/skills/**`, `plugin/agents/**`, `plugin/templates/**`, `plugin/scripts/{block-dangerous,verify-before-stop}.sh`. Factory-wide: see `factory/decision-log.md` (same date). Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Skill `teach` renamed to `learn` (name refinement) · v3.0.0
**What:** The owner preferred `learn` over `teach` for the command (the factory *learns* the know-how). `/pandacorp:teach` → `/pandacorp:learn` was renamed (same skill, same function). Renaming a skill = **MAJOR** (DR-034). It's a name refinement on the same day as v2.0.0 (codify→teach), which was never actually used (no restart in between).
**Why:** `learn` communicates better that the factory incorporates/learns the durable know-how.
**Impact:** `plugin/skills/teach/` → `plugin/skills/learn/SKILL.md`, `plugin/.claude-plugin/plugin.json` (3.0.0), `factory/standards/README.md`, `mission-control/docs/frds/frd-07-configuration.md`, `mission-control/prototype/index.html`, `docs/proposals/08-standards-rules-catalog.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Rename `codify` → `teach` (+ teach creates skills by delegating to skill-creator) · v2.0.0
**What:** The `/pandacorp:codify` command was renamed to `/pandacorp:teach` (the name "codify" read as "write code", the opposite of what it does: record durable know-how). `teach` now covers a third artifact besides standard and decision rule: **create/improve a skill**, delegating authorship and evaluation to the native `skill-creator` (compose, don't reinvent); teach only adds the owner's gate, placement in the plugin, the security/vendoring policy for external skills and the activation ritual. Renaming a skill = **MAJOR** (DR-034).
**Why:** The name was confusing; and creating skills is durable know-how (`teach`'s domain), not product (`implement`'s domain) — reusing `skill-creator` instead of cloning it.
**Impact:** `plugin/skills/codify/` → `plugin/skills/teach/SKILL.md`, `plugin/.claude-plugin/plugin.json` (2.0.0), `factory/standards/README.md`, `mission-control/docs/frds/frd-07-configuration.md`, `mission-control/prototype/index.html`, `docs/proposals/08-standards-rules-catalog.md`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — App/panel rename reflected in the skills (cockpit→Mission Control, panel→Party) · v1.2.3
**What:** After the app rename (see [factory/decision-log.md](../../factory/decision-log.md) and [mission-control/docs/decision-log.md](../../mission-control/docs/decision-log.md)), the vocabulary in the plugin's skills and artifacts was updated: `implement`, `decide`, `recommend`, `codify`, `bug`, `blueprint` (prose and descriptions: "the cockpit" → "Mission Control"; "followed in Mission Control" where it previously said the panel), `plugin/scripts/emit-event.sh` and `plugin/templates/shared/docs/status.yaml.tpl` (comments). No behavior change → bump **PATCH 1.2.2 → 1.2.3**.
**Why:** The skills mention where "the build is followed"; they must use the correct name (Mission Control = the app; Party = the per-project RPG panel).
**Impact:** `plugin/.claude-plugin/plugin.json` (1.2.3), `plugin/skills/{implement,decide,recommend,codify,bug,blueprint}/SKILL.md`, `plugin/scripts/emit-event.sh`, `plugin/templates/shared/docs/status.yaml.tpl`. Activation: commit + `claude plugin update pandacorp@panda-corp` + restart.

## 2026-06-15 — Deep onboarding + discover/recommend based on the profile (two streams)
**What:** Onboarding now captures interests, hobbies, likes/dislikes, goals, ASSETS/levers (audience, network, niche), monetization appetite and project types. `discover` and `recommend` read that profile and recommend in two streams (~50/50: general high-return ones + profile-aligned ones even if the monetary ROI is lower), with return understood as monetary OR opportunity, and scope open to any technological solution. Model details in [factory/ideas/decision-log.md](../../factory/ideas/decision-log.md) and DR-039.
**Why:** So the factory recommends things well aligned to the owner (their tastes and assets), not generic ones, and so it isn't limited to monetizable apps.
**Impact:** `plugin/skills/{onboarding,discover,recommend,new-idea}/SKILL.md`, `factory/profile.example.md`, `factory/ideas/_idea-template.md`, `factory/decisions/registry.yaml` (DR-039), `mission-control/prototype/index.html`.

## 2026-06-15 — External services, accounts/secrets, payments (Polar) and Vercel warnings
**What:** New standard `factory/standards/external-services.md` (proven services stack, "1 shared org + 1 primitive per app" account model, API-first provisioning, secrets in SOPS+age, payments with Polar/MoR, owner notification at gates). DR-035..038. Skills/agents: the PRD explicitly states "does v1 include payments?", the blueprint/release warn (warning, not a block) when a version that charges uses Vercel (Hobby = non-commercial, whole-account ban → Pro), and any gate fires a push to the owner.
**Why:** The owner (in Peru) launches many test apps; they need to standardize services, manage accounts/secrets autonomously ("leave it running") and have Vercel be a warning, not an impediment. Direct Stripe doesn't operate in Peru → Polar (MoR).
**Impact:** `factory/standards/external-services.md` (new), `factory/standards/stack.md`, `factory/decisions/registry.yaml` (DR-035..038), `plugin/agents/{product-manager,devops,architect}.md`, `plugin/skills/{spec,blueprint,release}/SKILL.md`, `factory/standards/README.md` (index), `CLAUDE.md`, `plugin/.claude-plugin/plugin.json` (1.1.0 → 1.2.1). Includes §9 (SOPS+age operational setup) and §10 (source-verified foundations).

## 2026-06-14 — Plugin semver versioning (DR-034) + removal of reference to a private project
**What:** The plugin debuts `version` in `plugin/.claude-plugin/plugin.json` (starts at `1.0.0`) and the policy is adopted: bump the version on each change to `plugin/` according to semver (PATCH/MINOR/MAJOR). Also removed were the mentions of an owner's private project that was cited as a reference in `factory/standards/README.md` and `plugin/templates/stack-a-nextjs/STACK.md` — the content is kept as a standard/recommended stack, without tying it to a project no one else can see.
**Why:** The owner asked to version and to have the version always updated according to the size of the change; and for the public repo not to reference a project of theirs that no one else can see. The plugin validator was also warning about the missing `version`.
**Impact:** `plugin/.claude-plugin/plugin.json` (version), `CLAUDE.md` (Plugin maintenance + semver policy), `factory/decisions/registry.yaml` (DR-034), `factory/standards/README.md`, `plugin/templates/stack-a-nextjs/STACK.md`.

## 2026-06-14 — Skill `onboarding` + portable paths (preparation for public repo)
**What:** New skill `/pandacorp:onboarding`: interviews the owner and generates `factory/profile.md` (personal), with bootstrap of `portfolio.md` from the seed. Also, the skills/scripts stopped hardcoding the factory path: `scan-ideas.sh` auto-detects the root (repo root), `spec`/`scaffold` create projects relative to the factory (or the profile's `projects_path`), and `CLAUDE.md.tpl` uses the placeholder `{{FACTORY_PATH}}`.
**Why:** Part of opening up the factory as a reusable template (see [factory/decision-log.md](../../factory/decision-log.md)). Onboarding replaces the owner's hardcoded identity; relative paths make it work on any machine.
**Impact:** `plugin/skills/onboarding/SKILL.md` (new), `plugin/scripts/scan-ideas.sh`, `plugin/skills/{spec,scaffold,new-idea,recommend,explore,sync-portfolio}/SKILL.md`, `plugin/templates/shared/CLAUDE.md.tpl`.

## 2026-06-14 — Plugin decision log created
**What:** The plugin's decision log begins.
**Why:** Part of the factory's "document everything" discipline — see [factory/decision-log.md](../../factory/decision-log.md). Until now, decisions about skills/agents lived only in commits.

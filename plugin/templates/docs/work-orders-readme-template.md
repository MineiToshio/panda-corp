<!--
  TEMPLATE for a feature's docs/frds/frd-NN-<slug>/work-orders/README.md — the human-readable index
  of this FRD's work orders. Generated by /pandacorp:work-orders (or /pandacorp:blueprint).
  This is a SUMMARY of the authoritative Build Plan in ../blueprint.md (which the engine reads) —
  keep it in sync with it; don't invent ordering here.
-->

# Work orders — FRD-NN Replace with the feature title

> The authoritative order + parallelism is the **Build Plan** in `../blueprint.md` (the build engine
> reads that, not this). This file is the readable summary. Each WO is a **coarse slice** (one
> cohesive view/capability), carries its own `implementation_status` frontmatter, and copies in its
> acceptance criteria.

## Work orders

| WO | Slug | Status | Summary |
|---|---|---|---|
| WO-NN-MMM | descriptive-slug | PLANNED | one line |

## Build order

The waves from the Build Plan: what comes first, what runs **in parallel** once a prerequisite is
`VERIFIED`, and the integration order. Note any **cross-FRD** dependencies (by WO id).

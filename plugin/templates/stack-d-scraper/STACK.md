# Stack D — folded into Stack C

Stack D (data collection / scraping / notifications) was the same base as Stack C (Python 3.12 + uv + FastAPI + SQLAlchemy + ruff/mypy/pytest) plus scraping libraries and conventions — two templates to maintain, one real stack. It is now the **"Scraping/worker extension"** section of `${CLAUDE_PLUGIN_ROOT}/templates/stack-c-fastapi/STACK.md`: use that guide (base install + the extension block). This pointer stays so an existing `stack-d` reference still resolves.
